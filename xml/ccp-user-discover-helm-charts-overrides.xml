<?xml version="1.0"?>
<?xml-stylesheet href="urn:x-suse:xslt:profiling:docbook51-profile.xsl"
 type="text/xml"
 title="Profiling step"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entity-decl.ent"> %entities;
]>
<section xmlns="http://docbook.org/ns/docbook"
         xmlns:xi="http://www.w3.org/2001/XInclude"
         xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="sec.helm-chart-override">
 <title>Discover Helm Charts Overrides</title>
<para>
  The upstream Helm charts have values that can be used to alter the
  deployment of a chart to match your needs.
</para>
<para>
  Generate a list of the available overrides:
</para>
<procedure>
 <step>
  <para>
   Build all the Helm charts (make all in each of the
   <filename>/opt/openstack/openstack-helm</filename> folders) in your
   environment.
  </para>
 </step>
 <step>
  <para>
   Run the following on your deployer node:
  </para>
  <screen>for fname in /opt/openstack/openstack-helm{,-infra}/*.tgz; do
    chartname=$(basename $fname | rev | cut -f &quot;2-&quot; -d &quot;-&quot; | rev);
    foldername=$(dirname $fname);
    pushd $foldername;
        echo -e &quot;\nNow analysing: $chartname\n\n&quot; &gt;&gt; /opt/charts-details;
        helm inspect values $chartname &gt;&gt; /opt/charts-details;
    popd;
done
  less /opt/charts-details</screen>
 </step>
</procedure>
</section>
