<?xml version="1.0"?>
<!DOCTYPE chapter [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<chapter xml:id="config-availability-zones"
 xmlns="http://docbook.org/ns/docbook" version="5.1"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Configuring Availability Zones</title>
 <para>
  The &clm; only creates a default availability zone during
  installation. If your system has multiple failure/availability zones defined
  in your input model, these zones will not get created automatically.
 </para>
 <para>
  Once the installation has finished, you can run the
  <literal>nova-cloud-configure.yml</literal> playbook to configure
  availability zones and assign compute nodes to those zones based on the
  configuration specified in the model.
 </para>
 <para>
  You can run the playbook <literal>nova-cloud-configure.yml</literal> any time
  you make changes to the configuration of availability zones in your input
  model. Alternatively, you can use Horizon or the command line to perform the
  configuration.
 </para>
 <para>
  <!-- FIXME: HOS docs refer to <xref href="../operations/compute/creating_aggregates.xml"/>, which might be better suited when the operations guide is developed or converted. -->
  For more details, see the OpenStack Availability Zone documentation at
 <link xlink:href="https://docs.openstack.org/nova/pike/user/aggregates.html"/>.
 </para>
</chapter>
