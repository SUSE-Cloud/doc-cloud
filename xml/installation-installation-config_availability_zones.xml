<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<!--Edit status: not edited-->
<section xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="config_availability_zones"><title>Configuring Availability Zones</title>
        

        <para>The lifecycle manager only creates a default availability zone during installation.
                If your system has multiple failure/availability zones defined in your input model,
                these zones will not get created automatically. </para>
<para> Once the installation has finished, you can run the
                    <literal>nova-cloud-configure.yml</literal> playbook to configure availability
                zones and assign compute nodes to those zones based on the configuration specified
                in the model. </para>
<para>You can run the playbook <literal>nova-cloud-configure.yml</literal> any time you make
                changes to the configuration of availability zones in your input model.
                Alternatively, you can use Horizon or the command line to perform the configuration. </para>
<para>For more details, see <citetitle>FIXME: broken external xref</citetitle>.</para>


    </section>
