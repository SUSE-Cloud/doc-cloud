<?xml version="1.0"?>
<?xml-stylesheet href="urn:x-suse:xslt:profiling:docbook51-profile.xsl"
 type="text/xml"
 title="Profiling step"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entity-decl.ent"> %entities;
]>
<section xmlns="http://docbook.org/ns/docbook"
         xmlns:xi="http://www.w3.org/2001/XInclude"
         xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="sec.customize-helm-behavior">
 <title>Customizing Helm Testing Behavior</title>
<para>
  By default, all tests that have been defined for each Helm chart will be run
  as part of that chart's deployment. However, in some scenarios you may want
  to prevent these tests from running. To disable Helm tests, define the
  <literal>run_tests</literal> key in
  <filename>${WORKDIR}/env/extravars</filename> and set it to
  <literal>false</literal>:
</para>
<screen>run_tests: false</screen>
<note>
 <para>
  This will disable all Helm tests in all charts during a full site
  deployment. Tests for individual Helm charts can still be run by using
  the Helm CLI and the service name, such as:
 </para>
</note>
<screen>helm test airship-glance</screen>
<para>
  If needed, the default timeout value of 2700s for test completion can be
  increased by adding the <literal>test_timeout</literal> key in
  ${WORKDIR}/env/extravars and providing a value, in seconds:
</para>
<screen>test_timeout: 3000</screen>
</section>
