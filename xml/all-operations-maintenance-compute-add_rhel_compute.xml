<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<!---->
<section xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="add_rhel_compute"><title>&kw-hos-tm; &kw-hos-version-50;: Adding a RHEL Compute Node</title><abstract><para><para>Adding a RHEL compute node allows you to add
      additional capacity for more virtual machines.</para><para>You may have a need to add
      additional RHEL compute hosts for more virtual machine capacity or another purpose and these
      steps will help you achieve this.</para></para>
</abstract>
    <!---->
    <sidebar xml:id="idg-all-operations-maintenance-compute-add_rhel_compute-xml-4"><para>There are two methods you can use to add RHEL compute hosts to your environment:</para>
<orderedlist>
        <listitem><para>Adding RHEL pre-installed compute hosts. This method does not require the RHEL iso be on
          the lifecycle manager to complete.</para>
</listitem>
        <listitem><para>Using the provided Ansible playbooks and Cobbler, RHEL will be installed on your new
          compute hosts. This method requires that you provided a RHEL 7.2 iso during the initial
          installation of your cloud, following the instructions at <xref linkend="rhel_overview"/>. </para>
<para>If you want to use the
            provided Ansible playbooks and Cobbler to setup and configure your RHEL hosts and you
            did not have the RHEL 7.2 iso on your lifecycle manager during your initial installation
            then ensure you look at the note at the top of that section before proceeding.</para>
</listitem>
      </orderedlist></sidebar>

    <sidebar xml:id="idg-all-operations-maintenance-compute-add_rhel_compute-xml-5"><title>Prerequisites</title><para>You need to ensure your input model files are properly setup for RHEL compute host
        clusters. This must be done during the installation process of your cloud and is discussed
        further at <xref linkend="install_rhel"/> and <xref linkend="rhel_compute_model"/>.</para>
</sidebar>

    <formalpara xml:id="idg-all-operations-maintenance-compute-add_rhel_compute-xml-6"><title>Adding a RHEL compute node</title><para><para><emphasis role="bold">Adding pre-installed RHEL compute hosts</emphasis></para><para>This method requires that you have RHEL 7.2 pre-installed on the baremetal host prior to
          beginning these steps.</para></para>
<orderedlist>
          <listitem><para>Ensure you have RHEL 7.2 pre-installed on your baremetal host.</para>
</listitem>
          <listitem><para>Log in to the lifecycle manager.</para>
</listitem>
          <listitem><para>Edit your <literal>~/helion/my_cloud/definition/data/servers.yml</literal> file to
            include the details about your new compute host(s). </para>
<para>For example, if you already had a
              cluster of three RHEL compute hosts using the <literal>RHEL-COMPUTE-ROLE</literal> role
              and needed to add a fourth one you would add your details to the bottom of the file in
              the format. Note that we left out the iLo details because they won't be needed since
              you pre-installed the RHEL O/S on your
              host(s).</para>
<screen>- id: compute4
  ip-addr: 192.168.102.70
  role: RHEL-COMPUTE-ROLE
  server-group: RACK1  </screen><para>You
              can find detailed descriptions of these fields . Ensure that you use the same role for any new RHEL hosts you are
              adding as you specified on your existing RHEL hosts.</para>
<important><para>You
              will need to verify that the <literal>ip-addr</literal> value you choose for this host
              does not conflict with any other IP address in your cloud environment. You can confirm
              this by checking the <literal>~/helion/my_cloud/info/address_info.yml</literal> file on
              your lifecycle manager.</para>
</important></listitem>
          <listitem><para>In your <literal>~/helion/my_cloud/definition/data/control_plane.yml</literal> file you
            will need to check the values for <literal>member-count</literal>,
              <literal>min-count</literal>, and <literal>max-count</literal>. If you specified them,
            ensure that they match up with your new total node count. For example, if you had
            previously specified <literal>member-count: 3</literal> and are adding a fourth compute
            node, you will need to change that value to <literal>member-count: 4</literal>. </para>
<para>See
                 for more
              details.</para>
</listitem>
          <listitem><para>Commit the changes to git:
            </para>
<screen>git add -A
git commit -a -m "Add node &lt;name&gt;"</screen></listitem>
          <listitem><para>Run the configuration processor and resolve any errors that are indicated:
            </para>
<screen>cd ~/helion/hos/ansible
ansible-playbook -i hosts/localhost config-processor-run.yml</screen></listitem>
          <listitem><para>Update your deployment directory:
              </para>
<screen>cd ~/helion/hos/ansible
ansible-playbook -i hosts/localhost ready-deployment.yml</screen><para>Before
              proceeding, you may want to take a look at <emphasis role="bold">info/server_info.yml</emphasis> to see if the
              assignment of the node you have added is what you expect. It may not be, as nodes will
              not be numbered consecutively if any have previously been removed. This is to prevent
              loss of data; the config processor retains data about removed nodes and keeps their ID
              numbers from being reallocated. See the Persisted Server Allocations section in  for information
              on how this works.</para>
</listitem>
          <listitem><para>[OPTIONAL] - Run the <literal>wipe_disks.yml</literal> playbook to ensure all of your
            partitions on your nodes are completely wiped prior to continuing with the installation:
            </para>
<screen>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts wipe_disks.yml --limit &lt;hostname&gt;</screen></listitem>
          <listitem><para>Complete the compute host deployment with this playbook:
            </para>
<screen>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts site.yml --tag "generate_hosts_file"
ansible-playbook -i hosts/verb_hosts site.yml --limit &lt;hostname&gt;</screen></listitem>
        </orderedlist>
<para><para><emphasis role="bold">Using the provided Ansible playbooks and Cobbler</emphasis></para><para>These steps will show you how to add the new RHEL compute host to your
            <literal>servers.yml</literal> file and then run the playbooks that update your cloud
          configuration. You will run these playbooks from the lifecycle manager.</para><para>If you did not have the RHEL 7.2 iso available on your lifecycle manager during your
          initial installation, ensure that you follow these steps:<orderedlist xml:id="idg-all-operations-maintenance-compute-add_rhel_compute-xml-7">
            <listitem><para>Save the RHEL 7.2 iso in your home directory (example:
              <literal>~/rhel7.iso</literal>)</para></listitem>
            <listitem><para>Mount the &kw-hos-phrase; iso to
              <literal>/media/cdrom</literal></para></listitem>
            <listitem><para>Run the <literal>~/hos-4.0.0/hos-init.bash</literal> script again, like you did during
              the initial installation.</para></listitem>
          </orderedlist></para><para>See <xref linkend="provisioning_rhel"/> for other
          prerequisites that must be completed before continuing.</para><para>When you are prepared to continue, use these steps:</para></para>
<orderedlist>
          <listitem><para>Log in to your lifecycle manager.</para>
</listitem>
          <listitem><para>Checkout the <literal>site</literal> branch of your local git so you can begin to make
            the necessary edits:
            </para>
<screen>cd ~/helion/my_cloud/definition/data
git checkout site</screen></listitem>
          <listitem><para>Edit your <literal>~/helion/my_cloud/definition/data/servers.yml</literal> file to
            include the details about your new compute host(s). </para>
<para>For example, if you already had a
              cluster of three RHEL compute hosts using the <literal>RHEL-COMPUTE-ROLE</literal> role
              and needed to add a fourth one you would add your details to the bottom of the file in
              this format:</para>
<screen>- id: compute4
  ip-addr: 192.168.102.70
  role: RHEL-COMPUTE-ROLE
  server-group: RACK1
  mac-addr: e8:39:35:21:32:4e
  ilo-ip: 10.1.192.36
  ilo-password: password
  ilo-user: admin
  distro-id: rhel72-x86_64</screen><para>You can find detailed descriptions of these fields . Ensure that you use the same role for any new RHEL hosts you are
              adding as you specified on your existing RHEL hosts.</para>
<important><para>You
              will need to verify that the <literal>ip-addr</literal> value you choose for this host
              does not conflict with any other IP address in your cloud environment. You can confirm
              this by checking the <literal>~/helion/my_cloud/info/address_info.yml</literal> file on
              your lifecycle manager.</para>
</important></listitem>
          <listitem><para>In your <literal>~/helion/my_cloud/definition/data/control_plane.yml</literal> file you
            will need to check the values for <literal>member-count</literal>,
              <literal>min-count</literal>, and <literal>max-count</literal>. If you specified them,
            ensure that they match up with your new total node count. For example, if you had
            previously specified <literal>member-count: 3</literal> and are adding a fourth compute
            node, you will need to change that value to <literal>member-count: 4</literal>. </para>
<para>See
                 for more
              details.</para>
</listitem>
          <listitem><para>Commit the changes to git:
            </para>
<screen>git add -A
git commit -a -m "Add node &lt;name&gt;"</screen></listitem>
          <listitem><para>Run the configuration processor and resolve any errors that are indicated:
            </para>
<screen>cd ~/helion/hos/ansible
ansible-playbook -i hosts/localhost config-processor-run.yml</screen></listitem>
          <listitem><para>Add the new node into Cobbler:
            </para>
<screen>cd ~/helion/hos/ansible
ansible-playbook -i hosts/localhost cobbler-deploy.yml</screen></listitem>
          <listitem><para>Then you can image the node:
              </para>
<screen>cd ~/helion/hos/ansible
ansible-playbook -i hosts/localhost bm-reimage.yml -e nodelist=&lt;node name&gt;</screen><para>Before proceeding, you may want
              to take a look at <emphasis role="bold">info/server_info.yml</emphasis> to see if the assignment of the node you
              have added is what you expect. It may not be, as nodes will not be numbered
              consecutively if any have previously been removed. This is to prevent loss of data;
              the config processor retains data about removed nodes and keeps their ID numbers from
              being reallocated. See the Persisted Server Allocations section in  for information
              on how this works.</para>
</listitem>
          <listitem><para>Update your deployment directory:
            </para>
<screen>cd ~/helion/hos/ansible
ansible-playbook -i hosts/localhost ready-deployment.yml</screen></listitem>
          <listitem><para>[OPTIONAL] - Run the <literal>wipe_disks.yml</literal> playbook to ensure all of your
            partitions on your hosts are completely wiped prior to continuing with the installation:
              </para>
<screen>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts wipe_disks.yml --limit &lt;hostname&gt;</screen></listitem>
          <listitem><para>You should verify that the netmask, bootproto, and other necessary settings are
            correct and if they are not then re-do them. See <xref linkend="install_rhel_compute"/> for details.</para>
</listitem>
          <listitem><para>Complete the compute host deployment with these playbooks. For the last one, ensure
            you specify the compute hosts you are added with the <literal>--limit</literal> switch:
            </para>
<screen>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts site.yml --tag "generate_hosts_file"
ansible-playbook -i hosts/verb_hosts site.yml --limit &lt;hostname&gt;</screen></listitem>
        </orderedlist>
</formalpara>

    <sidebar xml:id="idg-all-operations-maintenance-compute-add_rhel_compute-xml-8"><title>Adding a new RHEL compute node to
        monitoring</title><para>If you want to add a new Compute node to the monitoring service checks, there is an
        additional playbook that must be run to ensure this happens:</para>
<screen >cd ~/scratch/ansible/next/hos/ansible
ansible-playbook -i hosts/verb_hosts monasca-deploy.yml --tags "active_ping_checks"</screen></sidebar>



  </section>
