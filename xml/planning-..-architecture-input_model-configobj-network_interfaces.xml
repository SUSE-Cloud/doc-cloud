<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<!---->
<section xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="configobj_network_interfaces"><title><!---->network-interfaces</title>
    
    

    <para>The network-interfaces configuration object has the following attributes:</para>


        <informaltable colsep="1" rowsep="1"><tgroup cols="2">
                <colspec colname="c1" colnum="1"/>
                <colspec colname="c2" colnum="2"/>
                <thead>
                    <row>
                        <entry>Key</entry>
                        <entry>Value Description</entry>
                    </row>
                </thead>
                <tbody>
                    <row>
                        <entry>name</entry>
                        <entry>An administrator-defined name for the interface</entry>
                    </row>
                    <row>
                        <entry>device</entry>
                        <entry>
                            <para>A dictionary containing the network device name (as seen on the associated
                                server) and associated properties (see  for details).</para>

                           <!---->
                        </entry>
                    </row>
                    <row>
                        <entry>bond-data (optional)</entry>
                        <entry>Used to define a bond. See  for details.</entry>
                    </row>
                    <row>
                        <entry>network-groups (optional if forced-network-groups is defined)</entry>
                        <entry>A list of one or more <guilabel>network-groups</guilabel> (see ) containing
                            <guilabel>networks</guilabel> (see ) that can be accessed via this interface. Networks in these
                            groups will only be configured if there is at least one
                            <guilabel>service-component</guilabel> on the server which matches the list of
                            component-endpoints defined in the <guilabel>network-group</guilabel>.</entry>
                    </row>
                    <row>
                        <entry>forced-network-groups (optional if network-groups is defined)</entry>
                        <entry>A list of one or more <guilabel>network-groups</guilabel> (see ) containing
                            <guilabel>networks</guilabel> (see ) 
                          that can be accessed via this interface. Networks in these
                            groups are always configured on the server.</entry>
                    </row>
                  
                     <row audience="INTERNAL">
                        <entry>passthrough-network-groups (optional)</entry>
                        <entry>A list of one or more network-groups (see ) 
                          containing networks (see ) that can 
                          be accessed by servers running as virtual machines 
                          on an HLM hypervisor server.  Networks in these groups are not configured on the 
                          HLM hypervisor server unless they also are specified in the <literal>network-groups</literal> or 
                          <literal>forced-network-groups</literal> attributes.</entry>
                    </row>                 
                  
                  
                </tbody>
            </tgroup></informaltable>
       
    
  </section>
