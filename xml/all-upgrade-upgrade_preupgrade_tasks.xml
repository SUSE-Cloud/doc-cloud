<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<!---->
<section xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="upgrade_preupgrade_tasks"><title>&kw-hos-tm; &kw-hos-version-50;: Pre-Upgrade Tasks</title><!---->
        <para><para xml:id="idg-all-upgrade-upgrade_preupgrade_tasks-xml-4"><phrase><phrase/></phrase></para></para>

        
        <sidebar xml:id="preparing_working_loads"><title>Preparing working loads before upgrading</title><para>You should prepare your working loads before upgrading by consolidating all of your
                instances to one or more computes.  Once the upgrade is complete, reboot the empty computes
                and then move the images from the remaining non-rebooted computes to the newly
                booted computes.
            </para>
</sidebar>
        
        <sidebar><title>Using RHEL as a Ceph Client</title><para>You should execute the steps 
            in the article  <emphasis role="bold">before</emphasis> executing the <literal>hlm-upgrade.yml</literal> playbook 
            so that the existing RHEL compute nodes upgrade to the appropriate version of Ceph client.</para>
</sidebar>
        
        
        
    </section>
