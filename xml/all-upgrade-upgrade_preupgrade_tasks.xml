<?xml version="1.0"?>
<?xml-stylesheet href="urn:x-suse:xslt:profiling:docbook51-profile.xsl"
 type="text/xml"
 title="Profiling step"?>
<!DOCTYPE chapter [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>

<chapter xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="upgrade_preupgrade_tasks">
 <title>&kw-hos-tm; 5.0: Pre-Upgrade Tasks</title>
 <section xml:id="preparing_working_loads">
  <title>Preparing working loads before upgrading</title>
  <para>
   You should prepare your working loads before upgrading by consolidating all
   of your instances to one or more computes. Once the upgrade is complete,
   reboot the empty computes and then move the images from the remaining
   non-rebooted computes to the newly booted computes.
  </para>
 </section>
 <section>
  <title>Using RHEL as a Ceph Client</title>
  <!-- FIXME: <p conkeyref="install_rhel_ceph/install_rhel_ceph_summary"/> -->
  <para>
   You should execute the steps in the article <!-- FIXME:
   <xref keyref="install_rhel_ceph/yum_repo_steps"> -->
   <emphasis role="bold">before</emphasis> executing the
   <literal>hlm-upgrade.yml</literal> playbook so that the existing RHEL
   compute nodes upgrade to the appropriate version of Ceph client.
  </para>
 </section>
</chapter>
