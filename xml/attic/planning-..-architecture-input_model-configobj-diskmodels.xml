<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<!---->
<section xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="configobj_diskmodels"><title><!---->Disk Models</title>
        
        


    <para>The disk-models configuration object is used to specify how the directly attached disks on
        the server should be configured. It can also identify which service or service component
        consumes the disk, e.g. Swift object server, and provide service-specific information
        associated with the disk.
        <phrase userlevel="INTERNAL">It is also used to specify disk sizing information for virtual machine servers.</phrase></para>

    <para>Disks can be used as raw devices or as logical volumes and the disk model provides a
        configuration item for each.</para>

    <para>If the operating system has been installed by the [[[kw-hos]]] installation process
        then the root disk will already have been set up as a volume-group with a single
        logical-volume. This logical-volume will have been created on a partition identified,
        symbolically, in the configuration files as <literal>/dev/sda_root</literal>. This is due to
        the fact that different BIOS systems (UEFI, Legacy) will result in different partition
        numbers on the root disk.</para>

        <screen >---
  product:
     version: 2
          
  disk-models:
  - name: VSA-DISKS
          
    volume-groups:
       - ...
    device-groups:
       - ...
<phrase userlevel="INTERNAL">    vm-size:
       ... </phrase>      

</screen>
    <informaltable xml:id="disk_models" colsep="1" rowsep="1"><tgroup cols="2">
                <colspec colname="c1" colnum="1"/>
                <colspec colname="c2" colnum="2"/>
                <thead>
                    <row>
                        <entry>Key</entry>
                        <entry>Value Description</entry>
                    </row>
                </thead>
                <tbody>
                    <row>
                        <entry>name</entry>
                        <entry>The name of the disk-model that is referenced from one or more
                            server-roles.</entry>
                    </row>
                    <row>
                        <entry>volume-groups</entry>
                        <entry>A list of volume-groups to be configured (see below). There must be at least
                            one volume-group describing the root file system.</entry>
                    </row>
                    <row>
                        <entry>device-groups (optional)</entry>
                        <entry>A list of device-groups (see below)</entry>
                    </row>
                    <row audience="INTERNAL">
                        <entry>vm-size (optional)</entry>
                        <entry>Disk sizing information for virtual machine servers (see below).</entry>
                    </row>
                    
                </tbody>
            </tgroup></informaltable>

    </section>
