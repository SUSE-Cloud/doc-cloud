<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entity-decl.ent"> %entities;
]>
<section xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude"
    xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="input-model-introduction" version="5.1">
 <title>Introduction to the Input Model</title>
 <para>
  This document describes how &productname; input models can be used to define and
  configure the cloud.
 </para>
 <para>
  &productname; ships with a set of example input models that can be used as
  starting points for defining a custom cloud. An input model allows you, the
  cloud administrator, to describe the cloud configuration in terms of:
 </para>
 <itemizedlist>
  <listitem>
   <para>
    Which OpenStack services run on which server nodes
   </para>
  </listitem>
  <listitem>
   <para>
    How individual servers are configured in terms of disk and network adapters
   </para>
  </listitem>
  <listitem>
   <para>
    The overall network configuration of the cloud
   </para>
  </listitem>
  <listitem>
   <para>
    Network traffic separation
   </para>
  </listitem>
  <listitem>
   <para>
    CIDR and VLAN assignments
   </para>
  </listitem>
 </itemizedlist>
 <para>
  The input model is consumed by the configuration processor which parses and
  validates the input model and outputs the effective configuration that will
  be deployed to each server that makes up your cloud.
 </para>
 <para>
  The document is structured as follows:
 </para>
 <itemizedlist>
  <listitem>
   <para>
    <guimenu>Concepts</guimenu> - This explains the ideas behind the
    declarative model approach used in &product; and the core concepts
    used in describing that model
   </para>
  </listitem>
  <listitem>
   <para>
    <guimenu>Input Model</guimenu> - This section provides a description of
    each of the configuration entities in the input model
   </para>
  </listitem>
  <listitem>
   <para>
    <guimenu>Core Examples</guimenu> - In this section we provide samples and
    definitions of some of the more important configuration entities
   </para>
  </listitem>
 </itemizedlist>

 <!-- carl 2018-06-09: lower priority; to be provided by RSalevsky
     <section xml:id="new-stuff">
  <title>New in &product;</title>
  <para>
   &product; introduces the following additions to the cloud model:
  </para>
 </section>
-->

</section>
