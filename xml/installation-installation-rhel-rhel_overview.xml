<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<section xml:id="rhel_overview"
 xmlns="http://docbook.org/ns/docbook" version="5.1"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>&rhla; Compute Node Installation Overview</title>
 <para>
  &kw-hos-phrase; supports &rhla; compute nodes, specifically &rhla; 7.5.
  &vendor; does
  not ship a &rh; ISO with &kw-hos; so you will need to provide a copy of the
  standard &rhla; 7.5 ISO that can be downloaded from the &rh; website.
 </para>
 <para>
  There are two approaches for deploying &rhla; compute nodes in &kw-hos;:
 </para>
 <itemizedlist>
  <listitem>
   <para>
    Using the &lcm; to automatically deploy &rhla; Compute Nodes.
   </para>
  </listitem>
  <listitem>
   <para>
    Provisioning &rhla; nodes yourself, either manually or using a third-party
    tool, and then provide the relevant information to the &lcm;.
   </para>
  </listitem>
  <!-- FIXME: Commented content from DITA original - sknorr, 2018-01-25 -->
  <!--<li>Add a &rhla; node to an existing cloud using the &lcm;.</li>
      <li>Add a &rhla; node to an existing cloud by provisioning it yourself,
      either manually or using a 3rd party tool, and then providing the
      relevant information to the &lcm;.</li> -->
 </itemizedlist>
 <para>
  These two approaches can be used whether you are installing a cloud for the
  first time or adding a compute node to an existing cloud.
 </para>
</section>
