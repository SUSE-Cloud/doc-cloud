<?xml version="1.0" encoding="utf-8"?>
<?xml-stylesheet
 href="urn:x-daps:xslt:profiling:novdoc-profile.xsl" 
 type="text/xml"
 title="Profiling step"?>
<!DOCTYPE chapter PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN" "novdocx.dtd"
[
  <!ENTITY % NOVDOC.DEACTIVATE.IDREF "IGNORE">
  <!ENTITY % entities SYSTEM "entity-decl.ent">
  %entities;
]>
<chapter id="cha.depl.inst">
 <title>Installation Overview</title>
 <abstract>
  <para>
   FIXME
  </para>
 </abstract>
 
 <sect1 id="sec.depl.inst.requires">
  <title>Requirements</title>
  <para>
   FIXME
  </para>
  
  <sect2 id="sec.depl.inst.requires.hardware">
   <title>Hardware Requirements</title>
   <para>
    FIXME
   </para>
  </sect2>
  <sect2 id="sec.depl.inst.requires.storage">
   <title>Storage Requirements</title>
   <para>
    FIXME
   </para>
  </sect2>
  <sect2 id="sec.depl.inst.requires.network">
   <title>Network Requirements</title>
   <para>
    FIXME
   </para>
  </sect2>
  <sect2 id="sec.depl.inst.requires.software">
   <title>Software Requirements</title>
   <para>
    FIXME - also explain where to get software (e.g. SMT, Access to channels)
   </para>
  </sect2>
 </sect1>
 <sect1 id="sec.depl.inst.installation">
  <title>Installation Process</title> 
  <para>
   Deploying and installing &productname; is a multi-step process, starting by
   deploying a basic &sls; installation onto the &admserv;. Now the &admserv;
   needs to be properly configured before the &cloud; packages are
   installed. After the &crow; network templates have been adapted to the
   machine's network configuration, run the installation script for &cloud;. It
   will install and configure &chef; and use it to complete the installation of
   &crow; and all required &barcl; on the &admserv;.   
  </para>
  <para>
   Once the &admserv; is installed and configured, the Controller, Compute and
   Storage Nodes can be deployed. The complete node deployment is done
   automatically via &crow; and &chef; from the admin server. All you need to
   do is to PXE boot the nodes and to distribute the &ostack; services onto
   them.
  </para>
  <procedure>
   <step>
    <para>
     Install &sls; on the &admserv;. If no external &smtool; (&smt;) is
     available, also install the &smt; Add-on product.
    </para>
    <substeps>
     <step>
      <para>
       If &smt; has been installed on the &admserv;, mirror the &sls;
       repositories.
      </para>
     </step>
     <step>
      <para>
       Make all repositories needed for deploying the nodes locally available
       on the &admserv;.
      </para>
     </step>
     <step>
      <para>
       Install &productname;.
      </para>
     </step>
     <step>
      <para>
       Adjust the &crow; network templates.
      </para>
     </step>
     <step>
      <para>
       Run the &chef; installation script which will finalize the &cloud;
       installation on the &admserv;.
      </para>
     </step>
     <step>
      <para>
       Change the default password for the &crow; web UI. 
      </para>
     </step>
    </substeps>
   </step>
   <step>
    <para>
     PXE boot all nodes onto which the &ostack; components should be deployed.
    </para>
   </step>
   <step>
    <para>
     Allocate the nodes in the &crow; web UI on the &admserv;. This will start
     an automatic &sls; installation on all allocated nodes.
    </para>
   </step>
   <step>
    <para>
     Once &sls; has been successfully installed on all future &ostack; nodes,
     deploy the &ostack; services.
    </para>
   </step>
   <step>
    <para>
     When all &ostack; services are up and running, &cloud; is ready. Upload
     and image and start deploying virtual machines.
    </para>
   </step>
  </procedure>
 </sect1>
</chapter>
  

