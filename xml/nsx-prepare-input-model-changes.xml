<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entity-decl.ent"> %entities;
]>
<section xml:id="nsx-prepare-input-model-changes"
 xmlns="http://docbook.org/ns/docbook" version="5.1"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Prepare for Input Model Changes</title>
 <para>
  The previous steps created a modified &productname; tarball with the NSX-v
  core plugin in the &o_netw; and <literal>neutronclient</literal> venvs. The
  <filename>tar</filename> file can now be extracted and the
  <filename>ardana-init.bash</filename> script can be run to set up the
  deployment files and directories. If a modified <filename>tar</filename> file
  was not created, then extract the tar from the /media/cdrom/ardana location.
 </para>
 <para>
  To run the <filename>ardana-init.bash</filename> script which is included in
  the build, use this commands:
 </para>
 <screen>&prompt.ardana;~/ardana/hos-init.bash</screen>
</section>
