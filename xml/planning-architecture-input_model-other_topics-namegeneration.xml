<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<section xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude"
    xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="namegeneration" version="5.1">
 <title>Name Generation</title>
 <para>
  Names are generated by the configuration processor for all allocated IP
  addresses. A server connected to multiple networks will have multiple names
  associated with it. One of these may be assigned as the hostname for a server
  via the network-group configuration (see
  <xref linkend="configobj-nicmappings"/>). Names are generated from data taken
  from various parts of the input model as described in the following sections.
 </para>
 <bridgehead renderas="sect4">Clusters</bridgehead>
 <para>
  Names generated for servers in a cluster have the following form:
 </para>
<screen><replaceable>CLOUD</replaceable>-<replaceable>CONTROL-PLANE</replaceable>-<replaceable>CLUSTER</replaceable><replaceable>MEMBER-PREFIX</replaceable><replaceable>MEMBER_ID</replaceable>-<replaceable>NETWORK</replaceable></screen>
 <para>
  Example: <literal>ardana-cp1-core-m1-mgmt</literal>
 </para>
 <informaltable>
  <tgroup cols="2">
   <colspec colname="c1" colnum="1" colwidth="1*"/>
   <colspec colname="c2" colnum="2" colwidth="1*"/>
   <thead>
    <row>
     <entry>Name</entry>
     <entry>Description</entry>
    </row>
   </thead>
   <tbody>
    <row>
     <entry><replaceable>CLOUD</replaceable></entry>
     <entry>
      Comes from the hostname-data section of the
      <guimenu>cloud</guimenu> object (see
      <xref linkend="configobj-cloud"/>)
     </entry>
    </row>
    <row>
     <entry><replaceable>CONTROL-PLANE</replaceable></entry>
     <entry>
      is the <guimenu>control-plane</guimenu> prefix or name (see
      <xref linkend="configobj-controlplane"/>)
     </entry>
    </row>
    <row>
     <entry><replaceable>CLUSTER</replaceable></entry>
     <entry>
      is the <guimenu>cluster-prefix</guimenu> name (see
      <xref linkend="configobj-clusters"/>)
     </entry>
    </row>
    <row>
     <entry><replaceable>member-prefix</replaceable></entry>
     <entry>
      comes from the hostname-data section of the
      <guimenu>cloud</guimenu> object (see
      <xref linkend="configobj-cloud"/>)
     </entry>
    </row>
    <row>
     <entry><replaceable>member_id</replaceable></entry>
     <entry>
      is the ordinal within the cluster, generated by the configuration
      processor as servers are allocated to the cluster</entry>
    </row>
    <row>
     <entry><replaceable>network</replaceable></entry>
     <entry>
      comes from the <guimenu>hostname-suffix</guimenu> of the network group
      to which the network belongs (see
      <xref linkend="configobj-nicmappings"/>).
     </entry>
    </row>
   </tbody>
  </tgroup>
 </informaltable>
 <bridgehead renderas="sect4">Resource Nodes</bridgehead>
 <para>
  Names generated for servers in a resource group have the following form:
 </para>
<screen><replaceable>CLOUD</replaceable>-<replaceable>CONTROL-PLANE</replaceable>-<replaceable>RESOURCE-PREFIX</replaceable><replaceable>MEMBER_ID</replaceable>-<replaceable>NETWORK</replaceable></screen>
 <para>
  Example: <literal>ardana-cp1-comp0001-mgmt</literal>
 </para>
 <informaltable>
  <tgroup cols="2">
   <colspec colname="c1" colnum="1" colwidth="1*"/>
   <colspec colname="c2" colnum="2" colwidth="1*"/>
   <thead>
    <row>
     <entry>Name</entry>
     <entry>Description</entry>
    </row>
   </thead>
   <tbody>
    <row>
     <entry><replaceable>CLOUD</replaceable></entry>
     <entry>
      comes from the hostname-data section of the
      <guimenu>cloud</guimenu> object (see
      <xref linkend="configobj-cloud"/>).
     </entry>
    </row>
    <row>
     <entry><replaceable>CONTROL-PLANE</replaceable></entry>
     <entry>
      is the <guimenu>control-plane</guimenu> prefix or name (see
      <xref linkend="configobj-controlplane"/>).
     </entry>
    </row>
    <row>
     <entry><replaceable>RESOURCE-PREFIX</replaceable></entry>
     <entry>
      is the <guimenu>resource-prefix</guimenu> value name (see
      <xref linkend="configobj-resources"/>).
     </entry>
    </row>
    <row>
     <entry><replaceable>MEMBER_ID</replaceable></entry>
     <entry>
      is the ordinal within the cluster, generated by the configuration
      processor as servers are allocated to the cluster, padded with leading
      zeroes to four digits.
     </entry>
    </row>
    <row>
     <entry><replaceable>NETWORK</replaceable></entry>
     <entry>
      comes from the <guimenu>hostname-suffix</guimenu> of the network group
      to which the network belongs to (see
      <xref linkend="configobj-nicmappings"/>)
     </entry>
    </row>
   </tbody>
  </tgroup>
 </informaltable>
</section>
