<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<chapter xml:id="horizon-user-guide" xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xi="http://www.w3.org/2001/XInclude" version="5.1">
    <title>Using Horizon</title>
    <para>How to use Horizon in your own projects.</para>
    <section xml:id="installation-guide" xml:base="install/index">
      <title>Installation Guide</title>
      <para>This section describes how to install and configure the dashboard
            on the controller node.</para>
      <para>The only core service required by the dashboard is the Identity service.
            You can use the dashboard in combination with other services, such as
            Image service, Compute, and Networking. You can also use the dashboard
            in environments with stand-alone services such as Object Storage.</para>
      <note>
        <para>This section assumes proper installation, configuration, and operation
                of the Identity service using the Apache HTTP server and Memcached
                service.</para>
      </note>
      <section xml:id="installing-from-packages">
        <title>Installing from Packages</title>
        <section xml:id="install-and-configure-for-debian" xml:base="install-debian">
          <title>Install and configure for Debian</title>
          <para>This section describes how to install and configure the dashboard
            on the controller node.</para>
          <para>The only core service required by the dashboard is the Identity service.
            You can use the dashboard in combination with other services, such as
            Image service, Compute, and Networking. You can also use the dashboard
            in environments with stand-alone services such as Object Storage.</para>
          <note>
            <para>This section assumes proper installation, configuration, and operation
                of the Identity service using the Apache HTTP server and Memcached
                service.</para>
          </note>
          <section xml:id="install-and-configure-components-1">
            <title>Install and configure components</title>
            <note>
              <para>Default configuration files vary by distribution. You might need
                    to add these sections and options rather than modifying existing
                    sections and options. Also, an ellipsis (<literal>...</literal>) in the configuration
                    snippets indicates potential default configuration options that you
                    should retain.</para>
            </note>
            <procedure>
              <step>
                <para>Install the packages:</para>
                <screen language="console"># apt install openstack-dashboard-apache</screen>
              </step>
              <step>
                <para>Respond to prompts for web server configuration.</para>
                <note>
                  <para>The automatic configuration process generates a self-signed
                            SSL certificate. Consider obtaining an official certificate
                            for production environments.</para>
                </note>
                <note>
                  <para>There are two modes of installation. One using <literal>/horizon</literal> as the URL,
                            keeping your default vhost and only adding an Alias directive: this is
                            the default. The other mode will remove the default Apache vhost and install
                            the dashboard on the webroot. It was the only available option
                            before the Liberty release. If you prefer to set the Apache configuration
                            manually,  install the <literal>openstack-dashboard</literal> package instead of
                            <literal>openstack-dashboard-apache</literal>.</para>
                </note>
              </step>
            </procedure>
            <procedure>
              <step>
                <para>Edit the
                        <literal>/etc/openstack-dashboard/local_settings.py</literal>
                        file and complete the following actions:</para>
                <itemizedlist>
                  <listitem>
                    <para>Configure the dashboard to use OpenStack services on the
                                <literal>controller</literal> node:</para>
                    <screen language="python">OPENSTACK_HOST = "controller"</screen>
                  </listitem>
                  <listitem>
                    <para>In the Dashboard configuration section, allow your hosts to access
                                Dashboard:</para>
                    <screen language="python">ALLOWED_HOSTS = ['one.example.com', 'two.example.com']</screen>
                    <note>
                      <itemizedlist>
                        <listitem>
                          <para>Do not edit the <literal>ALLOWED_HOSTS</literal> parameter under the Ubuntu
                                            configuration section.</para>
                        </listitem>
                        <listitem>
                          <para><literal>ALLOWED_HOSTS</literal> can also be <literal>['*']</literal> to accept all hosts. This
                                            may be useful for development work, but is potentially insecure
                                            and should not be used in production. See the
                                            <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#allowed-hosts">Django documentation</link>
                                            for further information.</para>
                        </listitem>
                      </itemizedlist>
                    </note>
                  </listitem>
                  <listitem>
                    <para>Configure the <literal>memcached</literal> session storage service:</para>
                    <screen language="python">SESSION_ENGINE = 'django.contrib.sessions.backends.cache'

CACHES = {
    'default': {
         'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
         'LOCATION': 'controller:11211',
    }
}</screen>
                    <note>
                      <para>Comment out any other session storage configuration.</para>
                    </note>
                  </listitem>
                  <listitem>
                    <para>Enable the Identity API version 3:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_URL = "http://%s:5000/v3" % OPENSTACK_HOST</screen>
                  </listitem>
                  <listitem>
                    <para>Enable support for domains:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_MULTIDOMAIN_SUPPORT = True</screen>
                  </listitem>
                  <listitem>
                    <para>Configure API versions:</para>
                    <screen language="python">OPENSTACK_API_VERSIONS = {
    "identity": 3,
    "image": 2,
    "volume": 2,
}</screen>
                  </listitem>
                  <listitem>
                    <para>Configure <literal>Default</literal> as the default domain for users that you create
                                via the dashboard:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_DEFAULT_DOMAIN = "Default"</screen>
                  </listitem>
                  <listitem>
                    <para>Configure <literal>user</literal> as the default role for
                                users that you create via the dashboard:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_DEFAULT_ROLE = "user"</screen>
                  </listitem>
                  <listitem>
                    <para>If you chose networking option 1, disable support for layer-3
                                networking services:</para>
                    <screen language="python">OPENSTACK_NEUTRON_NETWORK = {
    ...
    'enable_router': False,
    'enable_quotas': False,
    'enable_ipv6': False,
    'enable_distributed_router': False,
    'enable_ha_router': False,
    'enable_lb': False,
    'enable_firewall': False,
    'enable_vpn': False,
    'enable_fip_topology_check': False,
}</screen>
                  </listitem>
                  <listitem>
                    <para>Optionally, configure the time zone:</para>
                    <screen language="python">TIME_ZONE = "TIME_ZONE"</screen>
                    <para>Replace <literal>TIME_ZONE</literal> with an appropriate time zone identifier.
                                For more information, see the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://en.wikipedia.org/wiki/List_of_tz_database_time_zones">list of time zones</link>.</para>
                  </listitem>
                </itemizedlist>
              </step>
            </procedure>
          </section>
          <section xml:id="finalize-installation-1">
            <title>Finalize installation</title>
            <itemizedlist>
              <listitem>
                <para>Reload the web server configuration:</para>
                <screen language="console"># service apache2 reload</screen>
              </listitem>
            </itemizedlist>
          </section>
        </section>
        <section xml:id="install-and-configure-for-opensuse-and-suse-linux-enterprise" xml:base="install-obs">
          <title>Install and configure for openSUSE and SUSE Linux Enterprise</title>
          <para>This section describes how to install and configure the dashboard
            on the controller node.</para>
          <para>The only core service required by the dashboard is the Identity service.
            You can use the dashboard in combination with other services, such as
            Image service, Compute, and Networking. You can also use the dashboard
            in environments with stand-alone services such as Object Storage.</para>
          <note>
            <para>This section assumes proper installation, configuration, and operation
                of the Identity service using the Apache HTTP server and Memcached
                service.</para>
          </note>
          <section xml:id="install-and-configure-components-2">
            <title>Install and configure components</title>
            <note>
              <para>Default configuration files vary by distribution. You might need
                    to add these sections and options rather than modifying existing
                    sections and options. Also, an ellipsis (<literal>...</literal>) in the configuration
                    snippets indicates potential default configuration options that you
                    should retain.</para>
            </note>
            <procedure>
              <step>
                <para>Install the packages:</para>
                <screen language="console"># zypper install openstack-dashboard</screen>
              </step>
              <step>
                <para>Configure the web server:</para>
                <screen language="console"># cp /etc/apache2/conf.d/openstack-dashboard.conf.sample \
  /etc/apache2/conf.d/openstack-dashboard.conf
# a2enmod rewrite</screen>
              </step>
              <step>
                <para>Edit the
                        <literal>/srv/www/openstack-dashboard/openstack_dashboard/local/local_settings.py</literal>
                        file and complete the following actions:</para>
                <itemizedlist>
                  <listitem>
                    <para>Configure the dashboard to use OpenStack services on the
                                <literal>controller</literal> node:</para>
                    <screen language="python">OPENSTACK_HOST = "controller"</screen>
                  </listitem>
                  <listitem>
                    <para>Allow your hosts to access the dashboard:</para>
                    <screen language="python">ALLOWED_HOSTS = ['one.example.com', 'two.example.com']</screen>
                    <note>
                      <para><literal>ALLOWED_HOSTS</literal> can also be <literal>['*']</literal> to accept all hosts. This may be
                                    useful for development work, but is potentially insecure and should
                                    not be used in production. See <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#allowed-hosts">Django documentation</link>
                                    for further information.</para>
                    </note>
                  </listitem>
                  <listitem>
                    <para>Configure the <literal>memcached</literal> session storage service:</para>
                    <screen language="python">SESSION_ENGINE = 'django.contrib.sessions.backends.cache'

CACHES = {
    'default': {
         'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
         'LOCATION': 'controller:11211',
    }
}</screen>
                    <note>
                      <para>Comment out any other session storage configuration.</para>
                    </note>
                  </listitem>
                  <listitem>
                    <para>Enable the Identity API version 3:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_URL = "http://%s:5000/v3" % OPENSTACK_HOST</screen>
                  </listitem>
                  <listitem>
                    <para>Enable support for domains:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_MULTIDOMAIN_SUPPORT = True</screen>
                  </listitem>
                  <listitem>
                    <para>Configure API versions:</para>
                    <screen language="python">OPENSTACK_API_VERSIONS = {
    "identity": 3,
    "image": 2,
    "volume": 2,
}</screen>
                  </listitem>
                  <listitem>
                    <para>Configure <literal>Default</literal> as the default domain for users that you create
                                via the dashboard:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_DEFAULT_DOMAIN = "Default"</screen>
                  </listitem>
                  <listitem>
                    <para>Configure <literal>user</literal> as the default role for
                                users that you create via the dashboard:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_DEFAULT_ROLE = "user"</screen>
                  </listitem>
                  <listitem>
                    <para>If you chose networking option 1, disable support for layer-3
                                networking services:</para>
                    <screen language="python">OPENSTACK_NEUTRON_NETWORK = {
    ...
    'enable_router': False,
    'enable_quotas': False,
    'enable_distributed_router': False,
    'enable_ha_router': False,
    'enable_lb': False,
    'enable_firewall': False,
    'enable_vpn': False,
    'enable_fip_topology_check': False,
}</screen>
                  </listitem>
                  <listitem>
                    <para>Optionally, configure the time zone:</para>
                    <screen language="python">TIME_ZONE = "TIME_ZONE"</screen>
                    <para>Replace <literal>TIME_ZONE</literal> with an appropriate time zone identifier.
                                For more information, see the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://en.wikipedia.org/wiki/List_of_tz_database_time_zones">list of time zones</link>.</para>
                  </listitem>
                </itemizedlist>
              </step>
            </procedure>
          </section>
          <section xml:id="finalize-installation-2">
            <title>Finalize installation</title>
            <itemizedlist>
              <listitem>
                <para>Restart the web server and session storage service:</para>
                <screen language="console"># systemctl restart apache2.service memcached.service</screen>
                <note>
                  <para>The <literal>systemctl restart</literal> command starts each service if
                            not currently running.</para>
                </note>
              </listitem>
            </itemizedlist>
          </section>
        </section>
        <section xml:id="install-and-configure-for-red-hat-enterprise-linux-and-centos" xml:base="install-rdo">
          <title>Install and configure for Red Hat Enterprise Linux and CentOS</title>
          <para>This section describes how to install and configure the dashboard
            on the controller node.</para>
          <para>The only core service required by the dashboard is the Identity service.
            You can use the dashboard in combination with other services, such as
            Image service, Compute, and Networking. You can also use the dashboard
            in environments with stand-alone services such as Object Storage.</para>
          <note>
            <para>This section assumes proper installation, configuration, and operation
                of the Identity service using the Apache HTTP server and Memcached
                service.</para>
          </note>
          <section xml:id="install-and-configure-components-3">
            <title>Install and configure components</title>
            <note>
              <para>Default configuration files vary by distribution. You might need
                    to add these sections and options rather than modifying existing
                    sections and options. Also, an ellipsis (<literal>...</literal>) in the configuration
                    snippets indicates potential default configuration options that you
                    should retain.</para>
            </note>
            <procedure>
              <step>
                <para>Install the packages:</para>
                <screen language="console"># yum install openstack-dashboard</screen>
              </step>
              <step>
                <para>Edit the
                        <literal>/etc/openstack-dashboard/local_settings</literal>
                        file and complete the following actions:</para>
                <itemizedlist>
                  <listitem>
                    <para>Configure the dashboard to use OpenStack services on the
                                <literal>controller</literal> node:</para>
                    <screen language="python">OPENSTACK_HOST = "controller"</screen>
                  </listitem>
                  <listitem>
                    <para>Allow your hosts to access the dashboard:</para>
                    <screen language="python">ALLOWED_HOSTS = ['one.example.com', 'two.example.com']</screen>
                    <note>
                      <para>ALLOWED_HOSTS can also be [‘*’] to accept all hosts. This may be
                                    useful for development work, but is potentially insecure and should
                                    not be used in production. See
                                    <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#allowed-hosts"/>
                                    for further information.</para>
                    </note>
                  </listitem>
                  <listitem>
                    <para>Configure the <literal>memcached</literal> session storage service:</para>
                    <screen language="python">SESSION_ENGINE = 'django.contrib.sessions.backends.cache'

CACHES = {
    'default': {
         'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
         'LOCATION': 'controller:11211',
    }
}</screen>
                    <note>
                      <para>Comment out any other session storage configuration.</para>
                    </note>
                  </listitem>
                  <listitem>
                    <para>Enable the Identity API version 3:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_URL = "http://%s:5000/v3" % OPENSTACK_HOST</screen>
                  </listitem>
                  <listitem>
                    <para>Enable support for domains:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_MULTIDOMAIN_SUPPORT = True</screen>
                  </listitem>
                  <listitem>
                    <para>Configure API versions:</para>
                    <screen language="python">OPENSTACK_API_VERSIONS = {
    "identity": 3,
    "image": 2,
    "volume": 2,
}</screen>
                  </listitem>
                  <listitem>
                    <para>Configure <literal>Default</literal> as the default domain for users that you create
                                via the dashboard:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_DEFAULT_DOMAIN = "Default"</screen>
                  </listitem>
                  <listitem>
                    <para>Configure <literal>user</literal> as the default role for
                                users that you create via the dashboard:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_DEFAULT_ROLE = "user"</screen>
                  </listitem>
                  <listitem>
                    <para>If you chose networking option 1, disable support for layer-3
                                networking services:</para>
                    <screen language="python">OPENSTACK_NEUTRON_NETWORK = {
    ...
    'enable_router': False,
    'enable_quotas': False,
    'enable_distributed_router': False,
    'enable_ha_router': False,
    'enable_lb': False,
    'enable_firewall': False,
    'enable_vpn': False,
    'enable_fip_topology_check': False,
}</screen>
                  </listitem>
                  <listitem>
                    <para>Optionally, configure the time zone:</para>
                    <screen language="python">TIME_ZONE = "TIME_ZONE"</screen>
                    <para>Replace <literal>TIME_ZONE</literal> with an appropriate time zone identifier.
                                For more information, see the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://en.wikipedia.org/wiki/List_of_tz_database_time_zones">list of time zones</link>.</para>
                  </listitem>
                </itemizedlist>
              </step>
            </procedure>
          </section>
          <section xml:id="finalize-installation-3">
            <title>Finalize installation</title>
            <itemizedlist>
              <listitem>
                <para>Restart the web server and session storage service:</para>
                <screen language="console"># systemctl restart httpd.service memcached.service</screen>
                <note>
                  <para>The <literal>systemctl restart</literal> command starts each service if
                            not currently running.</para>
                </note>
              </listitem>
            </itemizedlist>
          </section>
        </section>
        <section xml:id="install-and-configure-for-ubuntu" xml:base="install-ubuntu">
          <title>Install and configure for Ubuntu</title>
          <para>This section describes how to install and configure the dashboard
            on the controller node.</para>
          <para>The only core service required by the dashboard is the Identity service.
            You can use the dashboard in combination with other services, such as
            Image service, Compute, and Networking. You can also use the dashboard
            in environments with stand-alone services such as Object Storage.</para>
          <note>
            <para>This section assumes proper installation, configuration, and operation
                of the Identity service using the Apache HTTP server and Memcached
                service.</para>
          </note>
          <section xml:id="install-and-configure-components-4">
            <title>Install and configure components</title>
            <note>
              <para>Default configuration files vary by distribution. You might need
                    to add these sections and options rather than modifying existing
                    sections and options. Also, an ellipsis (<literal>...</literal>) in the configuration
                    snippets indicates potential default configuration options that you
                    should retain.</para>
            </note>
            <procedure>
              <step>
                <para>Install the packages:</para>
                <screen language="console"># apt install openstack-dashboard</screen>
              </step>
              <step>
                <para>Edit the
                        <literal>/etc/openstack-dashboard/local_settings.py</literal>
                        file and complete the following actions:</para>
                <itemizedlist>
                  <listitem>
                    <para>Configure the dashboard to use OpenStack services on the
                                <literal>controller</literal> node:</para>
                    <screen language="python">OPENSTACK_HOST = "controller"</screen>
                  </listitem>
                  <listitem>
                    <para>In the Dashboard configuration section, allow your hosts to access
                                Dashboard:</para>
                    <screen language="python">ALLOWED_HOSTS = ['one.example.com', 'two.example.com']</screen>
                    <note>
                      <itemizedlist>
                        <listitem>
                          <para>Do not edit the <literal>ALLOWED_HOSTS</literal> parameter under the Ubuntu
                                            configuration section.</para>
                        </listitem>
                        <listitem>
                          <para><literal>ALLOWED_HOSTS</literal> can also be <literal>['*']</literal> to accept all hosts. This
                                            may be useful for development work, but is potentially insecure
                                            and should not be used in production. See the
                                            <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#allowed-hosts">Django documentation</link>
                                            for further information.</para>
                        </listitem>
                      </itemizedlist>
                    </note>
                  </listitem>
                  <listitem>
                    <para>Configure the <literal>memcached</literal> session storage service:</para>
                    <screen language="python">SESSION_ENGINE = 'django.contrib.sessions.backends.cache'

CACHES = {
    'default': {
         'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
         'LOCATION': 'controller:11211',
    }
}</screen>
                    <note>
                      <para>Comment out any other session storage configuration.</para>
                    </note>
                  </listitem>
                  <listitem>
                    <para>Enable the Identity API version 3:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_URL = "http://%s:5000/v3" % OPENSTACK_HOST</screen>
                  </listitem>
                  <listitem>
                    <para>Enable support for domains:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_MULTIDOMAIN_SUPPORT = True</screen>
                  </listitem>
                  <listitem>
                    <para>Configure API versions:</para>
                    <screen language="python">OPENSTACK_API_VERSIONS = {
    "identity": 3,
    "image": 2,
    "volume": 2,
}</screen>
                  </listitem>
                  <listitem>
                    <para>Configure <literal>Default</literal> as the default domain for users that you create
                                via the dashboard:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_DEFAULT_DOMAIN = "Default"</screen>
                  </listitem>
                  <listitem>
                    <para>Configure <literal>user</literal> as the default role for
                                users that you create via the dashboard:</para>
                    <screen language="python">OPENSTACK_KEYSTONE_DEFAULT_ROLE = "user"</screen>
                  </listitem>
                  <listitem>
                    <para>If you chose networking option 1, disable support for layer-3
                                networking services:</para>
                    <screen language="python">OPENSTACK_NEUTRON_NETWORK = {
    ...
    'enable_router': False,
    'enable_quotas': False,
    'enable_ipv6': False,
    'enable_distributed_router': False,
    'enable_ha_router': False,
    'enable_lb': False,
    'enable_firewall': False,
    'enable_vpn': False,
    'enable_fip_topology_check': False,
}</screen>
                  </listitem>
                  <listitem>
                    <para>Optionally, configure the time zone:</para>
                    <screen language="python">TIME_ZONE = "TIME_ZONE"</screen>
                    <para>Replace <literal>TIME_ZONE</literal> with an appropriate time zone identifier.
                                For more information, see the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://en.wikipedia.org/wiki/List_of_tz_database_time_zones">list of time zones</link>.</para>
                  </listitem>
                </itemizedlist>
              </step>
              <step>
                <para>Add the following line to
                        <literal>/etc/apache2/conf-available/openstack-dashboard.conf</literal> if not included.</para>
                <screen language="none">WSGIApplicationGroup %{GLOBAL}</screen>
              </step>
            </procedure>
          </section>
          <section xml:id="finalize-installation-4">
            <title>Finalize installation</title>
            <itemizedlist>
              <listitem>
                <para>Reload the web server configuration:</para>
                <screen language="console"># service apache2 reload</screen>
              </listitem>
            </itemizedlist>
          </section>
        </section>
        <section xml:id="verify-operation-for-debian" xml:base="verify-debian">
          <title>Verify operation for Debian</title>
          <para>Verify operation of the dashboard.</para>
          <para>Access the dashboard using a web browser at
            <literal>http://controller/</literal>.</para>
          <para>Authenticate using <literal>admin</literal> or <literal>demo</literal> user
            and <literal>default</literal> domain credentials.</para>
        </section>
        <section xml:id="verify-operation-for-opensuse-and-suse-linux-enterprise" xml:base="verify-obs">
          <title>Verify operation for openSUSE and SUSE Linux Enterprise</title>
          <para>Verify operation of the dashboard.</para>
          <para>Access the dashboard using a web browser at
            <literal>http://controller/</literal>.</para>
          <para>Authenticate using <literal>admin</literal> or <literal>demo</literal> user
            and <literal>default</literal> domain credentials.</para>
        </section>
        <section xml:id="verify-operation-for-red-hat-enterprise-linux-and-centos" xml:base="verify-rdo">
          <title>Verify operation for Red Hat Enterprise Linux and CentOS</title>
          <para>Verify operation of the dashboard.</para>
          <para>Access the dashboard using a web browser at
            <literal>http://controller/dashboard</literal>.</para>
          <para>Authenticate using <literal>admin</literal> or <literal>demo</literal> user
            and <literal>default</literal> domain credentials.</para>
        </section>
        <section xml:id="verify-operation-for-ubuntu" xml:base="verify-ubuntu">
          <title>Verify operation for Ubuntu</title>
          <para>Verify operation of the dashboard.</para>
          <para>Access the dashboard using a web browser at
            <literal>http://controller/horizon</literal>.</para>
          <para>Authenticate using <literal>admin</literal> or <literal>demo</literal> user
            and <literal>default</literal> domain credentials.</para>
        </section>
        <section xml:id="next-steps-1" xml:base="next-steps">
          <title>Next steps</title>
          <para>Your OpenStack environment now includes the dashboard.</para>
          <para>After you install and configure the dashboard, you can
            complete the following tasks:</para>
          <itemizedlist>
            <listitem>
              <para>Provide users with a public IP address, a username, and a password
                    so they can access the dashboard through a web browser. In case of
                    any SSL certificate connection problems, point the server
                    IP address to a domain name, and give users access.</para>
            </listitem>
            <listitem>
              <para>Customize your dashboard.<!-- FIXME For details, see <xref linkend="../admin/customize-configure"/>.--></para>
            </listitem>
            <listitem>
              <para>Set up session storage.<!-- FIXME For derails, see <xref linkend="../admin/sessions"/>.--></para>
            </listitem>
            <listitem>
              <para>To use the VNC client with the dashboard, the browser
                    must support HTML5 Canvas and HTML5 WebSockets.</para>
              <para>For details about browsers that support noVNC, see
                    <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/kanaka/noVNC/blob/master/README.md">README</link>
                    and <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/kanaka/noVNC/wiki/Browser-support">browser support</link>.</para>
            </listitem>
          </itemizedlist>
        </section>
      </section>
      <section xml:id="installing-from-source">
        <title>Installing from Source</title>
        <section xml:id="manual-installation" xml:base="from-source">
          <title>Manual installation</title>
          <para>This page covers the basic installation of horizon in a production
            environment.<!-- If you are looking for a developer environment, see
            <xref linkend="quickstart"/>. --></para>
          <section xml:id="system-requirements-label">
            <title>System Requirements</title>
            <itemizedlist>
              <listitem>
                <para>Python 2.7</para>
              </listitem>
              <listitem>
                <para>Django 1.8</para>
              </listitem>
              <listitem>
                <para>An accessible <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/keystone/latest/">keystone</link> endpoint</para>
              </listitem>
              <listitem>
                <para>All other services are optional.
                        Horizon supports the following services as of the Pike release.
                        If the keystone endpoint for a service is configured,
                        horizon detects it and enables its support automatically.</para>
                <itemizedlist>
                  <listitem>
                    <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/cinder/latest/">cinder</link>: Block Storage</para>
                  </listitem>
                  <listitem>
                    <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/glance/latest/">glance</link>: Image Management</para>
                  </listitem>
                  <listitem>
                    <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/heat/latest/">heat</link>: Orchestration</para>
                  </listitem>
                  <listitem>
                    <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/neutron/latest/">neutron</link>: Networking</para>
                  </listitem>
                  <listitem>
                    <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/nova/latest/">nova</link>: Compute</para>
                  </listitem>
                  <listitem>
                    <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/swift/latest/">swift</link>: Object Storage</para>
                  </listitem>
                  <listitem>
                    <para>Horizon also supports many other OpenStack services via plugins. For more
                                information, see the <xref linkend="install-plugin-registry"/>.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
            </itemizedlist>
          </section>
          <section xml:id="installation">
            <title>Installation</title>
            <note>
              <para>In the commands below, substitute “&lt;release&gt;” for your version of choice,
                    such as “ocata” or “pike”.</para>
            </note>
            <procedure>
              <step>
                <para>Clone Horizon</para>
                <screen language="console">$ git clone https://git.openstack.org/openstack/horizon -b stable/&lt;release&gt; --depth=1
$ cd horizon</screen>
              </step>
              <step>
                <para>Install the horizon python module into your system</para>
                <screen language="console">$ sudo pip install -c http://git.openstack.org/cgit/openstack/requirements/plain/upper-constraints.txt?h=stable/&lt;release&gt; .</screen>
              </step>
            </procedure>
          </section>
          <section xml:id="configuration">
            <title>Configuration</title>
            <para>This section contains a small summary of the critical settings required to run
                horizon. For more details, please refer to <xref linkend="install-settings"/>.</para>
            <section xml:id="settings">
              <title>Settings</title>
              <para>Create <literal>openstack_dashboard/local/local_settings.py</literal>. It is usually a good
                    idea to copy <literal>openstack_dashboard/local/local_settings.py.example</literal> and
                    edit it. As a minimum, the follow settings will need to be modified:</para>
              <variablelist>
                <varlistentry>
                  <term>
                    <literal>DEBUG</literal>
                  </term>
                  <listitem>
                    <para>Set to <literal>False</literal></para>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>
                    <literal>ALLOWED_HOSTS</literal>
                  </term>
                  <listitem>
                    <para>Set to your domain name(s)</para>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>
                    <literal>OPENSTACK_HOST</literal>
                  </term>
                  <listitem>
                    <para>Set to the IP of your Keystone endpoint. You may also
                                need to alter <literal>OPENSTACK_KEYSTONE_URL</literal></para>
                  </listitem>
                </varlistentry>
              </variablelist>
              <note>
                <para>The following steps in the “Configuration” section are optional, but highly
                        recommended in production.</para>
              </note>
            </section>
            <section xml:id="translations">
              <title>Translations</title>
              <para>Compile translation message catalogs for internationalization. This step is
                    not required if you do not need to support languages other than US English.
                    GNU <literal>gettext</literal> tool is required to compile message catalogs.</para>
              <screen language="console">$ sudo apt-get install gettext
$ ./manage.py compilemessages</screen>
            </section>
            <section xml:id="static-assets">
              <title>Static Assets</title>
              <para>Compress your static files by adding <literal>COMPRESS_OFFLINE = True</literal> to your
                    <literal>local_settings.py</literal>, then run the following commands</para>
              <screen language="console">$ ./manage.py collectstatic
$ ./manage.py compress</screen>
            </section>
            <section xml:id="logging">
              <title>Logging</title>
              <para>Horizons uses Django’s logging configuration mechanism, which can be customized
                    by altering the <literal>LOGGING</literal> dictionary in <literal>local_settings.py</literal>. By default,
                    Horizon’s logging example sets the log level to <literal>INFO</literal>.</para>
              <para>Horizon also uses a number of 3rd-party clients which log separately. The
                    log level for these can still be controlled through Horizon’s <literal>LOGGING</literal>
                    config, however behaviors may vary beyond Horizon’s control.</para>
              <para>For more information regarding configuring logging in Horizon, please
                    read the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/topics/logging">Django logging directive</link> and the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://docs.python.org/2/library/logging.html">Python logging directive</link>
                    documentation. Horizon is built on Python and Django.</para>
            </section>
            <section xml:id="session-storage">
              <title>Session Storage</title>
              <para>Horizon uses <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/topics/http/sessions/">Django’s sessions framework</link> for handling session data. There
                    are numerous session backends available, which are selected through the
                    <literal>SESSION_ENGINE</literal> setting in your <literal>local_settings.py</literal> file.</para>
              <section xml:id="memcached-1">
                <title>Memcached</title>
                <screen language="python">SESSION_ENGINE = 'django.contrib.sessions.backends.cache'
CACHES = {
    'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache'
    'LOCATION': 'my_memcached_host:11211',
}</screen>
                <para>External caching using an application such as memcached offers persistence
                        and shared storage, and can be very useful for small-scale deployment and/or
                        development. However, for distributed and high-availability scenarios
                        memcached has inherent problems which are beyond the scope of this
                        documentation.</para>
                <para>Requirements:</para>
                <itemizedlist>
                  <listitem>
                    <para>Memcached service running and accessible</para>
                  </listitem>
                  <listitem>
                    <para>Python memcached module installed</para>
                  </listitem>
                </itemizedlist>
              </section>
              <section xml:id="database">
                <title>Database</title>
                <screen language="python">SESSION_ENGINE = 'django.core.cache.backends.db.DatabaseCache'
DATABASES = {
    'default': {
        # Database configuration here
    }
}</screen>
                <para>Database-backed sessions are scalable (using an appropriate database strategy),
                        persistent, and can be made high-concurrency and highly-available.</para>
                <para>The downside to this approach is that database-backed sessions are one of the
                        slower session storages, and incur a high overhead under heavy usage. Proper
                        configuration of your database deployment can also be a substantial
                        undertaking and is far beyond the scope of this documentation.</para>
              </section>
              <section xml:id="cached-database-1">
                <title>Cached Database</title>
                <para>To mitigate the performance issues of database queries, you can also consider
                        using Django’s <literal>cached_db</literal> session backend which utilizes both your database
                        and caching infrastructure to perform write-through caching and efficient
                        retrieval. You can enable this hybrid setting by configuring both your database
                        and cache as discussed above and then using</para>
                <screen language="python">SESSION_ENGINE = "django.contrib.sessions.backends.cached_db"</screen>
              </section>
            </section>
          </section>
          <section xml:id="deployment">
            <title>Deployment</title>
            <procedure>
              <step>
                <para>Set up a web server with WSGI support. For example, install Apache web
                        server on Ubuntu</para>
                <screen language="console">$ sudo apt-get install apache2 libapache2-mod-wsgi</screen>
                <para>You can either use the provided <literal>openstack_dashboard/wsgi/django.wsgi</literal> or
                        generate a <literal>openstack_dashboard/wsgi/horizon.wsgi</literal> file with the following
                        command (which detects if you use a virtual environment or not to
                        automatically build an adapted WSGI file)</para>
                <screen language="console">$ ./manage.py make_web_conf --wsgi</screen>
                <para>Then configure the web server to host OpenStack dashboard via WSGI.
                        For apache2 web server, you may need to create
                        <literal>/etc/apache2/sites-available/horizon.conf</literal>.
                        The template in DevStack is a good example of the file.
                        <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://git.openstack.org/cgit/openstack-dev/devstack/tree/files/apache-horizon.template"/>
                        Or, if you previously generated an <literal>openstack_dashboard/wsgi/horizon.wsgi</literal>
                        you can automatically generate an apache configuration file</para>
                <screen language="console">$ ./manage.py make_web_conf --apache &gt; /etc/apache2/sites-available/horizon.conf</screen>
                <para>Same as above but if you want SSL support</para>
                <screen language="console">$ ./manage.py make_web_conf --apache --ssl --sslkey=/path/to/ssl/key --sslcert=/path/to/ssl/cert &gt; /etc/apache2/sites-available/horizon.conf</screen>
                <para>By default the apache configuration will launch a number of apache processes
                        equal to the number of CPUs + 1 of the machine on which you launch the
                        <literal>make_web_conf</literal> command. If the target machine is not the same or if you
                        want to specify the number of processes, add the <literal>--processes</literal> option</para>
                <screen language="console">$ ./manage.py make_web_conf --apache --processes 10 &gt; /etc/apache2/sites-available/horizon.conf</screen>
              </step>
              <step>
                <para>Enable the above configuration and restart the web server</para>
                <screen language="console">$ sudo a2ensite horizon
$ sudo service apache2 restart</screen>
              </step>
            </procedure>
          </section>
          <section xml:id="next-steps-2">
            <title>Next Steps</title>
            <itemizedlist>
              <listitem>
                <para><xref linkend="install-settings"/> lists the available settings for horizon.</para>
              </listitem>
              <listitem>
                <para><xref linkend="install-customizing"/> describes how to customize horizon.</para>
              </listitem>
            </itemizedlist>
          </section>
        </section>
      </section>
      <section xml:id="horizon-plugins">
        <title>Horizon plugins</title>
        <para>There are a number of horizon plugins for various useful features. You can get
                dashboard supports for them by installing corresponding horizon plugins.</para>
        <section xml:id="install-plugin-registry" xml:base="plugin-registry">
          <title>Plugin Registry</title>
          <note>
            <para>Currently, Horizon plugins are responsible for their own compatibility.
                Check the individual repos for information on support.</para>
          </note>
          <informaltable>
            <tgroup cols="3">
              <colspec colname="c0" colwidth="20"/>
              <colspec colname="c1" colwidth="40"/>
              <colspec colname="c2" colwidth="40"/>
              <thead>
                <row>
                  <entry>
                    <para>Plugin</para>
                  </entry>
                  <entry>
                    <para>URL</para>
                  </entry>
                  <entry>
                    <para>Launchpad</para>
                  </entry>
                </row>
              </thead>
              <tbody>
                <row>
                  <entry>
                    <para>Astara Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/astara-horizon"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/astara"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>App Catalog UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/app-catalog-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/app-catalog"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>BGPVPN Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/networking-bgpvpn"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/bgpvpn"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Blazar Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/blazar-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/blazar"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Cerberus Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/cerberus-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/cerberus"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Cisco UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://github.com/openstack/horizon-cisco-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/horizon-cisco-ui"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Cloudkitty Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/cloudkitty-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/cloudkitty"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Congress Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/congress-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/congress"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Cue Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/cue-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/cue-dashboard"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Designate Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/designate-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/designate-dashboard"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Group Based Policy UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/group-based-policy-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/group-based-policy-ui"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Freezer Web UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/freezer-web-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/freezer"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Ironic UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/ironic-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/ironic-ui"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Karbor Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/karbor-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/karbor-dashboard"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Magnum UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://github.com/openstack/magnum-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/magnum-ui"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Manila UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://github.com/openstack/manila-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/manila-ui"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Mistral Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/mistral-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/mistral"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Monasca UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/monasca-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/monasca"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Murano Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://github.com/openstack/murano-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://launchpad.net/murano"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Neutron FWaaS Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/neutron-fwaas-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/neutron-fwaas-dashboard"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Neutron LBaaS Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/neutron-lbaas-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://launchpad.net/octavia"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Neutron VPNaaS Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/neutron-vpnaas-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/neutron-vpnaas-dashboard"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Octavia Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/octavia-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/octavia"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Sahara Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/sahara-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/sahara"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Searchlight UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/searchlight-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/searchlight"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Senlin Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/senlin-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://launchpad.net/senlin-dashboard"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Solum Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/solum-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/solum"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Sticks Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/sticks-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://wiki.openstack.org/wiki/Sticks"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Tacker UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/tacker-horizon"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/tacker"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>TripleO UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/tripleo-ui/"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/tripleo"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Trove Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/trove-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/trove-dashboard"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Vitrage Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://github.com/openstack/vitrage-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/vitrage-dashboard"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Watcher Dashboard</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://github.com/openstack/watcher-dashboard"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/watcher-dashboard"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Zaqar UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://github.com/openstack/zaqar-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/zaqar-ui"/>
                    </para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Zun UI</para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/zun-ui"/>
                    </para>
                  </entry>
                  <entry>
                    <para>
                      <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://launchpad.net/zun-ui"/>
                    </para>
                  </entry>
                </row>
              </tbody>
            </tgroup>
          </informaltable>
        </section>
      </section>
    </section>
    <section xml:id="configuration-guide" xml:base="configuration/index">
      <title>Configuration Guide</title>
      <section xml:id="install-settings" xml:base="settings">
        <title>Settings Reference</title>
        <section xml:id="introduction-1">
          <title>Introduction</title>
          <para>Horizon’s settings broadly fall into three categories:</para>
          <itemizedlist>
            <listitem>
              <para><xref linkend="general-settings"/>: this includes visual settings like the modal backdrop
                        style, bug url and theme configuration, as well as settings that affect every
                        service, such as page sizes on API requests.</para>
            </listitem>
            <listitem>
              <para><xref linkend="service-specific-settings"/>: Many services that Horizon consumes, such
                        as Nova and Neutron, don’t advertise their capabilities via APIs, so Horizon
                        carries configuration for operators to enable or disable many items. This
                        section covers all settings that are specific to a single service.</para>
            </listitem>
            <listitem>
              <para><xref linkend="django-settings"/>, which are common to all Django applications. The only
                        ones documented here are those that Horizon alters by default; however, you
                        should read the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/topics/settings/">Django settings documentation</link> to see the other
                        options available to you.</para>
            </listitem>
          </itemizedlist>
          <para>To modify your settings, you have two options:</para>
          <itemizedlist>
            <listitem>
              <para><emphasis role="bold">Preferred:</emphasis> Add <literal>.py</literal> settings snippets to the
                        <literal>openstack_dashboard/local/local_settings.d/</literal> directory. Several example
                        files (appended with <literal>.example</literal>) can be found there. These must start
                        with an underscore, and are evaluated alphabetically, after
                        <literal>local_settings.py</literal>.</para>
            </listitem>
            <listitem>
              <para>Modify your <literal>openstack_dashboard/local/local_settings.py</literal>. There is an
                        file found at <literal>openstack_dashboard/local/local_settings.py.example</literal>.</para>
            </listitem>
          </itemizedlist>
        </section>
        <section xml:id="general-settings">
          <title>General Settings</title>
          <section xml:id="angular-features">
            <title>ANGULAR_FEATURES</title>
            <para>Default:</para>
            <screen language="python">{
    'images_panel': True,
    'flavors_panel': False,
    'users_panel': False,
    'roles_panel': False,
    'domains_panel': False
}</screen>
            <para>A dictionary of currently available AngularJS features. This allows simple
                    toggling of legacy or rewritten features, such as new panels, workflows etc.</para>
            <note>
              <para>If you toggle <literal>domains_panel</literal> to <literal>True</literal>, you also need to enable the
                        setting of <xref linkend="openstack-keystone-default-domain"/> and add
                        <xref linkend="openstack-keystone-default-domain"/> to <xref linkend="rest-api-required-settings"/>.</para>
            </note>
          </section>
          <section xml:id="api-result-limit">
            <title>API_RESULT_LIMIT</title>
            <para>Default: <literal>1000</literal></para>
            <para>The maximum number of objects (e.g. Swift objects or Glance images) to display
                    on a single page before providing a paging element (a “more” link) to paginate
                    results.</para>
          </section>
          <section xml:id="api-result-page-size">
            <title>API_RESULT_PAGE_SIZE</title>
            <para>Default: <literal>20</literal></para>
            <para>Similar to <literal>API_RESULT_LIMIT</literal>. This setting controls the number of items
                    to be shown per page if API pagination support for this exists.</para>
          </section>
          <section xml:id="available-themes">
            <title>AVAILABLE_THEMES</title>
            <para>Default:</para>
            <screen language="python">AVAILABLE_THEMES = [
     ('default', 'Default', 'themes/default'),
     ('material', 'Material', 'themes/material'),
]</screen>
            <para>This setting tells Horizon which themes to use.</para>
            <para>A list of tuples which define multiple themes. The tuple format is
                    <literal>('{{ theme_name }}', '{{ theme_label }}', '{{ theme_path }}')</literal>.</para>
            <para>The <literal>theme_name</literal> is the name used to define the directory which
                    the theme is collected into, under <literal>/{{ THEME_COLLECTION_DIR }}</literal>.
                    It also specifies the key by which the selected theme is stored in
                    the browser’s cookie.</para>
            <para>The <literal>theme_label</literal> is the user-facing label that is shown in the
                    theme picker.  The theme picker is only visible if more than one
                    theme is configured, and shows under the topnav’s user menu.</para>
            <para>By default, the <literal>theme path</literal> is the directory that will serve as
                    the static root of the theme and the entire contents of the directory
                    is served up at <literal>/{{ THEME_COLLECTION_DIR }}/{{ theme_name }}</literal>.
                    If you wish to include content other than static files in a theme
                    directory, but do not wish that content to be served up, then you
                    can create a sub directory named <literal>static</literal>. If the theme folder
                    contains a sub-directory with the name <literal>static</literal>, then
                    <literal>static/custom/static</literal> will be used as the root for the content
                    served at <literal>/static/custom</literal>.</para>
            <para>The static root of the theme folder must always contain a _variables.scss
                    file and a _styles.scss file.  These must contain or import all the
                    bootstrap and horizon specific variables and styles which are used to style
                    the GUI. For example themes, see: /horizon/openstack_dashboard/themes/</para>
            <para>Horizon ships with two themes configured. ‘default’ is the default theme,
                    and ‘material’ is based on Google’s Material Design.</para>
          </section>
          <section xml:id="custom-theme-path">
            <title>CUSTOM_THEME_PATH</title>
            <para>Default: <literal>"themes/default"</literal></para>
            <para>This setting tells Horizon to use a directory as a custom theme.</para>
            <para>By default, this directory will serve as the static root of the theme
                    and the entire contents of the directory will be served up at
                    <literal>/static/custom</literal>.  If you wish to include content other than static
                    files in a theme directory, but do not wish that content to be served up,
                    then you can create a sub directory named <literal>static</literal>. If the theme folder
                    contains a sub-directory with the name <literal>static</literal>, then
                    <literal>static/custom/static</literal> will be used as the root for the content
                    served at <literal>/static/custom</literal>.</para>
            <para>The static root of the theme folder must always contain a _variables.scss
                    file and a _styles.scss file.  These must contain or import all the
                    bootstrap and horizon specific variables and styles which are used to style
                    the GUI. For example themes, see: /horizon/openstack_dashboard/themes/</para>
            <para>Horizon ships with one alternate theme based on Google’s Material Design.  To
                    use the alternate theme, set your CUSTOM_THEME_PATH to <literal>themes/material</literal>.</para>
          </section>
          <section xml:id="default-theme">
            <title>DEFAULT_THEME</title>
            <para>Default: <literal>"default"</literal></para>
            <para>This setting tells Horizon which theme to use if the user has not
                    yet selected a theme through the theme picker and therefore set the
                    cookie value. This value represents the <literal>theme_name</literal> key that is
                    used from <xref linkend="available-themes"/>.  To use this setting, the theme must
                    also be configured inside of <literal>AVAILABLE_THEMES</literal>. Your default theme
                    must be configured as part of <xref linkend="selectable-themes"/>.  If it is not, then
                    your <literal>DEFAULT_THEME</literal> will default to the first theme in
                    <literal>SELECTABLE_THEMES</literal>.</para>
          </section>
          <section xml:id="default-theme-path">
            <title>DEFAULT_THEME_PATH</title>
            <para>Default: <literal>"themes/default"</literal></para>
            <para>This setting allows Horizon to collect an additional theme during static
                    collection and be served up via /static/themes/default.  This is useful
                    if CUSTOM_THEME_PATH inherits from another theme (like ‘default’).</para>
            <para>If DEFAULT_THEME_PATH is the same as CUSTOM_THEME_PATH, then collection
                    is skipped and /static/themes will not exist.</para>
          </section>
          <section xml:id="disallow-iframe-embed">
            <title>DISALLOW_IFRAME_EMBED</title>
            <para>Default: <literal>True</literal></para>
            <para>This setting can be used to defend against Clickjacking and prevent Horizon from
                    being embedded within an iframe. Legacy browsers are still vulnerable to a
                    Cross-Frame Scripting (XFS) vulnerability, so this option allows extra security
                    hardening where iframes are not used in deployment. When set to true, a
                    <literal>"frame-buster"</literal> script is inserted into the template header that prevents the
                    web page from being framed and therefore defends against clickjacking.</para>
            <para>For more information see: <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://tinyurl.com/anticlickjack"/></para>
            <note>
              <para>If your deployment requires the use of iframes, you can set this setting to
                        <literal>False</literal> to exclude the frame-busting code and allow iframe embedding.</para>
            </note>
          </section>
          <section xml:id="dropdown-max-items">
            <title>DROPDOWN_MAX_ITEMS</title>
            <para>Default: <literal>30</literal></para>
            <para>This setting sets the maximum number of items displayed in a dropdown.
                    Dropdowns that limit based on this value need to support a way to observe
                    the entire list.</para>
          </section>
          <section xml:id="filter-data-first">
            <title>FILTER_DATA_FIRST</title>
            <para>Default:</para>
            <screen language="python">{
    'admin.instances': False,
    'admin.images': False,
    'admin.networks': False,
    'admin.routers': False,
    'admin.volumes': False
}</screen>
            <para>If the dict key-value is True, when the view loads, an empty table will be
                    rendered and the user will be asked to provide a search criteria first (in case
                    no search criteria was provided) before loading any data.</para>
            <para>Examples:</para>
            <para>Override the dict:</para>
            <screen language="python">{
    'admin.instances': True,
    'admin.images': True,
    'admin.networks': False,
    'admin.routers': False,
    'admin.volumes': False
}</screen>
            <para>Or, if you want to turn this on for an specific panel/view do:</para>
            <screen language="python">FILTER_DATA_FIRST['admin.instances'] = True</screen>
          </section>
          <section xml:id="horizon-config">
            <title>HORIZON_CONFIG</title>
            <para>A dictionary of some Horizon configuration values. These are primarily
                    separated for historic design reasons.</para>
            <para>Default:</para>
            <screen language="python">HORIZON_CONFIG = {
    'user_home': 'openstack_dashboard.views.get_user_home',
    'ajax_queue_limit': 10,
    'auto_fade_alerts': {
        'delay': 3000,
        'fade_duration': 1500,
        'types': [
            'alert-success',
            'alert-info'
        ]
    },
    'bug_url': None,
    'help_url': "https://docs.openstack.org/",
    'exceptions': {
        'recoverable': exceptions.RECOVERABLE,
        'not_found': exceptions.NOT_FOUND,
        'unauthorized': exceptions.UNAUTHORIZED
    },
    'modal_backdrop': 'static',
    'angular_modules': [],
    'js_files': [],
    'js_spec_files': [],
    'external_templates': [],
}</screen>
            <section xml:id="ajax-poll-interval">
              <title>ajax_poll_interval</title>
              <para>Default: <literal>2500</literal></para>
              <para>How frequently resources in transition states should be polled for updates,
                        expressed in milliseconds.</para>
            </section>
            <section xml:id="ajax-queue-limit">
              <title>ajax_queue_limit</title>
              <para>Default: <literal>10</literal></para>
              <para>The maximum number of simultaneous AJAX connections the dashboard may try
                        to make. This is particularly relevant when monitoring a large number of
                        instances, volumes, etc. which are all actively trying to update/change state.</para>
            </section>
            <section xml:id="angular-modules">
              <title>angular_modules</title>
              <para>Default: <literal>[]</literal></para>
              <para>A list of AngularJS modules to be loaded when Angular bootstraps. These modules
                        are added as dependencies on the root Horizon application <literal>horizon</literal>.</para>
            </section>
            <section xml:id="auto-fade-alerts">
              <title>auto_fade_alerts</title>
              <para>Default:</para>
              <screen language="python">{
    'delay': 3000,
    'fade_duration': 1500,
    'types': []
}</screen>
              <para>If provided, will auto-fade the alert types specified. Valid alert types
                        include: [‘alert-default’, ‘alert-success’, ‘alert-info’, ‘alert-warning’,
                        ‘alert-danger’]  Can also define the delay before the alert fades and the fade
                        out duration.</para>
            </section>
            <section xml:id="bug-url">
              <title>bug_url</title>
              <para>Default: <literal>None</literal></para>
              <para>If provided, a “Report Bug” link will be displayed in the site header which
                        links to the value of this setting (ideally a URL containing information on
                        how to report issues).</para>
            </section>
            <section xml:id="disable-password-reveal">
              <title>disable_password_reveal</title>
              <para>Default: <literal>False</literal></para>
              <para>Setting this to True will disable the reveal button for password fields,
                        including on the login form.</para>
            </section>
            <section xml:id="exceptions">
              <title>exceptions</title>
              <para>Default:</para>
              <screen language="python">{
    'unauthorized': [],
    'not_found': [],
    'recoverable': []
}</screen>
              <para>A dictionary containing classes of exceptions which Horizon’s centralized
                        exception handling should be aware of. Based on these exception categories,
                        Horizon will handle the exception and display a message to the user.</para>
            </section>
            <section xml:id="help-url-1">
              <title>help_url</title>
              <para>Default: <literal>None</literal></para>
              <para>If provided, a “Help” link will be displayed in the site header which links
                        to the value of this setting (ideally a URL containing help information).</para>
            </section>
            <section xml:id="js-files">
              <title>js_files</title>
              <para>Default: <literal>[]</literal></para>
              <para>A list of javascript source files to be included in the compressed set of files
                        that are loaded on every page. This is needed for AngularJS modules that are
                        referenced in <literal>angular_modules</literal> and therefore need to be include in every
                        page.</para>
            </section>
            <section xml:id="js-spec-files">
              <title>js_spec_files</title>
              <para>Default: <literal>[]</literal></para>
              <para>A list of javascript spec files to include for integration with the Jasmine
                        spec runner. Jasmine is a behavior-driven development framework for testing
                        JavaScript code.</para>
            </section>
            <section xml:id="modal-backdrop">
              <title>modal_backdrop</title>
              <para>Default: <literal>"static"</literal></para>
              <para>Controls how bootstrap backdrop element outside of modals looks and feels.
                        Valid values are <literal>"true"</literal> (show backdrop element outside the modal, close
                        the modal after clicking on backdrop), <literal>"false"</literal> (do not show backdrop
                        element, do not close the modal after clicking outside of it) and <literal>"static"</literal>
                        (show backdrop element outside the modal, do not close the modal after
                        clicking on backdrop).</para>
            </section>
            <section xml:id="password-autocomplete">
              <title>password_autocomplete</title>
              <para>Default: <literal>"off"</literal></para>
              <para>Controls whether browser autocompletion should be enabled on the login form.
                        Valid values are <literal>"on"</literal> and <literal>"off"</literal>.</para>
            </section>
            <section xml:id="password-validator">
              <title>password_validator</title>
              <para>Default:</para>
              <screen language="python">{
    'regex': '.*',
    'help_text': _("Password is not accepted")
}</screen>
              <para>A dictionary containing a regular expression which will be used for password
                        validation and help text which will be displayed if the password does not
                        pass validation. The help text should describe the password requirements if
                        there are any.</para>
              <para>This setting allows you to set rules for passwords if your organization
                        requires them.</para>
            </section>
            <section xml:id="simple-ip-management">
              <title>simple_ip_management</title>
              <para>Default: <literal>True</literal></para>
              <para>Enable or disable simplified floating IP address management.</para>
              <para>“Simple” floating IP address management means that the user does not ever have
                        to select the specific IP addresses they wish to use, and the process of
                        allocating an IP and assigning it to an instance is one-click.</para>
              <para>The “advanced” floating IP management allows users to select the floating IP
                        pool from which the IP should be allocated and to select a specific IP address
                        when associating one with an instance.</para>
              <note>
                <para>Currently “simple” floating IP address management is not compatible with
                            Neutron. There are two reasons for this. First, Neutron does not support
                            the default floating IP pool at the moment. Second, a Neutron floating IP
                            can be associated with each VIF and we need to check whether there is only
                            one VIF for an instance to enable simple association support.</para>
              </note>
            </section>
            <section xml:id="user-home">
              <title>user_home</title>
              <para>Default: <literal>settings.LOGIN_REDIRECT_URL</literal></para>
              <para>This can be either a literal URL path (such as the default), or Python’s
                        dotted string notation representing a function which will evaluate what URL
                        a user should be redirected to based on the attributes of that user.</para>
            </section>
          </section>
          <section xml:id="messages-path">
            <title>MESSAGES_PATH</title>
            <para>Default: <literal>None</literal></para>
            <para>The absolute path to the directory where message files are collected.</para>
            <para>When the user logins to horizon, the message files collected are processed
                    and displayed to the user. Each message file should contain a JSON formatted
                    data and must have a .json file extension. For example:</para>
            <screen language="python">{
    "level": "info",
    "message": "message of the day here"
}</screen>
            <para>Possible values for level are: <literal>success</literal>, <literal>info</literal>, <literal>warning</literal> and
                    <literal>error</literal>.</para>
          </section>
          <section xml:id="ng-template-cache-age">
            <title>NG_TEMPLATE_CACHE_AGE</title>
            <para>Angular Templates are cached using this duration (in seconds)
	    if DEBUG <!-- FIXME <xref linkend="debug"/> -->
                    is set to <literal>False</literal>.  Default value is <literal>2592000</literal> (or 30 days).</para>
          </section>
          <section xml:id="openstack-api-versions">
            <title>OPENSTACK_API_VERSIONS</title>
            <para>Default:</para>
            <screen language="python">{
    "data-processing": 1.1,
    "identity": 2.0,
    "volume": 2,
    "compute": 2
}</screen>
            <para>Overrides for OpenStack API versions. Use this setting to force the
                    OpenStack dashboard to use a specific API version for a given service API.</para>
            <note>
              <para>The version should be formatted as it appears in the URL for the
                        service API. For example, the identity service APIs have inconsistent
                        use of the decimal point, so valid options would be “2.0” or “3”.
                        For example:</para>
              <screen language="python">OPENSTACK_API_VERSIONS = {
    "data-processing": 1.1,
    "identity": 3,
    "volume": 2,
    "compute": 2
}</screen>
            </note>
          </section>
          <section xml:id="openstack-clouds-yaml-name">
            <title>OPENSTACK_CLOUDS_YAML_NAME</title>
            <para>Default: <literal>"openstack"</literal></para>
            <para>The name of the entry to put into the user’s clouds.yaml file.</para>
          </section>
          <section xml:id="openstack-clouds-yaml-profile">
            <title>OPENSTACK_CLOUDS_YAML_PROFILE</title>
            <para>Default: <literal>None</literal></para>
            <para>If set, the name of the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/os-client-config/latest/user/vendor-support.html">vendor profile</link> from <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/os-client-config/latest/">os-client-config</link>.</para>
          </section>
          <section xml:id="openstack-endpoint-type">
            <title>OPENSTACK_ENDPOINT_TYPE</title>
            <para>Default: <literal>"publicURL"</literal></para>
            <para>A string which specifies the endpoint type to use for the endpoints in the
                    Keystone service catalog. The default value for all services except for
                    identity is <literal>"publicURL"</literal> . The default value for the identity service is
                    <literal>"internalURL"</literal>.</para>
          </section>
          <section xml:id="openstack-host">
            <title>OPENSTACK_HOST</title>
            <para>Default: <literal>"127.0.0.1"</literal></para>
            <para>The hostname of the Keystone server used for authentication if you only have
                    one region. This is often the <emphasis role="bold">only</emphasis> setting that needs to be set for a
                    basic deployment.</para>
            <para>If you have multiple regions you should use the <xref linkend="available-regions"/> setting
                    instead.</para>
          </section>
          <section xml:id="openstack-profiler">
            <title>OPENSTACK_PROFILER</title>
            <para>Default: <literal>{"enabled": False}</literal></para>
            <para>Various settings related to integration with osprofiler library. Since it is a
                    developer feature, it starts as disabled. To enable it, more than a single
                    <literal>"enabled"</literal> key should be specified. Additional keys that should be specified
                    in that dictionary are:</para>
            <itemizedlist>
              <listitem>
                <para><literal>"keys"</literal> is a list of strings, which are secret keys used to encode/decode
                            the profiler data contained in request headers. Encryption is used for
                            security purposes, other OpenStack components that are expected to profile
                            themselves with osprofiler using the data from the request that Horizon
                            initiated must share a common set of keys with the ones in Horizon
                            config. List of keys is used so that security keys could be changed in
                            non-obtrusive manner for every component in the cloud.
                            Example: <literal>"keys": ["SECRET_KEY", "MORE_SECRET_KEY"]</literal>.
                            For more details see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/osprofiler/latest/user/integration.html#how-to-initialize-profiler-to-get-one-trace-across-all-services">osprofiler documentation</link>.</para>
              </listitem>
              <listitem>
                <para><literal>"notifier_connection_string"</literal> is a url to which trace messages are sent by
                            Horizon. For other components it is usually the only URL specified in config,
                            because other components act mostly as traces producers. Example:
                            <literal>"notifier_connection_string": "mongodb://%s' % OPENSTACK_HOST"</literal>.</para>
              </listitem>
              <listitem>
                <para><literal>"receiver_connection_string"</literal> is a url from which traces are retrieved by
                            Horizon, needed because Horizon is not only the traces producer, but also a
                            consumer. Having 2 settings which usually contain the same value is legacy
                            feature from older versions of osprofiler when OpenStack components could use
                            oslo.messaging for notifications and the trace client used ceilometer as a
                            receiver backend. By default Horizon uses the same URL pointing to a MongoDB
                            cluster for both purposes, since ceilometer was too slow for using with UI.
                            Example: <literal>"receiver_connection_string": "mongodb://%s" % OPENSTACK_HOST</literal>.</para>
              </listitem>
            </itemizedlist>
          </section>
          <section xml:id="openstack-ssl-cacert">
            <title>OPENSTACK_SSL_CACERT</title>
            <para>Default: <literal>None</literal></para>
            <para>When unset or set to <literal>None</literal> the default CA certificate on the system is used
                    for SSL verification.</para>
            <para>When set with the path to a custom CA certificate file, this overrides use of
                    the default system CA certificate. This custom certificate is used to verify all
                    connections to openstack services when making API calls.</para>
          </section>
          <section xml:id="openstack-ssl-no-verify">
            <title>OPENSTACK_SSL_NO_VERIFY</title>
            <para>Default: <literal>False</literal></para>
            <para>Disable SSL certificate checks in the OpenStack clients (useful for self-signed
                    certificates).</para>
          </section>
          <section xml:id="operation-log-enabled">
            <title>OPERATION_LOG_ENABLED</title>
            <para>Default: <literal>False</literal></para>
            <para>This setting can be used to enable logging of all operations carried out by
                    users of Horizon. The format of the logs is configured via
                    <xref linkend="operation-log-options"/></para>
            <note>
              <para>If you use this feature, you need to configure the logger setting like
                        a outputting path for operation log in <literal>local_settings.py</literal>.</para>
            </note>
          </section>
          <section xml:id="operation-log-options">
            <title>OPERATION_LOG_OPTIONS</title>
            <para>Default:</para>
            <screen language="python">{
    'mask_fields': ['password'],
    'target_methods': ['POST'],
    'ignored_urls': ['/js/', '/static/', '^/api/'],
    'format': ("[%(domain_name)s] [%(domain_id)s] [%(project_name)s]"
        " [%(project_id)s] [%(user_name)s] [%(user_id)s] [%(request_scheme)s]"
        " [%(referer_url)s] [%(request_url)s] [%(message)s] [%(method)s]"
        " [%(http_status)s] [%(param)s]"),
}</screen>
            <para>This setting controls the behavior of the operation log.</para>
            <itemizedlist>
              <listitem>
                <para><literal>mask_fields</literal> is a list of keys of post data which should be masked from the
                            point of view of security. Fields like <literal>password</literal> should be included.
                            The fields specified in <literal>mask_fields</literal> are logged as <literal>********</literal>.</para>
              </listitem>
              <listitem>
                <para><literal>target_methods</literal> is a request method which is logged to a operation log.
                            The valid methods are <literal>POST</literal>, <literal>GET</literal>, <literal>PUT</literal>, <literal>DELETE</literal>.</para>
              </listitem>
              <listitem>
                <para><literal>ignored_urls</literal> is a list of request URLs to be hidden from a log.</para>
              </listitem>
              <listitem>
                <para><literal>format</literal> defines the operation log format.
                            Currently you can use the following keywords.
                            The default value contains all keywords.</para>
                <itemizedlist>
                  <listitem>
                    <para>
                      <literal>%(client_ip)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(domain_name)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(domain_id)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(project_name)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(project_id)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(user_name)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(user_id)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(request_scheme)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(referer_url)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(request_url)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(message)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(method)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(http_status)s</literal>
                    </para>
                  </listitem>
                  <listitem>
                    <para>
                      <literal>%(param)s</literal>
                    </para>
                  </listitem>
                </itemizedlist>
              </listitem>
            </itemizedlist>
          </section>
          <section xml:id="overview-days-range">
            <title>OVERVIEW_DAYS_RANGE</title>
            <para>Default:: <literal>1</literal></para>
            <para>When set to an integer N (as by default), the start date in the Overview panel
                    meters will be today minus N days. This setting is used to limit the amount of
                    data fetched by default when rendering the Overview panel. If set to <literal>None</literal>
                    (which corresponds to the behavior in past Horizon versions), the start date
                    will be from the beginning of the current month until the current date. The
                    legacy behaviour is not recommended for large deployments as Horizon suffers
                    significant lag in this case.</para>
          </section>
          <section xml:id="policy-files">
            <title>POLICY_FILES</title>
            <para>Default:</para>
            <screen language="python">{
    'identity': 'keystone_policy.json',
    'compute': 'nova_policy.json',
    'volume': 'cinder_policy.json',
    'image': 'glance_policy.json',
    'orchestration': 'heat_policy.json',
    'network': 'neutron_policy.json',
}</screen>
            <para>This should essentially be the mapping of the contents of <xref linkend="policy-files-path"/>
                    to service types. When policy.json files are added to <xref linkend="policy-files-path"/>,
                    they should be included here too.</para>
          </section>
          <section xml:id="policy-files-path">
            <title>POLICY_FILES_PATH</title>
            <para>Default:  <literal>os.path.join(ROOT_PATH, "conf")</literal></para>
            <para>Specifies where service based policy files are located.  These are used to
                    define the policy rules actions are verified against.</para>
          </section>
          <section xml:id="rest-api-required-settings">
            <title>REST_API_REQUIRED_SETTINGS</title>
            <para>Default:</para>
            <screen language="python">[
    'OPENSTACK_HYPERVISOR_FEATURES',
    'LAUNCH_INSTANCE_DEFAULTS',
    'OPENSTACK_IMAGE_FORMATS',
    'OPENSTACK_KEYSTONE_DEFAULT_DOMAIN'
]</screen>
            <para>This setting allows you to expose configuration values over Horizons internal
                    REST API, so that the AngularJS panels can access them. Please be cautious
                    about which values are listed here (and thus exposed on the frontend)</para>
          </section>
          <section xml:id="selectable-themes">
            <title>SELECTABLE_THEMES</title>
            <para>Default: <literal>AVAILABLE_THEMES</literal></para>
            <para>This setting tells Horizon which themes to expose to the user as selectable
                    in the theme picker widget.  This value defaults to all themes configured
                    in <xref linkend="available-themes"/>, but a brander may wish to simply inherit from an
                    existing theme and not allow that parent theme to be selected by the user.
                    <literal>SELECTABLE_THEMES</literal> takes the exact same format as <literal>AVAILABLE_THEMES</literal>.</para>
          </section>
          <section xml:id="session-timeout">
            <title>SESSION_TIMEOUT</title>
            <para>Default: <literal>"3600"</literal></para>
            <para>This SESSION_TIMEOUT is a method to supercede the token timeout with a shorter
                    horizon session timeout (in seconds).  So if your token expires in 60 minutes,
                    a value of 1800 will log users out after 30 minutes.</para>
          </section>
          <section xml:id="theme-collection-dir">
            <title>THEME_COLLECTION_DIR</title>
            <para>Default: <literal>"themes"</literal></para>
            <para>This setting tells Horizon which static directory to collect the
                    available themes into, and therefore which URL points to the theme
                    collection root.  For example, the default theme would be accessible
                    via <literal>/{{ STATIC_URL }}/themes/default</literal>.</para>
          </section>
          <section xml:id="theme-cookie-name">
            <title>THEME_COOKIE_NAME</title>
            <para>Default: <literal>"theme"</literal></para>
            <para>This setting tells Horizon in which cookie key to store the currently
                    set theme.  The cookie expiration is currently set to a year.</para>
          </section>
          <section xml:id="webroot">
            <title>WEBROOT</title>
            <para>Default: <literal>"/"</literal></para>
            <para>Specifies the location where the access to the dashboard is configured in
                    the web server.</para>
            <para>For example, if you’re accessing the Dashboard via
                    <literal>https://&lt;your server&gt;/dashboard</literal>, you would set this to <literal>"/dashboard/"</literal>.</para>
            <note>
              <para>Additional settings may be required in the config files of your webserver
                        of choice. For example to make <literal>"/dashboard/"</literal> the web root in Apache,
                        the <literal>"sites-available/horizon.conf"</literal> requires a couple of additional
                        aliases set:</para>
              <screen>Alias /dashboard/static %HORIZON_DIR%/static

Alias /dashboard/media %HORIZON_DIR%/openstack_dashboard/static</screen>
              <para>Apache also requires changing your WSGIScriptAlias to reflect the desired
                        path.  For example, you’d replace <literal>/</literal> with <literal>/dashboard</literal> for the
                        alias.</para>
            </note>
          </section>
        </section>
        <section xml:id="service-specific-settings">
          <title>Service-specific Settings</title>
          <para>The following settings inform the OpenStack Dashboard of information about the
                other OpenStack projects which are part of this cloud and control the behavior
                of specific dashboards, panels, API calls, etc.</para>
          <section xml:id="cinder">
            <title>Cinder</title>
            <section xml:id="openstack-cinder-features">
              <title>OPENSTACK_CINDER_FEATURES</title>
              <para>Default: <literal>{'enable_backup': False}</literal></para>
              <para>A dictionary of settings which can be used to enable optional services provided
                        by cinder.  Currently only the backup service is available.</para>
            </section>
          </section>
          <section xml:id="glance">
            <title>Glance</title>
            <section xml:id="create-image-defaults">
              <title>CREATE_IMAGE_DEFAULTS</title>
              <para>Default:</para>
              <screen language="python">{
    'image_visibility': "public",
}</screen>
              <para>A dictionary of default settings for create image modal.</para>
              <para>The <literal>image_visibility</literal> setting specifies the default visibility option.
                        Valid values are  <literal>"public"</literal> and <literal>"private"</literal>. By default, the visibility
                        option is public on create image modal. If it’s set to <literal>"private"</literal>, the
                        default visibility option is private.</para>
            </section>
            <section xml:id="horizon-images-allow-upload">
              <title>HORIZON_IMAGES_ALLOW_UPLOAD</title>
              <para>Default: <literal>True</literal></para>
              <para>If set to <literal>False</literal>, this setting disables <emphasis role="bold">local</emphasis> uploads to prevent filling
                        up the disk on the dashboard server since uploads to the Glance image store
                        service tend to be particularly large - in the order of hundreds of megabytes
                        to multiple gigabytes.</para>
              <para>The setting is marked as deprecated and will be removed in P or later release.
                        It is superseded by the setting HORIZON_IMAGES_UPLOAD_MODE. Until the removal
                        the <literal>False</literal> value of HORIZON_IMAGES_ALLOW_UPLOAD overrides the value of
                        HORIZON_IMAGES_UPLOAD_MODE.</para>
              <note>
                <para>This will not disable image creation altogether, as this setting does not
                            affect images created by specifying an image location (URL) as the image
                            source.</para>
              </note>
            </section>
            <section xml:id="horizon-images-upload-mode">
              <title>HORIZON_IMAGES_UPLOAD_MODE</title>
              <para>Default: <literal>"legacy"</literal></para>
              <para>Valid values are  <literal>"direct"</literal>, <literal>"legacy"</literal> (default) and <literal>"off"</literal>. <literal>"off"</literal>
                        disables the ability to upload images via Horizon. It is equivalent to setting
                        <literal>False</literal> on the deprecated setting <literal>HORIZON_IMAGES_ALLOW_UPLOAD</literal>. <literal>legacy</literal>
                        enables local file upload by piping the image file through the Horizon’s
                        web-server. It is equivalent to setting <literal>True</literal> on the deprecated setting
                        <literal>HORIZON_IMAGES_ALLOW_UPLOAD</literal>. <literal>direct</literal> sends the image file directly from
                        the web browser to Glance. This bypasses Horizon web-server which both reduces
                        network hops and prevents filling up Horizon web-server’s filesystem. <literal>direct</literal>
                        is the preferred mode, but due to the following requirements it is not the
                        default. The <literal>direct</literal> setting requires a modern web browser, network access
                        from the browser to the public Glance endpoint, and CORS support to be enabled
                        on the Glance API service. Without CORS support, the browser will forbid the
                        PUT request to a location different than the Horizon server. To enable CORS
                        support for Glance API service, you will need to edit [cors] section of
                        glance-api.conf file (see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/oslo.middleware/latest/reference/cors.html#configuration-for-oslo-config">here</link> how to do it). Set <literal>allowed_origin</literal> to the
                        full hostname of Horizon web-server (e.g. <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http:/"/>/&lt;HOST_IP&gt;/dashboard) and
                        restart glance-api process.</para>
              <note>
                <para>To maintain the compatibility with the deprecated HORIZON_IMAGES_ALLOW_UPLOAD
                            setting, neither <literal>"direct"</literal>, nor <literal>"legacy"</literal> modes will have an effect if
                            HORIZON_IMAGES_ALLOW_UPLOAD is set to <literal>False</literal> - as if HORIZON_IMAGES_UPLOAD_MODE
                            was set to <literal>"off"</literal> itself. When HORIZON_IMAGES_ALLOW_UPLOAD is set to <literal>True</literal>,
                            all three modes are considered, as if HORIZON_IMAGES_ALLOW_UPLOAD setting
                            was removed.</para>
              </note>
            </section>
            <section xml:id="image-custom-property-titles">
              <title>IMAGE_CUSTOM_PROPERTY_TITLES</title>
              <para>Default:</para>
              <screen language="python">{
    "architecture": _("Architecture"),
    "kernel_id": _("Kernel ID"),
    "ramdisk_id": _("Ramdisk ID"),
    "image_state": _("Euca2ools state"),
    "project_id": _("Project ID"),
    "image_type": _("Image Type")
}</screen>
              <para>Used to customize the titles for image custom property attributes that
                        appear on image detail pages.</para>
            </section>
            <section xml:id="image-reserved-custom-properties">
              <title>IMAGE_RESERVED_CUSTOM_PROPERTIES</title>
              <para>Default: <literal>[]</literal></para>
              <para>A list of image custom property keys that should not be displayed in the
                        Update Metadata tree.</para>
              <para>This setting can be used in the case where a separate panel is used for
                        managing a custom property or if a certain custom property should never be
                        edited.</para>
            </section>
            <section xml:id="images-allow-location">
              <title>IMAGES_ALLOW_LOCATION</title>
              <para>Default: <literal>False</literal></para>
              <para>If set to <literal>True</literal>, this setting allows users to specify an image location
                        (URL) as the image source when creating or updating images. Depending on
                        the Glance version, the ability to set an image location is controlled by
                        policies and/or the Glance configuration. Therefore IMAGES_ALLOW_LOCATION
                        should only be set to <literal>True</literal> if Glance is configured to allow specifying a
                        location. This setting has no effect when the Keystone catalog doesn’t contain
                        a Glance v2 endpoint.</para>
            </section>
            <section xml:id="images-list-filter-tenants">
              <title>IMAGES_LIST_FILTER_TENANTS</title>
              <para>Default: <literal>None</literal></para>
              <para>A list of dictionaries to add optional categories to the image fixed filters
                        in the Images panel, based on project ownership.</para>
              <para>Each dictionary should contain a <literal>tenant</literal> attribute with the project
                        id, and optionally a <literal>text</literal> attribute specifying the category name, and
                        an <literal>icon</literal> attribute that displays an icon in the filter button. The
                        icon names are based on the default icon theme provided by Bootstrap.</para>
              <para>Example:</para>
              <screen language="python">[{'text': 'Official',
  'tenant': '27d0058849da47c896d205e2fc25a5e8',
  'icon': 'fa-check'}]</screen>
            </section>
            <section xml:id="openstack-image-backend">
              <title>OPENSTACK_IMAGE_BACKEND</title>
              <para>Default:</para>
              <screen language="python">{
    'image_formats': [
        ('', _('Select format')),
        ('aki', _('AKI - Amazon Kernel Image')),
        ('ami', _('AMI - Amazon Machine Image')),
        ('ari', _('ARI - Amazon Ramdisk Image')),
        ('docker', _('Docker')),
        ('iso', _('ISO - Optical Disk Image')),
        ('qcow2', _('QCOW2 - QEMU Emulator')),
        ('raw', _('Raw')),
        ('vdi', _('VDI')),
        ('vhd', _('VHD')),
        ('vmdk', _('VMDK'))
    ]
}</screen>
              <para>Used to customize features related to the image service, such as the list of
                        supported image formats.</para>
            </section>
          </section>
          <section xml:id="heat">
            <title>Heat</title>
            <section xml:id="openstack-heat-stack">
              <title>OPENSTACK_HEAT_STACK</title>
              <para>Default:</para>
              <screen language="python">{
    'enable_user_pass': True
}</screen>
              <para>A dictionary of settings to use with heat stacks. Currently, the only setting
                        available is “enable_user_pass”, which can be used to disable the password
                        field while launching the stack. Currently HEAT API needs user password to
                        perform all the heat operations because in HEAT API trusts is not enabled by
                        default. So, this setting can be set as “False” in-case HEAT uses trusts by
                        default otherwise it needs to be set as “True”.</para>
            </section>
          </section>
          <section xml:id="keystone">
            <title>Keystone</title>
            <section xml:id="authentication-plugins">
              <title>AUTHENTICATION_PLUGINS</title>
              <para>Default:</para>
              <screen language="python">[
    'openstack_auth.plugin.password.PasswordPlugin',
    'openstack_auth.plugin.token.TokenPlugin'
]</screen>
              <para>A list of authentication plugins to be used. In most cases, there is no need to
                        configure this.</para>
            </section>
            <section xml:id="authentication-urls">
              <title>AUTHENTICATION_URLS</title>
              <para>Default: <literal>['openstack_auth.urls']</literal></para>
              <para>A list of modules from which to collate authentication URLs from. The default
                        option adds URLs from the django-openstack-auth module however others will be
                        required for additional authentication mechanisms.</para>
            </section>
            <section xml:id="available-regions">
              <title>AVAILABLE_REGIONS</title>
              <para>Default: <literal>None</literal></para>
              <para>A list of tuples which define multiple regions. The tuple format is
                        <literal>('http://{{ keystone_host }}:5000/v2.0', '{{ region_name }}')</literal>. If any regions
                        are specified the login form will have a dropdown selector for authenticating
                        to the appropriate region, and there will be a region switcher dropdown in
                        the site header when logged in.</para>
              <para>You should also define <xref linkend="openstack-keystone-url"/> to indicate which of
                        the regions is the default one.</para>
            </section>
            <section xml:id="default-service-regions">
              <title>DEFAULT_SERVICE_REGIONS</title>
              <para>Default: <literal>{}</literal></para>
              <para>The default service region is set on a per-endpoint basis, meaning that once
                        the user logs into some Keystone endpoint, if a default service region is
                        defined for it in this setting and exists within Keystone catalog, it will be
                        set as the initial service region in this endpoint. By default it is an empty
                        dictionary because upstream can neither predict service region names in a
                        specific deployment, nor tell whether this behavior is desired. The key of the
                        dictionary is a full url of a Keystone endpoint with version suffix, the value
                        is a region name.</para>
              <para>Example:</para>
              <screen language="python">DEFAULT_SERVICE_REGIONS = {
    OPENSTACK_KEYSTONE_URL: 'RegionOne'
}</screen>
            </section>
            <section xml:id="enable-client-token">
              <title>ENABLE_CLIENT_TOKEN</title>
              <para>Default: <literal>True</literal></para>
              <para>This setting will Enable/Disable access to the Keystone Token to the
                        browser.</para>
            </section>
            <section xml:id="enforce-password-check">
              <title>ENFORCE_PASSWORD_CHECK</title>
              <para>Default: <literal>False</literal></para>
              <para>This setting will display an ‘Admin Password’ field on the Change Password
                        form to verify that it is indeed the admin logged-in who wants to change
                        the password.</para>
            </section>
            <section xml:id="keystone-provider-idp-id">
              <title>KEYSTONE_PROVIDER_IDP_ID</title>
              <para>Default: <literal>"localkeystone"</literal></para>
              <para>This ID is only used for comparison with the service provider IDs.
                        This ID should not match any service provider IDs.</para>
            </section>
            <section xml:id="keystone-provider-idp-name">
              <title>KEYSTONE_PROVIDER_IDP_NAME</title>
              <para>Default: <literal>"Local Keystone"</literal></para>
              <para>The Keystone Provider drop down uses Keystone to Keystone federation to switch
                        between Keystone service providers. This sets the display name for the Identity
                        Provider (dropdown display name).</para>
            </section>
            <section xml:id="openstack-keystone-admin-roles">
              <title>OPENSTACK_KEYSTONE_ADMIN_ROLES</title>
              <para>Default: <literal>["admin"]</literal></para>
              <para>The list of roles that have administrator privileges in this OpenStack
                        installation. This check is very basic and essentially only works with
                        keystone v2.0 and v3 with the default policy file. The setting assumes there
                        is a common <literal>admin</literal> like role(s) across services. Example uses of this
                        setting are:</para>
              <itemizedlist>
                <listitem>
                  <para>to rename the <literal>admin</literal> role to <literal>cloud-admin</literal></para>
                </listitem>
                <listitem>
                  <para>allowing multiple roles to have administrative privileges, like
                                <literal>["admin", "cloud-admin", "net-op"]</literal></para>
                </listitem>
              </itemizedlist>
            </section>
            <section xml:id="openstack-keystone-backend">
              <title>OPENSTACK_KEYSTONE_BACKEND</title>
              <para>Default:</para>
              <screen language="python">{
    'name': 'native',
    'can_edit_user': True,
    'can_edit_group': True,
    'can_edit_project': True,
    'can_edit_domain': True,
    'can_edit_role': True,
}</screen>
              <para>A dictionary containing settings which can be used to identify the
                        capabilities of the auth backend for Keystone.</para>
              <para>If Keystone has been configured to use LDAP as the auth backend then set
                        <literal>can_edit_user</literal> and <literal>can_edit_project</literal> to <literal>False</literal> and name to <literal>"ldap"</literal>.</para>
            </section>
            <section xml:id="openstack-keystone-default-domain">
              <title>OPENSTACK_KEYSTONE_DEFAULT_DOMAIN</title>
              <para>Default: <literal>"Default"</literal></para>
              <para>Overrides the default domain used when running on single-domain model
                        with Keystone V3. All entities will be created in the default domain.</para>
            </section>
            <section xml:id="openstack-keystone-default-role">
              <title>OPENSTACK_KEYSTONE_DEFAULT_ROLE</title>
              <para>Default: <literal>"_member_"</literal></para>
              <para>The name of the role which will be assigned to a user when added to a project.
                        This value must correspond to an existing role name in Keystone. In general,
                        the value should match the <literal>member_role_name</literal> defined in <literal>keystone.conf</literal>.</para>
            </section>
            <section xml:id="openstack-keystone-domain-choices">
              <title>OPENSTACK_KEYSTONE_DOMAIN_CHOICES</title>
              <para>Default:</para>
              <screen language="python">(
    ('Default', 'Default'),
)</screen>
              <para>If <xref linkend="openstack-keystone-domain-dropdown"/> is enabled, this option can be used to
                        set the available domains to choose from. This is a list of pairs whose first
                        value is the domain name and the second is the display name.</para>
            </section>
            <section xml:id="openstack-keystone-domain-dropdown">
              <title>OPENSTACK_KEYSTONE_DOMAIN_DROPDOWN</title>
              <para>Default: <literal>False</literal></para>
              <para>Set this to True if you want available domains displayed as a dropdown menu on
                        the login screen. It is strongly advised NOT to enable this for public clouds,
                        as advertising enabled domains to unauthenticated customers irresponsibly
                        exposes private information. This should only be used for private clouds where
                        the dashboard sits behind a corporate firewall.</para>
            </section>
            <section xml:id="openstack-keystone-federation-management">
              <title>OPENSTACK_KEYSTONE_FEDERATION_MANAGEMENT</title>
              <para>Default: <literal>False</literal></para>
              <para>Set this to True to enable panels that provide the ability for users to manage
                        Identity Providers (IdPs) and establish a set of rules to map federation
                        protocol attributes to Identity API attributes. This extension requires v3.0+
                        of the Identity API.</para>
            </section>
            <section xml:id="openstack-keystone-multidomain-support">
              <title>OPENSTACK_KEYSTONE_MULTIDOMAIN_SUPPORT</title>
              <para>Default: <literal>False</literal></para>
              <para>Set this to True if running on multi-domain model. When this is enabled, it
                        will require user to enter the Domain name in addition to username for login.</para>
            </section>
            <section xml:id="openstack-keystone-url">
              <title>OPENSTACK_KEYSTONE_URL</title>
              <formalpara>
                <title>See also</title>
                <para>Horizon’s <xref linkend="openstack-host"/> documentation</para>
              </formalpara>
              <para>Default: <literal>"http://%s:5000/v2.0" % OPENSTACK_HOST</literal></para>
              <para>The full URL for the Keystone endpoint used for authentication. Unless you
                        are using HTTPS, running your Keystone server on a nonstandard port, or using
                        a nonstandard URL scheme you shouldn’t need to touch this setting.</para>
            </section>
            <section xml:id="openstack-token-hash-algorithm">
              <title>OPENSTACK_TOKEN_HASH_ALGORITHM</title>
              <para>Default: <literal>"md5"</literal></para>
              <para>The hash algorithm to use for authentication tokens. This must match the hash
                        algorithm that the identity (Keystone) server and the auth_token middleware
                        are using. Allowed values are the algorithms supported by Python’s hashlib
                        library.</para>
            </section>
            <section xml:id="openstack-token-hash-enabled">
              <title>OPENSTACK_TOKEN_HASH_ENABLED</title>
              <para>Default: <literal>True</literal></para>
              <para>Hashing tokens from Keystone keeps the Horizon session data smaller, but it
                        doesn’t work in some cases when using PKI tokens.  Uncomment this value and
                        set it to False if using PKI tokens and there are 401 errors due to token
                        hashing.</para>
            </section>
            <section xml:id="password-expires-warning-threshold-days">
              <title>PASSWORD_EXPIRES_WARNING_THRESHOLD_DAYS</title>
              <para>Default: <literal>-1</literal></para>
              <para>Password will have an expiration date when using keystone v3 and enabling the
                        feature. This setting allows you to set the number of days that the user will
                        be alerted prior to the password expiration. Once the password expires keystone
                        will deny the access and users must contact an admin to change their password.
                        Setting this value to <literal>N</literal> days means the user will be alerted when the
                        password expires in less than <literal>N+1</literal> days. <literal>-1</literal> disables the feature.</para>
            </section>
            <section xml:id="project-table-extra-info">
              <title>PROJECT_TABLE_EXTRA_INFO</title>
              <formalpara>
                <title>See also</title>
                <para><xref linkend="user-table-extra-info"/> for the equivalent setting on the Users table</para>
              </formalpara>
              <para>Default: <literal>{}</literal></para>
              <para>Adds additional information for projects as extra attributes. Projects can have
                        extra attributes as defined by Keystone v3. This setting allows those
                        attributes to be shown in Horizon.</para>
              <para>For example:</para>
              <screen language="python">PROJECT_TABLE_EXTRA_INFO = {
    'phone_num': _('Phone Number'),
}</screen>
            </section>
            <section xml:id="secure-proxy-addr-header">
              <title>SECURE_PROXY_ADDR_HEADER</title>
              <para>Default: <literal>False</literal></para>
              <para>If horizon is behind a proxy server and the proxy is configured, the IP address
                        from request is passed using header variables inside the request. The header
                        name depends on a proxy or a load-balancer. This setting specifies the name of
                        the header with remote IP address. The main use is for authentication log
                        (success or fail) displaing the IP address of the user.
                        The commom value for this setting is <literal>HTTP_X_REAL_IP</literal> or
                        <literal>HTTP_X_FORWARDED_FOR</literal>.
                        If not present, then <literal>REMOTE_ADDR</literal> header is used. (<literal>REMOTE_ADDR</literal> is the
                        field of Django HttpRequest object which contains IP address of the client.)</para>
            </section>
            <section xml:id="token-timeout-margin">
              <title>TOKEN_TIMEOUT_MARGIN</title>
              <para>Default: <literal>0</literal></para>
              <para>A time margin in seconds to subtract from the real token’s validity. An example
                        use case is that the token can be valid once the middleware passed, and
                        invalid (timed-out) during a view rendering and this generates authorization
                        errors during the view rendering. By setting this value to a few seconds, you
                        can avoid token expiration during a view rendering.</para>
            </section>
            <section xml:id="user-table-extra-info">
              <title>USER_TABLE_EXTRA_INFO</title>
              <formalpara>
                <title>See also</title>
                <para><xref linkend="project-table-extra-info"/> for the equivalent setting on the Projects table</para>
              </formalpara>
              <para>Default: <literal>{}</literal></para>
              <para>Adds additional information for users as extra attributes. Users can have
                        extra attributes as defined by Keystone v3. This setting allows those
                        attributes to be shown in Horizon.</para>
              <para>For example:</para>
              <screen language="python">USER_TABLE_EXTRA_INFO = {
    'phone_num': _('Phone Number'),
}</screen>
            </section>
            <section xml:id="websso-choices">
              <title>WEBSSO_CHOICES</title>
              <para>Default:</para>
              <screen language="python">(
    ("credentials", _("Keystone Credentials")),
    ("oidc", _("OpenID Connect")),
    ("saml2", _("Security Assertion Markup Language"))
)</screen>
              <para>This is the list of authentication mechanisms available to the user. It
                        includes Keystone federation protocols such as OpenID Connect and SAML, and
                        also keys that map to specific identity provider and federation protocol
                        combinations (as defined in <xref linkend="websso-idp-mapping"/>). The list of choices is
                        completely configurable, so as long as the id remains intact. Do not remove
                        the credentials mechanism unless you are sure. Once removed, even admins will
                        have no way to log into the system via the dashboard.</para>
            </section>
            <section xml:id="websso-enabled">
              <title>WEBSSO_ENABLED</title>
              <para>Default: <literal>False</literal></para>
              <para>Enables keystone web single-sign-on if set to True. For this feature to work,
                        make sure that you are using Keystone V3 and Django OpenStack Auth V1.2.0 or
                        later.</para>
            </section>
            <section xml:id="websso-idp-mapping">
              <title>WEBSSO_IDP_MAPPING</title>
              <para>Default: <literal>{}</literal></para>
              <para>A dictionary of specific identity provider and federation protocol combinations.
                        From the selected authentication mechanism, the value will be looked up as keys
                        in the dictionary. If a match is found, it will redirect the user to a identity
                        provider and federation protocol specific WebSSO endpoint in keystone,
                        otherwise it will use the value as the protocol_id when redirecting to the
                        WebSSO by protocol endpoint.</para>
              <para>Example:</para>
              <screen language="python">WEBSSO_CHOICES =  (
    ("credentials", _("Keystone Credentials")),
    ("oidc", _("OpenID Connect")),
    ("saml2", _("Security Assertion Markup Language")),
    ("acme_oidc", "ACME - OpenID Connect"),
    ("acme_saml2", "ACME - SAML2")
)

WEBSSO_IDP_MAPPING = {
    "acme_oidc": ("acme", "oidc"),
    "acme_saml2": ("acme", "saml2")
}</screen>
              <note>
                <para>The value is expected to be a tuple formatted as: (&lt;idp_id&gt;, &lt;protocol_id&gt;)</para>
              </note>
            </section>
            <section xml:id="websso-initial-choice">
              <title>WEBSSO_INITIAL_CHOICE</title>
              <para>Default: <literal>"credentials"</literal></para>
              <para>Specifies the default authentication mechanism. When user lands on the login
                        page, this is the first choice they will see.</para>
            </section>
          </section>
          <section xml:id="neutron">
            <title>Neutron</title>
            <section xml:id="allowed-private-subnet-cidr">
              <title>ALLOWED_PRIVATE_SUBNET_CIDR</title>
              <para>Default:</para>
              <screen language="python">{
    'ipv4': [],
    'ipv6': []
}</screen>
              <para>A dictionary used to restrict user private subnet CIDR range.
                        An empty list means that user input will not be restricted
                        for a corresponding IP version. By default, there is
                        no restriction for both IPv4 and IPv6.</para>
              <para>Example:</para>
              <screen language="python">{
    'ipv4': [
        '192.168.0.0/16',
        '10.0.0.0/8'
    ],
    'ipv6': [
        'fc00::/7',
    ]
}</screen>
            </section>
            <section xml:id="openstack-neutron-network">
              <title>OPENSTACK_NEUTRON_NETWORK</title>
              <para>Default:</para>
              <screen language="python">{
    'default_dns_nameservers': [],
    'enable_distributed_router': False,
    'enable_fip_topology_check': True,
    'enable_ha_router': False,
    'enable_ipv6': True,
    'enable_quotas': False,
    'enable_router': True,
    'extra_provider_types': {},
    'physical_networks': [],
    'segmentation_id_range': {},
    'supported_provider_types': ["*"],
    'supported_vnic_types': ["*"],
}</screen>
              <para>A dictionary of settings which can be used to enable optional services provided
                        by Neutron and configure Neutron specific features.  The following options are
                        available.</para>
              <section xml:id="default-dns-nameservers">
                <title>default_dns_nameservers</title>
                <para>Default: <literal>None</literal> (Empty)</para>
                <para>Default DNS servers you would like to use when a subnet is created. This is
                            only a default. Users can still choose a different list of dns servers.</para>
                <para>Example: <literal>["8.8.8.8", "8.8.4.4", "208.67.222.222"]</literal></para>
              </section>
              <section xml:id="enable-distributed-router">
                <title>enable_distributed_router</title>
                <para>Default: <literal>False</literal></para>
                <para>Enable or disable Neutron distributed virtual router (DVR) feature in
                            the Router panel. For the DVR feature to be enabled, this option needs
                            to be set to True and your Neutron deployment must support DVR. Even
                            when your Neutron plugin (like ML2 plugin) supports DVR feature, DVR
                            feature depends on l3-agent configuration, so deployers should set this
                            option appropriately depending on your deployment.</para>
              </section>
              <section xml:id="enable-fip-topology-check">
                <title>enable_fip_topology_check</title>
                <para>Default: <literal>True</literal></para>
                <para>The Default Neutron implementation needs a router with a gateway to associate a
                            FIP. So by default a topology check will be performed by horizon to list only
                            VM ports attached to a network which is itself attached to a router with an
                            external gateway. This is to prevent from setting a FIP to a port which will
                            fail with an error.
                            Some Neutron vendors do not require it. Some can even attach a FIP to any port
                            (e.g.: OpenContrail) owned by a tenant.
                            Set to False if you want to be able to associate a FIP to an instance on a
                            subnet with no router if your Neutron backend allows it.</para>
              </section>
              <section xml:id="enable-ha-router">
                <title>enable_ha_router</title>
                <para>Default: <literal>False</literal></para>
                <para>Enable or disable HA (High Availability) mode in Neutron virtual router
                            in the Router panel. For the HA router mode to be enabled, this option needs
                            to be set to True and your Neutron deployment must support HA router mode.
                            Even when your Neutron plugin (like ML2 plugin) supports HA router mode,
                            the feature depends on l3-agent configuration, so deployers should set this
                            option appropriately depending on your deployment.</para>
              </section>
              <section xml:id="enable-ipv6">
                <title>enable_ipv6</title>
                <para>Default: <literal>False</literal></para>
                <para>Enable or disable IPv6 support in the Network panels. When disabled, Horizon
                            will only expose IPv4 configuration for networks.</para>
              </section>
              <section xml:id="enable-quotas">
                <title>enable_quotas</title>
                <para>Default: <literal>False</literal></para>
                <para>Enable support for Neutron quotas feature. To make this feature work
                            appropriately, you need to use Neutron plugins with quotas extension support
                            and quota_driver should be DbQuotaDriver (default config).</para>
              </section>
              <section xml:id="enable-router">
                <title>enable_router</title>
                <para>Default: <literal>True</literal></para>
                <para>Enable (<literal>True</literal>) or disable (<literal>False</literal>) the panels and menus related to router
                            and Floating IP features. This option only affects when Neutron is enabled. If
                            your Neutron deployment has no support for Layer-3 features, or you do not wish
                            to provide the Layer-3 features through the Dashboard, this should be set to
                            <literal>False</literal>.</para>
              </section>
              <section xml:id="extra-provider-types">
                <title>extra_provider_types</title>
                <para>Default: <literal>{}</literal></para>
                <para>For use with the provider network extension.
                            This is a dictionary to define extra provider network definitions.
                            Network types supported by Neutron depend on the configured plugin.
                            Horizon has predefined provider network types but horizon cannot cover
                            all of them. If you are using a provider network type not defined
                            in advance, you can add a definition through this setting.</para>
                <para>The <emphasis role="bold">key</emphasis> name of each item in this must be a network type used
                            in the Neutron API. <emphasis role="bold">value</emphasis> should be a dictionary which contains
                            the following items:</para>
                <itemizedlist>
                  <listitem>
                    <para><literal>display_name</literal>: string displayed in the network creation form.</para>
                  </listitem>
                  <listitem>
                    <para><literal>require_physical_network</literal>: a boolean parameter which indicates
                                    this network type requires a physical network.</para>
                  </listitem>
                  <listitem>
                    <para><literal>require_segmentation_id</literal>: a boolean parameter which indicates
                                    this network type requires a segmentation ID.
                                    If True, a valid segmentation ID range must be configured
                                    in <literal>segmentation_id_range</literal> settings above.</para>
                  </listitem>
                </itemizedlist>
                <para>Example:</para>
                <screen language="python">{
    'awesome': {
        'display_name': 'Awesome',
        'require_physical_network': False,
        'require_segmentation_id': True,
    },
}</screen>
              </section>
              <section xml:id="physical-networks">
                <title>physical_networks</title>
                <para>Default: <literal>[]</literal></para>
                <para>Default to an empty list and the physical network field on the admin create
                            network modal will be a regular input field where users can type in the name
                            of the physical network to be used.
                            If it is set to a list of available physical networks, the physical network
                            field will be shown as a dropdown menu where users can select a physical
                            network to be used.</para>
                <para>Example: <literal>['default', 'test']</literal></para>
              </section>
              <section xml:id="segmentation-id-range">
                <title>segmentation_id_range</title>
                <para>Default: <literal>{}</literal></para>
                <para>For use with the provider network extension. This is a dictionary where each
                            key is a provider network type and each value is a list containing two numbers.
                            The first number is the minimum segmentation ID that is valid. The second
                            number is the maximum segmentation ID. Pertains only to the vlan, gre, and
                            vxlan network types. By default this option is not provided and each minimum
                            and maximum value will be the default for the provider network type.</para>
                <para>Example:</para>
                <screen language="python">{
    'vlan': [1024, 2048],
    'gre': [4094, 65536]
}</screen>
              </section>
              <section xml:id="supported-provider-types">
                <title>supported_provider_types</title>
                <para>Default: <literal>["*"]</literal></para>
                <para>For use with the provider network extension. Use this to explicitly set which
                            provider network types are supported. Only the network types in this list will
                            be available to choose from when creating a network.
                            Network types defined in Horizon or defined in <xref linkend="extra-provider-types"/>
                            settings can be specified in this list.
                            As of the Newton release, the network types defined in Horizon include
                            network types supported by Neutron ML2 plugin with Open vSwitch driver
                            (<literal>local</literal>, <literal>flat</literal>, <literal>vlan</literal>, <literal>gre</literal>, <literal>vxlan</literal> and <literal>geneve</literal>)
                            and supported by Midonet plugin (<literal>midonet</literal> and <literal>uplink</literal>).
                            <literal>["*"]</literal> means that all provider network types supported by Neutron
                            ML2 plugin will be available to choose from.</para>
                <para>Example: <literal>['local', 'flat', 'gre']</literal></para>
              </section>
              <section xml:id="supported-vnic-types">
                <title>supported_vnic_types</title>
                <para>Default <literal>['*']</literal></para>
                <para>For use with the port binding extension. Use this to explicitly set which VNIC
                            types are available for users to choose from, when creating or editing a port.
                            The VNIC types actually supported are determined by resource availability and
                            Neutron ML2 plugin support.
                            Currently, error reporting for users selecting an incompatible or unavailable
                            VNIC type is restricted to receiving a message from the scheduler that the
                            instance cannot spawn because of insufficient resources.
                            VNIC types include <literal>normal</literal>, <literal>direct</literal>, <literal>direct-physical</literal>, <literal>macvtap</literal>,
                            <literal>baremetal</literal> and <literal>virtio-forwarder</literal>. By default all VNIC types will be
                            available to choose from.</para>
                <para>Example: <literal>['normal', 'direct']</literal></para>
                <para>To disable VNIC type selection, set an empty list (<literal>[]</literal>) or <literal>None</literal>.</para>
              </section>
            </section>
          </section>
          <section xml:id="nova">
            <title>Nova</title>
            <section xml:id="create-instance-flavor-sort">
              <title>CREATE_INSTANCE_FLAVOR_SORT</title>
              <para>Default:</para>
              <screen language="python">{
    'key': 'ram'
}</screen>
              <para>When launching a new instance the default flavor is sorted by RAM usage in
                        ascending order.
                        You can customize the sort order by: id, name, ram, disk and vcpus.
                        Additionally, you can insert any custom callback function. You can also
                        provide a flag for reverse sort.
                        See the description in local_settings.py.example for more information.</para>
              <para>This example sorts flavors by vcpus in descending order:</para>
              <screen language="python">CREATE_INSTANCE_FLAVOR_SORT = {
     'key':'vcpus',
     'reverse': True,
}</screen>
            </section>
            <section xml:id="console-type">
              <title>CONSOLE_TYPE</title>
              <para>Default:  <literal>"AUTO"</literal></para>
              <para>This setting specifies the type of in-browser console used to access the VMs.
                        Valid values are  <literal>"AUTO"</literal>, <literal>"VNC"</literal>, <literal>"SPICE"</literal>, <literal>"RDP"</literal>,
                        <literal>"SERIAL"</literal>, and <literal>None</literal>.</para>
            </section>
            <section xml:id="enable-flavor-edit">
              <title>ENABLE_FLAVOR_EDIT</title>
              <para>Default: <literal>False</literal></para>
              <para>This setting enables the ability to edit flavors.</para>
              <warning>
                <para>Historically, Horizon has provided the ability to edit Flavors by deleting
                            and creating a new one with the same information. This is not supported in
                            the Nova API and causes unexpected issues and breakages. To avoid breaking
                            standard deprecation procedure, this code is still in Horizon, but disabled
                            by default. It will be removed during the 14.0.0 (‘R’) release cycle.</para>
                <para>See <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://lists.openstack.org/pipermail/openstack-dev/2017-August/120540.html">this email thread</link>
                            for further information.</para>
              </warning>
            </section>
            <section xml:id="instance-log-length">
              <title>INSTANCE_LOG_LENGTH</title>
              <para>Default:  <literal>35</literal></para>
              <para>This setting enables you to change the default number of lines displayed for
                        the log of an instance.
                        Valid value must be a positive integer.</para>
            </section>
            <section xml:id="launch-instance-defaults">
              <title>LAUNCH_INSTANCE_DEFAULTS</title>
              <para>Default:</para>
              <screen language="python">{
    "config_drive": False,
    "create_volume": True,
    "disable_image": False,
    "disable_instance_snapshot": False,
    "disable_volume": False,
    "disable_volume_snapshot": False,
    "enable_scheduler_hints": True,
}</screen>
              <para>A dictionary of settings which can be used to provide the default values for
                        properties found in the Launch Instance modal. An explanation of each setting
                        is provided below.</para>
              <section xml:id="config-drive">
                <title>config_drive</title>
                <para>Default: <literal>False</literal></para>
                <para>This setting specifies the default value for the Configuration Drive property.</para>
              </section>
              <section xml:id="create-volume">
                <title>create_volume</title>
                <para>Default: <literal>True</literal></para>
                <para>This setting allows you to specify the default value for the option of creating
                            a new volume in the workflow for image and instance snapshot sources.</para>
              </section>
              <section xml:id="disable-image">
                <title>disable_image</title>
                <para>Default: <literal>False</literal></para>
                <para>This setting disables Images as a valid boot source for launching instances.
                            Image sources won’t show up in the Launch Instance modal.</para>
              </section>
              <section xml:id="disable-instance-snapshot">
                <title>disable_instance_snapshot</title>
                <para>Default: <literal>False</literal></para>
                <para>This setting disables Snapshots as a valid boot source for launching instances.
                            Snapshots sources won’t show up in the Launch Instance modal.</para>
              </section>
              <section xml:id="disable-volume">
                <title>disable_volume</title>
                <para>Default: <literal>False</literal></para>
                <para>This setting disables Volumes as a valid boot source for launching instances.
                            Volumes sources won’t show up in the Launch Instance modal.</para>
              </section>
              <section xml:id="disable-volume-snapshot">
                <title>disable_volume_snapshot</title>
                <para>Default: <literal>False</literal></para>
                <para>This setting disables Volume Snapshots as a valid boot source for launching
                            instances. Volume Snapshots sources won’t show up in the Launch Instance modal.</para>
              </section>
              <section xml:id="enable-scheduler-hints">
                <title>enable_scheduler_hints</title>
                <para>Default: <literal>True</literal></para>
                <para>This setting specifies whether or not Scheduler Hints can be provided when
                            launching an instance.</para>
              </section>
            </section>
            <section xml:id="launch-instance-legacy-enabled">
              <title>LAUNCH_INSTANCE_LEGACY_ENABLED</title>
              <para>Default: <literal>False</literal></para>
              <para>This setting enables the Python Launch Instance workflow.</para>
              <note>
                <para>It is possible to run both the AngularJS and Python workflows simultaneously,
                            so the other may be need to be toggled with <xref linkend="launch-instance-ng-enabled"/></para>
              </note>
            </section>
            <section xml:id="launch-instance-ng-enabled">
              <title>LAUNCH_INSTANCE_NG_ENABLED</title>
              <para>Default: <literal>True</literal></para>
              <para>This setting enables the AngularJS Launch Instance workflow.</para>
              <note>
                <para>It is possible to run both the AngularJS and Python workflows simultaneously,
                            so the other may be need to be toggled with <xref linkend="launch-instance-legacy-enabled"/></para>
              </note>
            </section>
            <section xml:id="openstack-enable-password-retrieve">
              <title>OPENSTACK_ENABLE_PASSWORD_RETRIEVE</title>
              <para>Default: <literal>"False"</literal></para>
              <para>When set, enables the instance action “Retrieve password” allowing password
                        retrieval from metadata service.</para>
            </section>
            <section xml:id="openstack-hypervisor-features">
              <title>OPENSTACK_HYPERVISOR_FEATURES</title>
              <para>Default:</para>
              <screen language="python">{
    'can_set_mount_point': False,
    'can_set_password': False,
    'requires_keypair': False,
    'enable_quotas': True
}</screen>
              <para>A dictionary containing settings which can be used to identify the
                        capabilities of the hypervisor for Nova.</para>
              <para>The Xen Hypervisor has the ability to set the mount point for volumes attached
                        to instances (other Hypervisors currently do not). Setting
                        <literal>can_set_mount_point</literal> to <literal>True</literal> will add the option to set the mount point
                        from the UI.</para>
              <para>Setting <literal>can_set_password</literal> to <literal>True</literal> will enable the option to set
                        an administrator password when launching or rebuilding an instance.</para>
              <para>Setting <literal>requires_keypair</literal> to <literal>True</literal> will require users to select
                        a key pair when launching an instance.</para>
              <para>Setting <literal>enable_quotas</literal> to <literal>False</literal> will make Horizon treat all Nova
                        quotas as disabled, thus it won’t try to modify them. By default, quotas are
                        enabled.</para>
            </section>
            <section xml:id="openstack-nova-extensions-blacklist">
              <title>OPENSTACK_NOVA_EXTENSIONS_BLACKLIST</title>
              <para>Default: <literal>[]</literal></para>
              <para>Ignore all listed Nova extensions, and behave as if they were unsupported.
                        Can be used to selectively disable certain costly extensions for performance
                        reasons.</para>
            </section>
          </section>
          <section xml:id="sahara">
            <title>Sahara</title>
            <warning>
              <para>The Sahara dashboard was removed from Horizon during the Newton cycle,
                        and any settings here should be considered legacy. For more up to date
                        information, see the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/sahara-dashboard">Sahara Dashboard repo</link></para>
            </warning>
            <section xml:id="sahara-auto-ip-allocation-enabled">
              <title>SAHARA_AUTO_IP_ALLOCATION_ENABLED</title>
              <para>Default:  <literal>False</literal></para>
              <para>This setting notifies the Data Processing (Sahara) system whether or not
                        automatic IP allocation is enabled.  You would want to set this to <literal>True</literal>
                        if you were running Nova Networking with <literal>auto_assign_floating_ip</literal> set to
                        <literal>True</literal>.</para>
            </section>
          </section>
          <section xml:id="swift">
            <title>Swift</title>
            <section xml:id="swift-file-transfer-chunk-size">
              <title>SWIFT_FILE_TRANSFER_CHUNK_SIZE</title>
              <para>Default: <literal>512 * 1024</literal></para>
              <para>This setting specifies the size of the chunk (in bytes) for downloading objects
                        from Swift. Do not make it very large (higher than several dozens of Megabytes,
                        exact number depends on your connection speed), otherwise you may encounter
                        socket timeout. The default value is 524288 bytes (or 512 Kilobytes).</para>
            </section>
          </section>
          <section xml:id="trove">
            <title>Trove</title>
            <warning>
              <para>The Trove dashboard was removed from Horizon during the Newton cycle,
                        and any settings here should be considered legacy. For more up to date
                        information, see the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/openstack/trove-dashboard">Trove Dashboard repo</link></para>
            </warning>
            <section xml:id="trove-add-database-perms">
              <title>TROVE_ADD_DATABASE_PERMS</title>
              <para>Default: <literal>[]</literal></para>
              <para>Trove database extension support. By default, support for creating databases on
                        database instances is turned on. To disable this extensions set the permission
                        to something unusable such as <literal>[!]</literal>.</para>
            </section>
            <section xml:id="trove-add-user-perms">
              <title>TROVE_ADD_USER_PERMS</title>
              <para>Default: <literal>[]</literal></para>
              <para>Trove users extension support. By default, support for creating users on
                        database instances is turned on. To disable this extensions set the permission
                        to something unusable such as <literal>[!]</literal>.</para>
            </section>
          </section>
        </section>
        <section xml:id="django-settings">
          <title>Django Settings</title>
          <note>
            <para>This is not meant to be anywhere near a complete list of settings for
                    Django. You should always consult the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/topics/settings/">upstream documentation</link>, especially
                    with regards to deployment considerations and security best-practices.</para>
          </note>
          <section xml:id="add-installed-apps-1">
            <title>ADD_INSTALLED_APPS</title>
            <formalpara>
              <title>See also</title>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#installed_apps">Django’s INSTALLED_APPS documentation</link>
              </para>
            </formalpara>
            <para>A list of Django applications to be prepended to the <literal>INSTALLED_APPS</literal>
                    setting. Allows extending the list of installed applications without having
                    to override it completely.</para>
          </section>
          <section xml:id="allowed-hosts">
            <title>ALLOWED_HOSTS</title>
            <formalpara>
              <title>See also</title>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#allowed-hosts">Django’s ALLOWED_HOSTS documentation</link>
              </para>
            </formalpara>
            <para>Default: <literal>['localhost']</literal></para>
            <para>This list should contain names (or IP addresses) of the host
                    running the dashboard; if it’s being accessed via name, the
                    DNS name (and probably short-name) should be added, if it’s accessed via
                    IP address, that should be added. The setting may contain more than one entry.</para>
            <note>
              <para>ALLOWED_HOSTS is required. If Horizon is running in production (DEBUG is
                        False), set this with the list of host/domain names that the application
                        can serve. For more information see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#allowed-hosts">Django’s Allowed Hosts documentation</link></para>
            </note>
          </section>
          <section xml:id="debug-setting">
            <title>DEBUG</title>
            <formalpara>
              <title>See also</title>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#debug">Django’s DEBUG documentation</link>
              </para>
            </formalpara>
            <para>Default: <literal>True</literal></para>
            <para>Controls whether unhandled exceptions should generate a generic 500 response
                    or present the user with a pretty-formatted debug information page.</para>
            <para>When set, <xref linkend="cached-template-loaders"/> will not be cached.</para>
            <para>This setting should <emphasis role="bold">always</emphasis> be set to <literal>False</literal> for production deployments
                    as the debug page can display sensitive information to users and attackers
                    alike.</para>
          </section>
          <section xml:id="secret-key">
            <title>SECRET_KEY</title>
            <formalpara>
              <title>See also</title>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#secret-key">Django’s SECRET_KEY documentation</link>
              </para>
            </formalpara>
            <para>This should absolutely be set to a unique (and secret) value for your
                    deployment. Unless you are running a load-balancer with multiple Horizon
                    installations behind it, each Horizon instance should have a unique secret key.</para>
            <note>
              <para>Setting a custom secret key:</para>
              <para>You can either set it to a specific value or you can let Horizon generate a
                        default secret key that is unique on this machine, regardless of the
                        amount of Python WSGI workers (if used behind Apache+mod_wsgi). However,
                        there may be situations where you would want to set this explicitly, e.g.
                        when multiple dashboard instances are distributed on different machines
                        (usually behind a load-balancer). Either you have to make sure that a
                        session gets all requests routed to the same dashboard instance or you set
                        the same SECRET_KEY for all of them.</para>
            </note>
            <screen language="python">from horizon.utils import secret_key

SECRET_KEY = secret_key.generate_or_read_from_file(
os.path.join(LOCAL_PATH, '.secret_key_store'))</screen>
            <para>The <literal>local_settings.py.example</literal> file includes a quick-and-easy way to
                    generate a secret key for a single installation.</para>
          </section>
          <section xml:id="static-root">
            <title>STATIC_ROOT</title>
            <formalpara>
              <title>See also</title>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#static-root">Django’s STATIC_ROOT documentation</link>
              </para>
            </formalpara>
            <para>Default: <literal>&lt;path_to_horizon&gt;/static</literal></para>
            <para>The absolute path to the directory where static files are collected when
                    collectstatic is run.</para>
          </section>
          <section xml:id="static-url">
            <title>STATIC_URL</title>
            <formalpara>
              <title>See also</title>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#static-url">Django’s STATIC_URL documentation</link>
              </para>
            </formalpara>
            <para>Default: <literal>/static/</literal></para>
            <para>URL that refers to files in <xref linkend="static-root"/>.</para>
            <para>By default this value is <literal>WEBROOT/static/</literal>.</para>
            <para>This value can be changed from the default. When changed, the alias in your
                    webserver configuration should be updated to match.</para>
            <note>
              <para>The value for STATIC_URL must end in ‘/’.</para>
            </note>
            <para>This value is also available in the scss namespace with the variable name
                    $static_url.  Make sure you run <literal>python manage.py collectstatic</literal> and
                    <literal>python manage.py compress</literal> after any changes to this value in settings.py.</para>
          </section>
          <section xml:id="templates-horizon">
            <title>TEMPLATES</title>
            <formalpara>
              <title>See also</title>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/ref/settings/#templates">Django’s TEMPLATES documentation</link>
              </para>
            </formalpara>
            <para>Horizon’s usage of the <literal>TEMPLATES</literal> involves 3 further settings below;
                    it is generally advised to use those before attempting to alter the
                    <literal>TEMPLATES</literal> setting itself.</para>
            <section xml:id="add-template-loaders">
              <title>ADD_TEMPLATE_LOADERS</title>
              <para>Template loaders defined here will be loaded at the end of <xref linkend="template-loaders"/>,
                        after the <xref linkend="cached-template-loaders"/> and will never have a cached output.</para>
            </section>
            <section xml:id="cached-template-loaders">
              <title>CACHED_TEMPLATE_LOADERS</title>
              <para>Template loaders defined here will have their output cached
	      if DEBUG <!-- FIXME <xref linkend="debug"/> -->
                        is set to <literal>False</literal>.</para>
            </section>
            <section xml:id="template-loaders">
              <title>TEMPLATE_LOADERS</title>
              <para>These template loaders will be the first loaders and get loaded before the
                        CACHED_TEMPLATE_LOADERS. Use ADD_TEMPLATE_LOADERS if you want to add loaders at
                        the end and not cache loaded templates.
                        After the whole settings process has gone through, TEMPLATE_LOADERS will be:</para>
              <screen language="python">TEMPLATE_LOADERS += (
    ('django.template.loaders.cached.Loader', CACHED_TEMPLATE_LOADERS),
) + tuple(ADD_TEMPLATE_LOADERS)</screen>
            </section>
          </section>
        </section>
      </section>
      <section xml:id="pluggable-settings-label" xml:base="pluggable_panels">
        <title>Pluggable Panels and Groups</title>
        <section xml:id="introduction-2">
          <title>Introduction</title>
          <para>Horizon allows dashboards, panels and panel groups to be added without
                modifying the default settings. Pluggable settings are a mechanism to allow
                settings to be stored in separate files.  Those files are read at startup and
                used to modify the default settings.</para>
          <para>The default location for the dashboard configuration files is
                <literal>openstack_dashboard/enabled</literal>, with another directory,
                <literal>openstack_dashboard/local/enabled</literal> for local overrides. Both sets of files
                will be loaded, but the settings in <literal>openstack_dashboard/local/enabled</literal> will
                overwrite the default ones. The settings are applied in alphabetical order of
                the filenames. If the same dashboard has configuration files in <literal>enabled</literal> and
                <literal>local/enabled</literal>, the local name will be used. Note, that since names of
                python modules can’t start with a digit, the files are usually named with a
                leading underscore and a number, so that you can control their order easily.</para>
        </section>
        <section xml:id="general-pluggbale-settings">
          <title>General Pluggbale Settings</title>
          <para>Before we describe the specific use cases, the following keys can be used in
                any pluggable settings file:</para>
          <section xml:id="add-exceptions">
            <title>
              <literal>ADD_EXCEPTIONS</literal>
            </title>
            <para>A dictionary of exception classes to be added to <literal>HORIZON['exceptions']</literal>.</para>
          </section>
          <section xml:id="add-installed-apps-2">
            <title>
              <literal>ADD_INSTALLED_APPS</literal>
            </title>
            <para>A list of applications to be prepended to <literal>INSTALLED_APPS</literal>.
                    This is needed to expose static files from a plugin.</para>
          </section>
          <section xml:id="add-angular-modules">
            <title>
              <literal>ADD_ANGULAR_MODULES</literal>
            </title>
            <para>A list of AngularJS modules to be loaded when Angular bootstraps. These modules
                    are added as dependencies on the root Horizon application <literal>horizon</literal>.</para>
          </section>
          <section xml:id="add-js-files">
            <title>
              <literal>ADD_JS_FILES</literal>
            </title>
            <para>A list of javascript source files to be included in the compressed set of files
                    that are loaded on every page. This is needed for AngularJS modules that are
                    referenced in <literal>ADD_ANGULAR_MODULES</literal> and therefore need to be included in
                    every page.</para>
          </section>
          <section xml:id="add-js-spec-files">
            <title>
              <literal>ADD_JS_SPEC_FILES</literal>
            </title>
            <para>A list of javascript spec files to include for integration with the Jasmine
                    spec runner. Jasmine is a behavior-driven development framework for testing
                    JavaScript code.</para>
          </section>
          <section xml:id="add-scss-files">
            <title>
              <literal>ADD_SCSS_FILES</literal>
            </title>
            <para>A list of scss files to be included in the compressed set of files that are
                    loaded on every page. We recommend one scss file per dashboard, use @import if
                    you need to include additional scss files for panels.</para>
          </section>
          <section xml:id="auto-discover-static-files">
            <title>
              <literal>AUTO_DISCOVER_STATIC_FILES</literal>
            </title>
            <para>If set to <literal>True</literal>, JavaScript files and static angular html template files
                    will be automatically discovered from the <literal>static</literal> folder in each apps listed
                    in ADD_INSTALLED_APPS.</para>
            <para>JavaScript source files will be ordered based on naming convention: files with
                    extension <literal>.module.js</literal> listed first, followed by other JavaScript source files.</para>
            <para>JavaScript files for testing will also be ordered based on naming convention:
                    files with extension <literal>.mock.js</literal> listed first, followed by files with extension
                    <literal>.spec.js</literal>.</para>
            <para>If ADD_JS_FILES and/or ADD_JS_SPEC_FILES are also specified, files manually
                    listed there will be appended to the auto-discovered files.</para>
          </section>
          <section xml:id="disabled">
            <title>
              <literal>DISABLED</literal>
            </title>
            <para>If set to <literal>True</literal>, this settings file will not be added to the settings.</para>
          </section>
          <section xml:id="update-horizon-config">
            <title>
              <literal>UPDATE_HORIZON_CONFIG</literal>
            </title>
            <para>A dictionary of values that will replace the values in <literal>HORIZON_CONFIG</literal>.</para>
          </section>
        </section>
        <section xml:id="pluggable-settings-for-dashboards">
          <title>Pluggable Settings for Dashboards</title>
          <para>The following keys are specific to registering a dashboard:</para>
          <section xml:id="dashboard">
            <title>
              <literal>DASHBOARD</literal>
            </title>
            <para>The slug of the dashboard to be added to <literal>HORIZON['dashboards']</literal>. Required.</para>
          </section>
          <section xml:id="default">
            <title>
              <literal>DEFAULT</literal>
            </title>
            <para>If set to <literal>True</literal>, this dashboard will be set as the default dashboard.</para>
          </section>
          <section xml:id="examples">
            <title>Examples</title>
            <para>To disable a dashboard locally, create a file
                    <literal>openstack_dashboard/local/enabled/_40_dashboard-name.py</literal> with the following
                    content:</para>
            <screen>DASHBOARD = '&lt;dashboard-name&gt;'
DISABLED = True</screen>
            <para>To add a Tuskar-UI (Infrastructure) dashboard, you have to install it, and then
                    create a file <literal>openstack_dashboard/local/enabled/_50_tuskar.py</literal> with:</para>
            <screen>from tuskar_ui import exceptions

DASHBOARD = 'infrastructure'
ADD_INSTALLED_APPS = [
    'tuskar_ui.infrastructure',
]
ADD_EXCEPTIONS = {
    'recoverable': exceptions.RECOVERABLE,
    'not_found': exceptions.NOT_FOUND,
    'unauthorized': exceptions.UNAUTHORIZED,
}</screen>
          </section>
        </section>
        <section xml:id="pluggable-settings-for-panels">
          <title>Pluggable Settings for Panels</title>
          <para>The following keys are specific to registering or removing a panel:</para>
          <section xml:id="panel">
            <title>
              <literal>PANEL</literal>
            </title>
            <para>The slug of the panel to be added to <literal>HORIZON_CONFIG</literal>. Required.</para>
          </section>
          <section xml:id="panel-dashboard">
            <title>
              <literal>PANEL_DASHBOARD</literal>
            </title>
            <para>The slug of the dashboard the <literal>PANEL</literal> associated with. Required.</para>
          </section>
          <section xml:id="panel-group">
            <title>
              <literal>PANEL_GROUP</literal>
            </title>
            <para>The slug of the panel group the <literal>PANEL</literal> is associated with. If you want the
                    panel to show up without a panel group, use the panel group “default”.</para>
          </section>
          <section xml:id="default-panel">
            <title>
              <literal>DEFAULT_PANEL</literal>
            </title>
            <para>If set, it will update the default panel of the <literal>PANEL_DASHBOARD</literal>.</para>
          </section>
          <section xml:id="add-panel">
            <title>
              <literal>ADD_PANEL</literal>
            </title>
            <para>Python panel class of the <literal>PANEL</literal> to be added.</para>
          </section>
          <section xml:id="remove-panel">
            <title>
              <literal>REMOVE_PANEL</literal>
            </title>
            <para>If set to <literal>True</literal>, the PANEL will be removed from PANEL_DASHBOARD/PANEL_GROUP.</para>
          </section>
          <section xml:id="id2-horizon">
            <title>Examples</title>
            <para>To add a new panel to the Admin panel group in Admin dashboard, create a file
                    <literal>openstack_dashboard/local/enabled/_60_admin_add_panel.py</literal> with the following
                    content:</para>
            <screen>PANEL = 'plugin_panel'
PANEL_DASHBOARD = 'admin'
PANEL_GROUP = 'admin'
ADD_PANEL = 'test_panels.plugin_panel.panel.PluginPanel'</screen>
            <para>To remove Info panel from Admin panel group in Admin dashboard locally, create
                    a file <literal>openstack_dashboard/local/enabled/_70_admin_remove_panel.py</literal> with
                    the following content:</para>
            <screen>PANEL = 'info'
PANEL_DASHBOARD = 'admin'
PANEL_GROUP = 'admin'
REMOVE_PANEL = True</screen>
            <para>To change the default panel of Admin dashboard to Instances panel, create a
                    file <literal>openstack_dashboard/local/enabled/_80_admin_default_panel.py</literal> with the
                    following content:</para>
            <screen>PANEL = 'instances'
PANEL_DASHBOARD = 'admin'
PANEL_GROUP = 'admin'
DEFAULT_PANEL = 'instances'</screen>
          </section>
        </section>
        <section xml:id="pluggable-settings-for-panel-groups">
          <title>Pluggable Settings for Panel Groups</title>
          <para>The following keys are specific to registering a panel group:</para>
          <section xml:id="id3-horizon">
            <title>
              <literal>PANEL_GROUP</literal>
            </title>
            <para>The slug of the panel group to be added to <literal>HORIZON_CONFIG</literal>. Required.</para>
          </section>
          <section xml:id="panel-group-name">
            <title>
              <literal>PANEL_GROUP_NAME</literal>
            </title>
            <para>The display name of the PANEL_GROUP. Required.</para>
          </section>
          <section xml:id="panel-group-dashboard">
            <title>
              <literal>PANEL_GROUP_DASHBOARD</literal>
            </title>
            <para>The slug of the dashboard the <literal>PANEL_GROUP</literal> associated with. Required.</para>
          </section>
          <section xml:id="id4-horizon">
            <title>Examples</title>
            <para>To add a new panel group to the Admin dashboard, create a file
                    <literal>openstack_dashboard/local/enabled/_90_admin_add_panel_group.py</literal> with the
                    following content:</para>
            <screen>PANEL_GROUP = 'plugin_panel_group'
PANEL_GROUP_NAME = 'Plugin Panel Group'
PANEL_GROUP_DASHBOARD = 'admin'</screen>
          </section>
        </section>
      </section>
      <section xml:id="install-customizing" xml:base="customizing">
        <title>Customizing Horizon</title>
        <formalpara>
          <title>See also</title>
          <para>You may also be interested in <xref linkend="themes"/> and <xref linkend="branding-horizon"/>.</para>
        </formalpara>
        <section xml:id="changing-the-site-title">
          <title>Changing the Site Title</title>
          <para>The OpenStack Dashboard Site Title branding (i.e. “<emphasis role="bold">OpenStack</emphasis> Dashboard”)
                can be overwritten by adding the attribute <literal>SITE_BRANDING</literal>
                to <literal>local_settings.py</literal> with the value being the desired name.</para>
          <para>The file <literal>local_settings.py</literal> can be found at the Horizon directory path of
                <literal>openstack_dashboard/local/local_settings.py</literal>.</para>
        </section>
        <section xml:id="changing-the-brand-link">
          <title>Changing the Brand Link</title>
          <para>The logo also acts as a hyperlink. The default behavior is to redirect to
                <literal>horizon:user_home</literal>. By adding the attribute <literal>SITE_BRANDING_LINK</literal> with
                the desired url target e.g., <literal>http://sample-company.com</literal> in
                <literal>local_settings.py</literal>, the target of the hyperlink can be changed.</para>
        </section>
        <section xml:id="customizing-the-footer">
          <title>Customizing the Footer</title>
          <para>It is possible to customize the global and login footers using a theme’s
                template override.  Simply add <literal>_footer.html</literal> for a global footer
                override or <literal>_login_footer.html</literal> for the login page’s footer to your
                theme’s template directory.</para>
        </section>
        <section xml:id="modifying-existing-dashboards-and-panels">
          <title>Modifying Existing Dashboards and Panels</title>
          <para>If you wish to alter dashboards or panels which are not part of your codebase,
                you can specify a custom python module which will be loaded after the entire
                Horizon site has been initialized, but prior to the URLconf construction.
                This allows for common site-customization requirements such as:</para>
          <itemizedlist>
            <listitem>
              <para>Registering or unregistering panels from an existing dashboard.</para>
            </listitem>
            <listitem>
              <para>Changing the names of dashboards and panels.</para>
            </listitem>
            <listitem>
              <para>Re-ordering panels within a dashboard or panel group.</para>
            </listitem>
          </itemizedlist>
          <para>Default Horizon panels are loaded based upon files within the
                openstack_dashboard/enabled/ folder. These files are loaded based upon the
                filename order, with space left for more files to be added. There are some
                example files available within this folder, with the .example suffix
                added. Developers and deployers should strive to use this method of
                customization as much as possible, and support for this is given preference
                over more exotic methods such as monkey patching and overrides files.</para>
        </section>
        <section xml:id="horizon-customization-module">
          <title>Horizon customization module (overrides)</title>
          <para>Horizon has a global overrides mechanism available to perform customizations
                that are not yet customizable via configuration settings. This file can perform
                monkey patching and other forms of customization which are not possible via the
                enabled folder’s customization method.</para>
          <para>This method of customization is meant to be available for deployers of Horizon,
                and use of this should be avoided by Horizon plugins at all cost. Plugins
                needing this level of monkey patching and flexibility should instead look for
                changing their __init__.py file and performing customizations through other
                means.</para>
          <para>To specify the python module containing your modifications, add the key
                <literal>customization_module</literal> to your <literal>HORIZON_CONFIG</literal> dictionary in
                <literal>local_settings.py</literal>. The value should be a string containing the path to your
                module in dotted python path notation. Example:</para>
          <screen>HORIZON_CONFIG["customization_module"] = "my_project.overrides"</screen>
          <para>You can do essentially anything you like in the customization module. For
                example, you could change the name of a panel:</para>
          <screen>from django.utils.translation import ugettext_lazy as _

import horizon

# Rename "User Settings" to "User Options"
settings = horizon.get_dashboard("settings")
user_panel = settings.get_panel("user")
user_panel.name = _("User Options")</screen>
          <para>Or get the instances panel:</para>
          <screen>projects_dashboard = horizon.get_dashboard("project")
instances_panel = projects_dashboard.get_panel("instances")</screen>
          <para>Or just remove it entirely:</para>
          <screen>projects_dashboard.unregister(instances_panel.__class__)</screen>
          <para>You cannot unregister a <literal>default_panel</literal>. If you wish to remove a
                <literal>default_panel</literal>, you need to make a different panel in the dashboard as a
                <literal>default_panel</literal> and then unregister the former. For example, if you wished
                to remove the <literal>overview_panel</literal> from the <literal>Project</literal> dashboard, you could do
                the following:</para>
          <screen>project = horizon.get_dashboard('project')
project.default_panel = "instances"
overview = project.get_panel('overview')
project.unregister(overview.__class__)</screen>
          <para>You can also override existing methods with your own versions:</para>
          <screen>from openstack_dashboard.dashboards.admin.info import tabs
from openstack_dashboard.dashboards.project.instances import tables

NO = lambda *x: False

tabs.HeatServiceTab.allowed = NO
tables.AssociateIP.allowed = NO
tables.SimpleAssociateIP.allowed = NO
tables.SimpleDisassociateIP.allowed = NO</screen>
          <para>You could also customize what columns are displayed in an existing
                table, by redefining the <literal>columns</literal> attribute of its <literal>Meta</literal>
                class. This can be achieved in 3 steps:</para>
          <procedure>
            <step>
              <para>Extend the table that you wish to modify</para>
            </step>
            <step>
              <para>Redefine the <literal>columns</literal> attribute under the <literal>Meta</literal> class for this
                        new table</para>
            </step>
            <step>
              <para>Modify the <literal>table_class</literal> attribute for the related view so that it
                        points to the new table</para>
            </step>
          </procedure>
          <para>For example, if you wished to remove the Admin State column from the
                <literal>NetworksTable</literal>,
                you could do the following:</para>
          <screen>from openstack_dashboard.dashboards.project.networks import tables
from openstack_dashboard.dashboards.project.networks import views

class MyNetworksTable(tables.NetworksTable):

    class Meta(tables.NetworksTable.Meta):
        columns = ('name', 'subnets', 'shared', 'status')

views.IndexView.table_class = MyNetworksTable</screen>
          <para>If you want to add a column you can override the parent table in a
                similar way, add the new column definition and then use the <literal>Meta</literal><literal>columns</literal> attribute to control the column order as needed.</para>
          <note>
            <para><literal>my_project.overrides</literal> needs to be importable by the python process running
                    Horizon.
                    If your module is not installed as a system-wide python package,
                    you can either make it installable (e.g., with a setup.py)
                    or you can adjust the python path used by your WSGI server to include its location.</para>
            <para>Probably the easiest way is to add a <literal>python-path</literal> argument to
                    the <literal>WSGIDaemonProcess</literal> line in Apache’s Horizon config.</para>
            <para>Assuming your <literal>my_project</literal> module lives in <literal>/opt/python/my_project</literal>,
                    you’d make it look like the following:</para>
            <screen>WSGIDaemonProcess [... existing options ...] python-path=/opt/python</screen>
          </note>
        </section>
        <section xml:id="customize-the-project-and-user-table-columns">
          <title>Customize the project and user table columns</title>
          <para>Keystone V3 has a place to store extra information regarding project and user.
                Using the override mechanism described in <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#horizon-customization-module">Horizon customization module (overrides)</link>,
                Horizon is able to show these extra information as a custom column.
                For example, if a user in Keystone has an attribute <literal>phone_num</literal>, you could
                define new column:</para>
          <screen>from django.utils.translation import ugettext_lazy as _

from horizon import forms
from horizon import tables

from openstack_dashboard.dashboards.identity.users import tables as user_tables
from openstack_dashboard.dashboards.identity.users import views

class MyUsersTable(user_tables.UsersTable):
    phone_num = tables.Column('phone_num',
                              verbose_name=_('Phone Number'),
                              form_field=forms.CharField(),)

    class Meta(user_tables.UsersTable.Meta):
        columns = ('name', 'description', 'phone_num')

views.IndexView.table_class = MyUsersTable</screen>
        </section>
        <section xml:id="customize-angular-dashboards">
          <title>Customize Angular dashboards</title>
          <para>In Angular, you may write a plugin to extend certain features. Two components
                in the Horizon framework that make this possible are the extensibility service
                and the resource type registry service. The <literal>extensibleService</literal> allows
                certain Horizon elements to be extended dynamically, including add, remove, and
                replace. The <literal>resourceTypeRegistry</literal> service provides methods to set and get
                information pertaining to a resource type object. We use Heat type names like
                <literal>OS::Glance::Image</literal> as our reference name.</para>
          <para>Some information you may place in the registry include:</para>
          <itemizedlist>
            <listitem>
              <para>API to fetch data from</para>
            </listitem>
            <listitem>
              <para>Property names</para>
            </listitem>
            <listitem>
              <para>Actions (e.g. “Create Volume”)</para>
            </listitem>
            <listitem>
              <para>URL paths to detail view or detail drawer</para>
            </listitem>
            <listitem>
              <para>Property information like labels or formatting for property values</para>
            </listitem>
          </itemizedlist>
          <para>These properties in the registry use the extensibility service (as of Newton
                release):</para>
          <itemizedlist>
            <listitem>
              <para>globalActions</para>
            </listitem>
            <listitem>
              <para>batchActions</para>
            </listitem>
            <listitem>
              <para>itemActions</para>
            </listitem>
            <listitem>
              <para>detailViews</para>
            </listitem>
            <listitem>
              <para>tableColumns</para>
            </listitem>
            <listitem>
              <para>filterFacets</para>
            </listitem>
          </itemizedlist>
          <para>Using the information from the registry, we can build out our dashboard panels.
                Panels use the high-level directive <literal>hzResourceTable</literal> that replaces common
                templates so we do not need to write boilerplate HTML and controller code. It
                gives developers a quick way to build a new table or change an existing table.</para>
          <note>
            <para>You may still choose to use the HTML template for complete control of form
                    and functionality. For example, you may want to create a custom footer.
                    You may also use the <literal>hzDynamicTable</literal> directive (what <literal>hzResourceTable</literal>
                    uses under the hood) directly. However, neither of these is extensible.
                    You would need to override the panel completely.</para>
          </note>
          <para>This is a sample module file to demonstrate how to make some customizations to
                the Images Panel.:</para>
          <screen>(function() {
  'use strict';

  angular
    .module('horizon.app.core.images')
    .run(customizeImagePanel);

  customizeImagePanel.$inject = [
    'horizon.framework.conf.resource-type-registry.service',
    'horizon.app.core.images.basePath',
    'horizon.app.core.images.resourceType',
    'horizon.app.core.images.actions.surprise.service'
  ];

  function customizeImagePanel(registry, basePath, imageResourceType, surpriseService) {
    // get registry for ``OS::Glance::Image``
    registry = registry.getResourceType(imageResourceType);

    // replace existing Size column to make the font color red
    var column = {
      id: 'size',
      priority: 2,
      template: '&lt;a style="color:red;"&gt;{$ item.size | bytes $}&lt;/a&gt;'
    };
    registry.tableColumns.replace('size', column);

    // add a new detail view
    registry.detailsViews
      .append({
        id: 'anotherDetailView',
        name: gettext('Another Detail View'),
        template: basePath + 'demo/detail.html'
    });

    // set a different summary drawer template
    registry.setSummaryTemplateUrl(basePath + 'demo/drawer.html');

    // add a new global action
    registry.globalActions
      .append({
        id: 'surpriseAction',
        service: surpriseService,
        template: {
          text: gettext('Surprise')
        }
    });
  }
})();</screen>
          <para>Additionally, you should have content defined in <literal>detail.html</literal> and
                <literal>drawer.html</literal>, as well as define the <literal>surpriseService</literal> which is based off
                the <literal>actions</literal> directive and needs allowed and perform methods defined.</para>
        </section>
        <section xml:id="icons">
          <title>Icons</title>
          <para>Horizon uses font icons from Font Awesome.  Please see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://fortawesome.github.io/Font-Awesome/">Font Awesome</link> for
                instructions on how to use icons in the code.</para>
          <para>To add icon to Table Action, use icon property. Example:</para>
          <variablelist>
            <varlistentry>
              <term>class CreateSnapshot(tables.LinkAction):</term>
              <listitem>
                <para>name = “snapshot”
                                verbose_name = _(“Create Snapshot”)
                                icon = “camera”</para>
              </listitem>
            </varlistentry>
          </variablelist>
          <para>Additionally, the site-wide default button classes can be configured by
                setting <literal>ACTION_CSS_CLASSES</literal> to a tuple of the classes you wish to appear
                on all action buttons in your <literal>local_settings.py</literal> file.</para>
        </section>
        <section xml:id="custom-stylesheets">
          <title>Custom Stylesheets</title>
          <para>It is possible to define custom stylesheets for your dashboards. Horizon’s base
                template <literal>openstack_dashboard/templates/base.html</literal> defines multiple blocks
                that can be overridden.</para>
          <para>To define custom css files that apply only to a specific dashboard, create
                a base template in your dashboard’s templates folder, which extends Horizon’s
                base template e.g. <literal>openstack_dashboard/dashboards/my_custom_dashboard/
templates/my_custom_dashboard/base.html</literal>.</para>
          <para>In this template, redefine <literal>block css</literal>. (Don’t forget to include
                <literal>_stylesheets.html</literal> which includes all Horizon’s default stylesheets.):</para>
          <screen>{% extends 'base.html' %}

{% block css %}
  {% include "_stylesheets.html" %}

  {% load compress %}
  {% compress css %}
  &lt;link href='{{ STATIC_URL }}my_custom_dashboard/scss/my_custom_dashboard.scss' type='text/scss' media='screen' rel='stylesheet' /&gt;
  {% endcompress %}
{% endblock %}</screen>
          <para>The custom stylesheets then reside in the dashboard’s own <literal>static</literal> folder
                <literal>openstack_dashboard/dashboards/my_custom_dashboard/static/
my_custom_dashboard/scss/my_custom_dashboard.scss</literal>.</para>
          <para>All dashboard’s templates have to inherit from dashboard’s base.html:</para>
          <screen>{% extends 'my_custom_dashboard/base.html' %}
...</screen>
        </section>
        <section xml:id="custom-javascript">
          <title>Custom Javascript</title>
          <para>Similarly to adding custom styling (see above), it is possible to include
                custom javascript files.</para>
          <para>All Horizon’s javascript files are listed in the <literal>openstack_dashboard/
templates/horizon/_scripts.html</literal> partial template, which is included in
                Horizon’s base template in <literal>block js</literal>.</para>
          <para>To add custom javascript files, create an <literal>_scripts.html</literal> partial template in
                your dashboard <literal>openstack_dashboard/dashboards/my_custom_dashboard/
templates/my_custom_dashboard/_scripts.html</literal> which extends
                <literal>horizon/_scripts.html</literal>. In this template override the
                <literal>block custom_js_files</literal> including your custom javascript files:</para>
          <screen>{% extends 'horizon/_scripts.html' %}

{% block custom_js_files %}
    &lt;script src='{{ STATIC_URL }}my_custom_dashboard/js/my_custom_js.js' type='text/javascript' charset='utf-8'&gt;&lt;/script&gt;
{% endblock %}</screen>
          <para>In your dashboard’s own base template <literal>openstack_dashboard/dashboards/
my_custom_dashboard/templates/my_custom_dashboard/base.html</literal> override
                <literal>block js</literal> with inclusion of dashboard’s own <literal>_scripts.html</literal>:</para>
          <screen>{% block js %}
    {% include "my_custom_dashboard/_scripts.html" %}
{% endblock %}</screen>
          <para>The result is a single compressed js file consisting both Horizon and
                dashboard’s custom scripts.</para>
          <para>Additionally, some marketing and analytics scripts require you to place them
                within the page’s &lt;head&gt; tag. To do this, place them within the
                <literal>horizon/_custom_head_js.html</literal> file. Similar to the <literal>_scripts.html</literal> file
                mentioned above, you may link to an existing file:</para>
          <screen>&lt;script src='{{ STATIC_URL }}/my_custom_dashboard/js/my_marketing_js.js' type='text/javascript' charset='utf-8'&gt;&lt;/script&gt;</screen>
          <para>or you can paste your script directly in the file, being sure to use
                appropriate tags:</para>
          <screen>&lt;script type="text/javascript"&gt;
//some javascript
&lt;/script&gt;</screen>
        </section>
        <section xml:id="customizing-meta-attributes">
          <title>Customizing Meta Attributes</title>
          <para>To add custom metadata attributes to your project’s base template, include
                them in the <literal>horizon/_custom_meta.html</literal> file. The contents of this file will
                be inserted into the page’s &lt;head&gt; just after the default Horizon meta tags.</para>
        </section>
      </section>
      <section xml:id="themes" xml:base="themes">
        <title>Themes</title>
        <para>As of the Kilo release, styling for the OpenStack Dashboard can be altered
            through the use of a theme. A theme is a directory containing a
            <literal>_variables.scss</literal> file to override the color codes used throughout the SCSS
            and a <literal>_styles.scss</literal> file with additional styles to load after dashboard
            styles have loaded.</para>
        <para>As of the Mitaka release, Horizon can be configured to run with multiple
            themes available at run time.  It uses a browser cookie to allow users to
            toggle between the configured themes.  By default, Horizon is configured
            with the two standard themes available: ‘default’ and ‘material’.</para>
        <para>To configure or alter the available themes, set <literal>AVAILABLE_THEMES</literal> in
            <literal>local_settings.py</literal> to a list of tuples, such that
            <literal>('name', 'label', 'path')</literal></para>
        <variablelist>
          <varlistentry>
            <term>
              <literal>name</literal>
            </term>
            <listitem>
              <para>The key by which the theme value is stored within the cookie</para>
            </listitem>
          </varlistentry>
          <varlistentry>
            <term>
              <literal>label</literal>
            </term>
            <listitem>
              <para>The label shown in the theme toggle under the User Menu</para>
            </listitem>
          </varlistentry>
          <varlistentry>
            <term>
              <literal>path</literal>
            </term>
            <listitem>
              <para>The directory location for the theme. The path must be relative to the
                        <literal>openstack_dashboard</literal> directory or an absolute path to an accessible
                        location on the file system</para>
            </listitem>
          </varlistentry>
        </variablelist>
        <para>To use a custom theme, set <literal>AVAILABLE_THEMES</literal> in <literal>local_settings.py</literal> to
            a list of themes.  If you wish to run in a mode similar to legacy Horizon,
            set <literal>AVAILABLE_THEMES</literal> with a single tuple, and the theme toggle will not
            be available at all through the application to allow user configuration themes.</para>
        <para>For example, a configuration with multiple themes:</para>
        <screen>AVAILABLE_THEMES = [
    ('default', 'Default', 'themes/default'),
    ('material', 'Material', 'themes/material'),
]</screen>
        <para>A configuration with a single theme:</para>
        <screen>AVAILABLE_THEMES = [
    ('default', 'Default', 'themes/default'),
]</screen>
        <para>Both the Dashboard custom variables and Bootstrap variables can be overridden.
            For a full list of the Dashboard SCSS variables that can be changed,
            see the variables file at
            <literal>openstack_dashboard/static/dashboard/scss/_variables.scss</literal>.</para>
        <para>In order to build a custom theme, both <literal>_variables.scss</literal> and <literal>_styles.scss</literal>
            are required and <literal>_variables.scss</literal> must provide all the default Bootstrap
            variables.</para>
        <section xml:id="inherit-from-an-existing-theme">
          <title>Inherit from an Existing Theme</title>
          <para>Custom themes must implement all of the Bootstrap variables required by
                Horizon in <literal>_variables.scss</literal> and <literal>_styles.scss</literal>. To make this easier, you
                can inherit the variables needed in the default theme and only override those
                that you need to customize. To inherit from the default theme, put this in your
                theme’s <literal>_variables.scss</literal>:</para>
          <screen>@import "/themes/default/variables";</screen>
          <variablelist>
            <varlistentry>
              <term>Once you have made your changes you must re-generate the static files with</term>
              <listitem>
                <para><literal>tox -e manage -- collectstatic</literal>.</para>
              </listitem>
            </varlistentry>
          </variablelist>
          <para>By default, all of the themes configured by <literal>AVAILABLE_THEMES</literal> setting are
                collected by horizon during the <literal>collectstatic</literal> process. By default, the themes
                are collected into the dynamic <literal>static/themes</literal> directory, but this location can
                be customized via the <literal>local_settings.py</literal> variable: <literal>THEME_COLLECTION_DIR</literal></para>
          <para>Once collected, any theme configured via <literal>AVAILABLE_THEMES</literal> is available to
                inherit from by importing its variables and styles from its collection
                directory.  The following is an example of inheriting from the material theme:</para>
          <screen>@import "/themes/material/variables";
@import "/themes/material/styles";</screen>
          <para>All themes will need to be configured in <literal>AVAILABLE_THEMES</literal> to allow
                inheritance.  If you wish to inherit from a theme, but not show that theme
                as a selectable option in the theme picker widget, then simply configure the
                <literal>SELECTABLE_THEMES</literal> to exclude the parent theme.  <literal>SELECTABLE_THEMES</literal> must
                be of the same format as <literal>AVAILABLE_THEMES</literal>.  It defaults to
                <literal>AVAILABLE_THEMES</literal> if it is not set explicitly.</para>
          <section xml:id="bootswatch">
            <title>Bootswatch</title>
            <para>Horizon packages the Bootswatch SCSS files for use with its <literal>material</literal> theme.
                    Because of this, it is simple to use an existing Bootswatch theme as a base.
                    This is due to the fact that Bootswatch is loaded as a 3rd party static asset,
                    and therefore is automatically collected into the <literal>static</literal> directory in
                    <literal>/horizon/lib/</literal>.  The following is an example of how to inherit from
                    Bootswatch’s <literal>darkly</literal> theme:</para>
            <screen>@import "/horizon/lib/bootswatch/darkly/variables";
@import "/horizon/lib/bootswatch/darkly/bootswatch";</screen>
          </section>
        </section>
        <section xml:id="organizing-your-theme-directory">
          <title>Organizing Your Theme Directory</title>
          <para>A custom theme directory can be organized differently, depending on the
                level of customization that is desired, as it can include static files
                as well as Django templates.  It can include special subdirectories that will
                be used differently: <literal>static</literal>, <literal>templates</literal> and <literal>img</literal>.</para>
          <section xml:id="the-static-folder">
            <title>The <literal>static</literal> Folder</title>
            <para>If the theme folder contains a sub-folder called <literal>static</literal>, then that sub
                    folder will be used as the <emphasis role="bold">static root of the theme</emphasis>.  I.e., Horizon will
                    look in that sub-folder for the _variables.scss and _styles.scss files.
                    The contents of this folder will also be served up at <literal>/static/custom</literal>.</para>
          </section>
          <section xml:id="the-templates-folder">
            <title>The <literal>templates</literal> Folder</title>
            <para>If the theme folder contains a sub-folder <literal>templates</literal>, then the path
                    to that sub-folder will be prepended to the <literal>TEMPLATE_DIRS</literal> tuple to
                    allow for theme specific template customizations.</para>
          </section>
          <section xml:id="using-the-templates-folder">
            <title>Using the <literal>templates</literal> Folder</title>
            <para>Any Django template that is used in Horizon can be overridden through a theme.
                    This allows highly customized user experiences to exist within the scope of
                    different themes.  Any template that is overridden must adhere to the same
                    directory structure that the extending template expects.</para>
            <para>For example, if you wish to customize the sidebar, Horizon expects the template
                    to live at <literal>horizon/_sidebar.html</literal>.  You would need to duplicate that
                    directory structure under your templates directory, such that your override
                    would live at <literal>{ theme_path }/templates/horizon/_sidebar.html</literal>.</para>
          </section>
          <section xml:id="the-img-folder">
            <title>The <literal>img</literal> Folder</title>
            <para>If the static root of the theme folder contains an <literal>img</literal> directory,
                    then all images that make use of the {% themable_asset %} templatetag
                    can be overridden.</para>
            <para>These assets include logo.svg, splash-logo.svg and favicon.ico, however
                    overriding the SVG/GIF assets used by Heat within the <literal>dashboard/img</literal> folder
                    is not currently supported.</para>
          </section>
        </section>
        <section xml:id="customizing-the-logo">
          <title>Customizing the Logo</title>
          <section xml:id="simple">
            <title>Simple</title>
            <para>If you wish to customize the logo that is used on the splash screen or in the
                    top navigation bar, then you need to create an <literal>img</literal> directory under your
                    theme’s static root directory and place your custom <literal>logo.svg</literal> or
                    <literal>logo-splash.svg</literal> within it.</para>
            <para>If you wish to override the <literal>logo.svg</literal> using the previous method, and if the
                    image used is larger than the height of the top navigation, then the image will
                    be constrained to fit within the height of nav.  You can customize the height
                    of the top navigation bar by customizing the SCSS variable: <literal>$navbar-height</literal>.
                    If the image’s height is smaller than the navbar height, then the image
                    will retain its original resolution and size, and simply be centered
                    vertically in the available space.</para>
            <para>Prior to the Kilo release the images files inside of Horizon needed to be
                    replaced by your images files or the Horizon stylesheets needed to be altered
                    to point to the location of your image.</para>
          </section>
          <section xml:id="advanced">
            <title>Advanced</title>
            <para>If you need to do more to customize the logo than simply replacing the existing
                    PNG, then you can also override the _brand.html through a custom theme.  To use
                    this technique, simply add a <literal>templates/header/_brand.html</literal> to the root of
                    your custom theme, and add markup directly to the file.  For an example of how
                    to do this, see
                    <literal>openstack_dashboard/themes/material/templates/header/_brand.html</literal>.</para>
            <para>The splash / login panel can also be customized by adding
                    <literal>templates/auth/_splash.html</literal>.  See
                    <literal>openstack_dashboard/themes/material/templates/auth/_splash.html</literal> for an
                    example.</para>
          </section>
        </section>
      </section>
      <section xml:id="branding-horizon" xml:base="branding">
        <title>Branding Horizon</title>
        <para>As of the Liberty release, Horizon has begun to conform more strictly to
            Bootstrap standards in an effort to embrace more responsive web design as well
            as alleviate the future need to re-brand new functionality for every release.</para>
        <section xml:id="supported-components">
          <title>Supported Components</title>
          <para>The following components, organized by release, are the only ones that make
                full use of the Bootstrap theme architecture.</para>
          <itemizedlist>
            <listitem>
              <para>8.0.0 (Liberty)</para>
              <itemizedlist>
                <listitem>
                  <para>
                    <xref linkend="top-navbar"/>
                  </para>
                </listitem>
                <listitem>
                  <para>
                    <xref linkend="side-nav"/>
                  </para>
                </listitem>
                <listitem>
                  <para>
                    <xref linkend="pie-charts"/>
                  </para>
                </listitem>
              </itemizedlist>
            </listitem>
            <listitem>
              <para>9.0.0 (Mitaka)</para>
              <itemizedlist>
                <listitem>
                  <para>
                    <xref linkend="tables"/>
                  </para>
                </listitem>
                <listitem>
                  <para>
                    <xref linkend="bar-charts"/>
                  </para>
                </listitem>
                <listitem>
                  <para>
                    <xref linkend="login"/>
                  </para>
                </listitem>
                <listitem>
                  <para>
                    <xref linkend="tabs"/>
                  </para>
                </listitem>
                <listitem>
                  <para>
                    <xref linkend="alerts"/>
                  </para>
                </listitem>
                <listitem>
                  <para>
                    <xref linkend="checkboxes"/>
                  </para>
                </listitem>
              </itemizedlist>
            </listitem>
          </itemizedlist>
        </section>
        <section xml:id="step-1">
          <title>Step 1</title>
          <para>The first step needed to create a custom branded theme for Horizon is to create
                a custom Bootstrap theme.  There are several tools to aid in this. Some of the
                more useful ones include:</para>
          <itemizedlist>
            <listitem>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://bootswatchr.com/create#!">Bootswatchr</link>
              </para>
            </listitem>
            <listitem>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://paintstrap.com">Paintstrap</link>
              </para>
            </listitem>
            <listitem>
              <para>
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://getbootstrap.com/customize/">Bootstrap</link>
              </para>
            </listitem>
          </itemizedlist>
          <note>
            <para>Bootstrap uses LESS by default, but we use SCSS.  All of the above
                    tools will provide the <literal>variables.less</literal> file, which will need to be
                    converted to <literal>_variables.scss</literal></para>
          </note>
        </section>
        <section xml:id="top-navbar">
          <title>Top Navbar</title>
          <para>The top navbar in Horizon now uses a native Bootstrap <literal>navbar</literal>.  There are a
                number of variables that can be used to customize this element.  Please see the
                <emphasis role="bold">Navbar</emphasis> section of your variables file for specifics on what can be set: any
                variables that use <literal>navbar-default</literal>.</para>
          <para>It is important to also note that the navbar now uses native Bootstrap
                dropdowns, which are customizable with variables.  Please see the <emphasis role="bold">Dropdowns</emphasis>
                section of your variables file.</para>
          <para>The top navbar is now responsive on smaller screens.  When the window size hits
                your <literal>$screen-sm</literal> value, the topbar will compress into a design that is
                better suited for small screens.</para>
        </section>
        <section xml:id="side-nav">
          <title>Side Nav</title>
          <para>The side navigation component has been refactored to use the native Stacked
                Pills element from Bootstrap.  See <emphasis role="bold">Pills</emphasis> section of your variables file
                for specific variables to customize.</para>
        </section>
        <section xml:id="charts">
          <title>Charts</title>
          <section xml:id="pie-charts">
            <title>Pie Charts</title>
            <para>Pie Charts are SVG elements.  SVG elements allow CSS customizations for
                    only a basic element’s look and feel (i.e. colors, size).</para>
            <para>Since there is no native element in Bootstrap specifically for pie charts,
                    the look and feel of the charts are inheriting from other elements of the
                    theme. Please see <literal>_pie_charts.scss</literal> for specifics.</para>
          </section>
          <section xml:id="bar-charts">
            <title>Bar Charts</title>
            <para>Bar Charts can be either a Bootstrap Progress Bar or an SVG element. Either
                    implementation will use the Bootstrap Progress Bar styles.</para>
            <para>The SVG implementation will not make use of the customized Progress Bar
                    height though, so it is recommended that Bootstrap Progress Bars are used
                    whenever possible.</para>
            <para>Please see <literal>_bar_charts.scss</literal> for specifics on what can be customized for
                    SVGs.  See the <emphasis role="bold">Progress bars</emphasis> section of your variables file for specific
                    variables to customize.</para>
          </section>
        </section>
        <section xml:id="tables">
          <title>Tables</title>
          <para>The standard Django tables now make use of the native Bootstrap table markup.
                See <emphasis role="bold">Tables</emphasis> section of your variables file for variables to customize.</para>
          <para>The standard Bootstrap tables will be borderless by default.  If you wish to
                add a border, like the <literal>default</literal> theme, see
                <literal>openstack_dashboard/themes/default/horizon/components/_tables.scss</literal></para>
        </section>
        <section xml:id="login">
          <title>Login</title>
          <section xml:id="login-splash-page">
            <title>Login Splash Page</title>
            <para>The login splash page now uses a standard Bootstrap panel in its
                    implementation. See the <emphasis role="bold">Panels</emphasis> section in your variables file to variables
                    to easily customize.</para>
          </section>
          <section xml:id="modal-login">
            <title>Modal Login</title>
            <para>The modal login experience, as used when switching regions, uses a standard
                    Bootstrap dialog.  See the <emphasis role="bold">Modals</emphasis> section of your variables file for
                    specific variables to customize.</para>
          </section>
        </section>
        <section xml:id="tabs">
          <title>Tabs</title>
          <para>The standard tabs make use of the native Bootstrap tab markup.</para>
          <para>See <emphasis role="bold">Tabs</emphasis> section of your variables file for variables to customize.</para>
        </section>
        <section xml:id="alerts">
          <title>Alerts</title>
          <para>Alerts use the basic Bootstrap brand colors.  See <emphasis role="bold">Colors</emphasis> section of your
                variables file for specifics.</para>
        </section>
        <section xml:id="checkboxes">
          <title>Checkboxes</title>
          <para>Horizon uses icon fonts to represent checkboxes.  In order to customize
                this, you simply need to override the standard scss.  For an example of
                this, see themes/material/static/horizon/components/_checkboxes.scss</para>
        </section>
        <section xml:id="bootswatch-and-material-design">
          <title>Bootswatch and Material Design</title>
          <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://bootswatch.com">Bootswatch</link> is a collection of free themes for Bootstrap and is now
                available for use in Horizon.</para>
          <para>In order to showcase what can be done to enhance an existing Bootstrap theme,
                Horizon now includes a secondary theme, roughly based on <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://www.google.com/design/spec/material-design/introduction.html">Google’s Material
                    Design</link> called <literal>material</literal>.  Bootswatch’s <emphasis role="bold">Paper</emphasis> is a simple Bootstrap
                implementation of Material Design and is used by <literal>material</literal>.</para>
          <para>Bootswatch provides a number of other themes, that once Horizon is fully theme
                compliant, will allow easy toggling and customizations for darker or
                accessibility driven experiences.</para>
        </section>
        <section xml:id="development-tips">
          <title>Development Tips</title>
          <para>When developing a new theme for Horizon, it is required that the dynamically
                generated <literal>static</literal> directory be cleared after each change and the server
                restarted.  This is not always ideal.  If you wish to develop and not have
                to restart the server each time, it is recommended that you configure your
                development environment to not run in OFFLINE mode.  Simply verify the
                following settings in your local_settings.py:</para>
          <screen>COMPRESS_OFFLINE = False
COMPRESS_ENABLED = False</screen>
        </section>
      </section>
    </section>
    <section xml:id="openstack-dashboard-user-documentation" xml:base="user/index">
      <title>OpenStack Dashboard User Documentation</title>
      <para>As a cloud end user, you can use the OpenStack dashboard to provision
            your own resources within the limits set by administrators. You can
            modify the examples provided in this section to create other types and
            sizes of server instances.</para>
      <section xml:id="log-in-to-the-dashboard" xml:base="log-in">
        <title>Log in to the dashboard</title>
        <para>The dashboard is generally installed on the controller node.</para>
        <procedure>
          <step>
            <para>Ask the cloud operator for the host name or public IP address from
                    which you can access the dashboard, and for your user name and
                    password. If the cloud supports multi-domain model, you also need to
                    ask for your domain name.</para>
          </step>
          <step>
            <para>Open a web browser that has JavaScript and cookies enabled.</para>
            <note>
              <para>To use the Virtual Network Computing (VNC) client for the dashboard,
                        your browser must support HTML5 Canvas and HTML5 WebSockets. The VNC
                        client is based on noVNC. For details, see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/kanaka/noVNC/blob/master/README.md">noVNC: HTML5 VNC
                            Client</link>.
                        For a list of supported browsers, see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://github.com/kanaka/noVNC/wiki/Browser-support">Browser
                            support</link>.</para>
            </note>
          </step>
          <step>
            <para>In the address bar, enter the host name or IP address for the
                    dashboard, for example, <literal>https://ipAddressOrHostName/</literal>.</para>
            <note>
              <para>If a certificate warning appears when you try to access the URL for
                        the first time, a self-signed certificate is in use, which is not
                        considered trustworthy by default. Verify the certificate or add an
                        exception in the browser to bypass the warning.</para>
            </note>
          </step>
          <step>
            <para>On the <guimenu>Log In</guimenu> page, enter your user name and password, and
                    click <guimenu>Sign In</guimenu>. If the cloud supports multi-domain model, you
                    also need to enter your domain name.</para>
            <para>The top of the window displays your user name. You can also access the
                    <guimenu>Settings</guimenu> tab (<link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#dashboard-settings-tab">OpenStack dashboard — Settings tab</link>) or sign out
                    of the dashboard.</para>
            <para>The visible tabs and functions in the dashboard depend on the access
                    permissions, or roles, of the user you are logged in as.</para>
            <itemizedlist>
              <listitem>
                <para>If you are logged in as an end user, the <guimenu>Project</guimenu> tab
                            (<link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#dashboard-project-tab">OpenStack dashboard — Project tab</link>) and <guimenu>Identity</guimenu> tab
                            (<link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#dashboard-identity-tab">OpenStack dashboard — Identity tab</link>) are displayed.</para>
              </listitem>
              <listitem>
                <para>If you are logged in as an administrator, the <guimenu>Project</guimenu> tab
                            (<link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#dashboard-project-tab">OpenStack dashboard — Project tab</link>) and <guimenu>Admin</guimenu> tab
                            (<link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#dashboard-admin-tab">OpenStack dashboard — Admin tab</link>) and <guimenu>Identity</guimenu> tab
                            (<link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#dashboard-identity-tab">OpenStack dashboard — Identity tab</link>) are displayed.</para>
              </listitem>
            </itemizedlist>
          </step>
        </procedure>
        <note>
          <para>Some tabs, such as <guimenu>Orchestration</guimenu> and <guimenu>Firewalls</guimenu>,
                only appear on the dashboard if they are properly configured.</para>
        </note>
        <section xml:id="dashboard-project-tab">
          <title>OpenStack dashboard — Project tab</title>
          <para>Projects are organizational units in the cloud and are also known as
                tenants or accounts. Each user is a member of one or more projects.
                Within a project, a user creates and manages instances.</para>
          <para>From the <guimenu>Project</guimenu> tab, you can view and manage the resources in a
                selected project, including instances and images. You can select the project
                from the drop-down menu at the top left. If the cloud supports multi-domain
                model, you can also select the domain from this menu.</para>
          <figure>
            <title>Figure: Project tab</title>
            <mediaobject>
              <imageobject role="fo">
                <imagedata fileref="dashboard_project_tab.png" width="100%"/>
              </imageobject>
              <imageobject role="html">
                <imagedata fileref="dashboard_project_tab.png" width="100%"/>
              </imageobject>
            </mediaobject>
          </figure>
          <para>From the <guimenu>Project</guimenu> tab, you can access the following categories:</para>
          <section xml:id="compute-tab">
            <title>Compute tab</title>
            <itemizedlist>
              <listitem>
                <para><guimenu>Overview</guimenu>: View reports for the project.</para>
              </listitem>
              <listitem>
                <para><guimenu>Instances</guimenu>: View, launch, create a snapshot from, stop, pause,
                            or reboot instances, or connect to them through VNC.</para>
              </listitem>
              <listitem>
                <para><guimenu>Volumes</guimenu>: Use the following tabs to complete these tasks:</para>
                <itemizedlist>
                  <listitem>
                    <para><guimenu>Volumes</guimenu>: View, create, edit, and delete volumes.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Volume Snapshots</guimenu>: View, create, edit, and delete volume
                                    snapshots.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
              <listitem>
                <para><guimenu>Images</guimenu>: View images and instance snapshots created by project
                            users, plus any images that are publicly available. Create, edit, and
                            delete images, and launch instances from images and snapshots.</para>
              </listitem>
              <listitem>
                <para><guimenu>Access &amp; Security</guimenu>: Use the following tabs to complete these
                            tasks:</para>
                <itemizedlist>
                  <listitem>
                    <para><guimenu>Security Groups</guimenu>: View, create, edit, and delete security
                                    groups and security group rules.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Key Pairs</guimenu>: View, create, edit, import, and delete key pairs.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Floating IPs</guimenu>: Allocate an IP address to or release it from a
                                    project.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>API Access</guimenu>: View API endpoints.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
              <listitem>
                <para><guimenu>Shares</guimenu>: Use the following tabs to complete these tasks:</para>
                <itemizedlist>
                  <listitem>
                    <para><guimenu>Shares</guimenu>: View, create, manage, and delete shares.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Snapshots</guimenu>: View, manage, and delete volume snapshots.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Share Networks</guimenu>: View, manage, and delete share networks.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Security Services</guimenu>: View, manage, and delete security services.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
            </itemizedlist>
          </section>
          <section xml:id="network-tab">
            <title>Network tab</title>
            <itemizedlist>
              <listitem>
                <para><guimenu>Network Topology</guimenu>: View the network topology.</para>
              </listitem>
              <listitem>
                <para><guimenu>Networks</guimenu>: Create and manage public and private networks.</para>
              </listitem>
              <listitem>
                <para><guimenu>Routers</guimenu>: Create and manage routers.</para>
              </listitem>
              <listitem>
                <para><guimenu>Load Balancers</guimenu>: Create and manage load balancers.</para>
                <itemizedlist>
                  <listitem>
                    <para><guimenu>Pools</guimenu>: Add and manage pools.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Members</guimenu>: Add and manage members.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Monitors</guimenu>: Add and manage monitors.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
              <listitem>
                <para><guimenu>Firewalls</guimenu>: Create and manage firewalls.</para>
                <itemizedlist>
                  <listitem>
                    <para><guimenu>Firewalls</guimenu>: Create and manage firewalls.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Firewall Policies</guimenu>: Add and manage firewall policies.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Firewall Rules</guimenu>: Add and manage firewall rules.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
            </itemizedlist>
          </section>
          <section xml:id="orchestration-tab">
            <title>Orchestration tab</title>
            <itemizedlist>
              <listitem>
                <para><guimenu>Stacks</guimenu>: Use the REST API to orchestrate multiple composite
                            cloud applications.</para>
              </listitem>
              <listitem>
                <para><guimenu>Resource Types</guimenu>: Show a list of all the supported resource
                            types for HOT templates.</para>
              </listitem>
            </itemizedlist>
          </section>
          <section xml:id="object-store-tab">
            <title>Object Store tab</title>
            <itemizedlist>
              <listitem>
                <para><guimenu>Containers</guimenu>: Create and manage containers and objects.</para>
              </listitem>
            </itemizedlist>
          </section>
        </section>
        <section xml:id="dashboard-admin-tab">
          <title>OpenStack dashboard — Admin tab</title>
          <para>Administrative users can use the <guimenu>Admin</guimenu> tab to view usage and to
                manage instances, volumes, flavors, images, networks, and so on.</para>
          <figure>
            <title>Figure: Admin tab</title>
            <mediaobject>
              <imageobject role="fo">
                <imagedata fileref="dashboard_admin_tab.png" width="100%"/>
              </imageobject>
              <imageobject role="html">
                <imagedata fileref="dashboard_admin_tab.png" width="100%"/>
              </imageobject>
            </mediaobject>
          </figure>
          <para>From the <guimenu>Admin</guimenu> tab, you can access the following category
                to complete these tasks:</para>
          <section xml:id="system-tab">
            <title>System tab</title>
            <itemizedlist>
              <listitem>
                <para><guimenu>Overview</guimenu>: View basic reports.</para>
              </listitem>
              <listitem>
                <para><guimenu>Resource Usage</guimenu>: Use the following tabs to view the following
                            usages:</para>
                <itemizedlist>
                  <listitem>
                    <para><guimenu>Usage Report</guimenu>: View the usage report.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Stats</guimenu>: View the statistics of all resources.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
              <listitem>
                <para><guimenu>Hypervisors</guimenu>: View the hypervisor summary.</para>
              </listitem>
              <listitem>
                <para><guimenu>Host Aggregates</guimenu>: View, create, and edit host aggregates.
                            View the list of availability zones.</para>
              </listitem>
              <listitem>
                <para><guimenu>Instances</guimenu>: View, pause, resume, suspend, migrate, soft or hard
                            reboot, and delete running instances that belong to users of some, but not
                            all, projects. Also, view the log for an instance or access an instance
                            through VNC.</para>
              </listitem>
              <listitem>
                <para><guimenu>Volumes</guimenu>: Use the following tabs to complete these tasks:</para>
                <itemizedlist>
                  <listitem>
                    <para><guimenu>Volumes</guimenu>: View, create, manage, and delete volumes.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Volume Types</guimenu>: View, create, manage, and delete volume types.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Volume Snapshots</guimenu>: View, manage, and delete volume snapshots.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
              <listitem>
                <para><guimenu>Flavors</guimenu>: View, create, edit, view extra specifications for,
                            and delete flavors. A flavor is the size of an instance.</para>
              </listitem>
              <listitem>
                <para><guimenu>Images</guimenu>: View, create, edit properties for, and delete custom
                            images.</para>
              </listitem>
              <listitem>
                <para><guimenu>Networks</guimenu>: View, create, edit properties for, and delete
                            networks.</para>
              </listitem>
              <listitem>
                <para><guimenu>Routers</guimenu>: View, create, edit properties for, and delete routers.</para>
              </listitem>
              <listitem>
                <para><guimenu>Defaults</guimenu>: View default quota values. Quotas are hard-coded in
                            OpenStack Compute and define the maximum allowable size and number of
                            resources.</para>
              </listitem>
              <listitem>
                <para><guimenu>Metadata Definitions</guimenu>: Import namespace and view the metadata
                            information.</para>
              </listitem>
              <listitem>
                <para><guimenu>System Information</guimenu>: Use the following tabs to view the service
                            information:</para>
                <itemizedlist>
                  <listitem>
                    <para><guimenu>Services</guimenu>: View a list of the services.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Compute Services</guimenu>: View a list of all Compute services.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Block Storage Services</guimenu>: View a list of all Block Storage
                                    services.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Network Agents</guimenu>: View the network agents.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Orchestration Services</guimenu>: View a list of all Orchestration
                                    services.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
              <listitem>
                <para><guimenu>Shares</guimenu>: Use the following tabs to complete these tasks:</para>
                <itemizedlist>
                  <listitem>
                    <para><guimenu>Shares</guimenu>: View, create, manage, and delete shares.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Snapshots</guimenu>: View, manage, and delete volume snapshots.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Share Networks</guimenu>: View, manage, and delete share networks.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Security Services</guimenu>: View, manage, and delete security services.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Share Types</guimenu>: View, create, manage, and delete share types.</para>
                  </listitem>
                  <listitem>
                    <para><guimenu>Share Servers</guimenu>: View, manage, and delete share servers.</para>
                  </listitem>
                </itemizedlist>
              </listitem>
            </itemizedlist>
          </section>
        </section>
        <section xml:id="dashboard-identity-tab">
          <title>OpenStack dashboard — Identity tab</title>
          <figure>
            <title>Figure:Identity tab</title>
            <mediaobject>
              <imageobject role="fo">
                <imagedata fileref="dashboard_identity_tab.png" width="100%"/>
              </imageobject>
              <imageobject role="html">
                <imagedata fileref="dashboard_identity_tab.png" width="100%"/>
              </imageobject>
            </mediaobject>
          </figure>
          <itemizedlist>
            <listitem>
              <para><guimenu>Projects</guimenu>: View, create, assign users to, remove users from,
                        and delete projects.</para>
            </listitem>
            <listitem>
              <para><guimenu>Users</guimenu>: View, create, enable, disable, and delete users.</para>
            </listitem>
          </itemizedlist>
        </section>
        <section xml:id="dashboard-settings-tab">
          <title>OpenStack dashboard — Settings tab</title>
          <figure>
            <title>Figure:Settings tab</title>
            <mediaobject>
              <imageobject role="fo">
                <imagedata fileref="dashboard_settings_tab.png" width="100%"/>
              </imageobject>
              <imageobject role="html">
                <imagedata fileref="dashboard_settings_tab.png" width="100%"/>
              </imageobject>
            </mediaobject>
          </figure>
          <para>Click the <guimenu>Settings</guimenu> button from the user drop down menu at the
                top right of any page, you will see the <guimenu>Settings</guimenu> tab.</para>
          <itemizedlist>
            <listitem>
              <para><guimenu>User Settings</guimenu>: View and manage dashboard settings.</para>
            </listitem>
            <listitem>
              <para><guimenu>Change Password</guimenu>: Change the password of the user.</para>
            </listitem>
          </itemizedlist>
        </section>
      </section>
      <section xml:id="upload-and-manage-images" xml:base="manage-images">
        <title>Upload and manage images</title>
        <para>A virtual machine image, referred to in this document simply
            as an image, is a single file that contains a virtual disk that
            has a bootable operating system installed on it. Images are used
            to create virtual machine instances within the cloud. For information
            about creating image files, see the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/image-guide/">OpenStack Virtual Machine
                Image Guide</link>.</para>
        <para>Depending on your role, you may have permission to upload and manage
            virtual machine images. Operators might restrict the upload and
            management of images to cloud administrators or operators only. If you
            have the appropriate privileges, you can use the dashboard to upload and
            manage images in the admin project.</para>
        <note>
          <para>You can also use the <command>openstack</command> and <command>glance</command>
                command-line clients or the Image service to manage images.</para>
        </note>
        <section xml:id="upload-an-image">
          <title>Upload an image</title>
          <para>Follow this procedure to upload an image to a project:</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Images</guimenu> category.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Image</guimenu>.</para>
              <para>The <guimenu>Create An Image</guimenu> dialog box appears.</para>
              <figure>
                <title>Dashboard — Create Image</title>
                <mediaobject>
                  <imageobject role="fo">
                    <imagedata fileref="create_image.png"/>
                  </imageobject>
                  <imageobject role="html">
                    <imagedata fileref="create_image.png"/>
                  </imageobject>
                </mediaobject>
              </figure>
            </step>
            <step>
              <para>Enter the following values:</para>
              <informaltable>
                <tgroup cols="2">
                  <colspec colname="c0" colwidth="31"/>
                  <colspec colname="c1" colwidth="33"/>
                  <tbody>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Image Name</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter a name for the image.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Image Description</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter a brief description of
                                            the image.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Image Source</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Choose the image source from
                                            the dropdown list. Your choices
                                            are <guimenu>Image Location</guimenu>
                                            and <guimenu>Image File</guimenu>.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para><guimenu>Image File</guimenu> or
                                            <guimenu>Image Location</guimenu></para>
                      </entry>
                      <entry>
                        <para>Based on your selection for
                                            <guimenu>Image Source</guimenu>, you
                                            either enter the location URL
                                            of the image in the
                                            <guimenu>Image Location</guimenu>
                                            field, or browse for the image
                                            file on your file  system and
                                            add it.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Format</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Select the image format (for
                                            example, QCOW2) for the image.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Architecture</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the architecture. For
                                            example, <literal>i386</literal> for a 32-bit
                                            architecture or <literal>x86_64</literal> for
                                            a 64-bit architecture.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Minimum Disk (GB)</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Leave this field empty.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Minimum RAM (MB)</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Leave this field empty.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Copy Data</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify this option to copy
                                            image data to the Image service.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Visibility</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>The access permission for the
                                            image.
                                            <literal>Public</literal> or <literal>Private</literal>.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Protected</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Select this check box to ensure
                                            that only users with
                                            permissions can delete the
                                            image. <literal>Yes</literal> or <literal>No</literal>.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Image Metadata</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify this option to add
                                            resource metadata. The glance
                                            Metadata Catalog provides a list
                                            of metadata image definitions.
                                            (Note: Not all cloud providers
                                            enable this feature.)</para>
                      </entry>
                    </row>
                  </tbody>
                </tgroup>
              </informaltable>
            </step>
            <step>
              <para>Click <guimenu>Create Image</guimenu>.</para>
              <para>The image is queued to be uploaded. It might take some time before
                        the status changes from Queued to Active.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="update-an-image-horizon">
          <title>Update an image</title>
          <para>Follow this procedure to update an existing image.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>Select the image that you want to edit.</para>
            </step>
            <step>
              <para>In the <guimenu>Actions</guimenu> column, click the menu button and then
                        select <guimenu>Edit Image</guimenu> from the list.</para>
            </step>
            <step>
              <para>In the <guimenu>Edit Image</guimenu> dialog box, you can perform various
                        actions. For example:</para>
              <itemizedlist>
                <listitem>
                  <para>Change the name of the image.</para>
                </listitem>
                <listitem>
                  <para>Select the <guimenu>Public</guimenu> check box to make the image public.</para>
                </listitem>
                <listitem>
                  <para>Clear the <guimenu>Public</guimenu> check box to make the image private.</para>
                </listitem>
              </itemizedlist>
            </step>
            <step>
              <para>Click <guimenu>Edit Image</guimenu>.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="delete-an-image">
          <title>Delete an image</title>
          <para>Deletion of images is permanent and <emphasis role="bold">cannot</emphasis> be reversed. Only users
                with the appropriate permissions can delete images.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Images</guimenu> category.</para>
            </step>
            <step>
              <para>Select the images that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Images</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Images</guimenu> dialog box, click
                        <guimenu>Delete Images</guimenu> to confirm the deletion.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="configure-access-and-security-for-instances" xml:base="configure-access-and-security-for-instances">
        <title>Configure access and security for instances</title>
        <para>Before you launch an instance, you should add security group rules to
            enable users to ping and use SSH to connect to the instance. Security
            groups are sets of IP filter rules that define networking access and are
            applied to all instances within a project. To do so, you either add
            rules to the default security group <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#security-groups-add-rule">Add a rule to the default security group</link>
            or add a new security group with rules.</para>
        <para>Key pairs are SSH credentials that are injected into an instance when it
            is launched. To use key pair injection, the image that the instance is
            based on must contain the <literal>cloud-init</literal> package. Each project should
            have at least one key pair. For more information, see the section
            <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#keypair-add">Add a key pair</link>.</para>
        <para>If you have generated a key pair with an external tool, you can import
            it into OpenStack. The key pair can be used for multiple instances that
            belong to a project. For more information, see the section
            <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#dashboard-import-keypair">Import a key pair</link>.</para>
        <note>
          <para>A key pair belongs to an individual user, not to a project.
                To share a key pair across multiple users, each user
                needs to import that key pair.</para>
        </note>
        <para>When an instance is created in OpenStack, it is automatically assigned a
            fixed IP address in the network to which the instance is assigned. This
            IP address is permanently associated with the instance until the
            instance is terminated. However, in addition to the fixed IP address, a
            floating IP address can also be attached to an instance. Unlike fixed IP
            addresses, floating IP addresses are able to have their associations
            modified at any time, regardless of the state of the instances involved.</para>
        <section xml:id="security-groups-add-rule">
          <title>Add a rule to the default security group</title>
          <para>This procedure enables SSH and ICMP (ping) access to instances. The
                rules apply to all instances within a given project, and should be set
                for every project unless there is a reason to prohibit SSH or ICMP
                access to the instances.</para>
          <para>This procedure can be adjusted as necessary to add additional security
                group rules to a project, if your cloud requires them.</para>
          <note>
            <para>When adding a rule, you must specify the protocol used with the
                    destination port or source port.</para>
          </note>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Access &amp; Security</guimenu> category. The
                        <guimenu>Security Groups</guimenu> tab shows the security groups that are
                        available for this project.</para>
            </step>
            <step>
              <para>Select the default security group and click <guimenu>Manage Rules</guimenu>.</para>
            </step>
            <step>
              <para>To allow SSH access, click <guimenu>Add Rule</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Add Rule</guimenu> dialog box, enter the following values:</para>
              <itemizedlist>
                <listitem>
                  <para><emphasis role="bold">Rule</emphasis>: <literal>SSH</literal></para>
                </listitem>
                <listitem>
                  <para><emphasis role="bold">Remote</emphasis>: <literal>CIDR</literal></para>
                </listitem>
                <listitem>
                  <para><emphasis role="bold">CIDR</emphasis>: <literal>0.0.0.0/0</literal></para>
                </listitem>
              </itemizedlist>
              <note>
                <para>To accept requests from a particular range of IP
                            addresses, specify the IP address block in the
                            <guimenu>CIDR</guimenu> box.</para>
              </note>
            </step>
            <step>
              <para>Click <guimenu>Add</guimenu>.</para>
              <para>Instances will now have SSH port 22 open for requests from any IP
                        address.</para>
            </step>
            <step>
              <para>To add an ICMP rule, click <guimenu>Add Rule</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Add Rule</guimenu> dialog box, enter the following values:</para>
              <itemizedlist>
                <listitem>
                  <para><emphasis role="bold">Rule</emphasis>: <literal>All ICMP</literal></para>
                </listitem>
                <listitem>
                  <para><emphasis role="bold">Direction</emphasis>: <literal>Ingress</literal></para>
                </listitem>
                <listitem>
                  <para><emphasis role="bold">Remote</emphasis>: <literal>CIDR</literal></para>
                </listitem>
                <listitem>
                  <para><emphasis role="bold">CIDR</emphasis>: <literal>0.0.0.0/0</literal></para>
                </listitem>
              </itemizedlist>
            </step>
            <step>
              <para>Click <guimenu>Add</guimenu>.</para>
              <para>Instances will now accept all incoming ICMP packets.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="keypair-add">
          <title>Add a key pair</title>
          <para>Create at least one key pair for each project.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Access &amp; Security</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Key Pairs</guimenu> tab, which shows the key pairs that
                        are available for this project.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Key Pair</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Create Key Pair</guimenu> dialog box, enter a name for your
                        key pair, and click <guimenu>Create Key Pair</guimenu>.</para>
            </step>
            <step>
              <para>Respond to the prompt to download the key pair.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="dashboard-import-keypair">
          <title>Import a key pair</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Access &amp; Security</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Key Pairs</guimenu> tab, which shows the key pairs that
                        are available for this project.</para>
            </step>
            <step>
              <para>Click <guimenu>Import Key Pair</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Import Key Pair</guimenu> dialog box, enter the name of your
                        key pair, copy the public key into the <guimenu>Public Key</guimenu> box,
                        and then click <guimenu>Import Key Pair</guimenu>.</para>
            </step>
            <step>
              <para>Save the <literal>*.pem</literal> file locally.</para>
            </step>
            <step>
              <para>To change its permissions so that only you can read and write to the
                        file, run the following command:</para>
              <screen language="console">$ chmod 0600 yourPrivateKey.pem</screen>
              <note>
                <para>If you are using the Dashboard from a Windows computer, use PuTTYgen
                            to load the <literal>*.pem</literal> file and convert and save it as <literal>*.ppk</literal>. For
                            more information see the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://winscp.net/eng/docs/ui_puttygen">WinSCP web page for
                                PuTTYgen</link>.</para>
              </note>
            </step>
            <step>
              <para>To make the key pair known to SSH, run the <command>ssh-add</command> command.</para>
              <screen language="console">$ ssh-add yourPrivateKey.pem</screen>
            </step>
          </procedure>
          <para>The Compute database registers the public key of the key pair.</para>
          <para>The Dashboard lists the key pair on the <guimenu>Access &amp; Security</guimenu> tab.</para>
        </section>
        <section xml:id="allocate-a-floating-ip-address-to-an-instance">
          <title>Allocate a floating IP address to an instance</title>
          <para>When an instance is created in OpenStack, it is automatically assigned a
                fixed IP address in the network to which the instance is assigned. This
                IP address is permanently associated with the instance until the
                instance is terminated.</para>
          <para>However, in addition to the fixed IP address, a floating IP address can
                also be attached to an instance. Unlike fixed IP addresses, floating IP
                addresses can have their associations modified at any time, regardless
                of the state of the instances involved. This procedure details the
                reservation of a floating IP address from an existing pool of addresses
                and the association of that address with a specific instance.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Access &amp; Security</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Floating IPs</guimenu> tab, which shows the floating IP
                        addresses allocated to instances.</para>
            </step>
            <step>
              <para>Click <guimenu>Allocate IP To Project</guimenu>.</para>
            </step>
            <step>
              <para>Choose the pool from which to pick the IP address.</para>
            </step>
            <step>
              <para>Click <guimenu>Allocate IP</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Floating IPs</guimenu> list, click <guimenu>Associate</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Manage Floating IP Associations</guimenu> dialog box,
                        choose the following options:</para>
              <itemizedlist>
                <listitem>
                  <para>The <guimenu>IP Address</guimenu> field is filled automatically,
                                but you can add a new IP address by clicking the
                                <guimenu>+</guimenu> button.</para>
                </listitem>
                <listitem>
                  <para>In the <guimenu>Port to be associated</guimenu> field, select a port
                                from the list.</para>
                  <para>The list shows all the instances with their fixed IP addresses.</para>
                </listitem>
              </itemizedlist>
            </step>
            <step>
              <para>Click <guimenu>Associate</guimenu>.</para>
            </step>
          </procedure>
          <note>
            <para>To disassociate an IP address from an instance, click the
                    <guimenu>Disassociate</guimenu> button.</para>
          </note>
          <para>To release the floating IP address back into the floating IP pool, click
                the <guimenu>Release Floating IP</guimenu> option in the <guimenu>Actions</guimenu> column.</para>
        </section>
      </section>
      <section xml:id="launch-and-manage-instances" xml:base="launch-instances">
        <title>Launch and manage instances</title>
        <para>Instances are virtual machines that run inside the cloud.
            You can launch an instance from the following sources:</para>
        <itemizedlist>
          <listitem>
            <para>Images uploaded to the Image service.</para>
          </listitem>
          <listitem>
            <para>Image that you have copied to a persistent volume. The instance
                    launches from the volume, which is provided by the <literal>cinder-volume</literal>
                    API through iSCSI.</para>
          </listitem>
          <listitem>
            <para>Instance snapshot that you took.</para>
          </listitem>
        </itemizedlist>
        <section xml:id="launch-an-instance">
          <title>Launch an instance</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Instances</guimenu> category.</para>
              <para>The dashboard shows the instances with its name, its private and
                        floating IP addresses, size, status, task, power state, and so on.</para>
            </step>
            <step>
              <para>Click <guimenu>Launch Instance</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Launch Instance</guimenu> dialog box, specify the following values:</para>
              <para><guimenu>Details</guimenu> tab</para>
              <variablelist>
                <varlistentry>
                  <term>Instance Name</term>
                  <listitem>
                    <para>Assign a name to the virtual machine.</para>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>Availability Zone</term>
                  <listitem>
                    <para>By default, this value is set to the availability zone given by the
                                    cloud provider (for example, <literal>us-west</literal> or <literal>apac-south</literal>). For some
                                    cases, it could be <literal>nova</literal>.</para>
                    <note>
                      <para>The name you assign here becomes the initial host name of the server.
                                        If the name is longer than 63 characters, the Compute service
                                        truncates it automatically to ensure dnsmasq works correctly.</para>
                      <para>After the server is built, if you change the server name in the API
                                        or change the host name directly, the names are not updated in the
                                        dashboard.</para>
                      <para>Server names are not guaranteed to be unique when created so you
                                        could have two instances with the same host name.</para>
                    </note>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>Count</term>
                  <listitem>
                    <para>To launch multiple instances, enter a value greater than <literal>1</literal>. The
                                    default is <literal>1</literal>.</para>
                  </listitem>
                </varlistentry>
              </variablelist>
              <para><guimenu>Source</guimenu> tab</para>
              <variablelist>
                <varlistentry>
                  <term>Instance Boot Source</term>
                  <listitem>
                    <para>Your options are:</para>
                    <variablelist>
                      <varlistentry>
                        <term>Boot from image</term>
                        <listitem>
                          <para>If you choose this option, a new field for <guimenu>Image Name</guimenu>
                                                displays. You can select the image from the list.</para>
                        </listitem>
                      </varlistentry>
                      <varlistentry>
                        <term>Boot from snapshot</term>
                        <listitem>
                          <para>If you choose this option, a new field for <guimenu>Instance
                                                    Snapshot</guimenu> displays. You can select the snapshot from the list.</para>
                        </listitem>
                      </varlistentry>
                      <varlistentry>
                        <term>Boot from volume</term>
                        <listitem>
                          <para>If you choose this option, a new field for <guimenu>Volume</guimenu>
                                                displays. You can select the volume from the list.</para>
                        </listitem>
                      </varlistentry>
                      <varlistentry>
                        <term>Boot from image (creates a new volume)</term>
                        <listitem>
                          <para>With this option, you can boot from an image and create a volume
                                                by entering the <guimenu>Device Size</guimenu> and <guimenu>Device
                                                    Name</guimenu> for your volume. Click the <guimenu>Delete Volume on
                                                    Instance Delete</guimenu> option to delete the volume on deleting the
                                                instance.</para>
                        </listitem>
                      </varlistentry>
                      <varlistentry>
                        <term>Boot from volume snapshot (creates a new volume)</term>
                        <listitem>
                          <para>Using this option, you can boot from a volume snapshot and create
                                                a new volume by choosing <guimenu>Volume Snapshot</guimenu> from a list
                                                and adding a <guimenu>Device Name</guimenu> for your volume. Click the
                                                <guimenu>Delete Volume on Instance Delete</guimenu> option to delete the
                                                volume on deleting the instance.</para>
                        </listitem>
                      </varlistentry>
                    </variablelist>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>Image Name</term>
                  <listitem>
                    <para>This field changes based on your previous selection. If you have
                                    chosen to launch an instance using an image, the <guimenu>Image Name</guimenu>
                                    field displays. Select the image name from the dropdown list.</para>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>Instance Snapshot</term>
                  <listitem>
                    <para>This field changes based on your previous selection. If you have
                                    chosen to launch an instance using a snapshot, the
                                    <guimenu>Instance Snapshot</guimenu> field displays.
                                    Select the snapshot name from the dropdown list.</para>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>Volume</term>
                  <listitem>
                    <para>This field changes based on your previous selection. If you have
                                    chosen to launch an instance using a volume, the <guimenu>Volume</guimenu>
                                    field displays. Select the volume name from the dropdown list.
                                    If you want to delete the volume on instance delete,
                                    check the <guimenu>Delete Volume on Instance Delete</guimenu> option.</para>
                  </listitem>
                </varlistentry>
              </variablelist>
              <para><guimenu>Flavor</guimenu> tab</para>
              <variablelist>
                <varlistentry>
                  <term>Flavor</term>
                  <listitem>
                    <para>Specify the size of the instance to launch.</para>
                    <note>
                      <para>The flavor is selected based on the size of the image selected
                                        for launching an instance. For example, while creating an image, if
                                        you have entered the value in the <guimenu>Minimum RAM (MB)</guimenu> field
                                        as 2048, then on selecting the image, the default flavor is
                                        <literal>m1.small</literal>.</para>
                    </note>
                  </listitem>
                </varlistentry>
              </variablelist>
              <para><guimenu>Networks</guimenu> tab</para>
              <variablelist>
                <varlistentry>
                  <term>Selected Networks</term>
                  <listitem>
                    <para>To add a network to the instance, click the <guimenu>+</guimenu> in the
                                    <guimenu>Available</guimenu> field.</para>
                  </listitem>
                </varlistentry>
              </variablelist>
              <para><guimenu>Network Ports</guimenu> tab</para>
              <variablelist>
                <varlistentry>
                  <term>Ports</term>
                  <listitem>
                    <para>Activate the ports that you want to assign to the instance.</para>
                  </listitem>
                </varlistentry>
              </variablelist>
              <para><guimenu>Security Groups</guimenu> tab</para>
              <variablelist>
                <varlistentry>
                  <term>Security Groups</term>
                  <listitem>
                    <para>Activate the security groups that you want to assign to the instance.</para>
                    <para>Security groups are a kind of cloud firewall that define which
                                    incoming network traffic is forwarded to instances.</para>
                    <para>If you have not created any security groups, you can assign
                                    only the default security group to the instance.</para>
                  </listitem>
                </varlistentry>
              </variablelist>
              <para><guimenu>Key Pair</guimenu> tab</para>
              <variablelist>
                <varlistentry>
                  <term>Key Pair</term>
                  <listitem>
                    <para>Specify a key pair.</para>
                    <para>If the image uses a static root password or a static key set
                                    (neither is recommended), you do not need to provide a key pair
                                    to launch the instance.</para>
                  </listitem>
                </varlistentry>
              </variablelist>
              <para><guimenu>Configuration</guimenu> tab</para>
              <variablelist>
                <varlistentry>
                  <term>Customization Script Source</term>
                  <listitem>
                    <para>Specify a customization script that runs after your instance
                                    launches.</para>
                  </listitem>
                </varlistentry>
              </variablelist>
              <para><guimenu>Metadata</guimenu> tab</para>
              <variablelist>
                <varlistentry>
                  <term>Available Metadata</term>
                  <listitem>
                    <para>Add Metadata items to your instance.</para>
                  </listitem>
                </varlistentry>
              </variablelist>
            </step>
            <step>
              <para>Click <guimenu>Launch Instance</guimenu>.</para>
              <para>The instance starts on a compute node in the cloud.</para>
            </step>
          </procedure>
          <note>
            <para>If you did not provide a key pair, security groups, or rules, users
                    can access the instance only from inside the cloud through VNC. Even
                    pinging the instance is not possible without an ICMP rule configured.</para>
          </note>
          <para>You can also launch an instance from the <guimenu>Images</guimenu> or
                <guimenu>Volumes</guimenu> category when you launch an instance from
                an image or a volume respectively.</para>
          <para>When you launch an instance from an image, OpenStack creates a local
                copy of the image on the compute node where the instance starts.</para>
          <para>For details on creating images, see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/image-guide/create-images-manually.html">Creating images
                    manually</link>
                in the <emphasis>OpenStack Virtual Machine Image Guide</emphasis>.</para>
          <para>When you launch an instance from a volume, note the following steps:</para>
          <itemizedlist>
            <listitem>
              <para>To select the volume from which to launch, launch an instance from
                        an arbitrary image on the volume. The arbitrary image that you select
                        does not boot. Instead, it is replaced by the image on the volume that
                        you choose in the next steps.</para>
              <para>To boot a Xen image from a volume, the image you launch in must be
                        the same type, fully virtualized or paravirtualized, as the one on
                        the volume.</para>
            </listitem>
            <listitem>
              <para>Select the volume or volume snapshot from which to boot. Enter a
                        device name. Enter <literal>vda</literal> for KVM images or <literal>xvda</literal> for Xen images.</para>
            </listitem>
          </itemizedlist>
          <note>
            <para>When running QEMU without support for the hardware virtualization, set
                    <literal>cpu_mode="none"</literal> alongside <literal>virt_type=qemu</literal> in
                    <literal>/etc/nova/nova-compute.conf</literal> to solve the following error:</para>
            <screen language="console">libvirtError: unsupported configuration: CPU mode 'host-model'
for ``x86_64`` qemu domain on ``x86_64`` host is not supported by hypervisor</screen>
          </note>
        </section>
        <section xml:id="connect-to-your-instance-by-using-ssh">
          <title>Connect to your instance by using SSH</title>
          <para>To use SSH to connect to your instance, use the downloaded keypair
                file.</para>
          <note>
            <para>The user name is <literal>ubuntu</literal> for the Ubuntu cloud images on TryStack.</para>
          </note>
          <procedure>
            <step>
              <para>Copy the IP address for your instance.</para>
            </step>
            <step>
              <para>Use the <command>ssh</command> command to make a secure connection to the instance.
                        For example:</para>
              <screen language="console">$ ssh -i MyKey.pem ubuntu@10.0.0.2</screen>
            </step>
            <step>
              <para>At the prompt, type <literal>yes</literal>.</para>
            </step>
          </procedure>
          <para>It is also possible to SSH into an instance without an SSH keypair, if the
                administrator has enabled root password injection.  For more information
                about root password injection, see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/nova/latest/admin/admin-password-injection.html">Injecting the administrator password</link>
                in the <emphasis>OpenStack Administrator Guide</emphasis>.</para>
        </section>
        <section xml:id="track-usage-for-instances">
          <title>Track usage for instances</title>
          <para>You can track usage for instances for each project. You can track costs
                per month by showing meters like number of vCPUs, disks, RAM, and
                uptime for all your instances.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Overview</guimenu> category.</para>
            </step>
            <step>
              <para>To query the instance usage for a month, select a month and click
                        <guimenu>Submit</guimenu>.</para>
            </step>
            <step>
              <para>To download a summary, click <guimenu>Download CSV Summary</guimenu>.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="create-an-instance-snapshot">
          <title>Create an instance snapshot</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click the <guimenu>Instances</guimenu> category.</para>
            </step>
            <step>
              <para>Select the instance from which to create a snapshot.</para>
            </step>
            <step>
              <para>In the actions column, click <guimenu>Create Snapshot</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Create Snapshot</guimenu> dialog box, enter a name for the
                        snapshot, and click <guimenu>Create Snapshot</guimenu>.</para>
              <para>The <guimenu>Images</guimenu> category shows the instance snapshot.</para>
            </step>
          </procedure>
          <para>To launch an instance from the snapshot, select the snapshot and click
                <guimenu>Launch</guimenu>. Proceed with launching an instance.</para>
        </section>
        <section xml:id="manage-an-instance">
          <title>Manage an instance</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Instances</guimenu> category.</para>
            </step>
            <step>
              <para>Select an instance.</para>
            </step>
            <step>
              <para>In the menu list in the actions column, select the state.</para>
              <para>You can resize or rebuild an instance. You can also choose to view
                        the instance console log, edit instance or the security groups.
                        Depending on the current state of the instance, you can pause,
                        resume, suspend, soft or hard reboot, or terminate it.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="create-and-manage-networks" xml:base="create-networks">
        <title>Create and manage networks</title>
        <para>The OpenStack Networking service provides a scalable system for managing
            the network connectivity within an OpenStack cloud deployment. It can
            easily and quickly react to changing network needs (for example,
            creating and assigning new IP addresses).</para>
        <para>Networking in OpenStack is complex. This section provides the basic
            instructions for creating a network and a router. For detailed
            information about managing networks, refer to the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/neutron/latest/admin/">OpenStack Networking Guide</link>.</para>
        <section xml:id="create-a-network">
          <title>Create a network</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Network</guimenu> tab and
                        click <guimenu>Networks</guimenu> category.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Network</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Create Network</guimenu> dialog box, specify the following values.</para>
              <para><guimenu>Network</guimenu> tab</para>
              <para><guimenu>Network Name</guimenu>: Specify a name to identify the network.</para>
              <para><guimenu>Shared</guimenu>: Share the network with other projects. Non admin users
                        are not allowed to set shared option.</para>
              <para><guimenu>Admin State</guimenu>: The state to start the network in.</para>
              <para><guimenu>Create Subnet</guimenu>: Select this check box to create a subnet</para>
              <para>You do not have to specify a subnet when you create a network, but if
                        you do not specify a subnet, the network can not be attached to an instance.</para>
              <para><guimenu>Subnet</guimenu> tab</para>
              <para><guimenu>Subnet Name</guimenu>: Specify a name for the subnet.</para>
              <para><guimenu>Network Address</guimenu>: Specify the IP address for the subnet.</para>
              <para><guimenu>IP Version</guimenu>: Select IPv4 or IPv6.</para>
              <para><guimenu>Gateway IP</guimenu>: Specify an IP address for a specific gateway. This
                        parameter is optional.</para>
              <para><guimenu>Disable Gateway</guimenu>: Select this check box to disable a gateway IP
                        address.</para>
              <para><guimenu>Subnet Details</guimenu> tab</para>
              <para><guimenu>Enable DHCP</guimenu>: Select this check box to enable DHCP.</para>
              <para><guimenu>Allocation Pools</guimenu>: Specify IP address pools.</para>
              <para><guimenu>DNS Name Servers</guimenu>: Specify a name for the DNS server.</para>
              <para><guimenu>Host Routes</guimenu>: Specify the IP address of host routes.</para>
            </step>
            <step>
              <para>Click <guimenu>Create</guimenu>.</para>
              <para>The dashboard shows the network on the <guimenu>Networks</guimenu> tab.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="create-a-router">
          <title>Create a router</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Network</guimenu> tab and
                        click <guimenu>Routers</guimenu> category.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Router</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Create Router</guimenu> dialog box, specify a name for the router
                        and <guimenu>External Network</guimenu>, and click <guimenu>Create Router</guimenu>.</para>
              <para>The new router is now displayed in the <guimenu>Routers</guimenu> tab.</para>
            </step>
            <step>
              <para>To connect a private network to the newly created router, perform the
                        following steps:</para>
              <substeps>
                <step>
                  <para>On the <guimenu>Routers</guimenu> tab, click the name of the router.</para>
                </step>
                <step>
                  <para>On the <guimenu>Router Details</guimenu> page, click the <guimenu>Interfaces</guimenu>
                                tab, then click <guimenu>Add Interface</guimenu>.</para>
                </step>
                <step>
                  <para>In the <guimenu>Add Interface</guimenu> dialog box, select a <guimenu>Subnet</guimenu>.</para>
                  <para>Optionally, in the <guimenu>Add Interface</guimenu> dialog box, set an
                                <guimenu>IP Address</guimenu> for the router interface for the selected subnet.</para>
                  <para>If you choose not to set the <guimenu>IP Address</guimenu> value, then by
                                default OpenStack Networking uses the first host IP address in the
                                subnet.</para>
                  <para>The <guimenu>Router Name</guimenu> and <guimenu>Router ID</guimenu> fields are
                                automatically updated.</para>
                </step>
              </substeps>
            </step>
            <step>
              <para>Click <guimenu>Add Interface</guimenu>.</para>
            </step>
          </procedure>
          <para>You have successfully created the router. You can view the new topology
                from the <guimenu>Network Topology</guimenu> tab.</para>
        </section>
        <section xml:id="create-a-port">
          <title>Create a port</title>
          <warning>
            <para>Creating and managing ports requires administrator privileges.
                    Contact an administrator before adding or changing ports.</para>
          </warning>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop-down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, click <guimenu>Networks</guimenu> category.</para>
            </step>
            <step>
              <para>Click on the <guimenu>Network Name</guimenu> of the network in which the port
                        has to be created.</para>
            </step>
            <step>
              <para>In the <guimenu>Create Port</guimenu> dialog box, specify the following values.</para>
              <para><guimenu>Name</guimenu>: Specify name to identify the port.</para>
              <para><guimenu>Device ID</guimenu>: Device ID attached to the port.</para>
              <para><guimenu>Device Owner</guimenu>: Device owner attached to the port.</para>
              <para><guimenu>Binding Host</guimenu>: The ID of the host where the port is allocated.</para>
              <para><guimenu>Binding VNIC Type</guimenu>: Select the VNIC type that is bound to the
                        neutron port.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Port</guimenu>.</para>
              <para>The new port is now displayed in the <guimenu>Ports</guimenu> list.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="create-and-manage-object-containers" xml:base="manage-containers">
        <title>Create and manage object containers</title>
        <para>OpenStack Object Storage (swift) is used for redundant, scalable data storage
            using clusters of standardized servers to store petabytes of accessible data.
            It is a long-term storage system for large amounts of static data which can be
            retrieved and updated.</para>
        <para>OpenStack Object Storage provides a distributed, API-accessible storage
            platform that can be integrated directly into an application or used to
            store any type of file, including VM images, backups, archives, or media
            files. In the OpenStack dashboard, you can only manage containers and
            objects.</para>
        <para>In OpenStack Object Storage, containers provide storage for objects in a
            manner similar to a Windows folder or Linux file directory, though they
            cannot be nested. An object in OpenStack consists of the file to be
            stored in the container and any accompanying metadata.</para>
        <section xml:id="create-a-container">
          <title>Create a container</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Object Store</guimenu> tab and
                        click <guimenu>Containers</guimenu> category.</para>
            </step>
            <step>
              <para>Click <guimenu>Container</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Create Container</guimenu> dialog box, enter a name for the
                        container, and then click <guimenu>Create</guimenu>.</para>
            </step>
          </procedure>
          <para>You have successfully created a container.</para>
          <note>
            <para>To delete a container, click the <guimenu>More</guimenu> button and select
                    <guimenu>Delete Container</guimenu>.</para>
          </note>
        </section>
        <section xml:id="upload-an-object">
          <title>Upload an object</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Object Store</guimenu> tab and
                        click <guimenu>Containers</guimenu> category.</para>
            </step>
            <step>
              <para>Select the container in which you want to store your object.</para>
            </step>
            <step>
              <para>Click the <guimenu>Upload File</guimenu> icon.</para>
              <para>The <guimenu>Upload File To Container: &lt;name&gt;</guimenu> dialog box
                        appears.
                        <literal>&lt;name&gt;</literal> is the name of the container to which you are uploading
                        the object.</para>
            </step>
            <step>
              <para>Enter a name for the object.</para>
            </step>
            <step>
              <para>Browse to and select the file that you want to upload.</para>
            </step>
            <step>
              <para>Click <guimenu>Upload File</guimenu>.</para>
            </step>
          </procedure>
          <para>You have successfully uploaded an object to the container.</para>
          <note>
            <para>To delete an object, click the <guimenu>More button</guimenu> and select
                    <guimenu>Delete Object</guimenu>.</para>
          </note>
        </section>
        <section xml:id="manage-an-object">
          <title>Manage an object</title>
          <para>
            <emphasis role="bold">To edit an object</emphasis>
          </para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Object Store</guimenu> tab and
                        click <guimenu>Containers</guimenu> category.</para>
            </step>
            <step>
              <para>Select the container in which you want to store your object.</para>
            </step>
            <step>
              <para>Click the menu button and choose <guimenu>Edit</guimenu> from the dropdown list.</para>
              <para>The <guimenu>Edit Object</guimenu> dialog box is displayed.</para>
            </step>
            <step>
              <para>Browse to and select the file that you want to upload.</para>
            </step>
            <step>
              <para>Click <guimenu>Update Object</guimenu>.</para>
            </step>
          </procedure>
          <note>
            <para>To delete an object, click the menu button and select
                    <guimenu>Delete Object</guimenu>.</para>
          </note>
          <para>
            <emphasis role="bold">To copy an object from one container to another</emphasis>
          </para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Object Store</guimenu> tab and
                        click <guimenu>Containers</guimenu> category.</para>
            </step>
            <step>
              <para>Select the container in which you want to store your object.</para>
            </step>
            <step>
              <para>Click the menu button and choose <guimenu>Copy</guimenu> from the dropdown list.</para>
            </step>
            <step>
              <para>In the <guimenu>Copy Object</guimenu> launch dialog box, enter the following
                        values:</para>
              <itemizedlist>
                <listitem>
                  <para><guimenu>Destination Container</guimenu>: Choose the destination container from
                                the list.</para>
                </listitem>
                <listitem>
                  <para><guimenu>Path</guimenu>: Specify a path in which the new copy should be stored
                                inside of the selected container.</para>
                </listitem>
                <listitem>
                  <para><guimenu>Destination object name</guimenu>: Enter a name for the object in the
                                new container.</para>
                </listitem>
              </itemizedlist>
            </step>
            <step>
              <para>Click <guimenu>Copy Object</guimenu>.</para>
            </step>
          </procedure>
          <para>
            <emphasis role="bold">To create a metadata-only object without a file</emphasis>
          </para>
          <para>You can create a new object in container without a file available and
                can upload the file later when it is ready. This temporary object acts a
                place-holder for a new object, and enables the user to share object
                metadata and URL info in advance.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Object Store</guimenu> tab and
                        click <guimenu>Containers</guimenu> category.</para>
            </step>
            <step>
              <para>Select the container in which you want to store your object.</para>
            </step>
            <step>
              <para>Click <guimenu>Upload Object</guimenu>.</para>
              <para>The <guimenu>Upload Object To Container</guimenu>: <literal>&lt;name&gt;</literal> dialog box is
                        displayed.</para>
              <para><literal>&lt;name&gt;</literal> is the name of the container to which you are uploading
                        the object.</para>
            </step>
            <step>
              <para>Enter a name for the object.</para>
            </step>
            <step>
              <para>Click <guimenu>Update Object</guimenu>.</para>
            </step>
          </procedure>
          <para>
            <emphasis role="bold">To create a pseudo-folder</emphasis>
          </para>
          <para>Pseudo-folders are similar to folders in your desktop operating system.
                They are virtual collections defined by a common prefix on the object’s
                name.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Object Store</guimenu> tab and
                        click <guimenu>Containers</guimenu> category.</para>
            </step>
            <step>
              <para>Select the container in which you want to store your object.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Pseudo-folder</guimenu>.</para>
              <para>The <guimenu>Create Pseudo-Folder in Container</guimenu><literal>&lt;name&gt;</literal> dialog box
                        is displayed. <literal>&lt;name&gt;</literal> is the name of the container to which you
                        are uploading the object.</para>
            </step>
            <step>
              <para>Enter a name for the pseudo-folder.</para>
              <para>A slash (/) character is used as the delimiter for pseudo-folders in
                        Object Storage.</para>
            </step>
            <step>
              <para>Click <guimenu>Create</guimenu>.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="create-and-manage-volumes" xml:base="manage-volumes">
        <title>Create and manage volumes</title>
        <para>Volumes are block storage devices that you attach to instances to enable
            persistent storage. You can attach a volume to a running instance or
            detach a volume and attach it to another instance at any time. You can
            also create a snapshot from or delete a volume. Only administrative
            users can create volume types.</para>
        <section xml:id="create-a-volume">
          <title>Create a volume</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Volumes</guimenu> category.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Volume</guimenu>.</para>
              <para>In the dialog box that opens, enter or select the following values.</para>
              <para><guimenu>Volume Name</guimenu>: Specify a name for the volume.</para>
              <para><guimenu>Description</guimenu>: Optionally, provide a brief description for the
                        volume.</para>
              <para><guimenu>Volume Source</guimenu>: Select one of the following options:</para>
              <itemizedlist>
                <listitem>
                  <para>No source, empty volume: Creates an empty volume. An empty volume does
                                not contain a file system or a partition table.</para>
                </listitem>
                <listitem>
                  <para>Snapshot: If you choose this option, a new field for
                                <guimenu>Use snapshot as a source</guimenu> displays. You can select the
                                snapshot from the list.</para>
                </listitem>
                <listitem>
                  <para>Image: If you choose this option, a new field for <guimenu>Use image
                                    as a source</guimenu> displays. You can select the image from the list.</para>
                </listitem>
                <listitem>
                  <para>Volume: If you choose this option, a new field for
                                <guimenu>Use volume as a source</guimenu> displays. You can select the volume
                                from the list. Options to use a snapshot or a volume as the source for a
                                volume are displayed only if there are existing snapshots or volumes.</para>
                </listitem>
              </itemizedlist>
              <para><guimenu>Type</guimenu>: Leave this field blank.</para>
              <para><guimenu>Size (GB)</guimenu>: The size of the volume in gibibytes (GiB).</para>
              <para><guimenu>Availability Zone</guimenu>: Select the Availability Zone from the list.
                        By default, this value is set to the availability zone given by the cloud
                        provider (for example, <literal>us-west</literal> or <literal>apac-south</literal>). For some cases,
                        it could be <literal>nova</literal>.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Volume</guimenu>.</para>
            </step>
          </procedure>
          <para>The dashboard shows the volume on the <guimenu>Volumes</guimenu> tab.</para>
        </section>
        <section xml:id="attach-a-volume-to-an-instance-dash">
          <title>Attach a volume to an instance</title>
          <para>After you create one or more volumes, you can attach them to instances.
                You can attach a volume to one instance at a time.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Volumes</guimenu> category.</para>
            </step>
            <step>
              <para>Select the volume to add to an instance and click
                        <guimenu>Manage Attachments</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Manage Volume Attachments</guimenu> dialog box, select an instance.</para>
            </step>
            <step>
              <para>Enter the name of the device from which the volume is accessible by
                        the instance.</para>
              <note>
                <para>The actual device name might differ from the volume name because
                            of hypervisor settings.</para>
              </note>
            </step>
            <step>
              <para>Click <guimenu>Attach Volume</guimenu>.</para>
              <para>The dashboard shows the instance to which the volume is now attached
                        and the device name.</para>
            </step>
          </procedure>
          <para>You can view the status of a volume in the Volumes tab of the dashboard.
                The volume is either Available or In-Use.</para>
          <para>Now you can log in to the instance and mount, format, and use the disk.</para>
        </section>
        <section xml:id="detach-a-volume-from-an-instance">
          <title>Detach a volume from an instance</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click the <guimenu>Volumes</guimenu> category.</para>
            </step>
            <step>
              <para>Select the volume and click <guimenu>Manage Attachments</guimenu>.</para>
            </step>
            <step>
              <para>Click <guimenu>Detach Volume</guimenu> and confirm your changes.</para>
            </step>
          </procedure>
          <para>A message indicates whether the action was successful.</para>
        </section>
        <section xml:id="create-a-snapshot-from-a-volume">
          <title>Create a snapshot from a volume</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Volumes</guimenu> category.</para>
            </step>
            <step>
              <para>Select a volume from which to create a snapshot.</para>
            </step>
            <step>
              <para>In the <guimenu>Actions</guimenu> column, click <guimenu>Create Snapshot</guimenu>.</para>
            </step>
            <step>
              <para>In the dialog box that opens, enter a snapshot name and a brief
                        description.</para>
            </step>
            <step>
              <para>Confirm your changes.</para>
              <para>The dashboard shows the new volume snapshot in Volume Snapshots tab.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="edit-a-volume">
          <title>Edit a volume</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Volumes</guimenu> category.</para>
            </step>
            <step>
              <para>Select the volume that you want to edit.</para>
            </step>
            <step>
              <para>In the <guimenu>Actions</guimenu> column, click <guimenu>Edit Volume</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Edit Volume</guimenu> dialog box, update the name and description
                        of the volume.</para>
            </step>
            <step>
              <para>Click <guimenu>Edit Volume</guimenu>.</para>
              <note>
                <para>You can extend a volume by using the <guimenu>Extend Volume</guimenu>
                            option available in the <guimenu>More</guimenu> dropdown list and entering the
                            new value for volume size.</para>
              </note>
            </step>
          </procedure>
        </section>
        <section xml:id="delete-a-volume">
          <title>Delete a volume</title>
          <para>When you delete an instance, the data in its attached volumes is not
                deleted.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Compute</guimenu> tab and
                        click <guimenu>Volumes</guimenu> category.</para>
            </step>
            <step>
              <para>Select the check boxes for the volumes that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Volumes</guimenu> and confirm your choice.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="create-and-manage-shares" xml:base="manage-shares">
        <title>Create and manage shares</title>
        <para>Shares are file storage that you provide access to instances. You can allow
            access to a share to a running instance or deny access to a share and allow
            access to it to another instance at any time. You can also delete a share.
            You can create snapshot from a share if the driver supports it. Only
            administrative users can create share types.</para>
        <section xml:id="create-a-share">
          <title>Create a share</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, and click <guimenu>Shares</guimenu>.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Share</guimenu>.</para>
              <para>In the dialog box that opens, enter or select the following values.</para>
              <para><guimenu>Share Name</guimenu>: Specify a name for the share.</para>
              <para><guimenu>Description</guimenu>: Optionally, provide a brief description for the
                        share.</para>
              <para><guimenu>Share Type</guimenu>: Choose a share type.</para>
              <para><guimenu>Size (GB)</guimenu>: The size of the share in gibibytes (GiB).</para>
              <para><guimenu>Share Protocol</guimenu>: Select NFS, CIFS, GlusterFS, or HDFS.</para>
              <para><guimenu>Share Network</guimenu>: Choose a share network.</para>
              <para><guimenu>Metadata</guimenu>: Enter metadata for the share creation if needed.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Share</guimenu>.</para>
            </step>
          </procedure>
          <para>The dashboard shows the share on the <guimenu>Shares</guimenu> tab.</para>
        </section>
        <section xml:id="delete-a-share">
          <title>Delete a share</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, and click <guimenu>Shares</guimenu>.</para>
            </step>
            <step>
              <para>Select the check boxes for the shares that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Shares</guimenu> and confirm your choice.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="allow-access">
          <title>Allow access</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, and click <guimenu>Shares</guimenu>.</para>
            </step>
            <step>
              <para>Go to the share that you want to allow access and choose
                        <guimenu>Manage Rules</guimenu> from Actions.</para>
            </step>
            <step>
              <para>Click <guimenu>Add rule</guimenu>.</para>
              <para><guimenu>Access Type</guimenu>: Choose ip, user, or cert.</para>
              <para><guimenu>Access Level</guimenu>: Choose read-write or read-only.</para>
              <para><guimenu>Access To</guimenu>: Fill in Access To field.</para>
            </step>
            <step>
              <para>Click <guimenu>Add Rule</guimenu>.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="deny-access">
          <title>Deny access</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, and click <guimenu>Shares</guimenu>.</para>
            </step>
            <step>
              <para>Go to the share that you want to deny access and choose
                        <guimenu>Manage Rules</guimenu> from Actions.</para>
            </step>
            <step>
              <para>Choose the rule you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete rule</guimenu> and confirm your choice.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="edit-share-metadata">
          <title>Edit share metadata</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, and click <guimenu>Shares</guimenu>.</para>
            </step>
            <step>
              <para>Go to the share that you want to edit and choose
                        <guimenu>Edit Share Metadata</guimenu> from Actions.</para>
            </step>
            <step>
              <para><guimenu>Metadata</guimenu>: To add share metadata, use key=value. To unset
                        metadata, use key.</para>
            </step>
            <step>
              <para>Click <guimenu>Edit Share Metadata</guimenu>.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="edit-share">
          <title>Edit share</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, and click <guimenu>Shares</guimenu>.</para>
            </step>
            <step>
              <para>Go to the share that you want to edit and choose <guimenu>Edit Share</guimenu> from
                        Actions.</para>
            </step>
            <step>
              <para><guimenu>Share Name</guimenu>: Enter a new share name.</para>
            </step>
            <step>
              <para><guimenu>Description</guimenu>: Enter a new description.</para>
            </step>
            <step>
              <para>Click <guimenu>Edit Share</guimenu>.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="extend-share">
          <title>Extend share</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, and click <guimenu>Shares</guimenu>.</para>
            </step>
            <step>
              <para>Go to the share that you want to edit and choose <guimenu>Extend Share</guimenu>
                        from Actions.</para>
            </step>
            <step>
              <para><guimenu>New Size (GB)</guimenu>: Enter new size.</para>
            </step>
            <step>
              <para>Click <guimenu>Extend Share</guimenu>.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="create-share-network">
          <title>Create share network</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, click <guimenu>Shares</guimenu>,
                        and click <guimenu>Share Networks</guimenu>.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Share Network</guimenu>.</para>
              <para>In the dialog box that opens, enter or select the following values.</para>
              <para><guimenu>Name</guimenu>: Specify a name for the share network.</para>
              <para><guimenu>Description</guimenu>: Optionally, provide a brief description for the
                        share network.</para>
              <para><guimenu>Neutron Net</guimenu>: Choose a neutron network.</para>
              <para><guimenu>Neutron Subnet</guimenu>: Choose a neutron subnet.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Share Network</guimenu>.</para>
            </step>
          </procedure>
          <para>The dashboard shows the share network on the <guimenu>Share Networks</guimenu> tab.</para>
        </section>
        <section xml:id="delete-a-share-network">
          <title>Delete a share network</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, click <guimenu>Shares</guimenu>, and
                        click <guimenu>Share Networks</guimenu>.</para>
            </step>
            <step>
              <para>Select the check boxes for the share networks that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Share Networks</guimenu> and confirm your choice.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="edit-share-network">
          <title>Edit share network</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, click <guimenu>Shares</guimenu>, and
                        click <guimenu>Share Networks</guimenu>.</para>
            </step>
            <step>
              <para>Go to the share network that you want to edit and choose
                        <guimenu>Edit Share Network</guimenu> from Actions.</para>
            </step>
            <step>
              <para><guimenu>Name</guimenu>: Enter a new share network name.</para>
            </step>
            <step>
              <para><guimenu>Description</guimenu>: Enter a new description.</para>
            </step>
            <step>
              <para>Click <guimenu>Edit Share Network</guimenu>.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="create-security-service">
          <title>Create security service</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, click <guimenu>Shares</guimenu>,
                        and click <guimenu>Security Services</guimenu>.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Security Service</guimenu>.</para>
              <para>In the dialog box that opens, enter or select the following values.</para>
              <para><guimenu>Name</guimenu>: Specify a name for the security service.</para>
              <para><guimenu>DNS IP</guimenu>: Enter the DNS IP address.</para>
              <para><guimenu>Server</guimenu>: Enter the server name.</para>
              <para><guimenu>Domain</guimenu>: Enter the domain name.</para>
              <para><guimenu>User</guimenu>: Enter the user name.</para>
              <para><guimenu>Password</guimenu>: Enter the password.</para>
              <para><guimenu>Confirm Password</guimenu>: Enter the password again to confirm.</para>
              <para><guimenu>Type</guimenu>: Choose the type from Active Directory, LDAP, or Kerberos.</para>
              <para><guimenu>Description</guimenu>: Optionally, provide a brief description for the
                        security service.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Security Service</guimenu>.</para>
            </step>
          </procedure>
          <para>The dashboard shows the security service on the <guimenu>Security Services</guimenu>
                tab.</para>
        </section>
        <section xml:id="delete-a-security-service">
          <title>Delete a security service</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, click <guimenu>Shares</guimenu>, and
                        click <guimenu>Security Services</guimenu>.</para>
            </step>
            <step>
              <para>Select the check boxes for the security services that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Security Services</guimenu> and confirm your choice.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="edit-security-service">
          <title>Edit security service</title>
          <procedure>
            <step>
              <para>Log in to the dashboard, choose a project, click <guimenu>Shares</guimenu>,
                        and click <guimenu>Security Services</guimenu>.</para>
            </step>
            <step>
              <para>Go to the security service that you want to edit and choose
                        <guimenu>Edit Security Service</guimenu> from Actions.</para>
            </step>
            <step>
              <para><guimenu>Name</guimenu>: Enter a new security service name.</para>
            </step>
            <step>
              <para><guimenu>Description</guimenu>: Enter a new description.</para>
            </step>
            <step>
              <para>Click <guimenu>Edit Security Service</guimenu>.</para>
              <para>A message indicates whether the action was successful.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="launch-and-manage-stacks" xml:base="stacks">
        <title>Launch and manage stacks</title>
        <para>OpenStack Orchestration is a service that you can use to
            orchestrate multiple composite cloud applications. This
            service supports the use of both the Amazon Web Services (AWS)
            CloudFormation template format through both a Query API that
            is compatible with CloudFormation and the native OpenStack
            Heat Orchestration Template (HOT) format through a REST API.</para>
        <para>These flexible template languages enable application
            developers to describe and automate the deployment of
            infrastructure, services, and applications. The templates
            enable creation of most OpenStack resource types, such as
            instances, floating IP addresses, volumes, security groups,
            and users. Once created, the resources are referred to as
            stacks.</para>
        <para>The template languages are described in the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/heat/latest/template_guide/">Template Guide</link>.</para>
        <section xml:id="launch-a-stack">
          <title>Launch a stack</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Orchestration</guimenu> tab and
                        click <guimenu>Stacks</guimenu> category.</para>
            </step>
            <step>
              <para>Click <guimenu>Launch Stack</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Select Template</guimenu> dialog box, specify the
                        following values:</para>
              <informaltable>
                <tgroup cols="2">
                  <colspec colname="c0" colwidth="39"/>
                  <colspec colname="c1" colwidth="31"/>
                  <tbody>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Template Source</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Choose the source of the
                                            template from the list.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Template URL/File/Data</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Depending on the source that
                                            you select, enter the URL,
                                            browse to the file location,
                                            or directly include the
                                            template.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Environment Source</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Choose the source of the
                                            environment from the list.
                                            The environment files contain
                                            additional settings for the
                                            stack.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Environment File/Data</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Depending on the source that
                                            you select, browse to the
                                            file location, directly
                                            include the environment</para>
                      </entry>
                    </row>
                  </tbody>
                </tgroup>
              </informaltable>
            </step>
            <step>
              <para>Click <guimenu>Next</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Launch Stack</guimenu> dialog box, specify the
                        following values:</para>
              <informaltable>
                <tgroup cols="2">
                  <colspec colname="c0" colwidth="33"/>
                  <colspec colname="c1" colwidth="33"/>
                  <tbody>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Stack Name</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter a name to identify
                                            the stack.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Creation Timeout</guimenu>
                          <guimenu>(minutes)</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the number of minutes
                                            that can elapse before the
                                            launch of the stack times out.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Rollback On Failure</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Select this check box if you
                                            want the service to roll back
                                            changes if the stack fails to
                                            launch.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Password for user</guimenu>
                          <guimenu>“demo”</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the password that
                                            the default user uses when the
                                            stack is created.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>DBUsername</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the name of the
                                            database user.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>LinuxDistribution</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the Linux distribution
                                            that is used in the stack.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>DBRootPassword</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the root password for
                                            the database.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>KeyName</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the name of the key pair
                                            to use to log in to the stack.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>DBName</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the name of the
                                            database.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>DBPassword</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the password of the
                                            database.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>InstanceType</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the flavor for the
                                            instance.</para>
                      </entry>
                    </row>
                  </tbody>
                </tgroup>
              </informaltable>
            </step>
            <step>
              <para>Click <guimenu>Launch</guimenu> to create a stack. The <guimenu>Stacks</guimenu>
                        tab shows the stack.</para>
            </step>
          </procedure>
          <para>After the stack is created, click on the stack name to see the
                following details:</para>
          <variablelist>
            <varlistentry>
              <term>Topology</term>
              <listitem>
                <para>The topology of the stack.</para>
              </listitem>
            </varlistentry>
            <varlistentry>
              <term>Overview</term>
              <listitem>
                <para>The parameters and details of the stack.</para>
              </listitem>
            </varlistentry>
            <varlistentry>
              <term>Resources</term>
              <listitem>
                <para>The resources used by the stack.</para>
              </listitem>
            </varlistentry>
            <varlistentry>
              <term>Events</term>
              <listitem>
                <para>The events related to the stack.</para>
              </listitem>
            </varlistentry>
            <varlistentry>
              <term>Template</term>
              <listitem>
                <para>The template for the stack.</para>
              </listitem>
            </varlistentry>
          </variablelist>
        </section>
        <section xml:id="manage-a-stack">
          <title>Manage a stack</title>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Orchestration</guimenu> tab and
                        click <guimenu>Stacks</guimenu> category.</para>
            </step>
            <step>
              <para>Select the stack that you want to update.</para>
            </step>
            <step>
              <para>Click <guimenu>Change Stack Template</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Select Template</guimenu> dialog box, select the
                        new template source or environment source.</para>
            </step>
            <step>
              <para>Click <guimenu>Next</guimenu>.</para>
              <para>The <guimenu>Update Stack Parameters</guimenu> window appears.</para>
            </step>
            <step>
              <para>Enter new values for any parameters that you want to update.</para>
            </step>
            <step>
              <para>Click <guimenu>Update</guimenu>.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="delete-a-stack">
          <title>Delete a stack</title>
          <para>When you delete a stack, you cannot undo this action.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>Select the appropriate project from the drop down menu at the top left.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Orchestration</guimenu> tab and
                        click <guimenu>Stacks</guimenu> category.</para>
            </step>
            <step>
              <para>Select the stack that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Stack</guimenu>.</para>
            </step>
            <step>
              <para>In the confirmation dialog box, click <guimenu>Delete Stack</guimenu>
                        to confirm the deletion.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="create-and-manage-databases" xml:base="databases">
        <title>Create and manage databases</title>
        <para>The Database service provides scalable and reliable cloud provisioning
            functionality for both relational and non-relational database engines.
            Users can quickly and easily use database features without the burden of
            handling complex administrative tasks.</para>
        <section xml:id="dashboard-create-db-instance">
          <title>Create a database instance</title>
          <para><emphasis role="bold">Prerequisites.</emphasis> Before you create a database instance, you need to
                configure a default datastore and make sure you have an appropriate
                flavor for the type of database instance you want.</para>
          <procedure>
            <step>
              <para>
                <emphasis role="bold">Configure a default datastore.</emphasis>
              </para>
              <para>Because the dashboard does not let you choose a specific datastore to
                        use with an instance, you need to configure a default datastore. The
                        dashboard then uses the default datastore to create the instance.</para>
              <substeps>
                <step>
                  <para>Add the following line to <literal>/etc/trove/trove.conf</literal>:</para>
                  <screen language="console">default_datastore = DATASTORE_NAME</screen>
                  <para>Replace <literal>DATASTORE_NAME</literal> with the name that the administrative
                                user set when issuing the <command>trove-manage</command> command to create the
                                datastore. You can use the trove <command>datastore-list</command> command to
                                display the datastores that are available in your environment.</para>
                  <para>For example, if your MySQL data store name is set to <literal>mysql</literal>,
                                your entry would look like this:</para>
                  <screen language="console">default_datastore = mysql</screen>
                </step>
                <step>
                  <para>Restart Database services on the controller node:</para>
                  <screen language="console"># service trove-api restart
# service trove-taskmanager restart
# service trove-conductor restart</screen>
                </step>
              </substeps>
            </step>
            <step>
              <para>
                <emphasis role="bold">Verify flavor.</emphasis>
              </para>
              <para>Make sure an appropriate flavor exists for the type of
                        database instance you want.</para>
            </step>
          </procedure>
          <para><emphasis role="bold">Create database instance.</emphasis> Once you have configured a default
                datastore and verified that you have an appropriate flavor, you can
                create a database instance.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard.</para>
            </step>
            <step>
              <para>From the CURRENT PROJECT on the <guimenu>Project</guimenu> tab, select the
                        appropriate project.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Database</guimenu> tab and
                        click <guimenu>Instances</guimenu> category.  This lists the instances that
                        already exist in your environment.</para>
            </step>
            <step>
              <para>Click <guimenu>Launch Instance</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Launch Database</guimenu> dialog box, specify the following values.</para>
              <para>Details</para>
              <para><guimenu>Database Name</guimenu>: Specify a name for the database instance.</para>
              <para><guimenu>Flavor</guimenu>: Select an appropriate flavor for the instance.</para>
              <para><guimenu>Volume Size</guimenu>: Select a volume size. Volume size is expressed in
                        GB.</para>
              <para><guimenu>Initialize Databases</guimenu>: Initial Database</para>
              <para>Optionally provide a comma separated list of databases to create, for
                        example:</para>
              <para><literal>database1</literal>, <literal>database2</literal>, <literal>database3</literal></para>
              <para><guimenu>Initial Admin User</guimenu>: Create an initial admin user. This user will
                        have access to all the databases you create.</para>
              <para><guimenu>Password</guimenu>: Specify a password associated with the initial admin
                        user you just named.</para>
              <para><guimenu>Host</guimenu>: Optionally, allow the user to connect only from this host.
                        If you do not specify a host, this user will be allowed to connect from
                        anywhere.</para>
            </step>
            <step>
              <para>Click the <guimenu>Launch</guimenu> button. The new database instance appears in
                        the databases list.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="backup-and-restore-a-database">
          <title>Backup and restore a database</title>
          <para>You can use Database services to backup a database and store the backup
                artifact in the Object Storage service. Later on, if the original
                database is damaged, you can use the backup artifact to restore the
                database. The restore process creates a database instance.</para>
          <para>This example shows you how to back up and restore a MySQL database.</para>
          <section xml:id="to-backup-the-database-instance">
            <title>To backup the database instance</title>
            <procedure>
              <step>
                <para>Log in to the dashboard.</para>
              </step>
              <step>
                <para>From the CURRENT PROJECT on the <guimenu>Project</guimenu> tab, select the
                            appropriate project.</para>
              </step>
              <step>
                <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Database</guimenu> tab and
                            click <guimenu>Instances</guimenu> category. This displays the existing
                            instances in your system.</para>
              </step>
              <step>
                <para>Click <guimenu>Create Backup</guimenu>.</para>
              </step>
              <step>
                <para>In the <guimenu>Backup Database</guimenu> dialog box, specify the following
                            values:</para>
                <para>Name</para>
                <para>Specify a name for the backup.</para>
                <para>Database Instance</para>
                <para>Select the instance you want to back up.</para>
              </step>
              <step>
                <para>Click <guimenu>Backup</guimenu>. The new backup appears in the backup list.</para>
              </step>
            </procedure>
          </section>
          <section xml:id="to-restore-a-database-instance">
            <title>To restore a database instance</title>
            <para>Now assume that your original database instance is damaged and you
                    need to restore it. You do the restore by using your backup to create
                    a new database instance.</para>
            <procedure>
              <step>
                <para>Log in to the dashboard.</para>
              </step>
              <step>
                <para>From the CURRENT PROJECT on the <guimenu>Project</guimenu> tab, select the
                            appropriate project.</para>
              </step>
              <step>
                <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Database</guimenu> tab and
                            click <guimenu>Backups</guimenu> category. This lists the available backups.</para>
              </step>
              <step>
                <para>Check the backup you want to use and click <guimenu>Restore Backup</guimenu>.</para>
              </step>
              <step>
                <para>In the <guimenu>Launch Database</guimenu> dialog box, specify the values you
                            want for the new database instance.</para>
              </step>
              <step>
                <para>Click the <guimenu>Restore From Database</guimenu> tab and make sure that this
                            new instance is based on the correct backup.</para>
              </step>
              <step>
                <para>Click <guimenu>Launch</guimenu>.</para>
                <para>The new instance appears in the database instances list.</para>
              </step>
            </procedure>
          </section>
        </section>
        <section xml:id="update-a-database-instance">
          <title>Update a database instance</title>
          <para>You can change various characteristics of a database instance,
                such as its volume size and flavor.</para>
          <section xml:id="to-change-the-volume-size-of-an-instance">
            <title>To change the volume size of an instance</title>
            <procedure>
              <step>
                <para>Log in to the dashboard.</para>
              </step>
              <step>
                <para>From the CURRENT PROJECT on the <guimenu>Project</guimenu> tab, select the
                            appropriate project.</para>
              </step>
              <step>
                <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Database</guimenu> tab and
                            click <guimenu>Instances</guimenu> category. This displays the existing
                            instances in your system.</para>
              </step>
              <step>
                <para>Check the instance you want to work with.
                            In the <guimenu>Actions</guimenu> column, expand the drop down menu
                            and select <guimenu>Resize Volume</guimenu>.</para>
              </step>
              <step>
                <para>In the <guimenu>Resize Database Volume</guimenu> dialog box,
                            fill in the <guimenu>New Size</guimenu> field with an integer indicating
                            the new size you want for the instance. Express the size in GB, and
                            note that the new size must be larger than the current size.</para>
              </step>
              <step>
                <para>Click <guimenu>Resize Database Volume</guimenu>.</para>
              </step>
            </procedure>
          </section>
          <section xml:id="to-change-the-flavor-of-an-instance">
            <title>To change the flavor of an instance</title>
            <procedure>
              <step>
                <para>Log in to the dashboard.</para>
              </step>
              <step>
                <para>From the CURRENT PROJECT on the <guimenu>Project</guimenu> tab, select the
                            appropriate project.</para>
              </step>
              <step>
                <para>On the <guimenu>Project</guimenu> tab, open the <guimenu>Database</guimenu> tab and
                            click <guimenu>Instances</guimenu> category. This displays the existing
                            instances in your system.</para>
              </step>
              <step>
                <para>Check the instance you want to work with. In the
                            <guimenu>Actions</guimenu> column, expand the drop down menu and
                            select <guimenu>Resize Instance</guimenu>.</para>
              </step>
              <step>
                <para>In the <guimenu>Resize Database Instance</guimenu> dialog box,
                            expand the drop down menu in the <guimenu>New Flavor</guimenu> field.
                            Select the new flavor you want for the instance.</para>
              </step>
              <step>
                <para>Click <guimenu>Resize Database Instance</guimenu>.</para>
              </step>
            </procedure>
          </section>
        </section>
      </section>
      <section xml:id="view-and-manage-load-balancers-v2" xml:base="manage-lbaasv2">
        <title>View and manage load balancers v2</title>
        <para>Load-Balancer-as-a-Service (LBaaS) enables networking to distribute incoming
            requests evenly among designated instances. This distribution ensures that
            the workload is shared predictably among instances and enables more effective
            use of system resources. Use one of these load-balancing methods to distribute
            incoming requests:</para>
        <itemizedlist>
          <listitem>
            <para>Round robin: Rotates requests evenly between multiple instances.</para>
          </listitem>
          <listitem>
            <para>Source IP: Requests from a unique source IP address are consistently
                    directed to the same instance.</para>
          </listitem>
          <listitem>
            <para>Least connections: Allocates requests to the instance with the
                    least number of active connections.</para>
          </listitem>
        </itemizedlist>
        <para>As an end user, you can create and manage load balancers and related
            objects for users in various projects. You can also delete load balancers
            and related objects.</para>
        <para>LBaaS v2 has several new concepts to understand:</para>
        <variablelist>
          <varlistentry>
            <term>Load balancer</term>
            <listitem>
              <para>The load balancer occupies a neutron network port and
                        has an IP address assigned from a subnet.</para>
            </listitem>
          </varlistentry>
          <varlistentry>
            <term>Listener</term>
            <listitem>
              <para>Each port that listens for traffic on a particular load balancer is
                        configured separately and tied to the load balancer. Multiple listeners can
                        be associated with the same load balancer.</para>
            </listitem>
          </varlistentry>
          <varlistentry>
            <term>Pool</term>
            <listitem>
              <para>A pool is a group of hosts that sits behind the load balancer and
                        serves traffic through the load balancer.</para>
            </listitem>
          </varlistentry>
          <varlistentry>
            <term>Member</term>
            <listitem>
              <para>Members are the actual IP addresses that receive traffic from
                        the load balancer. Members are associated with pools.</para>
            </listitem>
          </varlistentry>
          <varlistentry>
            <term>Health monitor</term>
            <listitem>
              <para>Members may go offline from time to time and health monitors
                        diverts traffic away from members that are not responding properly.
                        Health monitors are associated with pools.</para>
            </listitem>
          </varlistentry>
        </variablelist>
        <section xml:id="view-existing-load-balancers">
          <title>View existing load balancers</title>
          <procedure>
            <step>
              <para>Log in to the OpenStack dashboard.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the
                        <guimenu>Network</guimenu> tab, and click the
                        <guimenu>Load Balancers</guimenu> category.</para>
              <para>This view shows the list of existing load balancers. To view details
                        of any of the load balancers, click on the specific load balancer.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="create-a-load-balancer">
          <title>Create a load balancer</title>
          <procedure>
            <step>
              <para>Log in to the OpenStack dashboard.</para>
            </step>
            <step>
              <para>On the <guimenu>Project</guimenu> tab, open the
                        <guimenu>Network</guimenu> tab, and click the
                        <guimenu>Load Balancers</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Create Load Balancer</guimenu> button.</para>
              <para>Use the concepts described in the overview section to fill in
                        the necessary information about the load balancer you want to create.</para>
              <para>Keep in mind, the health checks routinely run against each instance
                        within a target load balancer and the result of the health check is
                        used to determine if the instance receives new connections.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
        <section xml:id="delete-a-load-balancer">
          <title>Delete a load balancer</title>
          <procedure>
            <step>
              <para>Select the load balancer you want to delete
                        and click the <guimenu>Delete Load Balancer</guimenu> button.</para>
              <para>To be deleted successfully, a load balancer must not
                        have any listeners or pools associated with
                        it. The delete action is also available in the
                        <guimenu>Actions</guimenu> column for the individual load balancers.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="supported-browsers" xml:base="browser_support">
        <title>Supported Browsers</title>
        <para>Horizon is primarily tested and supported on the latest version of Firefox,
            the latest version of Chrome, and IE9+. Issues related to Safari and Opera will
            also be considered.</para>
        <para>This page aims to informally document what that means for different releases,
            everyone is warmly encouraged to update this page based on the versions they’ve
            tested with.</para>
        <para>Legend:</para>
        <itemizedlist>
          <listitem>
            <para>Very good: Very well tested, should work as expected</para>
          </listitem>
          <listitem>
            <para>Good: Moderately tested, should look nice and work fine, maybe a few visual
                    hiccups</para>
          </listitem>
          <listitem>
            <para>Poor: Doesn’t look good</para>
          </listitem>
          <listitem>
            <para>Broken: Essential functionality not working (link to bug in the notes)</para>
          </listitem>
          <listitem>
            <para>No: Not supported</para>
          </listitem>
        </itemizedlist>
        <section xml:id="kilo">
          <title>Kilo</title>
          <informaltable>
            <tgroup cols="3">
              <colspec colname="c0" colwidth="20"/>
              <colspec colname="c1" colwidth="20"/>
              <colspec colname="c2" colwidth="30"/>
              <thead>
                <row>
                  <entry/>
                  <entry>
                    <para>Status</para>
                  </entry>
                  <entry>
                    <para>Notes</para>
                  </entry>
                </row>
              </thead>
              <tbody>
                <row>
                  <entry>
                    <para>Firefox</para>
                  </entry>
                  <entry>
                    <para>Very good</para>
                  </entry>
                  <entry>
                    <para>31+. (Earlier versions?)</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Firefox ESR</para>
                  </entry>
                  <entry>
                    <para>Very good</para>
                  </entry>
                  <entry>
                    <para>31+</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Chrome</para>
                  </entry>
                  <entry>
                    <para>Very good</para>
                  </entry>
                  <entry>
                    <para>43.0.2357.81</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>IE 11</para>
                  </entry>
                  <entry>
                    <para>Good?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>IE 10</para>
                  </entry>
                  <entry>
                    <para>Good?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>IE 9</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>IE 8 and below</para>
                  </entry>
                  <entry>
                    <para>Not supported.</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>Safari</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>Opera</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
              </tbody>
            </tgroup>
          </informaltable>
        </section>
        <section xml:id="juno">
          <title>Juno</title>
          <informaltable>
            <tgroup cols="3">
              <colspec colname="c0" colwidth="20"/>
              <colspec colname="c1" colwidth="20"/>
              <colspec colname="c2" colwidth="30"/>
              <thead>
                <row>
                  <entry/>
                  <entry>
                    <para>Status</para>
                  </entry>
                  <entry>
                    <para>Notes</para>
                  </entry>
                </row>
              </thead>
              <tbody>
                <row>
                  <entry>
                    <para>Firefox</para>
                  </entry>
                  <entry>
                    <para>Very good</para>
                  </entry>
                  <entry>
                    <para>31+. (Earlier versions?)</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Firefox ESR</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>Chrome</para>
                  </entry>
                  <entry>
                    <para>Very good</para>
                  </entry>
                  <entry>
                    <para>Versions?</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>IE 11</para>
                  </entry>
                  <entry>
                    <para>Good?</para>
                  </entry>
                  <entry>
                    <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://bugs.launchpad.net/horizon/+bugs?field.tag=ie">Open IE Bugs</link>.</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>IE 10</para>
                  </entry>
                  <entry>
                    <para>Good?</para>
                  </entry>
                  <entry>
                    <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://bugs.launchpad.net/horizon/+bugs?field.tag=ie">Open IE Bugs</link>.</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>IE 9</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry>
                    <para><link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://bugs.launchpad.net/horizon/+bugs?field.tag=ie">Open IE Bugs</link>.</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>IE 8 and below</para>
                  </entry>
                  <entry>
                    <para>Not supported.</para>
                  </entry>
                  <entry>
                    <para>No.</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Safari</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>Opera</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
              </tbody>
            </tgroup>
          </informaltable>
        </section>
        <section xml:id="icehouse">
          <title>Icehouse</title>
          <informaltable>
            <tgroup cols="3">
              <colspec colname="c0" colwidth="20"/>
              <colspec colname="c1" colwidth="17"/>
              <colspec colname="c2" colwidth="38"/>
              <thead>
                <row>
                  <entry/>
                  <entry>
                    <para>Status</para>
                  </entry>
                  <entry>
                    <para>Notes</para>
                  </entry>
                </row>
              </thead>
              <tbody>
                <row>
                  <entry>
                    <para>Firefox</para>
                  </entry>
                  <entry>
                    <para>Very good</para>
                  </entry>
                  <entry>
                    <para>Versions?</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Firefox ESR</para>
                  </entry>
                  <entry>
                    <para>Very good</para>
                  </entry>
                  <entry>
                    <para>Windows 24.7.0 ESR</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Chrome</para>
                  </entry>
                  <entry>
                    <para>Very good</para>
                  </entry>
                  <entry>
                    <para>Windows Version 36, RHEL version 27.0</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Chromium</para>
                  </entry>
                  <entry>
                    <para>Very good</para>
                  </entry>
                  <entry>
                    <para>version 31.0</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>IE 11</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>IE 10</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>IE 9</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>IE 8 and below</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>Safari</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
                <row>
                  <entry>
                    <para>Opera</para>
                  </entry>
                  <entry>
                    <para>?</para>
                  </entry>
                  <entry/>
                </row>
              </tbody>
            </tgroup>
          </informaltable>
        </section>
      </section>
    </section>
    <section xml:id="administration-guide" xml:base="admin/index">
      <title>Administration Guide</title>
      <para>The OpenStack Dashboard is a web-based interface that allows you to
            manage OpenStack resources and services. The Dashboard allows you to
            interact with the OpenStack Compute cloud controller using the OpenStack
            APIs.<!-- FIXME For more information about installing and configuring the
            Dashboard, see the <xref linkend="../install/index"/> for your operating system.--></para>
      <section xml:id="customize-and-configure-the-dashboard" xml:base="customize-configure">
        <title>Customize and configure the Dashboard</title>
        <para>Once you have the Dashboard installed, you can customize the way
            it looks and feels to suit the needs of your environment, your
            project, or your business.</para>
        <para>You can also configure the Dashboard for a secure HTTPS deployment, or
            an HTTP deployment. The standard OpenStack installation uses a non-encrypted
            HTTP channel, but you can enable SSL support for the Dashboard.</para>
        <para>For information on configuring HTTPS or HTTP, see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#configure-dashboard">Configure the Dashboard</link>.</para>
        <section xml:id="customize-the-dashboard">
          <title>Customize the Dashboard</title>
          <para>The OpenStack Dashboard on Ubuntu installs the
                <literal>openstack-dashboard-ubuntu-theme</literal> package by default. If you do not
                want to use this theme, remove it and its dependencies:</para>
          <screen language="console"># apt-get remove --auto-remove openstack-dashboard-ubuntu-theme</screen>
          <note>
            <para>This guide focuses on the <literal>local_settings.py</literal> file.</para>
          </note>
          <para>The following Dashboard content can be customized to suit your needs:</para>
          <itemizedlist>
            <listitem>
              <para>Logo</para>
            </listitem>
            <listitem>
              <para>Site colors</para>
            </listitem>
            <listitem>
              <para>HTML title</para>
            </listitem>
            <listitem>
              <para>Logo link</para>
            </listitem>
            <listitem>
              <para>Help URL</para>
            </listitem>
          </itemizedlist>
          <section xml:id="logo-and-site-colors">
            <title>Logo and site colors</title>
            <procedure>
              <step>
                <para>Create two PNG logo files with transparent backgrounds using
                            the following sizes:</para>
                <itemizedlist>
                  <listitem>
                    <para>Login screen: 365 x 50</para>
                  </listitem>
                  <listitem>
                    <para>Logged in banner: 216 x 35</para>
                  </listitem>
                </itemizedlist>
              </step>
              <step>
                <para>Upload your new images to
                            <literal>/usr/share/openstack-dashboard/openstack_dashboard/static/dashboard/img/</literal>.</para>
              </step>
              <step>
                <para>Create a CSS style sheet in
                            <literal>/usr/share/openstack-dashboard/openstack_dashboard/static/dashboard/scss/</literal>.</para>
              </step>
              <step>
                <para>Change the colors and image file names as appropriate. Ensure the
                            relative directory paths are the same. The following example file
                            shows you how to customize your CSS file:</para>
                <screen language="css">/*
* New theme colors for dashboard that override the defaults:
*  dark blue: #355796 / rgb(53, 87, 150)
*  light blue: #BAD3E1 / rgb(186, 211, 225)
*
* By Preston Lee &lt;plee@tgen.org&gt;
*/
h1.brand {
background: #355796 repeat-x top left;
border-bottom: 2px solid #BAD3E1;
}
h1.brand a {
background: url(../img/my_cloud_logo_small.png) top left no-repeat;
}
#splash .login {
background: #355796 url(../img/my_cloud_logo_medium.png) no-repeat center 35px;
}
#splash .login .modal-header {
border-top: 1px solid #BAD3E1;
}
.btn-primary {
background-image: none !important;
background-color: #355796 !important;
border: none !important;
box-shadow: none;
}
.btn-primary:hover,
.btn-primary:active {
border: none;
box-shadow: none;
background-color: #BAD3E1 !important;
text-decoration: none;
}</screen>
              </step>
              <step>
                <para>Open the following HTML template in an editor of your choice:</para>
                <screen language="console">/usr/share/openstack-dashboard/openstack_dashboard/templates/_stylesheets.html</screen>
              </step>
              <step>
                <para>Add a line to include your newly created style sheet. For example,
                            <literal>custom.css</literal> file:</para>
                <screen language="html">&lt;link href='{{ STATIC_URL }}bootstrap/css/bootstrap.min.css' media='screen' rel='stylesheet' /&gt;
&lt;link href='{{ STATIC_URL }}dashboard/css/{% choose_css %}' media='screen' rel='stylesheet' /&gt;
&lt;link href='{{ STATIC_URL }}dashboard/css/custom.css' media='screen' rel='stylesheet' /&gt;</screen>
              </step>
              <step>
                <para>Restart the Apache service.</para>
              </step>
              <step>
                <para>To view your changes, reload your Dashboard. If necessary, go back
                            and modify your CSS file as appropriate.</para>
              </step>
            </procedure>
          </section>
          <section xml:id="html-title">
            <title>HTML title</title>
            <procedure>
              <step>
                <para>Set the HTML title, which appears at the top of the browser window, by
                            adding the following line to <literal>local_settings.py</literal>:</para>
                <screen language="python">SITE_BRANDING = "Example, Inc. Cloud"</screen>
              </step>
              <step>
                <para>Restart Apache for this change to take effect.</para>
              </step>
            </procedure>
          </section>
          <section xml:id="logo-link">
            <title>Logo link</title>
            <procedure>
              <step>
                <para>The logo also acts as a hyperlink. The default behavior is to redirect
                            to <literal>horizon:user_home</literal>. To change this, add the following attribute to
                            <literal>local_settings.py</literal>:</para>
                <screen language="python">SITE_BRANDING_LINK = "http://example.com"</screen>
              </step>
              <step>
                <para>Restart Apache for this change to take effect.</para>
              </step>
            </procedure>
          </section>
          <section xml:id="help-url-2">
            <title>Help URL</title>
            <procedure>
              <step>
                <para>By default, the help URL points to <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org"/>. To change
                            this, edit the following attribute in <literal>local_settings.py</literal>:</para>
                <screen language="python">HORIZON_CONFIG["help_url"] = "http://openstack.mycompany.org"</screen>
              </step>
              <step>
                <para>Restart Apache for this change to take effect.</para>
              </step>
            </procedure>
          </section>
        </section>
        <section xml:id="configure-dashboard">
          <title>Configure the Dashboard</title>
          <para>The following section on configuring the Dashboard for a
                secure HTTPS deployment, or a HTTP deployment, uses concrete
                examples to ensure the procedure is clear. The file path varies
                by distribution, however. If needed, you can also configure
                the VNC window size in the Dashboard.</para>
          <section xml:id="configure-the-dashboard-for-http">
            <title>Configure the Dashboard for HTTP</title>
            <para>You can configure the Dashboard for a simple HTTP deployment.
                    The standard installation uses a non-encrypted HTTP channel.</para>
            <procedure>
              <step>
                <para>Specify the host for your Identity service endpoint in the
                            <literal>local_settings.py</literal> file with the <literal>OPENSTACK_HOST</literal> setting.</para>
                <para>The following example shows this setting:</para>
                <screen language="python">import os

from django.utils.translation import ugettext_lazy as _

DEBUG = False
TEMPLATE_DEBUG = DEBUG
PROD = True
USE_SSL = False

SITE_BRANDING = 'OpenStack Dashboard'

# Ubuntu-specific: Enables an extra panel in the 'Settings' section
# that easily generates a Juju environments.yaml for download,
# preconfigured with endpoints and credentials required for bootstrap
# and service deployment.
ENABLE_JUJU_PANEL = True

# Note: You should change this value
SECRET_KEY = 'elj1IWiLoWHgryYxFT6j7cM5fGOOxWY0'

# Specify a regular expression to validate user passwords.
# HORIZON_CONFIG = {
#     "password_validator": {
#         "regex": '.*',
#         "help_text": _("Your password does not meet the requirements.")
#     }
# }

LOCAL_PATH = os.path.dirname(os.path.abspath(__file__))

CACHES = {
    'default': {
        'BACKEND' : 'django.core.cache.backends.memcached.MemcachedCache',
        'LOCATION' : '127.0.0.1:11211'
    }
}

# Send email to the console by default
EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'
# Or send them to /dev/null
#EMAIL_BACKEND = 'django.core.mail.backends.dummy.EmailBackend'

# Configure these for your outgoing email host
# EMAIL_HOST = 'smtp.my-company.com'
# EMAIL_PORT = 25
# EMAIL_HOST_USER = 'djangomail'
# EMAIL_HOST_PASSWORD = 'top-secret!'

# For multiple regions uncomment this configuration, and add (endpoint, title).
# AVAILABLE_REGIONS = [
#     ('http://cluster1.example.com:5000/v2.0', 'cluster1'),
#     ('http://cluster2.example.com:5000/v2.0', 'cluster2'),
# ]

OPENSTACK_HOST = "127.0.0.1"
OPENSTACK_KEYSTONE_URL = "http://%s:5000/v2.0" % OPENSTACK_HOST
OPENSTACK_KEYSTONE_DEFAULT_ROLE = "Member"

# The OPENSTACK_KEYSTONE_BACKEND settings can be used to identify the
# capabilities of the auth backend for Keystone.
# If Keystone has been configured to use LDAP as the auth backend then set
# can_edit_user to False and name to 'ldap'.
#
# TODO(tres): Remove these once Keystone has an API to identify auth backend.
OPENSTACK_KEYSTONE_BACKEND = {
    'name': 'native',
    'can_edit_user': True
}

# OPENSTACK_ENDPOINT_TYPE specifies the endpoint type to use for the endpoints
# in the Keystone service catalog. Use this setting when Horizon is running
# external to the OpenStack environment. The default is 'internalURL'.
#OPENSTACK_ENDPOINT_TYPE = "publicURL"

# The number of Swift containers and objects to display on a single page before
# providing a paging element (a "more" link) to paginate results.
API_RESULT_LIMIT = 1000

# If you have external monitoring links, eg:
# EXTERNAL_MONITORING = [
#     ['Nagios','http://foo.com'],
#     ['Ganglia','http://bar.com'],
# ]

LOGGING = {
        'version': 1,
        # When set to True this will disable all logging except
        # for loggers specified in this configuration dictionary. Note that
        # if nothing is specified here and disable_existing_loggers is True,
        # django.db.backends will still log unless it is disabled explicitly.
        'disable_existing_loggers': False,
        'handlers': {
            'null': {
                'level': 'DEBUG',
                'class': 'logging.NullHandler',
                },
            'console': {
                # Set the level to "DEBUG" for verbose output logging.
                'level': 'INFO',
                'class': 'logging.StreamHandler',
                },
            },
        'loggers': {
            # Logging from django.db.backends is VERY verbose, send to null
            # by default.
            'django.db.backends': {
                'handlers': ['null'],
                'propagate': False,
                },
            'horizon': {
                'handlers': ['console'],
                'propagate': False,
            },
            'novaclient': {
                'handlers': ['console'],
                'propagate': False,
            },
            'keystoneclient': {
                'handlers': ['console'],
                'propagate': False,
            },
            'nose.plugins.manager': {
                'handlers': ['console'],
                'propagate': False,
            }
        }
}</screen>
                <para>The service catalog configuration in the Identity service determines
                            whether a service appears in the Dashboard.
                            For the full listing, see <xref linkend="install-settings"/>.</para>
              </step>
              <step>
                <para>Restart the Apache HTTP Server.</para>
              </step>
              <step>
                <para>Restart <literal>memcached</literal>.</para>
              </step>
            </procedure>
          </section>
          <section xml:id="configure-the-dashboard-for-https">
            <title>Configure the Dashboard for HTTPS</title>
            <para>You can configure the Dashboard for a secured HTTPS deployment.
                    While the standard installation uses a non-encrypted HTTP channel,
                    you can enable SSL support for the Dashboard.</para>
            <para>This example uses the <literal>http://openstack.example.com</literal> domain.
                    Use a domain that fits your current setup.</para>
            <procedure>
              <step>
                <para>In the <literal>local_settings.py</literal> file, update the following options:</para>
                <screen language="python">USE_SSL = True
CSRF_COOKIE_SECURE = True
SESSION_COOKIE_SECURE = True
SESSION_COOKIE_HTTPONLY = True</screen>
                <para>To enable HTTPS, the <literal>USE_SSL = True</literal> option is required.</para>
                <para>The other options require that HTTPS is enabled;
                            these options defend against cross-site scripting.</para>
              </step>
              <step>
                <para>Edit the <literal>openstack-dashboard.conf</literal> file as shown in the
                            <emphasis role="bold">Example After</emphasis>:</para>
                <para>
                  <emphasis role="bold">Example Before</emphasis>
                </para>
                <screen language="apacheconf">WSGIScriptAlias / /usr/share/openstack-dashboard/openstack_dashboard/wsgi/django.wsgi
WSGIDaemonProcess horizon user=www-data group=www-data processes=3 threads=10
Alias /static /usr/share/openstack-dashboard/openstack_dashboard/static/
&lt;Directory /usr/share/openstack-dashboard/openstack_dashboard/wsgi&gt;
# For Apache http server 2.2 and earlier:
Order allow,deny
Allow from all

# For Apache http server 2.4 and later:
# Require all granted
&lt;/Directory&gt;</screen>
                <para>
                  <emphasis role="bold">Example After</emphasis>
                </para>
                <screen language="none">&lt;VirtualHost *:80&gt;
ServerName openstack.example.com
&lt;IfModule mod_rewrite.c&gt;
RewriteEngine On
RewriteCond %{HTTPS} off
RewriteRule (.*) https://%{HTTP_HOST}%{REQUEST_URI}
&lt;/IfModule&gt;
&lt;IfModule !mod_rewrite.c&gt;
RedirectPermanent / https://openstack.example.com
&lt;/IfModule&gt;
&lt;/VirtualHost&gt;
&lt;VirtualHost *:443&gt;
ServerName openstack.example.com

SSLEngine On
# Remember to replace certificates and keys with valid paths in your environment
SSLCertificateFile /etc/apache2/SSL/openstack.example.com.crt
SSLCACertificateFile /etc/apache2/SSL/openstack.example.com.crt
SSLCertificateKeyFile /etc/apache2/SSL/openstack.example.com.key
SetEnvIf User-Agent ".*MSIE.*" nokeepalive ssl-unclean-shutdown

# HTTP Strict Transport Security (HSTS) enforces that all communications
# with a server go over SSL. This mitigates the threat from attacks such
# as SSL-Strip which replaces links on the wire, stripping away https prefixes
# and potentially allowing an attacker to view confidential information on the
# wire
Header add Strict-Transport-Security "max-age=15768000"

WSGIScriptAlias / /usr/share/openstack-dashboard/openstack_dashboard/wsgi/django.wsgi
WSGIDaemonProcess horizon user=www-data group=www-data processes=3 threads=10
Alias /static /usr/share/openstack-dashboard/openstack_dashboard/static/
&lt;Directory /usr/share/openstack-dashboard/openstack_dashboard/wsgi&gt;
# For Apache http server 2.2 and earlier:
    &lt;ifVersion &lt;2.4&gt;
        Order allow,deny
        Allow from all
    &lt;/ifVersion&gt;
# For Apache http server 2.4 and later:
    &lt;ifVersion &gt;=2.4&gt;
#The following two lines have been added by bms for error "AH01630: client denied
#by server configuration:
#/usr/share/openstack-dashboard/openstack_dashboard/static/dashboard/cssa"
        Options All
        AllowOverride All
        Require all granted
    &lt;/ifVersion&gt;
&lt;/Directory&gt;
&lt;Directory /usr/share/openstack-dashboard/static&gt;
    &lt;ifVersion &gt;=2.4&gt;
        Options All
        AllowOverride All
        Require all granted
    &lt;/ifVersion&gt;
&lt;/Directory&gt;
&lt;/VirtualHost&gt;</screen>
                <para>In this configuration, the Apache HTTP Server listens on port 443 and
                            redirects all non-secure requests to the HTTPS protocol. The secured
                            section defines the private key, public key, and certificate to use.</para>
              </step>
              <step>
                <para>Restart the Apache HTTP Server.</para>
              </step>
              <step>
                <para>Restart <literal>memcached</literal>.</para>
                <para>If you try to access the Dashboard through HTTP, the browser redirects
                            you to the HTTPS page.</para>
                <note>
                  <para>Configuring the Dashboard for HTTPS also requires enabling SSL for
                                the noVNC proxy service. On the controller node, add the following
                                additional options to the <literal>[DEFAULT]</literal> section of the
                                <literal>/etc/nova/nova.conf</literal> file:</para>
                  <screen language="ini">[DEFAULT]
# ...
ssl_only = true
cert = /etc/apache2/SSL/openstack.example.com.crt
key = /etc/apache2/SSL/openstack.example.com.key</screen>
                  <para>On the compute nodes, ensure the <literal>nonvncproxy_base_url</literal> option
                                points to a URL with an HTTPS scheme:</para>
                  <screen language="ini">[DEFAULT]
# ...
novncproxy_base_url = https://controller:6080/vnc_auto.html</screen>
                </note>
              </step>
            </procedure>
          </section>
        </section>
      </section>
      <section xml:id="set-up-session-storage-for-the-dashboard" xml:base="sessions">
        <title>Set up session storage for the Dashboard</title>
        <para>The Dashboard uses <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/topics/http/sessions/">Django sessions
                framework</link>
            to handle user session data. However, you can use any available session
            back end. You customize the session back end through the
            <literal>SESSION_ENGINE</literal> setting in your <literal>local_settings.py</literal> file.</para>
        <para>After architecting and implementing the core OpenStack
            services and other required services, combined with the Dashboard
            service steps below, users and administrators can use
            the OpenStack dashboard.<!-- FIXME Refer to the
            <xref linkend="../user/index"/>
            chapter of the OpenStack End User Guide for
            further instructions on logging in to the Dashboard.--></para>
        <para>The following sections describe the pros and cons of each option as it
            pertains to deploying the Dashboard.</para>
        <section xml:id="local-memory-cache">
          <title>Local memory cache</title>
          <para>Local memory storage is the quickest and easiest session back end to set
                up, as it has no external dependencies whatsoever. It has the following
                significant drawbacks:</para>
          <itemizedlist>
            <listitem>
              <para>No shared storage across processes or workers.</para>
            </listitem>
            <listitem>
              <para>No persistence after a process terminates.</para>
            </listitem>
          </itemizedlist>
          <para>The local memory back end is enabled as the default for Horizon solely
                because it has no dependencies. It is not recommended for production
                use, or even for serious development work.</para>
          <screen language="python">SESSION_ENGINE = 'django.contrib.sessions.backends.cache'
CACHES = {
  'default' : {
    'BACKEND': 'django.core.cache.backends.locmem.LocMemCache'
  }
}</screen>
          <para>You can use applications such as <literal>Memcached</literal> or <literal>Redis</literal> for external
                caching. These applications offer persistence and shared storage and are
                useful for small-scale deployments and development.</para>
		<section xml:id="memcached-2">
		 <title>Memcached</title>
            <para>Memcached is a high-performance and distributed memory object caching
                    system providing in-memory key-value store for small chunks of arbitrary
                    data.</para>
            <para>Requirements:</para>
            <itemizedlist>
              <listitem>
                <para>Memcached service running and accessible.</para>
              </listitem>
              <listitem>
                <para>Python module <literal>python-memcached</literal> installed.</para>
              </listitem>
            </itemizedlist>
            <screen language="python">SESSION_ENGINE = 'django.contrib.sessions.backends.cache'
CACHES = {
  'default': {
    'BACKEND': 'django.core.cache.backends.memcached.MemcachedCache',
    'LOCATION': 'my_memcached_host:11211',
  }
}</screen>
          </section>
          <section xml:id="redis">
            <title>Redis</title>
            <para>Redis is an open source, BSD licensed, advanced key-value store. It is
                    often referred to as a data structure server.</para>
            <para>Requirements:</para>
            <itemizedlist>
              <listitem>
                <para>Redis service running and accessible.</para>
              </listitem>
              <listitem>
                <para>Python modules <literal>redis</literal> and <literal>django-redis</literal> installed.</para>
              </listitem>
            </itemizedlist>
            <screen language="python">SESSION_ENGINE = 'django.contrib.sessions.backends.cache'
CACHES = {
    "default": {
        "BACKEND": "redis_cache.cache.RedisCache",
        "LOCATION": "127.0.0.1:6379:1",
        "OPTIONS": {
            "CLIENT_CLASS": "redis_cache.client.DefaultClient",
        }
    }
}</screen>
          </section>
          <section xml:id="initialize-and-configure-the-database">
            <title>Initialize and configure the database</title>
            <para>Database-backed sessions are scalable, persistent, and can be made
                    high-concurrency and highly available.</para>
            <para>However, database-backed sessions are one of the slower session storages
                    and incur a high overhead under heavy usage. Proper configuration of
                    your database deployment can also be a substantial undertaking and is
                    far beyond the scope of this documentation.</para>
            <procedure>
              <step>
                <para>Start the MySQL command-line client.</para>
                <screen language="console">$ mysql -u root -p</screen>
              </step>
              <step>
                <para>Enter the MySQL root user’s password when prompted.</para>
              </step>
              <step>
                <para>To configure the MySQL database, create the dash database.</para>
                <screen language="console">mysql&gt; CREATE DATABASE dash;</screen>
              </step>
              <step>
                <para>Create a MySQL user for the newly created dash database that has full
                            control of the database. Replace DASH_DBPASS with a password for the
                            new user.</para>
                <screen language="console">mysql&gt; GRANT ALL PRIVILEGES ON dash.* TO 'dash'@'%' IDENTIFIED BY 'DASH_DBPASS';
mysql&gt; GRANT ALL PRIVILEGES ON dash.* TO 'dash'@'localhost' IDENTIFIED BY 'DASH_DBPASS';</screen>
              </step>
              <step>
                <para>Enter <literal>quit</literal> at the <literal>mysql&gt;</literal> prompt to exit MySQL.</para>
              </step>
              <step>
                <para>In the <literal>local_settings.py</literal> file, change these options:</para>
                <screen language="python">SESSION_ENGINE = 'django.contrib.sessions.backends.db'
DATABASES = {
    'default': {
        # Database configuration here
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'dash',
        'USER': 'dash',
        'PASSWORD': 'DASH_DBPASS',
        'HOST': 'localhost',
        'default-character-set': 'utf8'
    }
}</screen>
              </step>
              <step>
                <para>After configuring the <literal>local_settings.py</literal> file as shown, you can run the
                            <command>manage.py syncdb</command> command to populate this newly created
                            database.</para>
                <screen language="console"># /usr/share/openstack-dashboard/manage.py syncdb</screen>
              </step>
              <step>
                <para>The following output is returned:</para>
                <screen language="console">Installing custom SQL ...
Installing indexes ...
DEBUG:django.db.backends:(0.008) CREATE INDEX `django_session_c25c2c28` ON `django_session` (`expire_date`);; args=()
No fixtures found.</screen>
              </step>
              <step>
                <para>To avoid a warning when you restart Apache on Ubuntu, create a
                            <literal>blackhole</literal> directory in the Dashboard directory, as follows.</para>
                <screen language="console"># mkdir -p /var/lib/dash/.blackhole</screen>
              </step>
              <step>
                <para>Restart the Apache service.</para>
              </step>
              <step>
                <para>On Ubuntu, restart the <literal>nova-api</literal> service to ensure that the API server
                            can connect to the Dashboard without error.</para>
                <screen language="console"># service nova-api restart</screen>
              </step>
            </procedure>
          </section>
        </section>
        <section xml:id="cached-database-2">
          <title>Cached database</title>
          <para>To mitigate the performance issues of database queries, you can use the
                Django <literal>cached_db</literal> session back end, which utilizes both your database
                and caching infrastructure to perform write-through caching and
                efficient retrieval.</para>
          <para>Enable this hybrid setting by configuring both your database and cache,
                as discussed previously. Then, set the following value:</para>
          <screen language="python">SESSION_ENGINE = "django.contrib.sessions.backends.cached_db"</screen>
        </section>
        <section xml:id="cookies">
          <title>Cookies</title>
          <para>If you use Django 1.4 or later, the <literal>signed_cookies</literal> back end avoids
                server load and scaling problems.</para>
          <para>This back end stores session data in a cookie, which is stored by the
                user’s browser. The back end uses a cryptographic signing technique to
                ensure session data is not tampered with during transport. This is not
                the same as encryption; session data is still readable by an attacker.</para>
          <para>The pros of this engine are that it requires no additional dependencies
                or infrastructure overhead, and it scales indefinitely as long as the
                quantity of session data being stored fits into a normal cookie.</para>
          <para>The biggest downside is that it places session data into storage on the
                user’s machine and transports it over the wire. It also limits the
                quantity of session data that can be stored.</para>
          <para>See the Django <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.djangoproject.com/en/dev/topics/http/sessions/#using-cookie-based-sessions">cookie-based
                    sessions</link>
                documentation.</para>
        </section>
      </section>
      <section xml:id="create-and-manage-images" xml:base="manage-images">
        <title>Create and manage images</title>
        <para>As an administrative user, you can create and manage images
            for the projects to which you belong. You can also create
            and manage images for users in all projects to which you have
            access.</para>
        <!-- FIXME <para>To create and manage images in specified projects as an end
            user, see the <xref linkend="../user/manage-images"/>
            and <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/glance/latest/admin/manage-images.html">manage images with CLI in OpenStack End User Guide</link>.</para>-->
        <para>To create and manage images as an administrator for other
            users, use the following procedures.</para>
        <section xml:id="create-images">
          <title>Create images</title>
          <para>For details about image creation, see the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/image-guide/">Virtual Machine Image
                    Guide</link>.</para>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Images</guimenu> category. The images that you
                        can administer for cloud users appear on this page.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Image</guimenu>, which opens the
                        <guimenu>Create An Image</guimenu> window.</para>
              <figure>
                <title>Figure Dashboard — Create Image</title>
                <mediaobject>
                  <imageobject role="fo">
                    <imagedata fileref="create_image.png"/>
                  </imageobject>
                  <imageobject role="html">
                    <imagedata fileref="create_image.png"/>
                  </imageobject>
                </mediaobject>
              </figure>
            </step>
            <step>
              <para>In the <guimenu>Create An Image</guimenu> window, enter or select the
                        following values:</para>
              <informaltable>
                <tgroup cols="2">
                  <colspec colname="c0" colwidth="31"/>
                  <colspec colname="c1" colwidth="33"/>
                  <tbody>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Name</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter a name for the image.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Description</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter a brief description of
                                            the image.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Image Source</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Choose the image source from
                                            the dropdown list. Your choices
                                            are <guimenu>Image Location</guimenu>
                                            and <guimenu>Image File</guimenu>.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para><guimenu>Image File</guimenu> or
                                            <guimenu>Image Location</guimenu></para>
                      </entry>
                      <entry>
                        <para>Based on your selection, there
                                            is an <guimenu>Image File</guimenu> or
                                            <guimenu>Image Location</guimenu>
                                            field. You can include the
                                            location URL or browse for the
                                            image file on your file system
                                            and add it.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Format</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Select the image format.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Architecture</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify the architecture. For
                                            example, <literal>i386</literal> for a 32-bit
                                            architecture or <literal>x86_64</literal> for
                                            a 64-bit architecture.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Minimum Disk (GB)</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Leave this field empty.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Minimum RAM (MB)</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Leave this field empty.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Copy Data</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Specify this option to copy
                                            image data to the Image service.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Public</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Select this option to make the
                                            image public to all users.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <guimenu>Protected</guimenu>
                        </para>
                      </entry>
                      <entry>
                        <para>Select this option to ensure
                                            that only users with
                                            permissions can delete it.</para>
                      </entry>
                    </row>
                  </tbody>
                </tgroup>
              </informaltable>
            </step>
            <step>
              <para>Click <guimenu>Create Image</guimenu>.</para>
              <para>The image is queued to be uploaded. It might take several minutes
                        before the status changes from <literal>Queued</literal> to <literal>Active</literal>.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="update-images">
          <title>Update images</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project from the
                        drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Images</guimenu> category.</para>
            </step>
            <step>
              <para>Select the images that you want to edit. Click <guimenu>Edit Image</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Edit Image</guimenu> window, you can change the image name.</para>
              <para>Select the <guimenu>Public</guimenu> check box to make the image public.
                        Clear this check box to make the image private. You cannot change
                        the <guimenu>Kernel ID</guimenu>, <guimenu>Ramdisk ID</guimenu>, or
                        <guimenu>Architecture</guimenu> attributes for an image.</para>
            </step>
            <step>
              <para>Click <guimenu>Edit Image</guimenu>.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="delete-images">
          <title>Delete images</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project from the
                        drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin tab</guimenu>, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Images</guimenu> category.</para>
            </step>
            <step>
              <para>Select the images that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Images</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Images</guimenu> window, click <guimenu>Delete
                            Images</guimenu> to confirm the deletion.</para>
              <para>You cannot undo this action.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="create-and-manage-roles" xml:base="admin-manage-roles">
        <title>Create and manage roles</title>
        <para>A role is a personality that a user assumes to perform a specific set
            of operations. A role includes a set of rights and privileges. A user
            assumes that role inherits those rights and privileges.</para>
        <note>
          <para>OpenStack Identity service defines a user’s role on a
                project, but it is completely up to the individual service
                to define what that role means. This is referred to as the
                service’s policy. To get details about what the privileges
                for each role are, refer to the <literal>policy.json</literal> file
                available for each service in the
                <literal>/etc/SERVICE/policy.json</literal> file. For example, the
                policy defined for OpenStack Identity service is defined
                in the <literal>/etc/keystone/policy.json</literal> file.</para>
        </note>
        <section xml:id="create-a-role">
          <title>Create a role</title>
          <procedure>
            <step>
              <para>Log in to the dashboard and select the <guimenu>admin</guimenu> project from the
                        drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Identity</guimenu> tab, click the <guimenu>Roles</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Create Role</guimenu> button.</para>
              <para>In the <guimenu>Create Role</guimenu> window, enter a name for the role.</para>
            </step>
            <step>
              <para>Click the <guimenu>Create Role</guimenu> button to confirm your changes.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="edit-a-role">
          <title>Edit a role</title>
          <procedure>
            <step>
              <para>Log in to the dashboard and select the <guimenu>Identity</guimenu> project from the
                        drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Identity</guimenu> tab, click the <guimenu>Roles</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Edit</guimenu> button.</para>
              <para>In the <guimenu>Update Role</guimenu> window, enter a new name for the role.</para>
            </step>
            <step>
              <para>Click the <guimenu>Update Role</guimenu> button to confirm your changes.</para>
            </step>
          </procedure>
          <note>
            <para>Using the dashboard, you can edit only the name assigned to
                    a role.</para>
          </note>
        </section>
        <section xml:id="delete-a-role">
          <title>Delete a role</title>
          <procedure>
            <step>
              <para>Log in to the dashboard and select the <guimenu>Identity</guimenu> project from the
                        drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Identity</guimenu> tab, click the <guimenu>Roles</guimenu> category.</para>
            </step>
            <step>
              <para>Select the role you want to delete and click the <guimenu>Delete
                            Roles</guimenu> button.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Roles</guimenu> window, click <guimenu>Delete
                            Roles</guimenu> to confirm the deletion.</para>
              <para>You cannot undo this action.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="manage-projects-and-users" xml:base="manage-projects-and-users">
        <title>Manage projects and users</title>
        <para>OpenStack administrators can create projects, and create accounts for new users
            using the OpenStack Dasboard. Projects own specific resources in your
            OpenStack environment. You can associate users with roles, projects, or both.</para>
        <section xml:id="add-a-new-project">
          <title>Add a new project</title>
          <procedure>
            <step>
              <para>Log into the OpenStack Dashboard as the Admin user.</para>
            </step>
            <step>
              <para>Click on the <guimenu>Identity</guimenu> label on the left column, and click
                        <guimenu>Projects</guimenu>.</para>
            </step>
            <step>
              <para>Select the <guimenu>Create Project</guimenu> push button.
                        The <guimenu>Create Project</guimenu> window will open.</para>
            </step>
            <step>
              <para>Enter the Project name and description. Leave the <guimenu>Domain ID</guimenu>
                        field set at <emphasis>default</emphasis>.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Project</guimenu>.</para>
            </step>
          </procedure>
          <note>
            <para>Your new project will appear in the list of projects displayed under the
                    <guimenu>Projects</guimenu> page of the dashboard. Projects are listed in
                    alphabetical order, and you can check on the <emphasis role="bold">Project ID</emphasis>, <emphasis role="bold">Domain
                        name</emphasis>, and status of the project in this section.</para>
          </note>
        </section>
        <section xml:id="delete-a-project">
          <title>Delete a project</title>
          <procedure>
            <step>
              <para>Log into the OpenStack Dashboard as the Admin user.</para>
            </step>
            <step>
              <para>Click on the <guimenu>Identity</guimenu> label on the left column, and click
                        <guimenu>Projects</guimenu>.</para>
            </step>
            <step>
              <para>Select the checkbox to the left of the project you would like to delete.</para>
            </step>
            <step>
              <para>Click on the <guimenu>Delete Projects</guimenu> push button.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="update-a-project">
          <title>Update a project</title>
          <procedure>
            <step>
              <para>Log into the OpenStack Dashboard as the Admin user.</para>
            </step>
            <step>
              <para>Click on the <guimenu>Identity</guimenu> label on the left column, and click
                        <guimenu>Projects</guimenu>.</para>
            </step>
            <step>
              <para>Locate the project you wish to update, and under the <guimenu>Actions</guimenu>
                        column click on the drop down arrow next to the <guimenu>Manage Members</guimenu>
                        push button. The <guimenu>Update Project</guimenu> window will open.</para>
            </step>
            <step>
              <para>Update the name of the project, enable the project, or disable the project
                        as needed.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="add-a-new-user">
          <title>Add a new user</title>
          <procedure>
            <step>
              <para>Log into the OpenStack Dashboard as the Admin user.</para>
            </step>
            <step>
              <para>Click on the <guimenu>Identity</guimenu> label on the left column, and click
                        <guimenu>Users</guimenu>.</para>
            </step>
            <step>
              <para>Click <guimenu>Create User</guimenu>.</para>
            </step>
            <step>
              <para>Enter a <guimenu>Domain Name</guimenu>, the <guimenu>Username</guimenu>, and a
                        <guimenu>password</guimenu> for the new user. Enter an email for the new user,
                        and specify which <guimenu>Primary Project</guimenu> they belong to. Leave the
                        <guimenu>Domain ID</guimenu> field set at <emphasis>default</emphasis>. You can also enter a
                        decription for the new user.</para>
            </step>
            <step>
              <para>Click the <guimenu>Create User</guimenu> push button.</para>
            </step>
          </procedure>
          <note>
            <para>The new user will then appear in the list of projects displayed under
                    the <guimenu>Users</guimenu> page of the dashboard. You can check on the
                    <emphasis role="bold">User Name</emphasis>, <emphasis role="bold">User ID</emphasis>, <emphasis role="bold">Domain name</emphasis>, and the User status in this
                    section.</para>
          </note>
        </section>
        <section xml:id="delete-a-new-user">
          <title>Delete a new user</title>
          <procedure>
            <step>
              <para>Log into the OpenStack Dashboard as the Admin user.</para>
            </step>
            <step>
              <para>Click on the <guimenu>Identity</guimenu> label on the left column, and click
                        <guimenu>Users</guimenu>.</para>
            </step>
            <step>
              <para>Select the checkbox to the left of the user you would like to delete.</para>
            </step>
            <step>
              <para>Click on the <guimenu>Delete Users</guimenu> push button.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="update-a-user">
          <title>Update a user</title>
          <procedure>
            <step>
              <para>Log into the OpenStack Dashboard as the Admin user.</para>
            </step>
            <step>
              <para>Click on the <guimenu>Identity</guimenu> label on the left column, and click
                        <guimenu>Users</guimenu>.</para>
            </step>
            <step>
              <para>Locate the User you would like to update, and select the <guimenu>Edit</guimenu>
                        push button under the <guimenu>Actions</guimenu> column.</para>
            </step>
            <step>
              <para>Adjust the <guimenu>Domain Name</guimenu>, <guimenu>User Name</guimenu>,
                        <guimenu>Description</guimenu>, <guimenu>Email</guimenu>, and <guimenu>Primary Project</guimenu>.</para>
            </step>
          </procedure>
          <section xml:id="enable-or-disable-a-user">
            <title>Enable or disable a user</title>
            <procedure>
              <step>
                <para>Log into the OpenStack Dashboard as the Admin user.</para>
              </step>
              <step>
                <para>Click on the <guimenu>Identity</guimenu> label on the left column, and click
                            <guimenu>Users</guimenu>.</para>
              </step>
              <step>
                <para>Locate the User you would like to update, and select the arrow to the right
                            of the <guimenu>Edit</guimenu> push button. This will open a drop down menu.</para>
              </step>
              <step>
                <para>Select <guimenu>Disable User</guimenu>.</para>
              </step>
            </procedure>
            <note>
              <para>To reactivate a disabled user, select <guimenu>Enable User</guimenu> under
                        the drop down menu.</para>
            </note>
          </section>
        </section>
      </section>
      <section xml:id="manage-instances" xml:base="manage-instances">
        <title>Manage instances</title>
        <para>As an administrative user, you can manage instances for users in various
            projects. You can view, terminate, edit, perform a soft or hard reboot,
            create a snapshot from, and migrate instances. You can also view the
            logs for instances or launch a VNC console for an instance.</para>
        <!-- FIXME <para>For information about using the Dashboard to launch instances as an end
            user, see the <xref linkend="../user/launch-instances"/>.</para>-->
        <section xml:id="create-instance-snapshots">
          <title>Create instance snapshots</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project from the
                        drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Instances</guimenu> category.</para>
            </step>
            <step>
              <para>Select an instance to create a snapshot from it. From the
                        Actions drop-down list, select <guimenu>Create Snapshot</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Create Snapshot</guimenu> window, enter a name for the snapshot.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Snapshot</guimenu>. The Dashboard shows the instance snapshot
                        in the <guimenu>Images</guimenu> category.</para>
            </step>
            <step>
              <para>To launch an instance from the snapshot, select the snapshot and
                        click <guimenu>Launch</guimenu>.<!-- FIXME For information about launching
                        instances, see the
                        <xref linkend="../user/launch-instances"/>.--></para>
            </step>
          </procedure>
        </section>
        <section xml:id="control-the-state-of-an-instance">
          <title>Control the state of an instance</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project from the
                        drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Instances</guimenu> category.</para>
            </step>
            <step>
              <para>Select the instance for which you want to change the state.</para>
            </step>
            <step>
              <para>From the drop-down list in the Actions column,
                        select the state.</para>
              <para>Depending on the current state of the instance, you can perform various
                        actions on the instance. For example, pause, un-pause, suspend, resume,
                        soft or hard reboot, or terminate (actions in red are dangerous).</para>
            </step>
          </procedure>
          <figure>
            <title>Figure Dashboard — Instance Actions</title>
            <mediaobject>
              <imageobject role="fo">
                <imagedata fileref="change_instance_state.png" width="100%"/>
              </imageobject>
              <imageobject role="html">
                <imagedata fileref="change_instance_state.png" width="100%"/>
              </imageobject>
            </mediaobject>
          </figure>
        </section>
        <section xml:id="track-usage">
          <title>Track usage</title>
          <para>Use the <guimenu>Overview</guimenu> category to track usage of instances
                for each project.</para>
          <para>You can track costs per month by showing meters like number of VCPUs,
                disks, RAM, and uptime of all your instances.</para>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project from the
                        drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Overview</guimenu> category.</para>
            </step>
            <step>
              <para>Select a month and click <guimenu>Submit</guimenu> to query the instance usage for
                        that month.</para>
            </step>
            <step>
              <para>Click <guimenu>Download CSV Summary</guimenu> to download a CSV summary.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="manage-flavors" xml:base="manage-flavors">
        <title>Manage flavors</title>
        <para>In OpenStack, a flavor defines the compute, memory, and storage
            capacity of a virtual server, also known as an instance. As an
            administrative user, you can create, edit, and delete flavors.</para>
        <para>As of Newton, there are no default flavors.  The following table
            lists the default flavors for Mitaka and earlier.</para>
        <informaltable>
          <tgroup cols="4">
            <colspec colname="c0" colwidth="12"/>
            <colspec colname="c1" colwidth="9"/>
            <colspec colname="c2" colwidth="15"/>
            <colspec colname="c3" colwidth="13"/>
            <thead>
              <row>
                <entry>
                  <para>Flavor</para>
                </entry>
                <entry>
                  <para>VCPUs</para>
                </entry>
                <entry>
                  <para>Disk (in GB)</para>
                </entry>
                <entry>
                  <para>RAM (in MB)</para>
                </entry>
              </row>
            </thead>
            <tbody>
              <row>
                <entry>
                  <para>m1.tiny</para>
                </entry>
                <entry>
                  <para>1</para>
                </entry>
                <entry>
                  <para>1</para>
                </entry>
                <entry>
                  <para>512</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>m1.small</para>
                </entry>
                <entry>
                  <para>1</para>
                </entry>
                <entry>
                  <para>20</para>
                </entry>
                <entry>
                  <para>2048</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>m1.medium</para>
                </entry>
                <entry>
                  <para>2</para>
                </entry>
                <entry>
                  <para>40</para>
                </entry>
                <entry>
                  <para>4096</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>m1.large</para>
                </entry>
                <entry>
                  <para>4</para>
                </entry>
                <entry>
                  <para>80</para>
                </entry>
                <entry>
                  <para>8192</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>m1.xlarge</para>
                </entry>
                <entry>
                  <para>8</para>
                </entry>
                <entry>
                  <para>160</para>
                </entry>
                <entry>
                  <para>16384</para>
                </entry>
              </row>
            </tbody>
          </tgroup>
        </informaltable>
        <section xml:id="create-flavors">
          <title>Create flavors</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>In the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu>
                        tab and click the <guimenu>Flavors</guimenu> category.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Flavor</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Create Flavor</guimenu> window, enter or select the
                        parameters for the flavor in the <guimenu>Flavor Information</guimenu> tab.</para>
              <figure>
                <title>Dashboard — Create Flavor</title>
                <mediaobject>
                  <imageobject role="fo">
                    <imagedata fileref="create_flavor.png"/>
                  </imageobject>
                  <imageobject role="html">
                    <imagedata fileref="create_flavor.png"/>
                  </imageobject>
                </mediaobject>
              </figure>
              <informaltable>
                <tgroup cols="2">
                  <colspec colname="c0" colwidth="25"/>
                  <colspec colname="c1" colwidth="39"/>
                  <tbody>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">Name</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter the flavor name.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">ID</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>Unique ID (integer or UUID) for the
                                            new flavor. If specifying ‘auto’, a
                                            UUID will be automatically generated.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">VCPUs</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter the number of virtual CPUs to
                                            use.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">RAM (MB)</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter the amount of RAM to use, in
                                            megabytes.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">Root Disk (GB)</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter the amount of disk space in
                                            gigabytes to use for the root (/)
                                            partition.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">Ephemeral Disk (GB)</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter the amount of disk space in
                                            gigabytes to use for the ephemeral
                                            partition. If unspecified, the value
                                            is 0 by default.</para>
                        <para>Ephemeral disks offer machine local
                                            disk storage linked to the lifecycle
                                            of a VM instance. When a VM is
                                            terminated, all data on the ephemeral
                                            disk is lost. Ephemeral disks are not
                                            included in any snapshots.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">Swap Disk (MB)</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>Enter the amount of swap space (in
                                            megabytes) to use. If unspecified,
                                            the default is 0.</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">RX/TX Factor</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>Optional property allows servers with
                                            a different bandwidth to be created
                                            with the RX/TX Factor. The default
                                            value is 1. That is, the new bandwidth
                                            is the same as that of the attached
                                            network.</para>
                      </entry>
                    </row>
                  </tbody>
                </tgroup>
              </informaltable>
            </step>
            <step>
              <para>In the <guimenu>Flavor Access</guimenu> tab, you can control access to
                        the flavor by moving projects from the <guimenu>All Projects</guimenu>
                        column to the <guimenu>Selected Projects</guimenu> column.</para>
              <para>Only projects in the <guimenu>Selected Projects</guimenu> column can
                        use the flavor. If there are no projects in the right column,
                        all projects can use the flavor.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Flavor</guimenu>.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="update-flavors">
          <title>Update flavors</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>In the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Flavors</guimenu> category.</para>
            </step>
            <step>
              <para>Select the flavor that you want to edit. Click <guimenu>Edit
                            Flavor</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Edit Flavor</guimenu> window, you can change the flavor
                        name, VCPUs, RAM, root disk, ephemeral disk, and swap disk values.</para>
            </step>
            <step>
              <para>Click <guimenu>Save</guimenu>.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="update-metadata">
          <title>Update Metadata</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>In the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Flavors</guimenu> category.</para>
            </step>
            <step>
              <para>Select the flavor that you want to update. In the drop-down
                        list, click <guimenu>Update Metadata</guimenu> or click <guimenu>No</guimenu> or
                        <guimenu>Yes</guimenu> in the <guimenu>Metadata</guimenu> column.</para>
            </step>
            <step>
              <para>In the <guimenu>Update Flavor Metadata</guimenu> window, you can customize
                        some metadata keys, then add it to this flavor and set them values.</para>
            </step>
            <step>
              <para>Click <guimenu>Save</guimenu>.</para>
              <para>
                <emphasis role="bold">Optional metadata keys</emphasis>
              </para>
              <informaltable>
                <tgroup cols="2">
                  <colspec colname="c0" colwidth="31"/>
                  <colspec colname="c1" colwidth="31"/>
                  <tbody>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">CPU limits</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>quota:cpu_shares</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:cpu_period</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:cpu_limit</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:cpu_reservation</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:cpu_quota</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">Disk tuning</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>quota:disk_read_bytes_sec</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:disk_read_iops_sec</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:disk_write_bytes_sec</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:disk_write_iops_sec</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:disk_total_bytes_sec</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:disk_total_iops_sec</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">Bandwidth I/O</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>quota:vif_inbound_average</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:vif_inbound_burst</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:vif_inbound_peak</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:vif_outbound_average</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:vif_outbound_burst</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>quota:vif_outbound_peak</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">Watchdog behavior</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>hw:watchdog_action</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>
                          <emphasis role="bold">Random-number generator</emphasis>
                        </para>
                      </entry>
                      <entry>
                        <para>hw_rng:allowed</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>hw_rng:rate_bytes</para>
                      </entry>
                    </row>
                    <row>
                      <entry>
                        <para>hw_rng:rate_period</para>
                      </entry>
                    </row>
                  </tbody>
                </tgroup>
              </informaltable>
              <para>For information about supporting metadata keys, see the
                        the Compute service documentation.</para>
            </step>
          </procedure>
        </section>
        <section xml:id="delete-flavors">
          <title>Delete flavors</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>In the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Flavors</guimenu> category.</para>
            </step>
            <step>
              <para>Select the flavors that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Flavors</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Flavors</guimenu> window, click
                        <guimenu>Delete Flavors</guimenu> to confirm the deletion. You cannot
                        undo this action.</para>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="manage-volumes-and-volume-types" xml:base="manage-volumes">
        <title>Manage volumes and volume types</title>
        <para>Volumes are the Block Storage devices that you attach to instances to enable
            persistent storage. Users can attach a volume to a running instance or detach
            a volume and attach it to another instance at any time.<!-- FIXME For information about
            using the dashboard to create and manage volumes as an end user, see the
            <xref linkend="../user/manage-volumes"/>.--></para>
        <para>As an administrative user, you can manage volumes and volume types for users
            in various projects. You can create and delete volume types, and you can view
            and delete volumes. Note that a volume can be encrypted by using the steps
            outlined below.</para>
        <section xml:id="create-a-volume-type">
          <title>Create a volume type</title>
          <procedure>
            <step>
              <para>Log in to the dashboard and select the <guimenu>admin</guimenu>
                        project from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Volumes</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Volume Types</guimenu> tab, and click
                        <guimenu>Create Volume Type</guimenu> button. In the
                        <guimenu>Create Volume Type</guimenu> window, enter a name for the volume type.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Volume Type</guimenu> button to confirm your changes.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
        <section xml:id="create-an-encrypted-volume-type">
          <title>Create an encrypted volume type</title>
          <procedure>
            <step>
              <para>Create a volume type using the steps above for <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="#create-a-volume-type">Create a volume type</link>.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Encryption</guimenu> in the Actions column of the newly
                        created volume type.</para>
            </step>
            <step>
              <para>Configure the encrypted volume by setting the parameters below from
                        available options (see table):</para>
              <variablelist>
                <varlistentry>
                  <term>Provider</term>
                  <listitem>
                    <para>Specifies the class responsible for configuring the encryption.</para>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>Control Location</term>
                  <listitem>
                    <para>Specifies whether the encryption is from the front end (nova) or the
                                    back end (cinder).</para>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>Cipher</term>
                  <listitem>
                    <para>Specifies the encryption algorithm.</para>
                  </listitem>
                </varlistentry>
                <varlistentry>
                  <term>Key Size (bits)</term>
                  <listitem>
                    <para>Specifies the encryption key size.</para>
                  </listitem>
                </varlistentry>
              </variablelist>
            </step>
            <step>
              <para>Click <guimenu>Create Volume Type Encryption</guimenu>.</para>
            </step>
          </procedure>
          <figure>
            <title>Encryption Options</title>
            <mediaobject>
              <imageobject role="fo">
                <imagedata fileref="create_volume_type_encryption.png"/>
              </imageobject>
              <imageobject role="html">
                <imagedata fileref="create_volume_type_encryption.png"/>
              </imageobject>
            </mediaobject>
          </figure>
          <para>The table below provides a few alternatives available for creating encrypted
                volumes.</para>
          <informaltable>
            <tgroup cols="3">
              <colspec colname="c0" colwidth="20"/>
              <colspec colname="c1" colwidth="23"/>
              <colspec colname="c2" colwidth="28"/>
              <thead>
                <row>
                  <entry>
                    <para>Encryption
                                    parameters</para>
                  </entry>
                  <entry>
                    <para>Parameter
                                    options</para>
                  </entry>
                  <entry>
                    <para>Comments</para>
                  </entry>
                </row>
              </thead>
              <tbody>
                <row>
                  <entry>
                    <para>Provider</para>
                  </entry>
                  <entry>
                    <para>nova.volume.encryptors.
                                    luks.LuksEncryptor
                                    (Recommended)</para>
                  </entry>
                  <entry>
                    <para>Allows easier import and
                                    migration of imported
                                    encrypted volumes, and
                                    allows access key to be
                                    changed without
                                    re-encrypting the volume</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>nova.volume.encryptors.
                                    cryptsetup.
                                    CryptsetupEncryptor</para>
                  </entry>
                  <entry>
                    <para>Less disk overhead than
                                    LUKS</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Control Location</para>
                  </entry>
                  <entry>
                    <para>front-end
                                    (Recommended)</para>
                  </entry>
                  <entry>
                    <para>The encryption occurs within
                                    nova so that the data
                                    transmitted over the network
                                    is encrypted</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>back-end</para>
                  </entry>
                  <entry>
                    <para>This could be selected if a
                                    cinder plug-in supporting
                                    an encrypted back-end block
                                    storage device becomes
                                    available in the future.
                                    TLS or other network
                                    encryption would also be
                                    needed to protect data as it
                                    traverses the network</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Cipher</para>
                  </entry>
                  <entry>
                    <para>aes-xts-plain64
                                    (Recommended)</para>
                  </entry>
                  <entry>
                    <para>See NIST reference below
                                    to see advantages*</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>aes-cbc-essiv</para>
                  </entry>
                  <entry>
                    <para>Note: On the command line,
                                    type ‘cryptsetup benchmark’
                                    for additional options</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>Key Size (bits)</para>
                  </entry>
                  <entry>
                    <para>512 (Recommended for
                                    aes-xts-plain64. 256
                                    should be used for
                                    aes-cbc-essiv)</para>
                  </entry>
                  <entry>
                    <para>Using this selection for
                                    aes-xts, the underlying key
                                    size would only be 256-bits*</para>
                  </entry>
                </row>
                <row>
                  <entry>
                    <para>256</para>
                  </entry>
                  <entry>
                    <para>Using this selection for
                                    aes-xts, the underlying key
                                    size would only be 128-bits*</para>
                  </entry>
                </row>
              </tbody>
            </tgroup>
          </informaltable>
          <para><literal>*</literal> Source <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="http://nvlpubs.nist.gov/nistpubs/Legacy/SP/nistspecialpublication800-38e.pdf">NIST SP 800-38E</link></para>
          <note>
            <para>To see further information and CLI instructions, see
                        <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/cinder/latest/configuration/block-storage/volume-encryption.html">Create an encrypted volume type</link>
                        in the OpenStack Configuration Reference.</para>
          </note>
        </section>
        <section xml:id="delete-volume-types">
          <title>Delete volume types</title>
          <para>When you delete a volume type, volumes of that type are not deleted.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard and select the <guimenu>admin</guimenu> project from
                        the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Volumes</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Volume Types</guimenu> tab, select the volume type
                        or types that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Volume Types</guimenu> button.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Volume Types</guimenu> window, click the
                        <guimenu>Delete Volume Types</guimenu> button to confirm the action.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
        <section xml:id="delete-volumes">
          <title>Delete volumes</title>
          <para>When you delete an instance, the data of its attached volumes is not
                destroyed.</para>
          <procedure>
            <step>
              <para>Log in to the dashboard and select the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Volumes</guimenu> category.</para>
            </step>
            <step>
              <para>Select the volume or volumes that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Volumes</guimenu> button.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Volumes</guimenu> window, click the
                        <guimenu>Delete Volumes</guimenu> button to confirm the action.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
      </section>
      <section xml:id="manage-shares-and-share-types" xml:base="manage-shares">
        <title>Manage shares and share types</title>
        <para>Shares are file storage that instances can access. Users can
            allow or deny a running instance to have access to a share at any time.<!-- FIXME
            For information about using the Dashboard to create and manage shares as
            an end user, see the
            <xref linkend="../user/manage-shares"/>.--></para>
        <para>As an administrative user, you can manage shares and share types for users
            in various projects. You can create and delete share types, and view
            or delete shares.</para>
        <section xml:id="create-a-share-type">
          <title>Create a share type</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and choose the <guimenu>admin</guimenu>
                        project from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Shares</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Share Types</guimenu> tab, and click
                        <guimenu>Create Share Type</guimenu> button. In the
                        <guimenu>Create Share Type</guimenu> window, enter or select the
                        following values.</para>
              <para><guimenu>Name</guimenu>: Enter a name for the share type.</para>
              <para><guimenu>Driver handles share servers</guimenu>: Choose True or False</para>
              <para><guimenu>Extra specs</guimenu>: To add extra specs, use key=value.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Share Type</guimenu> button to confirm your changes.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
        <section xml:id="update-share-type">
          <title>Update share type</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and choose the <guimenu>admin</guimenu> project from
                        the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Shares</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Share Types</guimenu> tab, select the share type
                        that you want to update.</para>
            </step>
            <step>
              <para>Select <guimenu>Update Share Type</guimenu> from Actions.</para>
            </step>
            <step>
              <para>In the <guimenu>Update Share Type</guimenu> window, update extra specs.</para>
              <para><guimenu>Extra specs</guimenu>: To add extra specs, use key=value.
                        To unset extra specs, use key.</para>
            </step>
            <step>
              <para>Click <guimenu>Update Share Type</guimenu> button to confirm your changes.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
        <section xml:id="delete-share-types">
          <title>Delete share types</title>
          <para>When you delete a share type, shares of that type are not deleted.</para>
          <procedure>
            <step>
              <para>Log in to the Dashboard and choose the <guimenu>admin</guimenu> project from
                        the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Shares</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Share Types</guimenu> tab, select the share type
                        or types that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Share Types</guimenu> button.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Share Types</guimenu> window, click the
                        <guimenu>Delete Share Types</guimenu> button to confirm the action.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
        <section xml:id="delete-shares">
          <title>Delete shares</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and choose the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Shares</guimenu> category.</para>
            </step>
            <step>
              <para>Select the share or shares that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Shares</guimenu> button.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Shares</guimenu> window, click the
                        <guimenu>Delete Shares</guimenu> button to confirm the action.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
        <section xml:id="delete-share-server">
          <title>Delete share server</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and choose the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Share Servers</guimenu> category.</para>
            </step>
            <step>
              <para>Select the share that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Share Server</guimenu> button.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Share Server</guimenu> window, click the
                        <guimenu>Delete Share Server</guimenu> button to confirm the action.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
        <section xml:id="delete-share-networks">
          <title>Delete share networks</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and choose the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Share Networks</guimenu> category.</para>
            </step>
            <step>
              <para>Select the share network or share networks that you want to delete.</para>
            </step>
            <step>
              <para>Click <guimenu>Delete Share Networks</guimenu> button.</para>
            </step>
            <step>
              <para>In the <guimenu>Confirm Delete Share Networks</guimenu> window, click the
                        <guimenu>Delete Share Networks</guimenu> button to confirm the action.</para>
            </step>
          </procedure>
          <note>
            <para>A message indicates whether the action succeeded.</para>
          </note>
        </section>
      </section>
      <section xml:id="dashboard-set-quotas" xml:base="set-quotas">
        <title>View and manage quotas</title>
        <para>To prevent system capacities from being exhausted without notification,
            you can set up quotas. Quotas are operational limits. For example, the
            number of gigabytes allowed for each project can be controlled so that
            cloud resources are optimized. Quotas can be enforced at both the project
            and the project-user level.</para>
        <para>Typically, you change quotas when a project needs more than ten
            volumes or 1 TB on a compute node.</para>
        <para>Using the Dashboard, you can view default Compute and Block Storage
            quotas for new projects, as well as update quotas for existing projects.</para>
        <note>
          <para>Using the command-line interface, you can manage quotas for
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/nova/latest/admin/quotas.html">the OpenStack Compute service</link>,
                <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/cinder/latest/cli/cli-set-quotas.html">the OpenStack Block Storage service</link>,
                and the OpenStack Networking service (For CLI details,
                see <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/python-openstackclient/latest/cli/command-objects/quota.html">OpenStackClient CLI reference</link>).
                Additionally, you can update Compute service quotas for
                project users.</para>
        </note>
        <para>The following table describes the Compute and Block Storage service quotas:</para>
        <para>
          <emphasis role="bold">Quota Descriptions</emphasis>
        </para>
        <informaltable>
          <tgroup cols="3">
            <colspec colname="c0" colwidth="20"/>
            <colspec colname="c1" colwidth="36"/>
            <colspec colname="c2" colwidth="15"/>
            <thead>
              <row>
                <entry>
                  <para>Quota Name</para>
                </entry>
                <entry>
                  <para>Defines the number of</para>
                </entry>
                <entry>
                  <para>Service</para>
                </entry>
              </row>
            </thead>
            <tbody>
              <row>
                <entry>
                  <para>Gigabytes</para>
                </entry>
                <entry>
                  <para>Volume gigabytes allowed for
                                each project.</para>
                </entry>
                <entry>
                  <para>Block Storage</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>Instances</para>
                </entry>
                <entry>
                  <para>Instances allowed for each
                                project.</para>
                </entry>
                <entry>
                  <para>Compute</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>Injected Files</para>
                </entry>
                <entry>
                  <para>Injected files allowed for each
                                project.</para>
                </entry>
                <entry>
                  <para>Compute</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>Injected File
                                Content Bytes</para>
                </entry>
                <entry>
                  <para>Content bytes allowed for each
                                injected file.</para>
                </entry>
                <entry>
                  <para>Compute</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>Keypairs</para>
                </entry>
                <entry>
                  <para>Number of keypairs.</para>
                </entry>
                <entry>
                  <para>Compute</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>Metadata Items</para>
                </entry>
                <entry>
                  <para>Metadata items allowed for each
                                instance.</para>
                </entry>
                <entry>
                  <para>Compute</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>RAM (MB)</para>
                </entry>
                <entry>
                  <para>RAM megabytes allowed for
                                each instance.</para>
                </entry>
                <entry>
                  <para>Compute</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>Security Groups</para>
                </entry>
                <entry>
                  <para>Security groups allowed for each
                                project.</para>
                </entry>
                <entry>
                  <para>Compute</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>Security Group
                                Rules</para>
                </entry>
                <entry>
                  <para>Security group rules allowed for
                                each project.</para>
                </entry>
                <entry>
                  <para>Compute</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>Snapshots</para>
                </entry>
                <entry>
                  <para>Volume snapshots allowed for
                                each project.</para>
                </entry>
                <entry>
                  <para>Block Storage</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>VCPUs</para>
                </entry>
                <entry>
                  <para>Instance cores allowed for each
                                project.</para>
                </entry>
                <entry>
                  <para>Compute</para>
                </entry>
              </row>
              <row>
                <entry>
                  <para>Volumes</para>
                </entry>
                <entry>
                  <para>Volumes allowed for each
                                project.</para>
                </entry>
                <entry>
                  <para>Block Storage</para>
                </entry>
              </row>
            </tbody>
          </tgroup>
        </informaltable>
        <section xml:id="dashboard-view-quotas-procedure">
          <title>View default project quotas</title>
          <procedure>
            <step>
              <para>Log in to the dashboard and select the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Defaults</guimenu> category.</para>
            </step>
            <step>
              <para>The default quota values are displayed.</para>
            </step>
          </procedure>
          <note>
            <para>You can sort the table by clicking on either the
                    <guimenu>Quota Name</guimenu> or <guimenu>Limit</guimenu> column headers.</para>
          </note>
        </section>
        <section xml:id="dashboard-update-project-quotas">
          <title>Update project quotas</title>
          <procedure>
            <step>
              <para>Log in to the dashboard and select the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                        and click the <guimenu>Defaults</guimenu> category.</para>
            </step>
            <step>
              <para>Click the <guimenu>Update Defaults</guimenu> button.</para>
            </step>
            <step>
              <para>In the <guimenu>Update Default Quotas</guimenu> window,
                        you can edit the default quota values.</para>
            </step>
            <step>
              <para>Click the <guimenu>Update Defaults</guimenu> button.</para>
            </step>
          </procedure>
          <note>
            <para>The dashboard does not show all possible project quotas.
                    To view and update the quotas for a service, use its
                    command-line client. See <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/admin-guide/cli-set-quotas.html">OpenStack Administrator Guide</link>.</para>
          </note>
        </section>
      </section>
      <section xml:id="view-services-information" xml:base="manage-services">
        <title>View services information</title>
        <para>As an administrative user, you can view information for OpenStack services.</para>
        <procedure>
          <step>
            <para>Log in to the Dashboard and select the
                    <guimenu>admin</guimenu> project from the drop-down list.</para>
          </step>
          <step>
            <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab
                    and click the <guimenu>System Information</guimenu> category.</para>
            <para>View the following information on these tabs:</para>
            <itemizedlist>
              <listitem>
                <para><guimenu>Services</guimenu>:
                            Displays the internal name and the public OpenStack name
                            for each service, the host on which the service runs,
                            and whether or not the service is enabled.</para>
              </listitem>
              <listitem>
                <para><guimenu>Compute Services</guimenu>:
                            Displays information specific to the Compute service. Both host
                            and zone are listed for each service, as well as its
                            activation status.</para>
              </listitem>
              <listitem>
                <para><guimenu>Block Storage Services</guimenu>:
                            Displays information specific to the Block Storage service. Both host
                            and zone are listed for each service, as well as its
                            activation status.</para>
              </listitem>
              <listitem>
                <para><guimenu>Network Agents</guimenu>:
                            Displays the network agents active within the cluster, such as L3 and
                            DHCP agents, and the status of each agent.</para>
              </listitem>
              <listitem>
                <para><guimenu>Orchestration Services</guimenu>:
                            Displays information specific to the Orchestration service. Name,
                            engine id, host and topic are listed for each service, as well as its
                            activation status.</para>
              </listitem>
            </itemizedlist>
          </step>
        </procedure>
      </section>
      <section xml:id="create-and-manage-host-aggregates" xml:base="manage-host-aggregates">
        <title>Create and manage host aggregates</title>
        <para>Host aggregates enable administrative users to assign key-value pairs to
            groups of machines.</para>
        <para>Each node can have multiple aggregates and each aggregate can have
            multiple key-value pairs. You can assign the same key-value pair to
            multiple aggregates.</para>
        <para>The scheduler uses this information to make scheduling decisions.
            For information, see
            <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/nova/latest/admin/configuration/schedulers.html">Scheduling</link>.</para>
        <section xml:id="to-create-a-host-aggregate">
          <title>To create a host aggregate</title>
          <procedure>
            <step>
              <para>Log in to the Dashboard and select the <guimenu>admin</guimenu> project
                        from the drop-down list.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab and click
                        the <guimenu>Host Aggregates</guimenu> category.</para>
            </step>
            <step>
              <para>Click <guimenu>Create Host Aggregate</guimenu>.</para>
            </step>
            <step>
              <para>In the <guimenu>Create Host Aggregate</guimenu> dialog box, enter or select the
                        following values on the <guimenu>Host Aggregate Information</guimenu> tab:</para>
              <itemizedlist>
                <listitem>
                  <para><guimenu>Name</guimenu>: The host aggregate name.</para>
                </listitem>
                <listitem>
                  <para><guimenu>Availability Zone</guimenu>: The cloud provider defines the default
                                availability zone, such as <literal>us-west</literal>, <literal>apac-south</literal>, or
                                <literal>nova</literal>. You can target the host aggregate, as follows:</para>
                  <itemizedlist>
                    <listitem>
                      <para>When the host aggregate is exposed as an availability zone,
                                        select the availability zone when you launch an instance.</para>
                    </listitem>
                    <listitem>
                      <para>When the host aggregate is not exposed as an availability zone,
                                        select a flavor and its extra specs to target the host
                                        aggregate.</para>
                    </listitem>
                  </itemizedlist>
                </listitem>
              </itemizedlist>
            </step>
            <step>
              <para>Assign hosts to the aggregate using the <guimenu>Manage Hosts within
                            Aggregate</guimenu> tab in the same dialog box.</para>
              <para>To assign a host to the aggregate, click <emphasis role="bold">+</emphasis> for the host. The host
                        moves from the <guimenu>All available hosts</guimenu> list to the
                        <guimenu>Selected hosts</guimenu> list.</para>
            </step>
          </procedure>
          <para>You can add one host to one or more aggregates. To add a host to an
                existing aggregate, edit the aggregate.</para>
        </section>
        <section xml:id="to-manage-host-aggregates">
          <title>To manage host aggregates</title>
          <procedure>
            <step>
              <para>Select the <guimenu>admin</guimenu> project from the drop-down list at the top
                        of the page.</para>
            </step>
            <step>
              <para>On the <guimenu>Admin</guimenu> tab, open the <guimenu>System</guimenu> tab and click
                        the <guimenu>Host Aggregates</guimenu> category.</para>
              <itemizedlist>
                <listitem>
                  <para>To edit host aggregates, select the host aggregate that you want
                                to edit. Click <guimenu>Edit Host Aggregate</guimenu>.</para>
                  <para>In the <guimenu>Edit Host Aggregate</guimenu> dialog box, you can change the
                                name and availability zone for the aggregate.</para>
                </listitem>
                <listitem>
                  <para>To manage hosts, locate the host aggregate that you want to edit
                                in the table. Click <guimenu>More</guimenu> and select <guimenu>Manage Hosts</guimenu>.</para>
                  <para>In the <guimenu>Add/Remove Hosts to Aggregate</guimenu> dialog box,
                                click <emphasis role="bold">+</emphasis> to assign a host to an aggregate. Click <emphasis role="bold">-</emphasis> to
                                remove a host that is assigned to an aggregate.</para>
                </listitem>
                <listitem>
                  <para>To delete host aggregates, locate the host aggregate that you want
                                to edit in the table. Click <guimenu>More</guimenu> and select
                                <guimenu>Delete Host Aggregate</guimenu>.</para>
                </listitem>
              </itemizedlist>
            </step>
          </procedure>
        </section>
      </section>
      <section xml:id="launch-and-manage-stacks-using-the-dashboard" xml:base="admin-manage-stacks">
        <title>Launch and manage stacks using the Dashboard</title>
        <para>The Orchestration service provides a template-based
            orchestration engine for the OpenStack cloud. Orchestration
            services create and manage cloud infrastructure
            resources such as storage, networking, instances, and
            applications as a repeatable running environment.</para>
        <para>Administrators use templates to create stacks, which are
            collections of resources. For example, a stack might
            include instances, floating IPs, volumes,
            security groups, or users. The Orchestration service
            offers access to all OpenStack
            core services via a single modular template, with additional
            orchestration capabilities such as auto-scaling and basic
            high availability.</para>
        <para>For information about:</para>
        <itemizedlist>
          <listitem>
            <para>administrative tasks on the command-line, see
                    the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/admin-guide/cli-admin-manage-stacks.html">OpenStack Administrator Guide</link>.</para>
            <note>
              <para>There are no administration-specific tasks that can be done through
                        the Dashboard.</para>
            </note>
          </listitem>
          <listitem>
            <para>the basic creation and deletion of Orchestration stacks, refer to
                    the <link xmlns:xl="http://www.w3.org/1999/xlink" xl:href="https://docs.openstack.org/user-guide/dashboard-stacks.html">OpenStack End User Guide</link>.</para>
          </listitem>
        </itemizedlist>
      </section>
<!--      <itemizedlist>
        <listitem>
          <para>To deploy the dashboard, see the <xref linkend="../install/index"/>.</para>
        </listitem>
        <listitem>
          <para>To launch instances with the dashboard as an end user, see the
                    <xref linkend="../user/launch-instances"/> in the OpenStack End User Guide.</para>
        </listitem>
        <listitem>
          <para>To create and manage ports, see the <xref linkend="../user/create-networks"/>
                    section of the OpenStack End User Guide.</para>
        </listitem>
      </itemizedlist> -->
    </section>
  </chapter>
