<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet 
 href="urn:x-suse:xslt:profiling:novdoc-profile.xsl"
 type="text/xml" 
 title="Profiling step"?>
<!DOCTYPE book PUBLIC "-//Novell//DTD NovDoc XML V1.0//EN"
                      "novdocx.dtd"
[
<!ENTITY % NOVDOC.DEACTIVATE.IDREF "IGNORE">
<!ENTITY % entities SYSTEM "entity-decl.ent">
%entities;
]>

<?provo dirname="cloud/"?>
<book lang="en" id="book.cloud.suppl">
 <bookinfo>
  <title>&cloudsuppl;</title>
  <productname>&productname;</productname>
  <productnumber>&productnumber;</productnumber>
  <date>
   <?dbtimestamp format="B d, Y"?></date>
  <xi:include href="copyright_suse_cloud.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>
  <xi:include href="authors.xml" xmlns:xi="http://www.w3.org/2001/XInclude"/>
 </bookinfo>
 
 <chapter id="cha.suppl.horizon.theme">
  <!--https://bugzilla.suse.com/show_bug.cgi?id=917622-->
  <title>Changing the &cloud; &dash; Theme</title>
  <para>The &cloud; &dash; theme can now be customized. The default
   &productname; theme is available in the <systemitem class="resource"
    >openstack-dashboard-theme-SUSE</systemitem> package. If you wan to replace
   it with a custom theme, you can explore the package contents as an example.
   When using a custom theme, make sure the resulting package name starts with <systemitem
    class="resource">openstack-dashboard-theme-</systemitem>. Apart from that, you need to
   adjust the <guimenu>site_theme</guimenu> attribute of the Horizon proposal in
   &crow; accordingly.
   <!--taroth 2015-02-12: todo-  enable pointer to DG
    once the attribute is covered there: See also the &Sclouddeploy;, section
    <citetitle>Deploying Horizon (OpenStack Dashboard)</citetitle>. The guide is
   available from &suse-onlinedoc;.--></para>
 </chapter>
 
 <chapter id="cha.adm.cli.img">
  <title>Managing Images</title> 
  &images; &img-manage; 
  <para>Managing images for
   &productname; requires the following basic steps: </para>
  <procedure>
   <step>
    <para>
     <xref linkend="sec.adm.cli.img.build" xrefstyle="select:title"/>. </para>
    <para>For general and hypervisor-specific requirements, refer to <xref
      linkend="sec.adm.cli.img.build.req"/>. </para>
   </step>
   <step>
    <para>
     <xref linkend="pro.adm.cli.img.upload" xrefstyle="select:title"/>. </para>
    <para> Images can either be uploaded to &productname; with the <command>glance</command>
     command line tool or with the &productname; &dash;. As the &dash; comes with some
     limitations with regards to image upload and modification of properties, it is recommended to
     use the <command>glance</command> command line tools for comprehensive image management.
    </para>
   </step>
   <step>
    <para>Specifying Image Properties. You can do so during image upload (using
     <command>glance&nbsp;image-create</command>) or with
     <command>glance&nbsp;image-update</command> after the image has already been uploaded.
     Refer to <xref linkend="pro.adm.cli.img.upload"/> and <xref
      linkend="pro.adm.cli.img.props.modify"/>. </para> &note-img-props; </step>
  </procedure>
  <sect1 id="sec.adm.cli.img.build.req">
   <title>Image Requirements</title>
   <para>To build the images to use within the cloud, use &susestudio; or &suseonsite; as
    they provide automatic insertion of management scripts and agents. Make sure any images that you
    build for &productname; fulfill the following requirements. </para>
   <sect2 id="vl.adm.img.req.general">
    <title>General Image Requirements</title>
    <itemizedlist>
     <listitem>
      <para> The network is set to DHCP. </para>
     </listitem>
     <listitem>
      <para> The image does not include &yast;2 Firstboot. </para>
     </listitem>
     <listitem>
      <para> The image does not include any end-user license agreement (EULA) dialogs. </para>
     </listitem>
     <listitem>
      <para> The image contains the <systemitem class="resource">cloud-init</systemitem> package.
       The package will be automatically added to the image if the following check box in
       &susestudio; or &suseonsite; is enabled: <guimenu>Integrate with
        &productname;/OpenStack</guimenu>. </para>
      <para> The <systemitem class="resource">cloud-init</systemitem> package contains tools used
       for communication with the instance metadata API, which is provided by &comp;. The
       metadata API is only accessible from inside the VM. The package is needed to pull keypairs
       into the virtual machine that will run the image. </para>
     </listitem>
    </itemizedlist>
    <para> If you intend to manage the image by the &orch; module, you also need to include the
     following package: <systemitem class="resource">openstack-heat-cfntools</systemitem> (part of
     the &productname; ISO). </para>
   </sect2>
   <sect2 id="vl.adm.img.req.virt">
    <title>Image Requirements Depending on Hypervisor</title>
    <para>For a list of supported VM guests, refer to the &slsreg;
     &virtual;, section <citetitle>Supported VM Guests</citetitle>. It is
     available at <ulink
      url="https://www.suse.com/documentation/sles-12/book_virt/data/virt_support_guests.html"
     />. </para>
    <para> Depending on the virtualization platform on which you want to use the image, make sure
     the image also fulfills the following requirements. </para>
    <variablelist>
     <varlistentry>
      <term>&kvm;</term>
      <listitem>
       <para> Appliance format: If you are using &susestudio; or &suseonsite; 1.3 to build
        images, use the <literal>&productname;/OpenStack/&kvm; (.qcow2)</literal> format.
       </para>
      </listitem>
     </varlistentry>
     <!--https://bugzilla.suse.com/show_bug.cgi?id=863876-->
     <varlistentry>
      <term>&xen;</term>
      <listitem>
       <para> Appliance format: If you are using &susestudio; or &suseonsite; 1.3 to build
        images, use the <literal>&productname;/OpenStack/&kvm; (.qcow2)</literal> format.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>&vmware;</term>
      <listitem>
       <para> Appliance format: If you are using &susestudio; or &suseonsite; 1.3 to build
        images, use the <literal>VMware/VirtualBox/KVM (.vmdk)</literal> format. </para>
       <!--taroth 2014-02-05: fix for https://bugzilla.suse.com/show_bug.cgi?id=862165#c3,
        as discussed with mjura-->
       <para> If you are using &susestudio; or &suseonsite; to build images, the resulting
        image will be a monolithic <literal>sparse</literal> file.</para>
       <note>
        <title>Image Conversion</title>
        <para>Sparse images can be uploaded to &ostack; &img;. However, it is recommended to
         convert sparse images into a different format before uploading them to &ostack;
         &img; (because starting VMs from sparse images may take longer).</para>
        <para>For a list of supported image types, refer to <ulink
          url="http://docs.openstack.org/juno/config-reference/content/vmware.html"/>, section
          <citetitle>Supported image types</citetitle>.</para>
        <para>For details on how to convert a sparse image into different formats, refer to <ulink
          url="http://docs.openstack.org/juno/config-reference/content/vmware.html"/>, section
          <citetitle>Optimize images</citetitle>. </para>
       </note>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>&hyper;</term>
      <listitem>
       <para> Appliance format: If you are using &susestudio; or &suseonsite; 1.3 to build
        images, use the <literal>Hyper-V (.vhd)</literal>, or
         <literal>&productname;/OpenStack/&kvm; (.qcow2)</literal> format. </para>
       <para> As &hyper; only supports images in <filename>*.vhd</filename> format, convert any
         <literal>qcow2</literal> images before uploading them to &productname;. For details,
        refer to <xref linkend="pro.adm.cli.img.convert" xrefstyle="select:label"/>. </para>
       <!--taroth 2013-10-17: fix for https://bugzilla.suse.com/show_bug.cgi?id=836765#c21-->
       <para> To ensure that an image is bootable on &hyper;, check the
         <literal>INITRD_MODULES</literal> lines in <filename>/etc/sysconfig/kernel</filename> for
        the parameters <literal>hv_storvsc</literal> and <literal>mkinitrd</literal>. If the
        parameters are missing, add them. Otherwise the Kernel module for storage devices will not
        be loaded and the root file system will not be available. </para>
      </listitem>
     </varlistentry>
    </variablelist>
   </sect2>
   <sect2 id="tip.adm.img.req.virt.mult">
    <!--https://bugzilla.suse.com/show_bug.cgi?id=836765#22-->
    <title>Images for Use With Multiple Hypervisors</title>
    <para> If you build the images for &productname; in &susestudio; or &suseonsite;,
     they are compatible with multiple hypervisors by default&mdash;even if you may have to
     convert the resulting image formats before uploading them to &ostack; &img;. See <xref
      linkend="pro.adm.cli.img.convert"/> for details. </para>
    <para> If your image is not made in &susestudio; or &suseonsite;, configure the image as
     follows to create an image that can be booted on &kvm;, &xen;, and &hyper;, for
     example: </para>
    <variablelist>
     <varlistentry>
      <term><filename>/etc/sysconfig/kernel</filename>
       </term>
      <listitem>
       <screen>INITRD_MODULES="virtio_blk virtio_pci ata_piix ata_generic hv_storvsc"</screen>
       <para> The resulting disk device will be called <filename>/dev/sda</filename> on &hyper;.
       </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><filename>/boot/grub/menu.lst</filename>
       </term>
      <listitem>
       <para> To name the partition that should be booted, use: </para>
       <screen>root=UUID=...</screen>
       <para> To find the respective UUID value to use, execute the following command: </para>
       <screen>tune2fs -l /dev/sda2|grep UUID</screen>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term><filename>/etc/fstab</filename>
       </term>
      <listitem>
       <para> Do not use device names (<filename>/dev/...</filename>), but
         <literal>UUID=...</literal> or <literal>LABEL=root</literal> entries. For the latter, add
        the label <literal>root</literal> to the root file system of your image (in this case,
         <filename>/dev/sda2</filename>): </para>
       <screen>tune2fs -L root /dev/sda2</screen>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>Disk Format</term>
      <listitem>
       <para>Use <filename>*.qcow2</filename> as disk format for your image. </para>
      </listitem>
     </varlistentry>
     <varlistentry>
      <term>Image Properties in &ostack; &img;</term>
      <listitem>
       <para>To upload the image to &productname; only once and to use the same image for
        &kvm;, &xen;, and &hyper;, specify the following image options during or after
        upload: </para>
       <screen>--is-public=True --container-format=bare \
--property&nbsp;architecture=x86_64 \
--property vm_mode=hvm \
--disk-format=qcow2</screen>
      </listitem>
     </varlistentry>

    </variablelist>
   </sect2>
  </sect1>
  <sect1 id="sec.adm.cli.img.build">
   <title>Building Images with &susestudio;</title>
   <para> When creating an appliance for &productname; the following steps are essential: </para>
   <procedure>
    <step>
     <para> In &susestudio; or &suseonsite;, switch to the <menuchoice>
       <guimenu>Configuration</guimenu>
       <guimenu>Appliance</guimenu>
      </menuchoice> tab. </para>
    </step>
    <step>
     <para> Enable the <guimenu>Integrate with &productname;/OpenStack</guimenu> check box.
     </para>
    </step>
    <step>
     <para> On the <guimenu>Build</guimenu> tab, choose the respective appliance format. It mainly
      depends on the hypervisor on which you want to use the image&mdash;see <xref
       linkend="vl.adm.img.req.virt"/>. </para>
    </step>
   </procedure>
   <para> For more detailed information on how to build appliance images, refer to the
     <citetitle>&suseonsite; Quick Start</citetitle> or the <citetitle>&suseonsite; User
     Guide</citetitle>, available at <ulink url="http://www.suse.com/documentation/suse_studio/"/>.
   </para>
  </sect1>
  <sect1 id="sec.adm.cli.img.props">
   <title>Image Properties</title>
   <!-- taroth 2013-08-29: fix for https://bugzilla.suse.com/show_bug.cgi?id=828862-->
   &img-props; </sect1>
  <sect1 id="sec.adm.cli.img.upload">
   <title>Uploading Images</title>
   <para> If you have created an image for <literal>&productname;/OpenStack/&kvm;</literal>
    with &susestudio; or with &suseonsite; 1.3, you can upload the image directly as
    described in <xref linkend="pro.adm.cli.img.upload"/>. </para>
   <para>If you use &hyper;, convert any <literal>qcow2</literal> images before uploading them
    to &productname;.</para>

   <procedure id="pro.adm.cli.img.convert">
    <title>Converting Disk Images to Different Formats</title>
    <step>
     <!--taroth 2013-08-30: for the records and later revisions: package
      virt-utils is called qemu-tools in openSUSE or newer (info by dmueller) -->
     <para> Make sure the <systemitem class="resource">virt-utils</systemitem> package is installed
      on the machine used for conversion. </para>
    </step>
    <step>
     <para> Download the image from &susestudio;. </para>
    </step>
    <step>
     <para>To convert <literal>qcow2</literal> to <literal>vhd</literal> images, use the following
      command:</para>
     <screen>qemu-img convert -O vpc&nbsp;<replaceable>CURRENT_IMAGE_FILE</replaceable>&nbsp;<replaceable>FINAL_IMAGE_FILE</replaceable>.vhd</screen>
    </step>
   </procedure>
   <!--taroth 2014-02-20: converting from raw to qcow2 should not be necessary anymore, still only
    comenting the following step in case it is needed again-->
   <!--<step>
    <para>
    To convert the image to a different format, use either of the
    following commands:
    </para>
    <itemizedlist>
    <listitem>
    <para>
    For conversion from <literal>raw</literal> to
    <literal>qcow2</literal>:
    </para>
    <screen>qemu-img convert -c -f raw -O&nbsp;qcow2&nbsp;\
    <replaceable>CURRENT_IMAGE_FILE</replaceable>&nbsp;<replaceable>FINAL_IMAGE_FILE</replaceable>.qcow2</screen>
    </listitem>
    <listitem>
    <para>
    For conversion from <literal>raw</literal> or
    <literal>qcow2</literal> to <literal>vhd</literal>:
    </para>
    <screen>qemu-img convert -O vpc&nbsp;<replaceable>CURRENT_IMAGE_FILE</replaceable>&nbsp;<replaceable>FINAL_IMAGE_FILE</replaceable>.vhd</screen>
    </listitem>
    </itemizedlist>
    </step>-->

   <procedure id="pro.adm.cli.img.upload">
    <title>Uploading Disk Images to &productname;</title>
    <para> Upload a disk image using the <command>glance</command> command line tool that is
     contained in the package <systemitem class="resource">python-glanceclient</systemitem>. </para>
    &img-ownership; <!--taroth 2013-10-17: fix for https://bugzilla.suse.com/show_bug.cgi?id=838040#c18-->
    <!--taroth 2014-02-21: commenting on behalf of vuntz, see https://bugzilla.suse.com/show_bug.cgi?id=863876#c3
     <tip>
     <title>Using Different Storage Backends</title>
     <para>
      By adding the <option>-/-store</option> option you can specify a
      different storage backend than the default storage backend that is
      configured. Possible values for <option>-/-store</option> are:
     </para>
     <itemizedlist>
      <listitem>
       <para>
        filesystem
       </para>
      </listitem>
      <listitem>
       <para>
        http
       </para>
      </listitem>
      <listitem>
       <para>
        rbd
       </para>
      </listitem>
      <listitem>
       <para>
        s3
       </para>
      </listitem>
      <listitem>
       <para>
        swift
       </para>
      </listitem>
     </itemizedlist>
    </tip>-->
    <para>Set or modify hypervisor-specific properties with the
       <option>--property&nbsp;<replaceable>key</replaceable>=<replaceable>value</replaceable></option>
     option. This can be done directly during image upload (as shown in the examples below). To
     change the properties after image upload, refer to <xref linkend="pro.adm.cli.img.props.modify"
     />.</para>
    <step>
     <para> In a shell, source the &ostack; RC file for the project that you want to upload an
      image to. For details, refer to <ulink url="http://docs.openstack.org/user-guide/content/cli_openrc.html"></ulink>. </para>
    </step>
    <step id="step.adm.cli.img.upload">
     <para> Upload the image using <command>glance image-create</command>. Find some example
      commands for different hypervisors below: </para>
     <itemizedlist>
      <!--taroth 2014-20-21: CAVE, the same hypervisor-specific props are mentioned in 2 procedures: 
       pro.adm.cli.img.upload and pro.adm.cli.img.props.modify -->
      <listitem>
       <para> For &kvm;: </para>
       <screen>glance image-create --name="<replaceable>IMAGE_NAME</replaceable>" --progress \
  --is-public=True --container-format=bare \
  --property&nbsp;architecture=x86_64 \
  --property&nbsp;hypervisor_type=kvm \
  --disk-format=qcow2 &lt; <replaceable>PATH_TO_FINAL_IMAGE_FILE</replaceable>.qcow2&nbsp;</screen>
      </listitem>
      <listitem>
       <para> For &xen;: </para>
       <screen>glance image-create --name="<replaceable>IMAGE_NAME</replaceable>" --progress \
  --is-public=True --container-format=bare \
  --property&nbsp;architecture=x86_64 \
  --property&nbsp;hypervisor_type=xen \
  --property&nbsp;vm_mode=xen \
  --disk-format=qcow2 &lt; <replaceable>PATH_TO_FINAL_IMAGE_FILE</replaceable>.qcow2&nbsp;</screen>
       &note-vm_mode;
       <!--taroth 2013-10-17: Xen:
        http://docserv.nue.suse.com/documents/SLES/SLES-xen/single-html/#sec.xen.config.disk-->
      </listitem>
      <listitem>
       <para> For &hyper;: </para>
       <screen>glance image-create --name="<replaceable>IMAGE_NAME</replaceable>" --progress \
  --is-public=True --container-format=bare \
  --property&nbsp;architecture=x86_64 \
  --property&nbsp;hypervisor_type=hyperv \
  --property&nbsp;vm_mode=hvm \
  --disk-format=vhd &lt; <replaceable>PATH_TO_FINAL_IMAGE_FILE</replaceable>.vhd</screen>
      </listitem>
      <listitem>
       <para> For &vmware;: </para>
       <screen>glance image-create --name="<replaceable>IMAGE_NAME</replaceable>"  --progress \ 
  --is-public=true --container-format=bare \
  --property&nbsp;vmware_adaptertype="lsiLogic" \
  --property vmware_disktype="preallocated" \
  --property hypervisor_type=vmware \
  --disk-format=vmdk&nbsp;--file&nbsp;<replaceable>PATH_TO_FINAL_IMAGE_FILE</replaceable>.vmdk</screen>
       &note-vmware_disktype;</listitem>
     </itemizedlist>
     <para> If the image upload has been successful, a message appears, displaying the ID that has
      been assigned to the image. </para>
    </step>
   </procedure>

   <!--taroth 2013-08-29: DEVs, is the following still true or can SUSE Studio inject any
    files into an image which make it possible to change the image?- taroth 2013-10-01: for the records:
    according to aj, studio can inject anything but it cannot change an image-->
   <note>
    <title>Updating Images</title>
    <para> After having uploaded an image to &cloud;, the image contents cannot be
     modified&mdash;only the image&apos;s metadata, see <xref
      linkend="pro.adm.cli.img.props.modify" xrefstyle="select:label"/>. To update image contents,
     you need to delete the current image and upload a modified version of the image. You can also
     launch an instance from the respective image, change it, create a snapshot of the instance and
     use the snapshot as a new image. </para>
   </note>
  </sect1>
  <sect1 id="sec.adm.cli.img.props.modify">
   <title>Modifying Image Properties</title>

   <para>Set or modify hypervisor-specific properties with the
      <option>--property&nbsp;<replaceable>key</replaceable>=<replaceable>value</replaceable></option>
    option. This can be done directly during image upload (see <xref
     linkend="pro.adm.cli.img.upload" xrefstyle="select:label"/>) or after the image has been
    uploaded (as described below).</para>


   <procedure id="pro.adm.cli.img.props.modify">
    <title>Modifying Image Properties</title>
    <step>
     <para> In a shell, source the &ostack; RC file for the project that you want to upload an
      image to. For details, refer to 
      <ulink url="http://docs.openstack.org/user-guide/content/cli_openrc.html"/>. </para>
    </step>
    <step>
     <para> If you do not know the ID or the exact name of the image whose properties you want to
      modify, look it up with: </para>
     <screen>glance&nbsp;image-list</screen>
    </step>
    <step>
     <para> Use the <command>glance&nbsp;image-update</command> command to set the properties
      for architecture, hypervisor type, and virtual machine mode. In the following, find some
      examples with properties for different hypervisors: </para>
     <itemizedlist>
      <!--taroth 2014-20-21: CAVE, the same hypervisor-specific props are mentioned in 2 procedures: 
       pro.adm.cli.img.upload and pro.adm.cli.img.props.modify -->
      <listitem>
       <para>For &kvm;:</para>
       <screen>glance&nbsp;image-update&nbsp;<replaceable>IMAGE_ID_OR_NAME</replaceable>&nbsp;\
  --property&nbsp;architecture=x86_64 \
  --property&nbsp;hypervisor_type=kvm</screen>
      </listitem>
      <listitem>
       <para>For &xen;:</para>
       <screen>glance&nbsp;image-update&nbsp;<replaceable>IMAGE_ID_OR_NAME</replaceable>&nbsp;\
  --property&nbsp;architecture=x86_64 \
  --property&nbsp;hypervisor_type=xen \
  --property&nbsp;vm_mode=xen</screen>
       &note-vm_mode; </listitem>
      <listitem>
       <para>For &vmware;:</para>
       <screen>glance&nbsp;image-update&nbsp;<replaceable>IMAGE_ID_OR_NAME</replaceable>&nbsp;\
  --property&nbsp;vmware_adaptertype="lsiLogic" \
  --property vmware_disktype="preallocated" \
  --property hypervisor_type=vmware</screen>
       &note-vmware_disktype; </listitem>
      <listitem>
       <para>For &hyper;:</para>
       <screen>glance&nbsp;image-update&nbsp;<replaceable>IMAGE_ID_OR_NAME</replaceable>&nbsp;\
  --property&nbsp;architecture=x86_64 \
  --property&nbsp;hypervisor_type=hyperv \
  --property&nbsp;vm_mode=hvm</screen>
      </listitem>
     </itemizedlist>
    </step>
   </procedure>
   <para> For more information about the <systemitem>architecture</systemitem>,
     <systemitem>hypervisor_type</systemitem>, and <systemitem>vm_mode</systemitem> properties,
    refer to <ulink url="http://docs.openstack.org/image-guide/content/image-metadata.html"/>.
   </para>
  </sect1>
  <sect1 id="sec.adm.cli.img.view">
   <title>Viewing Images and Image Properties, Deleting Images</title>
   <para> In the following, find some examples on how to view images or image properties or how to
    remove images from &ostack; &img;. </para>
   <variablelist>
    <varlistentry>
     <term>Listing Images</term>
     <listitem>
      <screen>glance image-list</screen>
      <para> Lists ID, name, disk format, and container format for all images in &img; that the
       current user can access. </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Showing Metadata for a Particular Image</term>
     <listitem>
      <screen>glance image-show <replaceable>IMAGE_ID_OR_NAME</replaceable>&nbsp;</screen>
      <para> Shows metadata of the specified image. </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Removing Image Properties</term>
     <listitem>
      <screen>glance image-update <replaceable>IMAGE_ID_OR_NAME</replaceable> --purge-props</screen>
     </listitem>
    </varlistentry>

    <varlistentry>
     <term>Deleting an Image</term>
     <listitem>
      <screen>glance image-delete <replaceable>IMAGE_ID_OR_NAME</replaceable>&nbsp;</screen>
      <para> Removes the specified image from &ostack; &img;. </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect1>
  <sect1 id="sec.adm.cli.img.member">
   <title>Viewing and Modifying Membership of Private Images</title>
   <para> In the following, find some examples on how to view or modify membership of private
    images: </para>
   <variablelist>
    <varlistentry>
     <term>Listing Members a Private Image is Shared With</term>
     <listitem>
      <screen>glance member-list --image-id <replaceable>IMAGE_ID</replaceable>&nbsp;</screen>
      <para> Lists the IDs of the projects whose members have access to the private image. </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Listing Private Images Shared With a Member</term>
     <listitem>
      <screen>glance member-list --tenant-id <replaceable>PROJECT_ID</replaceable>&nbsp;</screen>
      <para> Lists the IDs of private images that members of the specified project can access.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Granting Members Access to a Private Image</term>
     <listitem>
      <screen>glance member-create [--can-share]&nbsp;<replaceable>IMAGE_ID_OR_NAME</replaceable>&nbsp;<replaceable>PROJECT_ID_OR_NAME</replaceable>&nbsp;</screen>
      <para> Grants the specified member access to the specified private image. </para>
      <para> By adding the <option>--can-share</option> option, you can allow the members to further
       share the image. <remark>taroth 2012-08-08: however that may work, no idea...</remark>
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Revoking Member Access to a Private Image</term>
     <listitem>
      <screen>glance&nbsp;member-delete&nbsp;<replaceable>IMAGE_ID_OR_NAME</replaceable>&nbsp;<replaceable>PROJECT_ID_OR_NAME</replaceable>&nbsp;</screen>
      <para> Revokes the access of the specified member to the specified private image. </para>
     </listitem>
    </varlistentry>
   </variablelist>
  </sect1>
 </chapter>
 <chapter id="cha.user.dash.inst">
  <title>Launching Instances from the &cloud; Dashboard</title>
    &instances;
 
  <sect1 id="sec.user.dash.inst.launch.params">
   <title>Key Parameters</title>
   <para>
    When starting an instance, specify the following key parameters:
   </para>
   <variablelist>
    <varlistentry>
     <term>Flavor</term>
     <listitem> &flavors; <para>
      For more details and a list of default flavors available, refer to
      <!--<xref linkend="sec.adm.dash.flavors"/>.-->
     </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Key Pair (optional, but recommended)</term>
     <listitem>&keypairs; <para>
      For details, refer to
      <!--<xref
       linkend="sec.user.dash.inst.access.keys"/>.-->
     </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Security Group</term>
     <listitem>&security; <para>
      For details, refer to
      <!--<xref
       linkend="sec.user.dash.inst.access.security"/>.-->
     </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Network</term>
     <listitem>
      <para>
       Instances can belong to one or multiple networks. By default, each
       instance is given a fixed IP address, belonging to the internal
       network.
      </para>
     </listitem>
    </varlistentry>
    <varlistentry>
     <term>Boot Source of the Instance</term>
     <listitem>&instance-boot-source;</listitem>
    </varlistentry>
   </variablelist>
   <para> If needed, you can assign a floating (public) IP address to a running
    instance and attach a block storage device (<literal>volume</literal>) for
    persistent storage. For details, refer to <ulink
     url="http://docs.openstack.org/user-guide/content/Launching_Instances_using_Dashboard.html">
     <citetitle>Allocate a floating IP address to an instance</citetitle>
    </ulink> and <ulink
     url="http://docs.openstack.org/user-guide/content/dashboard_manage_volumes.html">
     <citetitle>Create and manage volumes</citetitle>
    </ulink>.
   </para>
  </sect1>
  
  <sect1 id="sec.user.dash.inst.launch.vol">
   <title>Launching Instances from Images or Snaphots</title>
   <para>For instructions on how to launch instances from images or snapshots,
    see <ulink
    url="http://docs.openstack.org/user-guide/content/dashboard_launch_instances_from_image.html"><citetitle>Launch
    an Instance</citetitle></ulink>.</para>
  </sect1>
  <sect1>
   <title>Launching Instances from Volumes</title>
   <para>If you want to launch an instance from a volume, the volume needs to
    contain an image. You can directly create a volume from an image, as
    described in 
    <ulink
     url="http://docs.openstack.org/user-guide/content/dashboard_manage_volumes.html#dashboard_create_volumes"
   ><citetitle>Create a volume</citetitle></ulink>, by using the respective image as volume source.</para>
    
   <para>In case the image you want to transfer to a volume is not available in
    &ostack; &img; yet, you can create an empty volume and manually copy
    an image to it as shown in <xref
     linkend="pro.user.instances.launch.prep.vol" xrefstyle="select: label"/>.</para> 
  <procedure id="pro.user.instances.launch.prep.vol">
   <title>Manually Preparing a Bootable Volume</title>
    <step>
    <para>Log in to &cloud; &dash; and select a project from the drop-down box at the top-level
     row.</para>
   </step>
   <step id="step.user.dash.vol.create">
    <para> Create an empty volume as described in <ulink
     url="http://docs.openstack.org/user-guide/content/dashboard_manage_volumes.html#dashboard_create_volumes"
    ><citetitle>Create a volume</citetitle></ulink>, by using the option <guimenu>No source, empty volume</guimenu>. The
     volume size must be large enough to store an unzipped image. </para>
   </step>
   <step id="step.user.img.studio">
    <para> Create an image with &susestudio; or &suseonsite;. For details, refer to <xref
     linkend="sec.adm.cli.img.build"/>. </para>
   </step>
   <!--<remark>taroth 2014-01-16: DEVs, I'm not sure if the following is still valid for Havana,
    though the upstream docs still list it, too - edit: just checked, seems to be correct as
    there simply is no way to launch an instance from the Volumes view...</remark>--> 
   <step>
    <para>
     To be able to copy the image to the empty volume, you need to execute the following steps first:</para>
    <substeps>
     <step>
      <para>Launch an instance from an arbitrary image as described in <ulink
       url="http://docs.openstack.org/user-guide/content/dashboard_launch_instances_from_image.html">
         <citetitle>Launch an Instance</citetitle></ulink>.</para>
      <note>
       <itemizedlist>
        <listitem>
          <para>
           <emphasis>Which</emphasis> image you select there is only important
           if you want to boot a <emphasis>&xen;</emphasis> image from the
           volume later on. In that case, the image you select in <ulink
            url="http://docs.openstack.org/user-guide/content/dashboard_launch_instances_from_image.html"><citetitle>Launch
            an Instance</citetitle></ulink>, Step 3 needs to be of the same type as
           the one on the volume: <literal>fully virtualized</literal> or
            <literal>paravirtualized</literal>. </para>
        </listitem>
        <listitem>
         <para>In any other case the image you select for starting the instance does
          <emphasis>not</emphasis> matter. It will be replaced by the image on the volume that you
          specify in <ulink
          url="http://docs.openstack.org/user-guide/content/dashboard_launch_instances_from_image.html"><citetitle>Launch
          an Instance</citetitle></ulink>, Step 3, by using the option
          <guimenu>Boot from volume</guimenu>.
         </para>
        </listitem>
        <listitem>
         <para>Make sure to select an appropriate flavor for the instance. The instance's hard disk
          must be big enough to store the image that you want to copy onto the volume later
          on.</para>
        </listitem>
       </itemizedlist>
      </note>
     </step>
     <step>
      <para>Attach the empty volume to the running instance as described in
        <ulink
         url="http://docs.openstack.org/user-guide/content/dashboard_manage_volumes.html#dashboard_create_volumes"
         ><citetitle>Attach a volume to an instance</citetitle></ulink>.</para>
     </step>
    </substeps>
   </step>
   <step>
    <para>Copy the image that you created in <xref linkend="step.user.img.studio"
     xrefstyle="select:label"/> to the running instance (for example, by using
     <command>scp</command>).
    </para>
   </step>
   <step>
    <para>Log in to the instance by using SSH or the VNC
     console. </para>
   </step>
   <step>
    <para> Assuming that the attached volume is mounted as <filename>/dev/vdb</filename>, use one
     of the following commands to copy the image to the attached volume: </para>
    <itemizedlist>
     <listitem>
      <para> For a raw image: </para>
      <screen>cat&nbsp;<replaceable>IMAGE</replaceable>&nbsp;&gt;/dev/vdb</screen>
      <para> (alternatively, use <command>dd</command>) </para>
     </listitem>
     <listitem>
      <para> For a non-raw image: </para>
      <screen>qemu-img convert -O raw&nbsp;<replaceable>IMAGE</replaceable>&nbsp;/dev/vdb</screen>
     </listitem>
     <listitem>
      <para> For a <filename>*.tar.bz2</filename> image: </para>
      <screen>tar xfjO&nbsp;<replaceable>IMAGE</replaceable>&nbsp;&gt;/dev/vdb </screen>
     </listitem>
    </itemizedlist>
   </step>
   <step>
    <para> As the image comes with a predefined disk size (that might be smaller than the size of
     the volume it has been copied to), the image will not use the full size of the volume. To
     change this, adjust the partition table within the image to match the size of the volume.
    </para>
   </step>
   <step>
    <para>As only <emphasis>detached</emphasis> volumes are available for booting, detach the
     volume. For details on how to do so, refer to <ulink
      url="http://docs.openstack.org/user-guide/content/dashboard_manage_volumes.html#dashboard_create_volumes"><citetitle>Detach
      a volume from an instance</citetitle></ulink>.
    </para>
   </step>
  </procedure>
   <para>Now, you can launch an instance from the image that has been copied to
    the volume. The instance will be booted from the volume, which is provided
    by &ostack; &blockstore; through iSCSI. To do so, proceed as
    described in 
    <ulink
     url="http://docs.openstack.org/user-guide/content/dashboard_launch_instances_from_image.html">
     <citetitle>Launch an Instance</citetitle>
    </ulink>, using the option <guimenu>Boot from
     volume</guimenu>. </para>
  
 
  &instance-launch-post;
  </sect1>
 </chapter>
 
 <chapter id="cha.user.dash.inst.access">
  <title>Configuring Access to the Instances</title>
  <!--&inst-access-params;-->
  <para>Access to an instance is mainly influenced by the following parameters:</para>
  <variablelist>
   <varlistentry>
    <term>Security Groups and Rules</term>
    <listitem>
     &security;
     <para>For instructions on how to configure security groups and security
      group rules, see <ulink
       url="http://docs.openstack.org/user-guide-admin/content/dashboard_manage_projects_users.html">
       <citetitle>Create a security group</citetitle>
      </ulink>, <ulink
       url="http://docs.openstack.org/user-guide-admin/content/dashboard_manage_projects_users.html">
       <citetitle>Add a security group rule</citetitle>
      </ulink>, and <xref linkend="sec.user.dash.inst.access.security.rules"
      />.</para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>Key Pairs</term>
    <listitem> &keypairs; <para>For details on how to create or import
      keypairs, see <ulink
       url="http://docs.openstack.org/user-guide-admin/content/dashboard_manage_projects_users.html">
       <citetitle>Add a key pair</citetitle> and <citetitle>Import a key
        pair</citetitle>
      </ulink>. </para>
    </listitem>
   </varlistentry>
   <varlistentry>
    <term>IP Addresses</term>
    <listitem>&ip-addr;
     <para>For information on how to assign floating IP addresses to instances,
      see <ulink
       url="http://docs.openstack.org/user-guide/content/Launching_Instances_using_Dashboard.html">
       <citetitle>Allocate a floating IP address to an instance</citetitle>
      </ulink>.</para>
    </listitem>
   </varlistentry>

  </variablelist>
  <sect1 id="sec.user.dash.inst.access.security.rules">
   <title>Security Group Rules</title> &sec-rules; &sec-rules-params;
   &security-default; <!--taroth 2012-08-27: IIRC, I read this in upstream docs, but according to
    bmwiedemann's tests, he could not access arbitrary ports of VMs in the same
    security group, therefore commenting-->
   <!--Unless you change the rules for the default group, this means that
    those instances are only accessible from IP addresses belonging to other members 
    of this group.-->
   <procedure id="pro.cloud.dash.security.rules">
    <title>Configuring Security Group Rules</title>
    <para>For quicker configuration, the &dash; provides templates for rules
     that are often-used, including rules for well-known protocols on top of TCP
     (such as HTTP or SSH), or rules to allow all ICMP traffic (for
     pings).</para>
    <step>
     <para>Log in to &cloud; &dash; and select a project from the
      drop-down box at the top-level row. </para>
    </step>
    <step>
     <para> Click <menuchoice>
       <guimenu>Project</guimenu>
       <guimenu>Compute</guimenu>
       <guimenu>Access &amp; Security</guimenu>
      </menuchoice>. The view shows the following tabs: <guimenu>Security
       Groups</guimenu>, <guimenu>Key Pairs</guimenu>, <guimenu>Floating
       IPs</guimenu>, and <guimenu>API Access</guimenu>. </para>
    </step>
    <step>
     <para> On the <guimenu>Security Group</guimenu> tab, click <guimenu>Manage
       Rules</guimenu> for the security group you want to modify. This opens the
       <guimenu> Security Group Rules</guimenu> screen that shows the existing
      rules for the group and lets you add or delete rules.
      <!-- <remark>taroth 2013-07-25: unfortunately, it does not allow to _edit_ rules, filed
       https://bugs.launchpad.net/horizon/+bug/1210513 for this</remark>-->
     </para>
    </step>
    <step>
     <para> Click <guimenu>Add Rule</guimenu> to open a new dialog. </para>
     <para>From the <guimenu>Rule</guimenu> drop-down box, you can select
      templates for rules that are often-used, including rules for well-known
      protocols on top of TCP (such as HTTP or SSH), or rules to allow all ICMP
      traffic (for pings). In the following steps, we will focus on the most
      commonly-used rules only:</para>
    </step>
    <step>
     <para>To enable SSH access to the instances:</para>
     <substeps>
      <step>
       <para>Set <guimenu>Rule</guimenu> to <literal>SSH</literal>. </para>
      </step>
      <step>
       <para>Decide whether to allow traffic to instances only from IP addresses
        inside the cloud (from other group members) or from
         <emphasis>all</emphasis> IP addresses. </para>
       <itemizedlist>
        <listitem>
         <para>To enable access from <emphasis>all</emphasis> IP addresses
          (specified as IP subnet in CIDR notation as
           <literal>0.0.0.0/0</literal>), leave the <guimenu>Remote</guimenu>
          and <guimenu>CIDR</guimenu> fields unchanged.</para>
        </listitem>
        <listitem>
         <para>Alternatively, allow only IP addresses from other security groups
          to access the specified port. In that case, set
           <guimenu>Remote</guimenu> to <literal>Security Group</literal>.
          Select the desired <guimenu>Security Group</guimenu> and
           <guimenu>Ether Type</guimenu><!--taroth 2014-01-23: SIC!-->
           (<literal>IPv4</literal> or <literal>IPv6</literal>).</para>
        </listitem>
       </itemizedlist>
      </step>
     </substeps>
    </step>
    <step>
     <para> To enable pinging the instances: </para>
     <substeps>
      <step>
       <para> Set <guimenu>Rule</guimenu> to <literal>ALL ICMP</literal>.</para>
      </step>
      <step>
       <para>Decide whether to allow traffic to instances only from IP addresses
        inside the cloud (from other group members) or from
         <emphasis>all</emphasis> IP addresses. </para>
       <itemizedlist>
        <listitem>
         <para>To enable access from <emphasis>all</emphasis> IP addresses
          (specified as IP subnet in CIDR notation as
           <literal>0.0.0.0/0</literal>), leave the <guimenu>Remote</guimenu>
          and <guimenu>CIDR</guimenu> fields unchanged.</para>
        </listitem>
        <listitem>
         <para>Alternatively, allow only IP addresses from other security groups
          to access the specified port. In that case, set
           <guimenu>Remote</guimenu> to <literal>Security Group</literal>.
          Select the desired <guimenu>Security Group</guimenu> and
           <guimenu>Ether Type</guimenu><!--taroth 2014-01-23: SIC!-->
           (<literal>IPv4</literal> or <literal>IPv6</literal>).</para>
        </listitem>
       </itemizedlist>
      </step>
     </substeps>
    </step>
    <step>
     <para> To enable access via a UDP port (for example, for syslog): </para>
     <substeps>
      <step>
       <para>Set <guimenu>Rule</guimenu> to <literal>Custom UDP</literal>.
       </para>
      </step>
      <step>
       <para>Leave the <guimenu>Direction</guimenu> and <guimenu>Open
         Port</guimenu> values untouched. </para>
      </step>
      <step>
       <para> In the <guimenu>Port</guimenu> input field, enter the value
         <guimenu>514</guimenu>. </para>
      </step>
      <step>
       <para>Decide whether to allow traffic to instances only from IP addresses
        inside the cloud (from other group members) or from
         <emphasis>all</emphasis> IP addresses. </para>
       <itemizedlist>
        <listitem>
         <para>To enable access from <emphasis>all</emphasis> IP addresses
          (specified as IP subnet in CIDR notation as
           <literal>0.0.0.0/0</literal>), leave the <guimenu>Remote</guimenu>
          and <guimenu>CIDR</guimenu> fields unchanged.</para>
        </listitem>
        <listitem>
         <para>Alternatively, allow only IP addresses from other security groups
          to access the specified port. In that case, set
           <guimenu>Remote</guimenu> to <literal>Security Group</literal>.
          Select the desired <guimenu>Security Group</guimenu> and
           <guimenu>Ether Type</guimenu><!--taroth 2014-01-23: SIC!-->
           (<literal>IPv4</literal> or <literal>IPv6</literal>).</para>
        </listitem>
       </itemizedlist>
      </step>
     </substeps>
    </step>
   </procedure>
  </sect1>
  
  </chapter>
</book>
