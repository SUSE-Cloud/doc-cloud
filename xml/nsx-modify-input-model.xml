<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entity-decl.ent"> %entities;
]>
<section xml:id="nsx-modify-input-model"
 xmlns="http://docbook.org/ns/docbook" version="5.1"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Modify the Input Model</title>
 <para>
  After the third-party import has completed successfully, modify the input
  model:
 </para>
 <procedure>
  <step>
   <para>
    Prepare for input model changes
   </para>
  </step>
  <step>
   <para>
    Define the servers and server roles needed for a NSX-v cloud.
   </para>
  </step>
  <step>
   <para>
    Define the necessary networks and network groups
   </para>
  </step>
  <step>
   <para>
    Specify the services needed to be deployed on the &lcm; controllers and the
    &o_comp; ESX compute proxy nodes.
   </para>
  </step>
  <step>
   <para>
    Commit the changes and run the configuration processor.
   </para>
  </step>
 </procedure>
 <xi:include href="nsx-prepare-input-model-changes.xml"/>
 <xi:include href="nsx-create-input-model.xml"/>
 <xi:include href="commit-config_processor.xml"/>
</section>
