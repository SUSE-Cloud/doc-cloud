<?xml version="1.0"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<!--Edit status: not edited-->
<section xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" xml:id="preinstall_overview">
 <title>Pre-Installation Overview</title>
 <para>
  We have a <xref linkend="preinstall_checklist"/> we recommend going through
  to ensure your environment meets the requirements of the Cloud model you
  choose.
 </para>
 <bridgehead renderas="sect4">&kw-hos-phrase; Installation</bridgehead>
 <para>
  Once you have an idea of the configuration you want to choose for your cloud
  and you have gone through the pre-installation steps, you have two options
  for installation. You can use a GUI that runs in your web browser or you can
  install via the command-line that exposes the full power and flexibility of
  &kw-hos-phrase;.
 </para>
 <para>
  <emphasis role="bold">Using the GUI</emphasis>
 </para>
 <para>
  You should use the GUI if:
 </para>
 <itemizedlist>
  <listitem>
   <para>
    You are not planning to deploy availability zones or use L3 segmentation in
    your initial deployment.
   </para>
  </listitem>
  <listitem>
   <para>
    You are happy with the tuned HPE default OpenStack configurations.
   </para>
  </listitem>
 </itemizedlist>
 <para>
  Instructions for installing via the GUI are here.
 </para>
 <itemizedlist>
  <listitem>
   <para>
    <!-- FIXME: <xref href="gui_installer.xml#install_gui">Installing via the GUI</xref> -->
   </para>
  </listitem>
 </itemizedlist>
 <para>
  Note that reconfiguration of your cloud, at the moment, can only be done via
  the command-line. The GUI installer is for initial installation only.
 </para>
 <xi:include href="installation-installation-preinstall_checklist.xml"/>
 <xi:include href="installation-installation-using_git.xml"/>
</section>
