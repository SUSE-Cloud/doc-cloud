<?xml version="1.0"?>
<!DOCTYPE part [
 <!ENTITY % entities SYSTEM "entities.ent"> %entities;
]>
<part xml:id="preinstall"
 xmlns="http://docbook.org/ns/docbook" version="5.1"
 xmlns:xi="http://www.w3.org/2001/XInclude"
 xmlns:xlink="http://www.w3.org/1999/xlink">
 <title>Pre-Installation</title>
 <chapter xml:id="preinstall_overview">
  <title>Overview</title>
  <para>
   To ensure that your environment meets the requirements of the cloud model you
   choose, see the check list in <xref linkend="preinstall_checklist"/>.
  </para>
  <para>
   After you have decided on a configuration to choose for your cloud
   and you have gone through the pre-installation steps, you will have two options
   for installation:
  </para>
  <itemizedlist>
   <listitem>
    <para>
     You can use a graphical user interface (GUI) that runs in your Web browser.
    </para>
   </listitem>
   <listitem>
    <para>
     You can install via the command line that gives you the flexibility and
     full control of &kw-hos-phrase;.
    </para>
   </listitem>
  </itemizedlist>
  <para>
   <emphasis role="bold">Using the GUI</emphasis>
  </para>
  <para>
   You should use the GUI if:
  </para>
  <itemizedlist>
   <listitem>
    <para>
     You are not planning to deploy availability zones or use L3 segmentation
     in your initial deployment.
    </para>
   </listitem>
   <listitem>
    <para>
     You are satisfied with the tuned &vendor;-default &ostack; configuration.
    </para>
   </listitem>
  </itemizedlist>
  <para>
   Instructions for GUI installation are in <xref linkend="install_gui"/>.
  </para>
  <note>
   <para>
    Reconfiguring your cloud can only be done via the command line. The GUI
    installer is for initial installation only.
   </para>
  </note>
  <para>
   <emphasis role="bold">Using the Command Line</emphasis>
  </para>
  <para>
   You should use the command line if:
  </para>
  <itemizedlist xml:id="idg-installation-installation-installation_overview-xml-7">
   <listitem>
    <para>
     You are installing a complex or large-scale cloud.
    </para>
   </listitem>
   <listitem>
    <para>
     You need to use availability zones or the server groups functionality of
     the cloud model. For more information, see the <xref
     linkend="cha.input-model-intro-concept"/>.
    </para>
   </listitem>
   <listitem>
    <para>
     You want to customize the cloud configuration beyond the tuned defaults
     that &vendor; provides out of the box.
    </para>
   </listitem>
   <listitem>
    <para>
     You need more extensive customizations than are possible using the GUI.
    </para>
   </listitem>
  </itemizedlist>
  <para>
   Instructions for installing via the command line are in <xref linkend="install_kvm"/>.
  </para>
  <note>
   <para>
    Ardana is an open-source project and a generalized lifecycle management
    framework.  &clm; is based on Ardana, and delivers the lifecycle management
    functionality required by &productname; &productnumber;. Due to this
    relationship, some &clm; commands refer to Ardana.
   </para>
  </note>
 </chapter>
 <xi:include href="installation-installation-preinstall_checklist.xml"/>
 <xi:include href="preinstall-prepare-deployer.xml" />
 <xi:include href="preinstall-smt-setup.xml" />
 <xi:include href="preinstall-configure-deployer-repos.xml" />
 <xi:include href="installation-installation-multipath_boot_from_san.xml"/>
</part>
