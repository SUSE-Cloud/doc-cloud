<?xml version="1.0"?>
<?xml-stylesheet href="urn:x-suse:xslt:profiling:docbook51-profile.xsl"
 type="text/xml"
 title="Profiling step"?>
<!DOCTYPE section [
 <!ENTITY % entities SYSTEM "entity-decl.ent"> %entities;
]>

<section xmlns="http://docbook.org/ns/docbook" xmlns:xi="http://www.w3.org/2001/XInclude" xmlns:xlink="http://www.w3.org/1999/xlink" version="5.0">
 <title>Log File Handling</title>
 <para>
  In case of trouble with the &productmonitoring;
  services, you can study their log files to find the reason. The log files are
  also useful if you need to contact your support organization. For storing the
  log files, the default installation uses the <literal>/var/log</literal>
  directory on the hosts where the agents or services are installed.
 </para>
 <para>
  You can use <literal>systemd</literal>, a system and session manager for
  LINUX, and <literal>journald</literal>, a LINUX logging interface, for
  addressing dispersed log files.
 </para>
 <para>
  The &productmonitoring; installer automatically
  puts all &productmonitoring; services under the
  control of <literal>systemd</literal>. <literal>journald</literal> provides a
  centralized management solution for the logging of all processes that are
  controlled by <literal>systemd</literal>. The logs are collected and managed
  in a so-called journal controlled by the <literal>journald</literal> daemon.
 </para>
 <para>
  For details on the <literal>systemd</literal> and <literal>journald</literal>
  utilities, refer to the
  <link xlink:href="https://www.suse.com/documentation/sles-12/singlehtml/book_sle_admin/book_sle_admin.html#part.system">SUSE Linux Enterprise Server documentation</link>.
 </para>
</section>
