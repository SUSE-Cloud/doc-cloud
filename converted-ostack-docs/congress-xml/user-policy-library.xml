<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xi="http://www.w3.org/2001/XInclude" version="5.1">
  <title>Policy Library</title>
  <para>Congress bundles a library of useful policies to help users get started.</para>
  <para>For example, the <literal>library/volume_encryption/servers_unencrypted_volume.yaml</literal>
            identifies and warns on servers with unencrypted volumes attached.</para>
  <screen><?dbsuse-fo font-size="8pt"?>---
name: VolumeEncryption
description: "Warn/error on servers with unencrypted volumes attached."
depends-on:
  - SecurityGroups
rules:
  -
    rule: &gt;
      servers_with_unencrypted_volume(server_id, server_name, volume_id, volume_name) :-
        nova:servers(id=server_id, name=server_name),
        cinder:attachments(volume_id=volume_id, server_id=server_id),
        cinder:volumes(id=volume_id, name=volume_name, encrypted=False)
  -
    comment: "Warn on servers with unencrypted volume."
    rule: &gt;
      warning(server_id, server_name, volume_id, volume_name) :-
        servers_with_unencrypted_volume(server_id, server_name, volume_id, volume_name)

  -
    comment: "Servers with unencrypted volume, which is also not covered by
      a protected security group."
    rule: &gt;
      unprotected_servers_with_unencrypted_volume(server_id, server_name, volume_id, volume_name) :-
        servers_with_unencrypted_volume(server_id, server_name, volume_id, volume_name),
        SecurityGroups:unprotected_servers(server_id)
  -
    comment: "Error on servers with unencrypted volume, which is also not covered by
      a protected security group."
    rule: &gt;
      error(server_id, server_name, volume_id, volume_name) :-
        unprotected_servers_with_unencrypted_volume(server_id, server_name, volume_id, volume_name)
</screen>
  <para>The latest collection library policies can be found here:
            <link xlink:href="https://github.com/openstack/congress/tree/master/library"/></para>
  <para>To import a library policy YAML file into Congress, use the following CLI
            command (python-congressclient version 1.8.0 or higher
            <link xlink:href="https://pypi.python.org/pypi/python-congressclient/"/>).</para>
  <screen language="console">$ openstack congress policy create-from-file &lt;path-to-policy-yaml&gt;</screen>
</section>
