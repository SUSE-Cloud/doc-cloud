<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xi="http://www.w3.org/2001/XInclude" version="5.1">
  <title>Installing development sandbox</title>
  <para>In a development environment created by <link xlink:href="https://docs.openstack.org/devstack/latest/">devstack</link>, Ceilometer can be tested
            alongside other OpenStack services.</para>
  <section>
    <title>Configuring devstack</title>
    <procedure>
      <step>
        <para>Download <link xlink:href="https://docs.openstack.org/devstack/latest/">devstack</link>.</para>
      </step>
      <step>
        <para>Create a <literal>local.conf</literal> file as input to devstack.</para>
      </step>
      <step>
        <para>The ceilometer services are not enabled by default, so they must be
                        enabled in <literal>local.conf</literal> but adding the following:</para>
        <screen># Enable the Ceilometer devstack plugin
enable_plugin ceilometer https://git.openstack.org/openstack/ceilometer.git</screen>
        <para>By default, all ceilometer services except for ceilometer-ipmi agent will
                        be enabled</para>
      </step>
      <step>
        <para>Enable Gnocchi storage support by including the following in <literal>local.conf</literal>:</para>
        <screen>CEILOMETER_BACKEND=gnocchi</screen>
        <para>Optionally, services which extend Ceilometer can be enabled:</para>
        <screen>enable_plugin aodh https://git.openstack.org/openstack/aodh
enable_plugin panko https://git.openstack.org/openstack/panko</screen>
        <para>These plugins should be added before ceilometer.</para>
      </step>
      <step>
        <para>
          <literal>./stack.sh</literal>
        </para>
      </step>
    </procedure>
  </section>
</section>
