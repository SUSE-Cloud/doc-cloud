<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<section xmlns="http://docbook.org/ns/docbook" xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xi="http://www.w3.org/2001/XInclude" version="5.1">
  <title>DNS service overview</title>
  <para>The DNS service provides DNS Zone and RecordSet management for OpenStack
            clouds. The DNS Service includes a REST API, a command-line client, and a
            Horizon Dashboard plugin.</para>
  <para>The DNS service consists of the following components:</para>
  <variablelist>
    <varlistentry>
      <term><literal>openstack</literal> command-line client plugin</term>
      <listitem>
        <para>A plugin for the OpenStack Client CLI that communicates with the REST API</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term><literal>designate-api</literal> component</term>
      <listitem>
        <para>An OpenStack-native REST API that processes API requests by sending
                        them to the <literal>designate-central</literal> over Remote Procedure Call (RPC).</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term><literal>designate-central</literal> component</term>
      <listitem>
        <para>Orchestrates the creation, deletion and update of Zones and RecordSets, and
                        delegates work to <literal>designate-pool-manager</literal> for interaction with the DNS
                        servers</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term><literal>designate-producer</literal> component</term>
      <listitem>
        <para>Orchestrates periodic tasks that are run by designate.</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term><literal>designate-worker</literal> component</term>
      <listitem>
        <para>Is a generic task runner, that runs both zone create / update and deletes,
                        and periodic tasks, from <literal>designate-producer</literal></para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term><literal>designate-mdns</literal> component</term>
      <listitem>
        <para>A small DNS Server that is responsible for pushing DNS Zone information to
                        the customer facing DNS Servers. Can also pull in DNS information about
                        DNS Zones hosted outside of the Designate infrastructure</para>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term><literal>designate-agent</literal> component</term>
      <listitem>
        <para>A small python daemon that can be used for a limited sub set of DNS Servers
                        Some DNS Servers requrire commands be run locally, and to do this we use
                        this component.</para>
        <note>
          <para>The majority of the DNS service installs will not need this
                            component.</para>
        </note>
      </listitem>
    </varlistentry>
    <varlistentry>
      <term>
        <literal>Customer Facing DNS Servers</literal>
      </term>
      <listitem>
        <para>Serves DNS requests to end users. They are orchestreated by the
                        <literal>designate-worker</literal>, and the supported list is maintained <link xlink:href="https://docs.openstack.org/designate/latest/contributor/support-matrix.html">here</link></para>
      </listitem>
    </varlistentry>
  </variablelist>
</section>
