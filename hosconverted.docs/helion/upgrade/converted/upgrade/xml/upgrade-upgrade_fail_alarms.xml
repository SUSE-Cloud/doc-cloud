<?xml version="1.0"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [ <!ENTITY % entities SYSTEM "entities.xml"> %entities; ]><!--Edit status: not edited-->
<section id="upgrade_fail_alarms">
   <title>Upgrade Can Fail while Creating or Removing Alarms</title>
        
        <para>If you experience a failure during upgrade from <phrase/> to <phrase/> 
             
            and the error indicates it failed while removing or creating alarm definitions, you should check that the <literal>monasca-api</literal> 
            is working and then restart the upgrade.</para>

   <para>
           Check if <literal>alarm-list</literal> works (it should output a list of alarms):
</para>
<screen>          
source ~/service.osrc
monasca alarm-list 
</screen>

   <para>
            
            If the <literal>monasca-api</literal> is not working, then restart the <literal>monasca-api</literal>:

</para>
<screen> 
cd ~/scratch/ansible/next/hos/ansible
ansible-playbook -i hosts/verb_hosts monasca-stop.yml --tags monasca-api
ansible-playbook -i hosts/verb_hosts monasca-start.yml --tags monasca-api
</screen>
<para>
            
            Check that <literal>alarm-list</literal> works and restart the upgrade.
            
          </para>

      
          
          
          
  </section>
