<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="DNS_NS">
  <title><ph conkeyref="HOS-conrefs/product-title"/>Configure DNS Domain and NS Records</title>
  <body>
    <p conkeyref="HOS-conrefs/applies-to"/>
    <section id="cgf_dns_ns">
      <title>Configure DNS Domain and NS Records</title>
      <p>To configure the default DNS domain and Name Server records for the default pool, follow
        these steps. <ol>
          <li>Ensure that <codeph>designate_config.yml</codeph> file is present in the
              <codeph>~/helion/my_cloud/definition/data/designate</codeph> folder. If the file or
            folder is not present, create the folder and copy <codeph>designate_config.yml</codeph>
            file from one of the example input models (for example,
              <codeph>~/helion/examples/entry-scale-esx-kvm-vsa/data/designate/designate_config.yml</codeph>). </li>
          <li>Modify the <b>dns_domain</b> and/or <b>ns_records</b> entries in the
              <codeph>designate_config.yml</codeph> file.
            <codeblock>data:
dns_domain: example.org.
ns_records:
    hostname: ns1.example.org.
    priority: 1  
    hostname: ns2.example.org.
    priority: 2</codeblock></li>
          <li>Edit your input model's <codeph>control_plane.yml</codeph> file to include
              <b>DESIGNATE-CONFIG-CP1</b> in <b>configuration-data</b> section.
            <codeblock>control-planes:
   - name: control-plane-1
     region-name: region1
     lifecycle-manager-target
     configuration-data:
        - DESIGNATE-CONFIG-CP1
        - NEUTRON-CONFIG-CP1</codeblock>
          </li>
          <li>Continue your cloud deployment by reviewing and committing your changes.
            <codeblock>$ git add ~/helion/my_cloud/definition/data/designate/designate_config.yml
$ git commit -m "Adding DNS Domain and NS Records"</codeblock>
          </li>
        </ol>
        <note>In an <xref keyref="entryscale_kvm_vsa">entry-scale model</xref>, you will have 3
          ns_records since the DNS service runs on all three control planes. In a <xref
            keyref="midscale_kvm_vsa">mid-scale model</xref> or <xref
            keyref="entryscale_kvm_dedicated">dedicated metering, monitoring and logging
            model</xref> the above example would be correct since there are only two controller
          nodes.</note>
      </p>
    </section>



  </body>
</topic>
