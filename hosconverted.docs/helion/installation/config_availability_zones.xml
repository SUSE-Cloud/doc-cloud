<?xml version="1.0" encoding="UTF-8"?>
<!--Edit status: not edited-->
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="config_availability_zones">
    <title><ph conkeyref="HOS-conrefs/product-title"/>Configuring Availability Zones</title>
    <abstract><shortdesc outputclass="hdphidden"> Availability Zones are not configured by default
            during installation. Use <codeph>nova-cloud-configure.yml</codeph> to configure them
            post-install. </shortdesc></abstract>
    <body>
        <p conkeyref="HOS-conrefs/applies-to"/>

        <section>
            <p>The lifecycle manager only creates a default availability zone during installation.
                If your system has multiple failure/availability zones defined in your input model,
                these zones will not get created automatically. </p>
            <p> Once the installation has finished, you can run the
                    <codeph>nova-cloud-configure.yml</codeph> playbook to configure availability
                zones and assign compute nodes to those zones based on the configuration specified
                in the model. </p>
            <p>You can run the playbook <codeph>nova-cloud-configure.yml</codeph> any time you make
                changes to the configuration of availability zones in your input model.
                Alternatively, you can use Horizon or the command line to perform the configuration. </p>
            <p>For more details, see <xref href="../operations/compute/creating_aggregates.xml"
                />.</p>
        </section>

    </body>
</topic>
