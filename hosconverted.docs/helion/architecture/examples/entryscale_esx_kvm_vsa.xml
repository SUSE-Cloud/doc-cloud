<?xml version="1.0" encoding="UTF-8"?>
<!--Edit status: not edited-->
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="example_entryscale_esx">
    <title><ph conkeyref="HOS-conrefs/product-title"/>Entry-scale ESX, KVM with VSA Model</title>
    <body>
        <!--not tested-->
        <p conkeyref="HOS-conrefs/applies-to"/>
        <p>This example shows how to integrate <keyword keyref="kw-hos"/> with ESX, KVM with VSA in
            the same Cloud. The controller configuration is essentially the same as in the
            Entry-scale KVM with VSA Model example, but the resource nodes supported are ESX
            (provided by vCenter), KVM and VSA. In addition, a number of controller virtual machines
            are created for each vCenter cluster: one ESX Compute virtual machine (which provides
            the nova-compute proxy for vCenter) and one OVSvApp virtual machine per cluster member
            (which provides network access). These virtual machines are created automatically by
                <keyword keyref="kw-hos"/> as part of activating the vCenter cluster, and are
            therefore not defined in the example.</p>
        <p><image href="../../../media/hos.docs/exampleconfigs/Entry-ScaleESX-KVMwithVSAmodel.png"
            /></p>
        <p><xref href="../../../media/hos.docs/exampleconfigs/Entry-ScaleESX-KVMwithVSAmodel.png"
                  format="html">Download</xref></p>
        <p product="HOS"><xref href="./../../../media/templates/HOS_Network_Diagram_Template.zip"  
                format="html">Download Editable Visio Network Diagram Template</xref></p>
        <p>The physical networking configuration is also largely the same as the KVM example, with
            the default GUEST network VxLAN as the Neutron networking model.</p>
        <p>A separate configuration network (CONF) is required for configuration access from the
            lifecycle manager. This network must be reachable from the Management network.</p>

        <section
            conref="../../planning/rec_min_entryscale_esx_kvm_vsa.xml#rec_min_entryscale_esx_kvm_vsa/rec_min"/>
    </body>
</topic>
