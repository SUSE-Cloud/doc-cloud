<?xml version="1.0"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [ <!ENTITY % entities SYSTEM "entities.xml"> %entities; ]><!--Edit status: not edited-->
<section id="horizonTimeout">
   <title>
      <phrase/>Changing the Dashboard Timeout Value</title>
    <para/>
    <para>The default session timeout for the dashboard is 1800 seconds or 30 minutes. This is the
        recommended default and best practice for those concerned with security.</para>
   <para>As an administrator, you can change the session timeout by changing the value of the
        SESSION_TIMEOUT to anything less than or equal to 14400, which is equal to four hours.
        Values greater than 14400 should not be used due to Keystone constraints.</para>

    
      <bridgehead renderas="sect4">How to Change the Dashboard Timeout Value</bridgehead>
      <para>Follow these steps to change and commit the Horizon timeout value.</para>
      <orderedlist>
        <listitem>
            <para>Log in to the lifecycle manager.</para>
         </listitem>
        <listitem>
            <para>Edit the Dashboard config file at
            <literal>~/helion/my_cloud/config/horizon/local_settings.py</literal> and, if it's not
          already present, add a line for <literal>SESSION_TIMEOUT</literal> above the line for
            <literal>SESSION_ENGINE</literal>. </para>
            <para>Here is an example snippet, in bold:</para>
            <screen>
               <emphasis role="bold">SESSION_TIMEOUT = &lt;timeout value&gt;</emphasis>
SESSION_ENGINE = 'django.contrib.sessions.backends.db'</screen>
            <important>
               <para>Do not exceed the maximum value of 14400.</para>
            </important>
         </listitem>
        <listitem>
            <para>Commit the changes to git:
          </para>
            <screen>git add -A
git commit -a -m "changed Horizon timeout value"</screen>
         </listitem>
        <listitem>
            <para>Run the configuration processor:
          </para>
            <screen>cd ~/helion/hos/ansible
ansible-playbook -i hosts/localhost config-processor-run.yml</screen>
         </listitem>
        <listitem>
            <para>Update your deployment directory:
          </para>
            <screen>cd ~/helion/hos/ansible
ansible-playbook -i hosts/localhost ready-deployment.yml</screen>
         </listitem>
        <listitem>
            <para>Run the Dashboard reconfigure playbook:
          </para>
            <screen>cd ~/scratch/ansible/next/hos/ansible
ansible-playbook -i hosts/verb_hosts horizon-reconfigure.yml</screen>
         </listitem>
      </orderedlist>
   
  </section>
