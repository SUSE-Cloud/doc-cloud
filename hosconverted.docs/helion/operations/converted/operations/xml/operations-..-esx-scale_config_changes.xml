<?xml version="1.0"?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [ <!ENTITY % entities SYSTEM "entities.xml"> %entities; ]><!--Edit status: not edited-->
<section id="topic_ijt_dyh_rt">
   <title>
      <phrase/>Making Scale Configuration Changes</title>
    
    <para>This procedure describes how to make the recommended configuration changes to achieve 8,000
      virtual machine instances. <!--changed to 8,000 per DOCS-2988--></para>

    <note>
         <para>In a scale environment for ESX computes, the configuration of vCenter Proxy VM has to be
        increased to 8 vCPUs and 16 GB RAM. By default it is 4 vCPUs and 4 GB RAM. </para>
      </note>
<orderedlist id="idg-operations----esx-scale_config_changes-dita-2">
        <listitem>
            <para>Change the directory. The <literal>nova.conf.j2</literal> file is present in following
          directories:</para>
            <screen>cd /home/stack/helion/hos/ansible/roles/nova-common/templates</screen>
         </listitem>
        <listitem>
            <para>Edit the DEFAULT section in the <literal>nova.conf.j2</literal> file as
          below:</para>
            <screen>[DEFAULT]
rpc_responce_timeout = 180
server_down_time = 300
report_interval = 30</screen>
         </listitem>
        <listitem>
            <para>Commit your
          configuration:</para>
            <screen>cd ~/helion/hos/ansible
git add -A
git commit -m "&lt;commit message&gt;"</screen>
         </listitem>
        <listitem>
            <para>Prepare your environment for
          deployment:</para>
            <screen>ansible-playbook -i hosts/localhost ready-deployment.yml;
cd /home/stack/scratch/ansible/next/hos/ansible;</screen>
         </listitem>
        <listitem>
            <para>Execute the <literal>nova-reconfigure</literal>
          playbook:</para>
            <screen>ansible-playbook -i hosts/verb_hosts nova-reconfigure.yml</screen>
         </listitem>
      </orderedlist>

  </section>
