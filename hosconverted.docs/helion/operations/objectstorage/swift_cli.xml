<?xml version="1.0" encoding="UTF-8"?>
<!--Edit status: not edited-->
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="topic_m13_dgp_nt">
  <title><ph conkeyref="HOS-conrefs/product-title"/>Using the Swift Command-line Client
    (CLI)</title>
  <body><!--not tested-->
    <p conkeyref="HOS-conrefs/applies-to"/>
    <p>The <codeph>swift</codeph> utility (or Swift CLI) is installed on the lifecycle manager node
      and also on all other nodes running the Swift proxy service. To use this utility on the
      lifecycle manager, you can use the <codeph>~/service.osrc</codeph> file as a basis and then
      edit it with the credentials of another user if you need
      to.<codeblock>cp ~/service.osrc ~/swiftuser.osrc</codeblock></p>
    <p>Then you can use your preferred editor to edit swiftuser.osrc so you can authenticate using
      the <codeph>OS_USERNAME</codeph>, <codeph>OS_PASSWORD</codeph>, and
        <codeph>OS_PROJECT_NAME</codeph> you wish to use.  For example, if you'd like to use the
        <codeph>demo</codeph> user that is created automatically for you, then it might look like
      this:</p>
    <codeblock>unset OS_DOMAIN_NAME
export OS_IDENTITY_API_VERSION=3
export OS_AUTH_VERSION=3
export OS_PROJECT_NAME=demo
export OS_PROJECT_DOMAIN_NAME=Default
export OS_USERNAME=demo
export OS_USER_DOMAIN_NAME=Default
export OS_PASSWORD=&lt;password>
export OS_AUTH_URL=&lt;auth_URL>
export OS_ENDPOINT_TYPE=internalURL
# OpenstackClient uses OS_INTERFACE instead of OS_ENDPOINT
export OS_INTERFACE=internal
export OS_CACERT=/etc/ssl/certs/ca-certificates.crt
export OS_COMPUTE_API_VERSION=2</codeblock>
    <p>You must use the appropriate password for the demo user and select the correct endpoint for
      the <b>OS_AUTH_URL</b> value, which should be in the <codeph>~/service.osrc</codeph> file you
      copied. </p>
    <p>You can then examine the following account data using this
      command:<codeblock>swift stat</codeblock></p>
    <p>Example showing an environment with no containers or objects:</p>
    <p>
      <codeblock>$ swift stat
        Account: AUTH_205804d000a242d385b8124188284998
     Containers: 0
        Objects: 0
          Bytes: 0
X-Put-Timestamp: 1442249536.31989
     Connection: keep-alive
    X-Timestamp: 1442249536.31989
     X-Trans-Id: tx5493faa15be44efeac2e6-0055f6fb3f
   Content-Type: text/plain; charset=utf-8</codeblock>
    </p>
    <p>Use the following command and create a
      container:<codeblock>swift post &lt;container_name></codeblock></p>
    <p>Example, creating a container named <codeph>documents</codeph>:</p>
    <p>
      <codeblock>swift post documents</codeblock>
    </p>
    <p>The newly created container appears. But there are no
      objects:<codeblock>$ swift stat documents
         Account: AUTH_205804d000a242d385b8124188284998
       Container: documents
         Objects: 0
           Bytes: 0
        Read ACL:
       Write ACL:
         Sync To:
        Sync Key:
   Accept-Ranges: bytes
X-Storage-Policy: General
      Connection: keep-alive
     X-Timestamp: 1442249637.69486
      X-Trans-Id: tx1f59d5f7750f4ae8a3929-0055f6fbcc
    Content-Type: text/plain; charset=utf-8</codeblock></p>
    <p>Upload a document:<codeblock>swift upload &lt;container_name> &lt;filename></codeblock></p>
    <p>Example:</p>
    <p>
      <codeblock>$ swift upload documents mydocument
mydocument</codeblock>
    </p>
    <p>List objects in the container:<codeblock>swift list &lt;container_name></codeblock></p>
    <p>Example:</p>
    <p>
      <codeblock>$ swift list documents
mydocument</codeblock>
    </p>
    <p>
      <note>This is a brief introduction to the <codeph>swift</codeph> CLI. Use the <codeph>swift
          --help</codeph> command for more information.  You can also use the OpenStack CLI, see
          <codeph>openstack -h</codeph> for more information.</note>
    </p>
  </body>
</topic>
