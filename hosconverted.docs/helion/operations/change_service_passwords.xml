<?xml version="1.0" encoding="UTF-8"?>
<!--Edit status: not edited-->
<!DOCTYPE topic PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd">
<topic id="servicePasswords">
  <title><ph conkeyref="HOS-conrefs/product-title"/>Changing Service Passwords</title>
  <abstract><shortdesc outputclass="hdphidden"><keyword keyref="kw-hos-phrase"/> provides a process
      for changing the default service passwords, including your admin user password, which you may
      want to do for security or other purposes.</shortdesc><keyword keyref="kw-hos-phrase"/>
    provides a process for changing the default service passwords, including your admin user
    password, which you may want to do for security or other purposes.</abstract>
  <body>
    <!--not tested-->
    <p conkeyref="HOS-conrefs/applies-to"/>
    <section>
      <p>You can easily change the inter-service passwords used for authenticating communications
        between services in your HPE Helion cloud deployment, promoting better compliance with your
        organization’s security policies. The inter-service passwords that can be changed include
        (but are not limited to) Keystone, MySQL, RabbitMQ, HLM cluster, Monasca and Barbican.</p>
      <p>The general process for changing the passwords is to:</p>
      <ul>
        <li>Indicate to the configuration processor which password(s) you want to change, and
          optionally include the value of that password </li>
        <li>Run the configuration processor to generate the new passwords (you do not need to run
          "git add" before this) </li>
        <li>Run ready-deployment </li>
        <li>Check your password name(s) against the tables included below to see which high-level
          credentials-change playbook(s) you need to run </li>
        <li>Run the appropriate high-level credentials-change playbook(s)</li>
      </ul>
    </section>
    
    
    <section id="password_strength">
      <title>Password Strength</title>
      <p>Encryption passwords supplied to the configuration processor for use with Ansible Vault and
        for encrypting the configuration processor’s persistent state must have a minimum length of
        12 characters and a maximum of 128 characters. Passwords must contain characters from each
        of the following three categories:</p>
      <ul>
        <li>Uppercase characters (A-Z)</li>
        <li>Lowercase characters (a-z)</li>
        <li>Base 10 digits (0-9)</li>
      </ul>
      <!-- Passwords should not included these
      <p>Passwords can optionally contain punctuation characters:
          <codeph>~!@#$%^&amp;*_-+=`|\(){}[]:;"'&lt;,.?/</codeph>
      </p>
      -->
      <p>Service Passwords that are automatically generated by the configuration processor are
        chosen randomly from uppercase, lowercase and numeric characters, with the minimum and
        maximum lengths being determined by the specific requirements of individual services.</p>
      <note type="important">Currently, you can not use any special characters with Ansible Vault,
        Service Passwords, or eon/vCenter configuration.</note>
    </section>
    
    
    <section>
      <title>Telling the configuration processor which password(s) you want to change</title>
      <p>In <keyword keyref="kw-hos-phrase"/>, the configuration processor will produce metadata
        about each of the passwords (and other variables) that it generates in the file
          <codeph>~/helion/my_cloud/info/private_data_metadata.yml</codeph>. A snippet of this file
        follows. Expand the header to see the file:</p>
    </section>
    <section id="privateMetadata"><title outputclass="headerH">private_data_metadata.yml</title>
      <sectiondiv outputclass="insideSection">
        <codeblock outputclass="nomaxheight">monitor_user_password:
    metadata:
    - clusters:
        - cluster1
        component: vertica
        cp: ccp
    version: '2.0'
mysql_admin_password:
    metadata:
    - clusters:
        - cluster1
        component: nova-api
        consumes: mysql
        cp: ccp
    - clusters:
        - cluster1
        component: neutron-server
        consumes: mysql
        cp: ccp
    - clusters:
        - cluster1
        component: keystone-api
        consumes: mysql
        cp: ccp
    - clusters:
        - cluster1
        component: cinder-api
        consumes: mysql
        cp: ccp
    - clusters:
        - cluster1
        component: glance-api
        consumes: mysql
        cp: ccp
    - clusters:
        - cluster1
        component: horizon
        consumes: mysql
        cp: ccp
    - clusters:
        - cluster1
        component: heat-engine
        consumes: mysql
        cp: ccp
    - clusters:
        - cluster1
        component: ceilometer-common
        consumes: mysql
        cp: ccp
    version: '2.0'
mysql_barbican_password:
    metadata:
    - clusters:
        - cluster1
        component: barbican-api
        consumes: mysql
        cp: ccp
    version: '2.0'</codeblock>
      </sectiondiv>
    </section>
    <section>
      <p>For each variable, there is a metadata entry for each pair of services that use the
        variable including a list of the clusters on which the service component that consumes the
        variable (defined as "component:" in <codeph>private_data_metadata.yml</codeph> above)
        runs.</p>
    </section>
    <section>
      <p>Note above that the variable mysql_admin_password is used by a number of service
        components, and the service that is consumed in each case is mysql.</p>
    </section>
    <section id="steps-to-change-password">
      <title>Steps to change a password</title>
      <p>First, make sure that you have a copy of private_data_metadata. If you don't, to generate
        one just run the configuration processor:</p>
      <codeblock>cd helion/hos/ansible
ansible-playbook -i hosts/localhost config-processor-run.yml</codeblock>
      <p>Make a copy of the <codeph>private_data_metadata.yml</codeph> file and place it into the
          <codeph>~/helion/change_credentials</codeph> directory:</p>
      <codeblock>cp ~/helion/my_cloud/info/private_data_metadata.yml ~/helion/change_credentials/</codeblock>
      <p>Edit the copied file in <codeph>~/helion/change_credentials</codeph> leaving only only
        those passwords you intend to change. All entries in this template file should be deleted
          <i>except for those passwords</i>. <note type="attention">If you leave other passwords in
          that file that you do <i>not</i> want to change, they will be regenerated and no longer
          match those in use.</note></p>
      <note>It is required that you change passwords in batches of each category listed
        below.</note>
      <p>For example, the snippet below would result in the configuration processor generating new
        random values for keystone_backup_password, keystone_ceilometer_password, and
        keystone_rgw_password:</p>
      <codeblock>keystone_backup_password:
    metadata:
    - clusters:
        - cluster0
        - cluster1
        - compute
        component: freezer-agent
        consumes: keystone-api
        cp: ccp
    version: '2.0'
keystone_ceilometer_password:
    metadata:
    - clusters:
        - cluster1
        component: ceilometer-common
        consumes: keystone-api
        cp: ccp
        version: '2.0'
keystone_rgw_password:
    metadata:
    - clusters:
        - rgw
        component: ceph-radosgw
        consumes: keystone-api
        cp: cpp
        version: '2.0'</codeblock>
    </section>
    <section id="specifying-password-value">
      <title>Specifying password value</title>
      <p>Optionally, you can specify a value for the password by including a "value:" key and value
        at the same level as metadata:</p>
      <codeblock>keystone_backup_password:
    value: 'new_password'
    metadata:
    - clusters:
        - cluster0
        - cluster1
        - compute
        component: freezer-agent
        consumes: keystone-api
        cp: ccp
        version: '2.0'</codeblock>
      <p>Note that you can have multiple files in helion/change_credentials. The configuration
        processor will only read files that end in .yml or .yaml.</p>
      <note>If you've specified a password value in your credential change file, you may want to
        encrypt it using ansible-vault. If you decide to encrypt with ansible-vault, make sure that
        you use the encryption key you've already used when running the configuration
        processor.</note>
      <p>To encrypt a file using ansible-vault, execute:</p>
      <codeblock>stack@padawan-ccp-c0-m1-mgmt:~/helion/change_credentials$ ansible-vault encrypt &lt;credential change file ending in .yml or .yaml></codeblock>
      <p>Be sure to provide the encryption key when prompted. Note that if you've specified the
        wrong ansible-vault password, the configuration-processor will error out with a message like
        the following:</p>
      <codeblock>################################################## Reading Persistent State ##################################################

################################################################################
# The configuration processor failed.
# PersistentStateCreds: User-supplied creds file test1.yml was not parsed properly
################################################################################</codeblock>
    </section>
    <section><title>Running the configuration processor to change passwords</title><p>The directory
        helion/change_credentials is not managed by git, so to rerun the configuration processor to
        generate new passwords and prepare for the next deployment just enter the following
        commands:</p><codeblock>cd ~/helion/hos/ansible
ansible-playbook -i hosts/localhost config-processor-run.yml
ansible-playbook -i hosts/localhost ready-deployment.yml</codeblock><note>The
        files that you placed in helion/change_credentials should be removed once you have run the
        configuration processor because the old password values and new password values will be
        stored in the configuration processor's persistent state. </note> Note that if you see
      output like the following after running the configuration processor:
      <codeblock>################################################################################
# The configuration processor completed with warnings.
# PersistentStateCreds: User-supplied password name 'blah' is not valid
################################################################################</codeblock>this
      tells you that the password name you have supplied, 'blah,' does not exist. A failure to
      correctly parse the credentials change file will result in the configuration processor
      erroring out with a message like the following:
      <codeblock>################################################## Reading Persistent State ##################################################

################################################################################
# The configuration processor failed.
# PersistentStateCreds: User-supplied creds file test1.yml was not parsed properly
################################################################################</codeblock>Once
      you've run the configuration processor to change passwords, an information file
        <codeph>~/helion/my_cloud/info/password_change.yml</codeph> similar to the
        <codeph>private_data_metadata.yml</codeph> is written to tell you which passwords have been
      changed, including metadata but not including the values. </section>
    <section><title>Password change playbooks and tables</title><p>Once you've completed the steps
        above to change password(s) value(s) and then prepare for the deployment that will actually
        switch over to the new passwords, you will need to run some high-level playbooks. The
        passwords that can be changed are grouped into six categories. The tables below list the
        password names that belong in each category. The categories are:</p><dl>
        <dlentry>
          <dt/>
          <dd/>
        </dlentry>
        <dlentry>
          <dt>Keystone</dt>
          <dd> Playbook: hlm-keystone-credentials-change.yml</dd>
        </dlentry>
        <dlentry>
          <dt>RabbitMQ</dt>
          <dd> Playbook: hlm-rabbitmq-credentials-change.yml</dd>
        </dlentry>
        <dlentry>
          <dt>MySQL</dt>
          <dd>Playbook: hlm-reconfigure.yml</dd>
        </dlentry>
        <dlentry>
          <dt>Cluster:</dt>
          <dd>Playbook: hlm-cluster-credentials-change.yml</dd>
        </dlentry>
        <dlentry>
          <dt>Monasca:</dt>
          <dd> Playbook: monasca-reconfigure-credentials-change.yml</dd>
        </dlentry>
        <dlentry>
          <dt>Other:</dt>
          <dd> Playbook: hlm-other-credentials-change.yml</dd>
        </dlentry>
      </dl>It is recommended that you change passwords in batches; in other words, run through a
      complete password change process for each batch of passwords, preferably in the above order.
      Once you have followed the process indicated above to change password(s), check the names
      against the tables below to see which password change playbook(s) you should run.
          <p><b>Changing identity service credentials</b></p> The following table lists identity
      service credentials you can change. <sectiondiv outputclass="width33">
        <table frame="all" rowsep="1" colsep="1" id="table_f3h_jmw_kv">
          <tgroup cols="1">
            <colspec colname="c1" colnum="1" colwidth="19em"/>
            <thead>
              <row>
                <entry>Keystone credentials </entry>
              </row>
            </thead>
            <tbody>
              <row>
                <entry>
                  <codeblock outputclass="whitebackground"><b>Password name</b>
barbican_admin_password
barbican_service_password
keystone_admin_pwd
keystone_admin_token
keystone_backup_password
keystone_ceilometer_password
keystone_cinder_password
keystone_cinderinternal_password
keystone_demo_pwd
keystone_designate_password
keystone_eon_password
keystone_freezer_password
keystone_glance_password
keystone_glance_swift_password
keystone_heat_password
keystone_magnum_password
keystone_monasca_agent_password
keystone_monasca_password
keystone_neutron_password
keystone_nova_password
keystone_octavia_password
keystone_swift_dispersion_password
keystone_swift_monitor_password
keystone_swift_password
logging_keystone_password
nova_monasca_password</codeblock>
                </entry>
              </row>
            </tbody>
          </tgroup>
        </table>
      </sectiondiv>The playbook to run to change Keystone credentials is
        <codeph>hlm-keystone-credentials-change.yml</codeph>. Execute the following commands to make
      the changes:
          <codeblock>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts hlm-keystone-credentials-change.yml</codeblock><p><b>Changing
          RabbitMQ credentials</b></p> The following table lists the RabbitMQ credentials you can
      change. <sectiondiv outputclass="width33">
        <table frame="all" rowsep="1" colsep="1" id="table_nzr_z4w_kv">
          <tgroup cols="1">
            <colspec colname="c1" colnum="1" colwidth="19em"/>
            <thead>
              <row>
                <entry>RabbitMQ credentials </entry>
              </row>
            </thead>
            <tbody>
              <row>
                <entry>
                  <codeblock outputclass="whitebackground"><b>Password name</b>
ops_mon_rmq_password
rmq_barbican_password
rmq_ceilometer_password
rmq_cinder_password
rmq_designate_password
rmq_keystone_password
rmq_magnum_password
rmq_monasca_monitor_password
rmq_nova_password
rmq_octavia_password
rmq_service_password</codeblock>
                </entry>
              </row>
            </tbody>
          </tgroup>
        </table>
      </sectiondiv>The playbook to run to change RabbitMQ credentials is
        <codeph>hlm-rabbitmq-credentials-change.yml</codeph>. Execute the following commands to make
      the changes:
          <codeblock>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts hlm-rabbitmq-credentials-change.yml</codeblock><p><b>Changing
          MySQL credentials</b></p> The following table lists the MySQL credentials you can change.
        <sectiondiv outputclass="width33">
        <table frame="all" rowsep="1" colsep="1" id="table_kds_fpw_kv">
          <tgroup cols="1">
            <colspec colname="c1" colnum="1" colwidth="19em"/>
            <thead>
              <row>
                <entry>MySQL credentials </entry>
              </row>
            </thead>
            <tbody>
              <row>
                <entry>
                  <codeblock outputclass="whitebackground"><b>Password name</b>
mysql_admin_password
mysql_barbican_password
mysql_clustercheck_pwd
mysql_designate_password
mysql_magnum_password
mysql_monasca_api_password
mysql_monasca_notifier_password
mysql_monasca_thresh_password
mysql_octavia_password
mysql_powerdns_password
mysql_root_pwd
mysql_service_pwd
mysql_sst_password
ops_mon_mdb_password
mysql_monasca_transform_password
mysql_nova_api_password
password</codeblock>
                </entry>
              </row>
            </tbody>
          </tgroup>
        </table>
      </sectiondiv>The playbook to run to change MySQL credentials is
        <codeph>hlm-reconfigure.yml</codeph>. To make the changes, execute the following
          commands:<codeblock>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts hlm-reconfigure.yml</codeblock><p><b>Changing
          cluster credentials</b></p> The following table lists the cluster credentials you can
      change. <sectiondiv outputclass="width33">
        <table frame="all" rowsep="1" colsep="1" id="table_wx3_kpw_kv">
          <tgroup cols="1">
            <colspec colname="c1" colnum="1" colwidth="19em"/>
            <thead>
              <row>
                <entry>cluster credentials </entry>
              </row>
            </thead>
            <tbody>
              <row>
                <entry>
                  <codeblock outputclass="whitebackground"><b>Password name</b>
haproxy_stats_password
keepalive_vrrp_password</codeblock>
                </entry>
              </row>
            </tbody>
          </tgroup>
        </table>
      </sectiondiv>The playbook to run to change cluster credentials is
        <codeph>hlm-cluster-credentials-change.yml</codeph>. To make changes, execute the following
          commands:<codeblock>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts hlm-cluster-credentials-change.yml</codeblock><p><b>Changing
          Monasca credentials</b></p> The following table lists the Monasca credentials you can
      change. <sectiondiv outputclass="width33">
        <table frame="all" rowsep="1" colsep="1" id="table_k2k_4pw_kv">
          <tgroup cols="1">
            <colspec colname="c1" colnum="1" colwidth="19em"/>
            <thead>
              <row>
                <entry>Monasca credentials </entry>
              </row>
            </thead>
            <tbody>
              <row>
                <entry>
                  <codeblock outputclass="whitebackground"><b>Password name</b>
influxdb_monasca_api_password
influxdb_monasca_persister_password
monitor_user_password
vertica_monasca_api_password
vertica_monasca_persister_password</codeblock>
                </entry>
              </row>
            </tbody>
          </tgroup>
        </table>
      </sectiondiv>The playbook to run to change Monasca credentials is
        <codeph>monasca-reconfigure-credentials-change.yml</codeph>. To make the changes, execute
      the following
          commands:<codeblock>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts monasca-reconfigure-credentials-change.yml</codeblock><p><b>Changing
          other credentials</b></p> The following table lists the other credentials you can change.
        <sectiondiv outputclass="width33">
        <table frame="all" rowsep="1" colsep="1" id="table_y52_rpw_kv">
          <tgroup cols="1">
            <colspec colname="c1" colnum="1" colwidth="19em"/>
            <thead>
              <row>
                <entry>Other credentials </entry>
              </row>
            </thead>
            <tbody>
              <row>
                <entry>
                  <codeblock outputclass="whitebackground"><b>Password name</b>
logging_beaver_password
logging_api_password
logging_monitor_password
logging_kibana_password</codeblock>
                </entry>
              </row>
            </tbody>
          </tgroup>
        </table>
      </sectiondiv> The playbook to run to change these credentials is
        <codeph>hlm-other-credentials-change.yml</codeph>. To make the changes, execute the
      following commands:
      <codeblock>cd ~/scratch/ansible/next/hos/ansible/
ansible-playbook -i hosts/verb_hosts hlm-other-credentials-change.yml</codeblock></section>
    <section id="changing-keystone-credentials-for-rgw">
      <p><b>Changing RADOS Gateway Credential</b></p>
      <p>To change the keystone credentials of RADOS Gateway, you must follow the preceeding steps
        documented at <xref href="#servicePasswords" format="dita">Changing Service Passwords</xref>
        by modifying the <codeph>keystone_rgw_password</codeph> section in
          <codeph>private_data_metadata.yml</codeph> file in <xref
          href="#servicePasswords/steps-to-change-password" format="dita">Steps to change a
          password</xref> or <xref href="#servicePasswords/specifying-password-value" format="dita"
          >Specifying password value</xref> section.</p>
    </section>
    <section><title>Immutable variables</title><p>The values of certain variables are immutable,
        which means that once they've been generated by the configuration processor they cannot be
        changed. These variables are:</p><ul>
        <li>barbican_master_kek_db_plugin</li>
        <li>swift_hash_path_suffix </li>
        <li>swift_hash_path_prefix </li>
        <li>mysql_cluster_name </li>
        <li>heartbeat_key </li>
        <li>erlang_cookie</li>
      </ul>The configuration processor will not re-generate the values of the above passwords, nor
      will it allow you to specify a value for them. In addition to the above variables, the
      following are immutable in <keyword keyref="kw-hos-phrase"/>:<ul>
        <li>All ssh keys generated by the configuration processor </li>
        <li>All UUIDs generated by the configuration processor</li>
        <li>metadata_proxy_shared_secret </li>
        <li>horizon_secret_key </li>
        <li>ceilometer_metering_secret</li>
      </ul>
    </section>
  </body>
</topic>
