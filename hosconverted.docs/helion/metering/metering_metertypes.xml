<?xml version="1.0" encoding="UTF-8"?>
<!--Edit status: not edited-->
<!DOCTYPE topic
  PUBLIC "-//OASIS//DTD DITA Topic//EN" "topic.dtd" >
<topic xml:lang="en-us" id="topic3051">
  <title><ph conkeyref="HOS-conrefs/product-title"/>Ceilometer Metering Available Meter
    Types</title>
  <body>
    <!--not tested-->
    <!--Needs Edit-->
    <p conkeyref="HOS-conrefs/applies-to"/>
    <p/>
    <p>The Metering service contains three types of meters:</p>
    <dl>
      <dlentry>
        <dt>Cumulative</dt>
        <dd>A cumulative meter measures data over time (for example, instance hours).</dd>
      </dlentry>
      <dlentry>
        <dt>Gauge</dt>
        <dd>A gauge measures discrete items (for example, floating IPs or image uploads) or
          fluctuating values (such as disk input or output).</dd>
      </dlentry>
      <dlentry>
        <dt>Delta</dt>
        <dd>A delta measures change over time, for example, monitoring bandwidth.</dd>
      </dlentry>
    </dl>
    <p>Each meter is populated from one or more <i>samples</i>, which are gathered from the
      messaging queue (listening agent), polling agents, or push agents. Samples are populated by
        <i>counter</i> objects.</p>
    <p>Each counter contains the following <i>fields</i>:</p>
    <dl>
      <dlentry>
        <dt>name</dt>
        <dd>the name of the meter</dd>
      </dlentry>
      <dlentry>
        <dt>type</dt>
        <dd> the type of meter (cumulative, gauge, or delta)</dd>
      </dlentry>
      <dlentry>
        <dt>amount</dt>
        <dd>the amount of data measured</dd>
      </dlentry>
      <dlentry>
        <dt>unit</dt>
        <dd> the unit of measure</dd>
      </dlentry>
      <dlentry>
        <dt>resource</dt>
        <dd>the resource being measured</dd>
      </dlentry>
      <dlentry>
        <dt>project ID</dt>
        <dd>the project the resource is assigned to</dd>
      </dlentry>
      <dlentry>
        <dt>user</dt>
        <dd>the user the resource is assigned to.</dd>
      </dlentry>
    </dl>
    <p>
      <b>Note</b>: The metering service shares the same High-availability proxy, messaging, and
      database clusters with the other Information services. To avoid unnecessarily high loads,
        <xref href="metering_bestpractice.dita">non-essential meters can be
        disabled</xref>.</p>
    <section id="helion-openstack-default-meters">
      <title><keyword keyref="kw-hos"/> Default Meters</title>
      <p>These meters are installed and enabled by default during an <keyword keyref="kw-hos"/>
        installation.</p>
      <p>Detailed information on the Ceilometer API can be found on the following page:</p>
      <p><xref href="http://docs.openstack.org/developer/ceilometer/webapi/v2.html" scope="external"
          format="html">Ceilometer Web API</xref>.</p>
    </section>
  </body>
</topic>
